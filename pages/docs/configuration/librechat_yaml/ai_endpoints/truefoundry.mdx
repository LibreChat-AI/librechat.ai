---
title: TrueFoundry AI
description: Example configuration for TrueFoundry AI Gateway
---

# [TrueFoundry Docs](https://docs.truefoundry.com/docs/introduction)

## Configuration Details

### Getting Base URL and Model Names

- **Base URL**: Get your TrueFoundry AI Gateway endpoint URL from your TrueFoundry workspace dashboard
- **Model Names**: Available models depend on your TrueFoundry deployment and configured providers
- **API Key**: Generate from your TrueFoundry account settings under API Keys section
- **Workspace ID**: Found in your TrueFoundry workspace settings

## Key Features

### Enterprise Security
- Role-based access control and authentication
- On-premises and multi-cloud deployment options
- Data sovereignty with no vendor lock-in

### Model Management
- Support for multiple AI providers through unified interface
- Custom model deployment capabilities
- Model versioning and lifecycle management

### Observability & Monitoring
- Request/response logging and analytics
- Performance metrics and cost tracking
- Rate limiting and quota management

### High Availability
- Load balancing across model instances
- Automatic failover and fallback mechanisms
- Intelligent routing based on latency and availability

## Using TrueFoundry AI Gateway with OpenAI Models
```yaml filename="librechat.yaml"
      - name: "TrueFoundry"
        apiKey: "${TRUEFOUNDRY_API_KEY}"
        baseURL: "${TRUEFOUNDRY_GATEWAY_URL}"
        headers:
            x-tfy-api-key: "${TRUEFOUNDRY_API_KEY}"
            x-tfy-workspace-id: "${TRUEFOUNDRY_WORKSPACE_ID}"
        models:
            default: ["openai-main/gpt-4o-mini", "openai-main/gpt-4o"]
            fetch: true
        titleConvo: true
        titleModel: "current_model"
        summarize: false
        summaryModel: "current_model"
        forcePrompt: false
        modelDisplayLabel: "TrueFoundry:OpenAI"
        iconURL: https://www.truefoundry.com/favicon.ico
```

## Using TrueFoundry AI Gateway with Open Source Models
```yaml filename="librechat.yaml"
    - name: "TrueFoundry-OSS"
      apiKey: "${TRUEFOUNDRY_API_KEY}"
      baseURL: "${TRUEFOUNDRY_GATEWAY_URL}"
      headers:
        x-tfy-api-key: "${TRUEFOUNDRY_API_KEY}"
        x-tfy-workspace-id: "${TRUEFOUNDRY_WORKSPACE_ID}"
        x-tfy-model-config: "llama-3.2-config"
      models:
        default: ["llama-3.2-90b-instruct", "mistral-8x7b-instruct"]
        fetch: true
      titleConvo: true
      titleModel: "current_model"
      summarize: false
      summaryModel: "current_model"
      forcePrompt: false
      modelDisplayLabel: "TrueFoundry:OSS"
      iconURL: https://www.truefoundry.com/favicon.ico
```

## Using TrueFoundry with Custom Model Deployments
```yaml filename="librechat.yaml"
    - name: "TrueFoundry-Custom"
      apiKey: "${TRUEFOUNDRY_API_KEY}"
      baseURL: "${TRUEFOUNDRY_CUSTOM_ENDPOINT_URL}"
      headers:
        Authorization: "Bearer ${TRUEFOUNDRY_API_KEY}"
        x-tfy-workspace-id: "${TRUEFOUNDRY_WORKSPACE_ID}"
      models:
        default: ["custom-fine-tuned-model"]
        fetch: false
      titleConvo: true
      titleModel: "current_model"
      summarize: false
      summaryModel: "current_model"
      forcePrompt: false
      modelDisplayLabel: "TrueFoundry:Custom"
      iconURL: https://www.truefoundry.com/favicon.ico
``` 