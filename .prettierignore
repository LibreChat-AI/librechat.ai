# Lockfiles are managed by the package manager, not formatted.
pnpm-lock.yaml

# Generated content
public/starters.json
public/starters-hashes.json

# MDX content is authored by hand and contains embedded expressions, JSX, and
# code samples that Prettier can reformat into invalid MDX (acorn parse errors).
# Formatting is left to the author; meta.json sidebar files are still formatted.
**/*.mdx
