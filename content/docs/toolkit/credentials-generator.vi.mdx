---
title: Trình tạo thông tin xác thực
description: Tạo thông tin xác thực ngẫu nhiên bảo mật cho tệp .env của LibreChat.
---

import { CredentialsGeneratorMDX } from '@/components/tools/CredentialsGeneratorMDX'

Tạo các giá trị ngẫu nhiên an toàn về mặt mật mã cho các secret bắt buộc trong tệp cấu hình `.env` của LibreChat. Nhấp vào **Generate** và sau đó sao chép từng giá trị riêng lẻ hoặc tất cả cùng một lúc.

<CredentialsGeneratorMDX />
