---
title: Docker
icon: Docker
description: >-
  Come installare LibreChat localmente con Docker, verificare la configurazione
  e configurare endpoint personalizzati
---

Nella maggior parte degli scenari, Docker Compose è il metodo di installazione consigliato grazie alla sua semplicità, facilità d'uso e affidabilità.

## Prerequisiti [#prerequisites]

- [`Git`](https://git-scm.com/downloads)
- [`Docker`](https://www.docker.com/products/docker-desktop/)

Docker Desktop è consigliato per la maggior parte degli utenti. Per le installazioni su server remoto, consulta la [Guida all'installazione di Docker su Ubuntu](/docs/remote/docker_linux).

<Callout type="warn" title="Mac con Apple Silicon (serie M)">

I computer Mac con processori Apple Silicon (M1, M2, M3, M4) non supportano le istruzioni AVX, richieste dall'immagine MongoDB predefinita utilizzata nella configurazione Docker Compose di LibreChat. Se utilizzi un Mac della serie M, MongoDB andrà in crash all'avvio.

**Correzione:** Crea un `docker-compose.override.yml` per utilizzare un'immagine MongoDB più vecchia e compatibile:

```yaml filename="docker-compose.override.yml"
services:
  mongodb:
    image: mongo:4.4.18
```

Consulta la [guida all'override di Docker](/docs/configuration/docker_override) per maggiori dettagli.

</Callout>

## Installazione [#installation]

<Steps>
  <Step>

### Clona il repository [#clone-the-repository]

```bash
git clone https://github.com/danny-avila/LibreChat.git
cd LibreChat
```

  </Step>
  <Step>

### Crea il tuo file di ambiente [#create-your-environment-file]

```bash
cp .env.example .env
```

Il file `.env` predefinito funziona immediatamente per una configurazione di base. Per una configurazione approfondita, consulta il [riferimento .env](/docs/configuration/dotenv).

<Callout type="info" title="Windows">

Su Windows, usa `copy .env.example .env` se `cp` non è disponibile

</Callout>

  </Step>
  <Step>

### Avviare LibreChat [#start-librechat]

```bash
docker compose up -d
```

Il primo avvio scarica le immagini Docker e potrebbe richiedere alcuni minuti. Gli avvii successivi sono molto più rapidi.

  </Step>
  <Step>

### Verifica e accedi [#verify-and-log-in]

Apri il tuo browser e visita [http://localhost:3080](http://localhost:3080). Dovresti vedere la pagina di login di LibreChat.

<Callout type="info" title="Primo account = Admin">

Il primo account che registri diventa l'account amministratore. Non esistono credenziali predefinite: crei il tuo nome utente e la tua password durante la registrazione.

</Callout>

Fai clic su **Register** per creare il tuo account e iniziare a usare LibreChat.

  </Step>
</Steps>

## Montaggio di librechat.yaml [#mounting-librechatyaml]

Per utilizzare un file di configurazione `librechat.yaml` personalizzato con Docker, è necessario montarlo come volume in modo che il container possa accedervi.

Copia il file di override di esempio e modificalo:

```bash
cp docker-compose.override.yml.example docker-compose.override.yml
```

Assicurati che il mount del volume librechat.yaml sia decommentato in `docker-compose.override.yml`:

```yaml filename="docker-compose.override.yml"
services:
  api:
    volumes:
      - type: bind
        source: ./librechat.yaml
        target: /app/librechat.yaml
```

Riavvia per rendere effettive le modifiche:

```bash
docker compose down && docker compose up -d
```

Per le istruzioni di configurazione complete, inclusa la creazione del file da zero, consulta la [guida a librechat.yaml](/docs/configuration/librechat_yaml). Per ulteriori opzioni di override, consulta la [guida all'override di Docker](/docs/configuration/docker_override).

## Aggiornamento di LibreChat [#updating-librechat]

I seguenti comandi recupereranno le ultime modifiche al progetto LibreChat, incluse eventuali modifiche necessarie ai file docker compose, così come le ultime immagini predefinite.

<Callout type="info" title="Autorizzazioni">

Potrebbe essere necessario anteporre `sudo` ai comandi in base ai permessi del proprio ambiente.

</Callout>

```bash filename="Stop the running container(s)"
docker compose down
```

```bash filename="Remove all existing docker images"
# Linux/Mac
docker images -a | grep "librechat" | awk '{print $3}' | xargs docker rmi

# Windows (PowerShell)
docker images -a --filter "reference=registry.librechat.ai/danny-avila/librechat*" --format "{{.ID}}" | ForEach-Object { docker rmi $_ }
docker images -a --filter "reference=ghcr.io/danny-avila/librechat*" --format "{{.ID}}" | ForEach-Object { docker rmi $_ }
```

```bash filename="Pull latest project changes"
git pull
```

```bash filename="Pull the latest LibreChat image"
docker compose pull
```

```bash filename="Start LibreChat"
docker compose up
```

## Risoluzione dei problemi [#troubleshooting]

### Porta già in uso [#port-already-in-use]

Se visualizzi un errore come `bind: address already in use` per la porta 3080, significa che un'altra applicazione sta utilizzando quella porta.

O interrompi l'applicazione in conflitto, o cambia la porta in `docker-compose.override.yml`:

```yaml filename="docker-compose.override.yml"
services:
  api:
    ports:
      - "3081:3080"
```

Quindi visita `http://localhost:3081` invece.

### Il container si arresta all'avvio [#container-crashes-on-startup]

Se i container si chiudono immediatamente dopo l'avvio, controlla i log:

```bash
docker compose logs api
```

Cause comuni:

- Sintassi di `librechat.yaml` non valida -- convalida con il [YAML Validator](/toolkit/yaml_checker)
- File `.env` mancante -- assicurati che `.env` esista nella root del progetto
- Docker non è in esecuzione -- assicurati che Docker Desktop sia aperto e in esecuzione

### Variabili d'ambiente mancanti [#missing-environment-variables]

Se le funzionalità non funzionano come previsto, verifica che le variabili d'ambiente richieste siano impostate nel tuo file `.env`.

```bash
docker compose exec api env | grep -i "your_variable"
```

Consulta il [.env reference](/docs/configuration/dotenv) per tutte le variabili disponibili e i relativi valori predefiniti.

## Prossimi passi [#next-steps]

<Cards num={3}>
  <Cards.Card title="Endpoint personalizzati" href="/docs/quick_start/custom_endpoints" arrow>
    Aggiungi OpenRouter, Ollama e altri provider di IA
  </Cards.Card>
  <Cards.Card title="Panoramica della configurazione" href="/docs/configuration" arrow>
    Comprendere come funzionano insieme i file di configurazione di LibreChat
  </Cards.Card>
  <Cards.Card title="Configurazione dell'autenticazione" href="/docs/configuration/authentication" arrow>
    Configura OAuth, LDAP e altri metodi di accesso
  </Cards.Card>
</Cards>
