---
title: Thiết lập cho người đóng góp
icon: Rocket
description: >-
  Tìm hiểu cách đóng góp bằng GitHub Desktop, các tiện ích mở rộng VS Code và
  Git rebase.
---

## Yêu cầu [#requirements]

- [Git](https://git-scm.com/downloads) (Thiết yếu)
- [Node.js](https://nodejs.org/en/download) `v24.16.0` (Thiết yếu)
- npm `v11.16.0` (Thiết yếu)
- [MongoDB](https://www.mongodb.com/try/download/community) (Thiết yếu, dành cho cơ sở dữ liệu)
- [Git LFS](https://git-lfs.com/) (Hữu ích cho các tệp tin lớn hơn)
- [GitHub Desktop](https://desktop.github.com/) (Tùy chọn)
- [VSCode](https://code.visualstudio.com/Download) (Trình chỉnh sửa mã nguồn được khuyến nghị)

### Các tiện ích mở rộng VSCode được đề xuất [#recommended-vscode-extensions]

Cài đặt các tiện ích mở rộng này trong VS Code:

- [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode)
- [ESLint](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint)
- [GitLens](https://marketplace.visualstudio.com/items?itemName=eamodio.gitlens)

## Chuẩn bị Môi trường [#prepare-the-environment]

### Node.js và npm [#nodejs-and-npm]

Nếu bạn sử dụng `nvm`, hãy cài đặt và chọn phiên bản Node.js được khuyến nghị trước khi cài đặt LibreChat:

```sh filename="Use Node.js 24"
nvm install 24.16.0
nvm use 24.16.0
npm install -g npm@11.16.0
```

Xác minh shell của bạn đang sử dụng các phiên bản mong đợi:

```sh filename="Check Node.js and npm"
node -v
npm -v
```

```txt filename="Expected versions"
v24.16.0
11.16.0
```

### GitHub [#github]

- Fork repository LibreChat: [https://github.com/danny-avila/LibreChat/fork](https://github.com/danny-avila/LibreChat/fork)

- Tạo một nhánh trên bản fork của bạn, đặt tên phù hợp và liên kết nó với kho lưu trữ gốc.

- Tải nhánh mới của bạn về máy tính cục bộ

```sh filename="Download your LibreChat branch"
git clone -b branch-name https://github.com/username/LibreChat.git
```

> Thay thế `branch-name` và `username` bằng thông tin của bạn

### Mở trong VS Code [#open-in-vs-code]

- Sau khi sao chép (clone) nhánh của bạn:
  ```sh filename="Navigate to LibreChat folder"
  cd LibreChat
  ```
  ```sh filename="Open in VS Code"
  code .
  ```

### Chuẩn bị LibreChat [#prepare-librechat]

- Open the terminal in VS Code with `ctrl`+`shift`+`` ```

  > Hoặc, sử dụng `ctrl`+`j` để mở ngăn dưới cùng và chọn terminal.

- ```sh filename="Install LibreChat dependencies"
  npm run smart-reinstall
  ```

  > Nếu bạn vừa thay đổi phiên bản Node.js hoặc npm, hãy sử dụng `npm run reinstall` một lần để cài đặt sạch.

- ```sh filename="Build all compiled code"
  npm run build
  ```

- Cấu hình .env
  - Tạo tệp `.env`. Nếu bạn chưa có, hãy sao chép `.env.example` và cấu hình nó.

<Callout type="warning" title="Cảnh báo">
  Các giá trị mặc định trong `.env.example` thường là ổn, ngoại trừ `MONGO_URI`. Hãy cung cấp giá trị của riêng bạn.
  Đảm bảo cài đặt MongoDB và cấu hình `MONGO_URI` chính xác để kết nối với instance MongoDB của bạn. Sử dụng [MongoDB Community Server](https://www.mongodb.com/try/download/community) hoặc [MongoDB Atlas Cloud](https://www.mongodb.com/cloud/atlas/register).
</Callout>

### Quy trình phát triển [#development-workflow]

Để làm việc hiệu quả trên LibreChat, hãy sử dụng các lệnh sau:

- **Khởi chạy Backend:**
  - Sử dụng `npm run backend` để vận hành bình thường.
  - Để phát triển tích cực, hãy sử dụng `npm run backend:dev` để theo dõi các thay đổi.
  - Truy cập tại `http://localhost:3080/`.

- **Chạy Frontend ở Chế độ Phát triển:**
  - **Đảm bảo backend đang chạy.**
  - Sử dụng `npm run frontend:dev` để theo dõi các thay đổi ở giao diện người dùng (frontend).
  - Xem tại `http://localhost:3090/`.

<Callout type="tip" title="Mẹo chuyên gia">
  - Để cập nhật theo thời gian thực trong quá trình phát triển frontend, hãy chạy `npm run frontend:dev` để các thay đổi ở frontend tự động làm mới trên cổng `3090`.
  - Đặt `DEBUG_CONSOLE=true` trong `.env` để nhận đầu ra máy chủ chi tiết trong console.
</Callout>

## Kiểm thử cục bộ [#local-testing]

Trước khi gửi, hãy kiểm tra các cập nhật của bạn tại máy cục bộ, xem: [Perform Tests Locally](/docs/development/testing)

Bằng cách chạy các bài kiểm thử, hãy đảm bảo các đóng góp của bạn luôn ổn định và sẵn sàng để tích hợp.

## Commit, Push, Pull Request (PR) [#commit-push-pull-request-pr]

### Thực hiện Commit [#make-a-commit]

**Commits** đánh dấu các điểm kiểm tra logic trong quá trình phát triển. Hãy bao gồm các thông báo rõ ràng giải thích những thay đổi.

**Ví dụ:**

```bash filename=" "
git add .
git commit -m "Add login functionality"
```

### Đẩy thay đổi [#push-changes]

**Push** các thay đổi lên kho lưu trữ từ xa sau khi hoàn thành một tính năng hoặc sửa một lỗi.

**Ví dụ:**

```bash filename=" "
git push origin feature-branch-name
```

### Tạo một Pull Request (PR) [#create-a-pull-request-pr]

**Pull Request** hợp nhất các thay đổi từ một nhánh tính năng vào nhánh chính.

1. Kéo các thay đổi mới nhất từ nhánh main và giải quyết xung đột.
2. Đẩy nhánh tính năng đã cập nhật.
3. Đảm bảo mã nguồn tuân thủ các hướng dẫn của dự án.

**Ví dụ:**

```bash filename=" "
git checkout main
git pull origin main
git checkout feature-branch-name
git rebase main
# Resolve conflicts if any
git push origin feature-branch-name
# Open PR on GitHub
```

Truy cập kho lưu trữ của bạn trong trình duyệt và nhấp vào "Contribute".

<Callout type="info" title="Lưu ý:">
  Cung cấp mô tả PR chi tiết giải thích các thay đổi và giá trị của chúng. Tham chiếu các issue liên quan.
</Callout>

<Callout type="tip" title="Mẹo">
  Sử dụng GitHub Desktop để theo dõi các thay đổi.
</Callout>

<Callout type="warning" title="Cảnh báo">
  Nếu `git commit` thất bại do các lỗi ESLint, hãy tìm hiểu và khắc phục vấn đề đó.
</Callout>

## Hoàn tác Commit một cách an toàn [#revert-commits-safely]

Để hoàn tác các thay đổi trong một feature branch, hãy thực hiện các bước sau một cách thận trọng:

- ```bash filename="1. Update local repository from feature branch:"
  git pull origin feature-branch-name
  ```

- ```bash filename="2. Review commit history to determine commits to revert:"
  git log
  ```

- ```bash filename="3. Start an interactive rebase for 'N' commits to revert:"
  git rebase -i HEAD~N
  ```

  > Thay thế `pick` bằng `drop` cho các commit cần xóa. Lưu và thoát trình chỉnh sửa.

- ```bash filename="4. Force push changes to remote repository:"
  git push --force-with-lease origin feature-branch-name
  ```
