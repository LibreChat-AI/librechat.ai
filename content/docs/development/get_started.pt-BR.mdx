---
title: Configuração para Contribuidores
icon: Rocket
description: >-
  Aprenda a contribuir usando o GitHub Desktop, extensões do VS Code e Git
  rebase.
---

## Requisitos [#requirements]

- [Git](https://git-scm.com/downloads) (Essencial)
- [Node.js](https://nodejs.org/en/download) `v24.16.0` (Essencial)
- npm `v11.16.0` (Essencial)
- [MongoDB](https://www.mongodb.com/try/download/community) (Essencial, para o banco de dados)
- [Git LFS](https://git-lfs.com/) (Útil para arquivos maiores)
- [GitHub Desktop](https://desktop.github.com/) (Opcional)
- [VSCode](https://code.visualstudio.com/Download) (Editor de código-fonte recomendado)

### Extensões recomendadas para o VSCode [#recommended-vscode-extensions]

Instale estas extensões no VS Code:

- [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode)
- [ESLint](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint)
- [GitLens](https://marketplace.visualstudio.com/items?itemName=eamodio.gitlens)

## Prepare o Ambiente [#prepare-the-environment]

### Node.js e npm [#nodejs-and-npm]

Se você usa o `nvm`, instale e selecione a versão recomendada do Node.js antes de instalar o LibreChat:

```sh filename="Use Node.js 24"
nvm install 24.16.0
nvm use 24.16.0
npm install -g npm@11.16.0
```

Verifique se o seu shell está usando as versões esperadas:

```sh filename="Check Node.js and npm"
node -v
npm -v
```

```txt filename="Expected versions"
v24.16.0
11.16.0
```

### GitHub [#github]

- Faça um fork do repositório LibreChat: [https://github.com/danny-avila/LibreChat/fork](https://github.com/danny-avila/LibreChat/fork)

- Crie uma branch no seu fork, nomeie-a adequadamente e vincule-a ao repositório original.

- Baixe sua nova branch para o seu PC local

```sh filename="Download your LibreChat branch"
git clone -b branch-name https://github.com/username/LibreChat.git
```

> Substitua `branch-name` e `username` pelos seus detalhes

### Abrir no VS Code [#open-in-vs-code]

- Após clonar sua branch:
  ```sh filename="Navigate to LibreChat folder"
  cd LibreChat
  ```
  ```sh filename="Open in VS Code"
  code .
  ```

### Prepare o LibreChat [#prepare-librechat]

- Abra o terminal no VS Code com `ctrl`+`shift`+`` ```

  > Alternativamente, use `ctrl`+`j` para abrir o painel inferior e selecione o terminal.

- ```sh filename="Install LibreChat dependencies"
  npm run smart-reinstall
  ```

  > Se você acabou de alterar as versões do Node.js ou npm, use `npm run reinstall` uma vez para uma instalação limpa.

- ```sh filename="Build all compiled code"
  npm run build
  ```

- Configuração do .env
  - Crie o arquivo `.env`. Se você não tiver um, duplique o `.env.example` e configure-o.

<Callout type="warning" title="Aviso">
  Os valores padrão em `.env.example` geralmente são adequados, exceto para `MONGO_URI`. Forneça o seu próprio.
  Certifique-se de instalar o MongoDB e configurar `MONGO_URI` corretamente para conectar à sua instância do MongoDB. Use o [MongoDB Community Server](https://www.mongodb.com/try/download/community) ou o [MongoDB Atlas Cloud](https://www.mongodb.com/cloud/atlas/register).
</Callout>

### Fluxo de Trabalho de Desenvolvimento [#development-workflow]

Para um trabalho eficiente no LibreChat, use estes comandos:

- **Iniciando o Backend:**
  - Use `npm run backend` para operação normal.
  - Para desenvolvimento ativo, use `npm run backend:dev` para monitorar alterações.
  - Acesse em `http://localhost:3080/`.

- **Executando o Frontend em Modo de Desenvolvimento:**
  - **Certifique-se de que o backend esteja em execução.**
  - Use `npm run frontend:dev` para monitorar alterações no frontend.
  - Acesse em `http://localhost:3090/`.

<Callout type="tip" title="Dicas Profissionais">
  - Para atualizações em tempo real durante o desenvolvimento do frontend, execute `npm run frontend:dev` para que as alterações no frontend sejam atualizadas na porta `3090`.
  - Defina `DEBUG_CONSOLE=true` no `.env` para obter uma saída detalhada do servidor no console.
</Callout>

## Testes Locais [#local-testing]

Antes de enviar, teste suas atualizações localmente, veja: [Perform Tests Locally](/docs/development/testing)

Ao executar testes, garanta que suas contribuições sejam robustas e estejam prontas para integração.

## Commit, Push, Pull Request (PR) [#commit-push-pull-request-pr]

### Fazer um Commit [#make-a-commit]

**Commits** marcam pontos de verificação lógicos no desenvolvimento. Inclua mensagens claras explicando as alterações.

**Exemplo:**

```bash filename=" "
git add .
git commit -m "Add login functionality"
```

### Enviar Alterações [#push-changes]

**Envie (Push)** as alterações para o repositório remoto após concluir uma funcionalidade ou corrigir um problema.

**Exemplo:**

```bash filename=" "
git push origin feature-branch-name
```

### Criar um Pull Request (PR) [#create-a-pull-request-pr]

**Pull Request** mescla alterações de um branch de funcionalidade para o branch principal.

1. Puxe as alterações mais recentes da branch main e resolva os conflitos.
2. Envie o branch de recurso atualizado.
3. Certifique-se de que o código siga as diretrizes do projeto.

**Exemplo:**

```bash filename=" "
git checkout main
git pull origin main
git checkout feature-branch-name
git rebase main
# Resolve conflicts if any
git push origin feature-branch-name
# Open PR on GitHub
```

Acesse seu repositório em um navegador e clique em "Contribute".

<Callout type="info" title="Nota:">
  Forneça uma descrição detalhada do PR explicando as alterações e seu valor. Referencie issues relacionadas.
</Callout>

<Callout type="tip" title="Dica">
  Use o GitHub Desktop para rastrear alterações.
</Callout>

<Callout type="warning" title="Aviso">
  Se `git commit` falhar devido a erros do ESLint, entenda e corrija o problema.
</Callout>

## Reverter Commits com Segurança [#revert-commits-safely]

Para desfazer alterações em uma feature branch, siga estes passos com cautela:

- ```bash filename="1. Update local repository from feature branch:"
  git pull origin feature-branch-name
  ```

- ```bash filename="2. Review commit history to determine commits to revert:"
  git log
  ```

- ```bash filename="3. Start an interactive rebase for 'N' commits to revert:"
  git rebase -i HEAD~N
  ```

  > Substitua `pick` por `drop` para os commits a serem removidos. Salve e saia do editor.

- ```bash filename="4. Force push changes to remote repository:"
  git push --force-with-lease origin feature-branch-name
  ```
