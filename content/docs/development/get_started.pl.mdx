---
title: Konfiguracja dla współtwórców
icon: Rocket
description: >-
  Dowiedz się, jak wnosić wkład przy użyciu GitHub Desktop, rozszerzeń VS Code
  oraz mechanizmu Git rebase.
---

## Wymagania [#requirements]

- [Git](https://git-scm.com/downloads) (Niezbędne)
- [Node.js](https://nodejs.org/en/download) `v24.16.0` (Niezbędne)
- npm `v11.16.0` (Niezbędne)
- [MongoDB](https://www.mongodb.com/try/download/community) (Niezbędne, dla bazy danych)
- [Git LFS](https://git-lfs.com/) (Przydatne w przypadku większych plików)
- [GitHub Desktop](https://desktop.github.com/) (Opcjonalnie)
- [VSCode](https://code.visualstudio.com/Download) (Zalecany edytor kodu źródłowego)

### Zalecane rozszerzenia VSCode [#recommended-vscode-extensions]

Zainstaluj te rozszerzenia w VS Code:

- [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode)
- [ESLint](https://marketplace.visualstudio.com/items?itemName=dbaeumer.vscode-eslint)
- [GitLens](https://marketplace.visualstudio.com/items?itemName=eamodio.gitlens)

## Przygotowanie środowiska [#prepare-the-environment]

### Node.js i npm [#nodejs-and-npm]

Jeśli używasz `nvm`, zainstaluj i wybierz zalecaną wersję Node.js przed instalacją LibreChat:

```sh filename="Use Node.js 24"
nvm install 24.16.0
nvm use 24.16.0
npm install -g npm@11.16.0
```

Zweryfikuj, czy twoja powłoka używa oczekiwanych wersji:

```sh filename="Check Node.js and npm"
node -v
npm -v
```

```txt filename="Expected versions"
v24.16.0
11.16.0
```

### GitHub [#github]

- Sforkuj repozytorium LibreChat: [https://github.com/danny-avila/LibreChat/fork](https://github.com/danny-avila/LibreChat/fork)

- Utwórz gałąź (branch) na swoim forku, nazwij ją odpowiednio i połącz z oryginalnym repozytorium.

- Pobierz nową gałąź na swój lokalny komputer

```sh filename="Download your LibreChat branch"
git clone -b branch-name https://github.com/username/LibreChat.git
```

> Zastąp `branch-name` oraz `username` swoimi danymi

### Otwórz w VS Code [#open-in-vs-code]

- Po sklonowaniu swojej gałęzi:
  ```sh filename="Navigate to LibreChat folder"
  cd LibreChat
  ```
  ```sh filename="Open in VS Code"
  code .
  ```

### Przygotowanie LibreChat [#prepare-librechat]

- Open the terminal in VS Code with `ctrl`+`shift`+`` ```

  > Alternatywnie użyj `ctrl`+`j`, aby otworzyć dolny panel i wybrać terminal.

- ```sh filename="Install LibreChat dependencies"
  npm run smart-reinstall
  ```

  > Jeśli właśnie zmieniłeś wersje Node.js lub npm, użyj jednorazowo `npm run reinstall` w celu wykonania czystej instalacji.

- ```sh filename="Build all compiled code"
  npm run build
  ```

- Konfiguracja .env
  - Utwórz plik `.env`. Jeśli go nie masz, skopiuj `.env.example` i skonfiguruj go.

<Callout type="warning" title="Ostrzeżenie">
  Domyślne wartości w `.env.example` są zazwyczaj wystarczające, z wyjątkiem `MONGO_URI`. Podaj własną.
  Upewnij się, że zainstalowałeś MongoDB i poprawnie skonfigurowałeś `MONGO_URI`, aby połączyć się ze swoją instancją MongoDB. Użyj [MongoDB Community Server](https://www.mongodb.com/try/download/community) lub [MongoDB Atlas Cloud](https://www.mongodb.com/cloud/atlas/register).
</Callout>

### Przepływ pracy programistycznej [#development-workflow]

Aby efektywnie pracować z LibreChat, użyj tych poleceń:

- **Uruchamianie backendu:**
  - Użyj `npm run backend` do normalnej pracy.
  - W przypadku aktywnego programowania użyj `npm run backend:dev`, aby monitorować zmiany.
  - Uzyskaj dostęp pod adresem `http://localhost:3080/`.

- **Uruchamianie frontendu w trybie deweloperskim:**
  - **Upewnij się, że backend jest uruchomiony.**
  - Użyj `npm run frontend:dev`, aby monitorować zmiany we frontendzie.
  - Zobacz pod adresem `http://localhost:3090/`.

<Callout type="tip" title="Wskazówki eksperta">
  - Aby uzyskać aktualizacje w czasie rzeczywistym podczas tworzenia frontendu, uruchom `npm run frontend:dev`, dzięki czemu zmiany we frontendzie będą odświeżane na porcie `3090`.
  - Ustaw `DEBUG_CONSOLE=true` w pliku `.env`, aby uzyskać szczegółowe dane wyjściowe serwera w konsoli.
</Callout>

## Lokalne testowanie [#local-testing]

Przed wysłaniem zmian przetestuj je lokalnie, zobacz: [Perform Tests Locally](/docs/development/testing)

Uruchamiając testy, upewnij się, że Twoje zmiany są solidne i gotowe do integracji.

## Commit, Push, Pull Request (PR) [#commit-push-pull-request-pr]

### Utwórz commit [#make-a-commit]

**Commity** wyznaczają logiczne punkty kontrolne w procesie tworzenia. Dołączaj jasne komunikaty wyjaśniające wprowadzone zmiany.

**Przykład:**

```bash filename=" "
git add .
git commit -m "Add login functionality"
```

### Wypchnij zmiany [#push-changes]

**Wypchnij** (push) zmiany do zdalnego repozytorium po ukończeniu funkcji lub naprawieniu błędu.

**Przykład:**

```bash filename=" "
git push origin feature-branch-name
```

### Utwórz Pull Request (PR) [#create-a-pull-request-pr]

**Pull Request** scala zmiany z gałęzi funkcji (feature branch) do głównej gałęzi (main branch).

1. Pobierz najnowsze zmiany z gałęzi main i rozwiąż konflikty.
2. Wypchnij zaktualizowaną gałąź funkcji.
3. Upewnij się, że kod jest zgodny z wytycznymi projektu.

**Przykład:**

```bash filename=" "
git checkout main
git pull origin main
git checkout feature-branch-name
git rebase main
# Resolve conflicts if any
git push origin feature-branch-name
# Open PR on GitHub
```

Uzyskaj dostęp do swojego repozytorium w przeglądarce i kliknij „Contribute”.

<Callout type="info" title="Uwaga:">
  Dostarcz szczegółowy opis PR wyjaśniający wprowadzone zmiany oraz ich wartość. Odwołaj się do powiązanych zgłoszeń (issues).
</Callout>

<Callout type="tip" title="Wskazówka">
  Użyj GitHub Desktop, aby śledzić zmiany.
</Callout>

<Callout type="warning" title="Ostrzeżenie">
  Jeśli `git commit` nie powiedzie się z powodu błędów ESLint, zrozum i napraw problem.
</Callout>

## Bezpieczne wycofywanie commitów [#revert-commits-safely]

Aby cofnąć zmiany w gałęzi funkcji (feature branch), wykonaj ostrożnie poniższe kroki:

- ```bash filename="1. Update local repository from feature branch:"
  git pull origin feature-branch-name
  ```

- ```bash filename="2. Review commit history to determine commits to revert:"
  git log
  ```

- ```bash filename="3. Start an interactive rebase for 'N' commits to revert:"
  git rebase -i HEAD~N
  ```

  > Zastąp `pick` przez `drop` dla commitów, które chcesz usunąć. Zapisz i zamknij edytor.

- ```bash filename="4. Force push changes to remote repository:"
  git push --force-with-lease origin feature-branch-name
  ```
