---
title: Cấu trúc đối tượng endpoint Agents
icon: Bot
---

Trang này áp dụng cho endpoint [`agents`](/docs/features/agents).

## Ví dụ [#example]

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    recursionLimit: 50
    maxRecursionLimit: 100
    maxToolCallArgBytes: 65536
    maxDeltaEventsPerTurn: 100000
    maxToolCallArgBytesByTool:
      create_file: 131072
    disableBuilder: false
    # (optional) Agent Capabilities available to all users. Omit the ones you wish to exclude. Defaults to list below.
    # Opt-in capabilities: "programmatic_tools", "stateful_code_sessions", "run_in_background", and "tool_intents".
    # capabilities: ["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]
    # (optional) File citation configuration for file_search capability
    maxCitations: 30 # Maximum total citations in responses (1-50)
    maxCitationsPerFile: 7 # Maximum citations from each file (1-10)
    minRelevanceScore: 0.45 # Minimum relevance score threshold (0.0-1.0)
    titleTiming: immediate
    activityLabel: true
    activityModel: gpt-4.1-nano
    activityPhaseLabel: true
    activityPhaseMaxPerRun: 5
    skills:
      maxCatalogSkills: 20
    toolApproval:
      enabled: true
      mode: default
      deny: ['mcp:*:delete_*']
      ask: ['mcp:*:*']
    checkpointer:
      type: mongo
      ttl: 86400
    remoteApi:
      auth:
        oidc:
          enabled: false
```

> Cấu hình này cho phép giao diện trình tạo (builder interface) cho các tác nhân (agents).

## recursionLimit

<OptionTable
  options={[
    [
      'recursionLimit',
      'Number',
      'Thiết lập số bước mặc định mà một agent có thể thực hiện trong một lần chạy.',
      'Controls recursion depth to prevent infinite loops. When limit is reached, raises GraphRecursionError. This value can be configured from the UI up to the maxRecursionLimit.',
    ],
  ]}
/>

**Mặc định:** `25`

**Ví dụ:**

```yaml filename="endpoints / agents / recursionLimit"
recursionLimit: 50
```

Để biết thêm thông tin về các bước của tác nhân (agent steps), hãy xem [Max Agent Steps](/docs/features/agents#max-agent-steps).

## maxRecursionLimit

<OptionTable
  options={[
    [
      'maxRecursionLimit',
      'Number',
      'Thiết lập số bước tối đa tuyệt đối mà một agent có thể thực hiện trong một lần chạy.',
      'Defines the upper limit for the recursionLimit that can be set from the UI. This prevents users from setting excessively high values.',
    ],
  ]}
/>

**Mặc định:** Nếu bỏ qua, giá trị mặc định sẽ là giá trị của recursionLimit hoặc 50 nếu recursionLimit cũng bị bỏ qua.

**Ví dụ:**

```yaml filename="endpoints / agents / maxRecursionLimit"
maxRecursionLimit: 100
```

Để biết thêm thông tin về các bước của tác nhân (agent steps), hãy xem [Max Agent Steps](/docs/features/agents#max-agent-steps).

## Bộ ngắt mạch luồng (Stream circuit breakers) [#stream-circuit-breakers]

Các cài đặt này sẽ dừng quá trình chạy Agent khi luồng dữ liệu từ nhà cung cấp có dấu hiệu tạo ra các đối số công cụ không kiểm soát được hoặc một chuỗi sự kiện delta vô hạn.

<OptionTable
  options={[
    [
      'maxToolCallArgBytes',
      'Number',
      'Số byte tích lũy tối đa cho các đối số của một lệnh gọi công cụ được truyền phát. Đặt thành 0 để tắt tính năng bảo vệ toàn cục.',
      '65536',
    ],
    [
      'maxDeltaEventsPerTurn',
      'Number',
      'Số lượng sự kiện truyền phát tối đa cho một lượt tạo mô hình. Đặt thành 0 hoặc bỏ qua để tắt tính năng bảo vệ này.',
      '0 (disabled)',
    ],
    [
      'maxToolCallArgBytesByTool',
      'Object',
      'Ghi đè byte đối số theo từng công cụ, được khóa bằng tên công cụ hiển thị với model. Giá trị 0 sẽ vô hiệu hóa cơ chế bảo vệ cho công cụ đó.',
      'create_file: 131072',
    ],
  ]}
/>

LibreChat sử dụng giới hạn đối số toàn cục 64 KiB và cung cấp mức ghi đè 128 KiB cho `create_file`, vì các payload hợp lệ của hàm này thường lớn hơn. Các mục trong `maxToolCallArgBytesByTool` sẽ hợp nhất đè lên mức ghi đè tích hợp sẵn đó. Nếu một trong hai cơ chế bảo vệ bị kích hoạt, LibreChat sẽ hủy luồng nhà cung cấp đang chạy và báo cáo giới hạn nào đã bị vượt quá.

```yaml filename="endpoints / agents / stream circuit breakers"
maxToolCallArgBytes: 65536
maxDeltaEventsPerTurn: 100000
maxToolCallArgBytesByTool:
  create_file: 262144
  large_import: 524288
```

Để vô hiệu hóa mọi cơ chế bảo vệ kích thước đối số (argument-size guard), hãy đặt cả giới hạn toàn cục và ghi đè `create_file` mặc định thành `0`:

```yaml
maxToolCallArgBytes: 0
maxToolCallArgBytesByTool:
  create_file: 0
```

## titleTiming

<OptionTable
  options={[
    [
      'titleTiming',
      'String',
      'Kiểm soát thời điểm tiêu đề cuộc trò chuyện được tạo cho endpoint agents. Các giá trị hợp lệ: "immediate" hoặc "final".',
      'Defaults to "immediate".',
    ],
  ]}
/>

**Mặc định:** `"immediate"`

**Các giá trị khả dụng:**

- **`"immediate"`**: Tạo tiêu đề ngay khi yêu cầu bắt đầu, song song với phản hồi của model, sử dụng tin nhắn đầu tiên của người dùng.
- **`"final"`**: Trì hoãn việc tạo tiêu đề cho đến khi phản hồi hoàn tất. Điều này bảo toàn hành vi cũ.

**Ví dụ:**

```yaml filename="endpoints / agents / titleTiming"
titleTiming: immediate
```

## activityLabel

Đặt `activityLabel: true` để nhóm từng khối suy luận của Agent và các lệnh gọi công cụ dưới một tiêu đề một dòng được tạo tự động. Sử dụng `activityModel`, `activityEndpoint`, `activityPrompt`, `activityMaxPerRun` và `activityCharLimit` để kiểm soát lệnh gọi model bổ sung và chi phí của nó. Xem [Agent Activity Groups](/docs/configuration/librechat_yaml/object_structure/shared_endpoint_settings#agent-activity-groups) để biết tài liệu tham khảo đầy đủ về các trường và quy tắc ưu tiên.

```yaml filename="endpoints / agents / activity groups"
activityLabel: true
activityEndpoint: openAI
activityModel: gpt-4.1-nano
activityMaxPerRun: 20
activityCharLimit: 600
activityPhaseLabel: true
activityPhaseMaxPerRun: 5
```

`activityPhaseLabel` có thể tự động thêm một bản tóm tắt cha đã thu gọn xung quanh mỗi giai đoạn chạy (run phase) chứa ít nhất hai hoạt động logic. Sử dụng `activityPhaseModel`, `activityPhaseEndpoint`, `activityPhasePrompt` và `activityPhaseMaxPerRun` để tinh chỉnh các lệnh gọi đó; `activityCharLimit` được chia sẻ với các nhãn hoạt động con.

## disableBuilder

<OptionTable
  options={[
    [
      'disableBuilder',
      'Boolean',
      'Kiểm soát khả năng hiển thị và việc sử dụng giao diện trình tạo cho các tác nhân (agents).',
      'When set to `true`, disables the builder interface for the agent, limiting direct manual interaction.',
    ],
  ]}
/>

**Mặc định:** `false`

**Ví dụ:**

```yaml filename="endpoints / agents / disableBuilder"
disableBuilder: false
```

## allowedProviders

<OptionTable
  options={[
    [
      'allowedProviders',
      'Array/List of Strings',
      'Chỉ định danh sách các nhà cung cấp endpoint (ví dụ: "openAI", "anthropic", "google") được phép sử dụng với tính năng Agents.',
      'If defined, only agents configured with these providers can be initialized. If omitted or empty, all configured providers are allowed.',
    ],
  ]}
/>

**Mặc định:** `[]` (danh sách trống, cho phép tất cả các nhà cung cấp)

**Lưu ý:** Phải là một trong các giá trị sau, hoặc tên endpoint tùy chỉnh như được định nghĩa trong [cấu hình](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#name) của bạn: - `openAI, azureOpenAI, google, anthropic, assistants, azureAssistants, bedrock`

**Ví dụ:**

```yaml filename="endpoints / agents / allowedProviders"
allowedProviders:
  - openAI
  - google
```

## capabilities

<OptionTable
  options={[
    [
      'capabilities',
      'Array/List of Strings',
      'Chỉ định các khả năng của tác nhân (agent) khả dụng cho tất cả người dùng đối với endpoint agents.',
      'Defines the agent capabilities that are available to all users for the agents endpoint. You can omit the capabilities you wish to exclude from the list.',
    ],
  ]}
/>

**Mặc định:** `["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]`

`programmatic_tools`, `stateful_code_sessions`, `run_in_background`, và `tool_intents` là các tính năng tùy chọn (opt-in) và phải được thêm một cách rõ ràng. Các công cụ lập trình (programmatic tools) và phiên làm việc có trạng thái (stateful sessions) cũng yêu cầu `execute_code` và một triển khai Code Interpreter tương thích. Việc thực thi mã trong nền (background code execution) yêu cầu `run_in_background`, `execute_code` và Code Interpreter.

**Ví dụ:**

```yaml filename="endpoints / agents / capabilities"
capabilities:
  - 'deferred_tools'
  # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
  # Requires execute_code and a Code Interpreter deployment with the Tool Call Server component.
  # - 'programmatic_tools'
  - 'execute_code'
  - 'file_search'
  - 'web_search'
  - 'artifacts'
  - 'subagents'
  - 'actions'
  - 'context'
  - 'skills'
  - 'memory'
  - 'ask_user_question'
  - 'tools'
  - 'chain'
  - 'ocr'
  # Optional: reuse one Code Interpreter workspace per conversation.
  # - 'stateful_code_sessions'
  # Optional: allow selected eligible MCP and Code Interpreter tools to run as background tasks.
  # - 'run_in_background'
  # Optional: show live model-written intent labels for native and selected MCP tools.
  # - 'tool_intents'
```

**Lưu ý:** Trường này là tùy chọn. Nếu bỏ qua, hành vi mặc định là bao gồm tất cả các khả năng được liệt kê trong phần mặc định.

## toolApproval

Kiểm soát việc con người xem xét các lệnh gọi công cụ của Agent. Tính năng này bị vô hiệu hóa theo mặc định. Khi được bật, các lệnh gọi khớp sẽ tạm dừng cho đến khi người dùng gửi quyết định cho phép, chẳng hạn như phê duyệt, từ chối hoặc chỉnh sửa các đối số.

<OptionTable
  options={[
    ['enabled', 'Boolean', 'Cho phép phê duyệt công cụ cho endpoint Agents.', 'false'],
    [
      'mode',
      'String',
      'Thiết lập hành vi cho các cuộc gọi không khớp: `default` sẽ hỏi, `dontAsk` sẽ từ chối và `bypass` sẽ phê duyệt trừ khi bị từ chối.',
      'default',
    ],
    ['allow', 'Array of strings', 'Các mẫu Glob cho các lệnh gọi có thể chạy mà không cần nhắc.', ''],
    ['deny', 'Array of strings', 'Các mẫu Glob cho các lệnh gọi luôn bị từ chối. Các quy tắc từ chối được ưu tiên.', ''],
    ['ask', 'Array of strings', 'Các mẫu Glob cho các lệnh gọi luôn yêu cầu xem xét.', ''],
    [
      'reason',
      'String',
      'Giải thích tùy chọn hiển thị trong lời nhắc phê duyệt. Sử dụng `{tool}` để chèn tên công cụ.',
      '',
    ],
    [
      'hooks',
      'Array of objects',
      'Các mô-đun chính sách lập trình đáng tin cậy cho các quyết định nhận biết ngữ cảnh. Hooks chỉ có thể thắt chặt chính sách tĩnh.',
      '',
    ],
  ]}
/>

Các mẫu sử dụng đối sánh glob. Các công cụ MCP có thể được giới hạn phạm vi bằng `mcp:<server>:<tool>`; ví dụ: `mcp:github:*` khớp với mọi công cụ từ máy chủ `github`. Việc đánh giá ưu tiên `deny` cao nhất, bao gồm cả ở chế độ `bypass`.

```yaml filename="endpoints / agents / toolApproval"
toolApproval:
  enabled: true
  mode: default
  allow:
    - 'mcp:analytics:read_*'
  deny:
    - 'mcp:*:delete_*'
  ask:
    - 'mcp:*:*'
  reason: 'Review {tool} before it runs.'
```

Các Hooks được tải một lần khi khởi động máy chủ và thực thi trong tiến trình LibreChat. Mỗi mục nhập chấp nhận một `module`, một `matcher` regex tùy chọn cho tên công cụ, và một đối tượng `options` tùy chọn được truyền tới trình xây dựng của module. Chỉ tải mã mà bạn tin tưởng. Các ghi đè theo vai trò hoặc theo đối tượng thuê (tenant) không tải lại các module hook; hãy triển khai hành vi theo ngữ cảnh bên trong hook.

```yaml filename="endpoints / agents / toolApproval / hooks"
toolApproval:
  enabled: true
  hooks:
    - module: '@acme/librechat-approval-hook'
      matcher: '^mcp:production:'
      options:
        requireTicket: true
```

## checkpointer

Cấu hình lưu trữ cho các lần chạy Agent bị tạm dừng do phê duyệt công cụ hoặc Ask User. Khi một trong hai tính năng này cần một checkpointer mà khối này bị bỏ qua, LibreChat sẽ sử dụng MongoDB chính với thời gian chờ quyết định là 24 giờ.

<OptionTable
  options={[
    [
      'type',
      'String',
      '`mongo` duy trì các lượt chạy bị tạm dừng qua các lần khởi động lại và bản sao; `memory` là cục bộ theo tiến trình và dành cho mục đích phát triển.',
      'mongo',
    ],
    ['ttl', 'Number', 'Thời gian chờ đợi quyết định cho một lượt chạy bị tạm dừng, tính bằng giây.', '86400'],
    [
      'checkpointCollectionName',
      'String',
      'Ghi đè tên collection checkpoint MongoDB tùy chọn.',
      'agent_checkpoints',
    ],
    [
      'checkpointWritesCollectionName',
      'String',
      'Ghi đè tên collection checkpoint-write của MongoDB tùy chọn.',
      'agent_checkpoint_writes',
    ],
  ]}
/>

```yaml filename="endpoints / agents / checkpointer"
checkpointer:
  type: mongo
  ttl: 86400
```

Chỉ sử dụng `type: memory` cho triển khai phát triển đơn tiến trình. Một tiến trình tạm dừng được lưu trữ trong bộ nhớ sẽ không thể tiếp tục sau khi khởi động lại hoặc trên một bản sao khác.

Các điểm kiểm tra (checkpoints) của MongoDB sẽ ghi lại cảnh báo khi trạng thái được tuần tự hóa vượt quá 8 MiB và từ chối tạm dừng bền vững (durable pause) nếu vượt quá 15 MiB, nhằm duy trì khoảng trống dưới giới hạn tài liệu 16 MiB của MongoDB. Các tệp phương tiện nội dòng (inlined media) hoặc đầu ra của công cụ (tool outputs) có dung lượng lớn là những nguyên nhân phổ biến; hãy bắt đầu một cuộc trò chuyện mới hoặc giảm bớt ngữ cảnh của cuộc trò chuyện nếu một lần chạy báo lỗi `CHECKPOINT_TOO_LARGE`.

## skills

Kiểm soát các cài đặt Skills ở cấp độ endpoint cho các agent.

<OptionTable
  options={[
    [
      'skills.maxCatalogSkills',
      'Number',
      'Giới hạn số lượng Skill khả dụng đang hoạt động được hiển thị trong danh mục mà mô hình có thể truy cập. Phải nằm trong khoảng từ 1 đến 100.',
      'maxCatalogSkills: 20',
    ],
  ]}
/>

**Mặc định:** Không có giới hạn nào được cấu hình ngoài giới hạn danh mục thời gian chạy.

**Ví dụ:**

```yaml filename="endpoints / agents / skills"
skills:
  maxCatalogSkills: 20
```

Điều này không vô hiệu hóa Skills. Hãy sử dụng khả năng `skills` và phạm vi kỹ năng theo từng agent/model-spec để kiểm soát việc liệu Skills có khả dụng hay không.

## maxCitations

<OptionTable
  options={[
    [
      'maxCitations',
      'Number',
      'Kiểm soát tổng số trích dẫn tối đa có thể được bao gồm trong một phản hồi của agent.',
      'When using file_search capability, limits the total number of source citations returned to prevent overwhelming responses while ensuring comprehensive coverage.',
    ],
  ]}
/>

**Mặc định:** `30`

**Phạm vi:** `1-50`

**Ví dụ:**

```yaml filename="endpoints / agents / maxCitations"
maxCitations: 30
```

## maxCitationsPerFile

<OptionTable
  options={[
    [
      'maxCitationsPerFile',
      'Number',
      'Giới hạn số lượng trích dẫn tối đa có thể được trích xuất từ bất kỳ tệp đơn lẻ nào.',
      'Ensures citation diversity by preventing any single file from dominating the citations, encouraging representation from multiple sources.',
    ],
  ]}
/>

**Mặc định:** `7`

**Phạm vi:** `1-10`

**Ví dụ:**

```yaml filename="endpoints / agents / maxCitationsPerFile"
maxCitationsPerFile: 7
```

## minRelevanceScore

<OptionTable
  options={[
    [
      'minRelevanceScore',
      'Number',
      'Đặt ngưỡng điểm liên quan tối thiểu để các nguồn được bao gồm trong phản hồi.',
      'Filters out low-quality matches based on vector similarity scores. Higher values (e.g., 0.7) ensure only highly relevant sources are cited, while lower values (e.g., 0.0) include all sources regardless of quality.',
    ],
  ]}
/>

**Mặc định:** `0.45` (ngưỡng liên quan 45%)

**Phạm vi:** `0.0-1.0`

**Ví dụ:**

```yaml filename="endpoints / agents / minRelevanceScore"
minRelevanceScore: 0.45
```

### Các ví dụ về cấu hình trích dẫn tệp [#file-citation-configuration-examples]

**Cấu hình mặc định (Cân bằng)**

```yaml
endpoints:
  agents:
    maxCitations: 30
    maxCitationsPerFile: 7
    minRelevanceScore: 0.45
```

Cung cấp các trích dẫn toàn diện trong khi vẫn ngăn chặn các phản hồi quá tải và lọc bỏ các kết quả khớp chất lượng thấp.

**Cấu hình nghiêm ngặt (Chất lượng cao)**

```yaml
endpoints:
  agents:
    maxCitations: 10
    maxCitationsPerFile: 3
    minRelevanceScore: 0.7
```

Chỉ bao gồm các trích dẫn có độ liên quan cao với các giới hạn nghiêm ngặt để có phản hồi tập trung.

**Cấu hình toàn diện (Nghiên cứu)**

```yaml
endpoints:
  agents:
    maxCitations: 50
    maxCitationsPerFile: 10
    minRelevanceScore: 0.0
```

Trích xuất thông tin tối đa cho các tác vụ nghiên cứu chuyên sâu, bao gồm tất cả các nguồn bất kể mức độ liên quan.

## Khả năng của Agent [#agent-capabilities]

Trường `capabilities` cho phép bạn bật hoặc tắt các chức năng cụ thể cho các tác nhân (agents). Các khả năng khả dụng bao gồm:

- **deferred_tools**: Cho phép các agent khám phá các MCP tool bị trì hoãn (deferred) tại thời điểm chạy thay vì tải mọi tool vào ngữ cảnh ngay từ đầu.
- **programmatic_tools**: Cho phép Gọi Công cụ Lập trình (Programmatic Tool Calling) đối với các công cụ MCP được đánh dấu là Programmatic trong Agent Builder. Yêu cầu `execute_code` và một triển khai Code Interpreter với thành phần Tool Call Server. Tính năng này là tùy chọn và không được bật theo mặc định.
- **execute_code**: Cho phép tác nhân (agent) thực thi mã.
- **stateful_code_sessions**: Cho phép các agent đã chọn sử dụng lại một không gian làm việc Code Interpreter cho mỗi cuộc trò chuyện. Yêu cầu `execute_code`, một triển khai Code Interpreter tương thích và cài đặt Advanced cho từng agent. Tính năng này mang tính thử nghiệm cao, không được bật theo mặc định và có thể thay đổi đáng kể trong quá trình thử nghiệm; đừng coi hành vi hoặc cấu hình hiện tại của nó là một hợp đồng sản xuất ổn định.
- **file_search**: Cho phép agent tìm kiếm và tương tác với các tệp tin. Khi được bật, hành vi trích dẫn sẽ được kiểm soát bởi các cài đặt `maxCitations`, `maxCitationsPerFile` và `minRelevanceScore`.
- **web_search**: Kích hoạt chức năng tìm kiếm web cho các tác nhân (agents), cho phép chúng tìm kiếm và truy xuất thông tin từ internet.
- **artifacts**: Cho phép tác nhân tạo ra các artifacts tương tác (các thành phần React, HTML, sơ đồ Mermaid).
- **subagents**: Cho phép chạy các agent con trong ngữ cảnh biệt lập. Xem [Subagents](/docs/features/subagents).
- **actions**: Cho phép tác nhân thực hiện các hành động được xác định trước.
- **context**: Kích hoạt chức năng "Upload as Text" (Tải lên dưới dạng văn bản) trong trò chuyện và "File Context" (Ngữ cảnh tệp) cho các tác nhân (agents), cho phép người dùng tải lên các tệp tin để nội dung của chúng được trích xuất và đưa trực tiếp vào cuộc trò chuyện.
- **skills**: Kích hoạt Skills trong bảng điều khiển bên, gọi thủ công bằng `$`, các kỹ năng do model gọi và danh sách cho phép kỹ năng của agent. Xem [Skills](/docs/features/skills).
- **memory**: Cho phép các agent sử dụng `set_memory` và `delete_memory` khi memory được cấu hình và người dùng có quyền truy cập. Được bật theo mặc định; bị loại bỏ tại thời điểm chạy khi memory bị vô hiệu hóa.
- **ask_user_question**: Cho phép các agent tạm dừng để đặt từ một đến bốn câu hỏi liên quan trong một biểu mẫu duy nhất và tiếp tục với các câu trả lời. Được bật theo mặc định.
- **tools**: Cấp cho tác nhân quyền truy cập vào các công cụ khác nhau.
- **chain**: Kích hoạt tính năng Beta cho việc tạo chuỗi tác nhân (agent chaining), còn được gọi là quy trình làm việc Mixture-of-Agents (MoA).
- **ocr**: Tùy chọn nâng cao tính năng "Tải lên dưới dạng văn bản" (Upload as Text) trong trò chuyện và "Ngữ cảnh tệp" (File Context) cho các tác nhân (agents), cho phép tải tệp lên và xử lý bằng OCR. **Yêu cầu phải cấu hình một dịch vụ OCR.**
- **run_in_background**: Cho phép thực thi Code Interpreter và các công cụ shell chạy ngầm theo mặc định và kích hoạt tùy chọn chạy ngầm cho từng công cụ MCP. Một agent có thể chủ động từ chối Code Interpreter. Cài đặt này cho phép thực thi chạy ngầm; mô hình sẽ quyết định cho mỗi lần gọi xem có sử dụng tính năng này hay không. Yêu cầu `execute_code` và một triển khai Code Interpreter đã được cấu hình cho mã nguồn. Khả năng này không được bật theo mặc định.
- **tool_intents**: Thêm các nhãn ý định (intent labels) do mô hình viết trực tiếp vào các lệnh gọi công cụ đủ điều kiện. Các công cụ gốc, bao gồm cả công cụ Code Interpreter, sẽ tự động tham gia; các công cụ MCP có thể được bật riêng lẻ hoặc hàng loạt trong Agent Builder. Model specs sử dụng `describeIntent`. Tính năng này không được bật theo mặc định.

Bằng cách chỉ định các capabilities, bạn có thể kiểm soát các tính năng khả dụng cho người dùng khi tương tác với các agent.

## Cấu hình ví dụ [#example-configuration]

Dưới đây là ví dụ về cách cấu hình endpoint `agents` với các khả năng tùy chỉnh và cài đặt trích dẫn tệp:

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    disableBuilder: false
    # File citation configuration
    maxCitations: 20
    maxCitationsPerFile: 5
    minRelevanceScore: 0.6
    # Custom capabilities
    capabilities:
      # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
      # - 'programmatic_tools'
      - 'execute_code'
      # Optional: makes Code Interpreter background-eligible and enables selected MCP tools.
      # - 'run_in_background'
      # Optional: enables live intent labels for native and selected MCP tools.
      # - 'tool_intents'
      - 'file_search'
      - 'skills'
      - 'subagents'
      - 'actions'
      - 'artifacts'
      - 'context'
      - 'ocr'
      - 'web_search'
```

Trong ví dụ này:

- Giao diện trình tạo đã được bật
- Các trích dẫn tệp bị giới hạn ở mức 20 trích dẫn, với tối đa 5 trích dẫn cho mỗi tệp
- Chỉ các nguồn có độ liên quan từ 60% trở lên mới được bao gồm
- Các Agent của LibreChat có quyền truy cập vào thực thi mã, tìm kiếm tệp (có trích dẫn), Skills, Subagents, actions, artifacts, ngữ cảnh tệp, các dịch vụ ocr nếu được cấu hình và khả năng tìm kiếm web.
- Programmatic Tool Calling vẫn bị vô hiệu hóa trừ khi bạn thêm khả năng `programmatic_tools` cùng với `execute_code`
- Các lệnh gọi công cụ trong nền vẫn bị vô hiệu hóa trừ khi bạn thêm `run_in_background`; các công cụ Code Interpreter sau đó sẽ mặc định đủ điều kiện, trong khi các công cụ MCP vẫn yêu cầu phải chọn từng công cụ một.
- Các nhãn ý định công cụ (tool intent labels) vẫn bị vô hiệu hóa trừ khi bạn thêm `tool_intents`; các công cụ gốc (native tools) sau đó sẽ tự động chọn tham gia, trong khi các công cụ MCP vẫn yêu cầu lựa chọn cho từng công cụ.

## remoteApi

Cấu hình cho xác thực API Remote Agent. Kiểm soát cách các dịch vụ bên ngoài xác thực khi gọi các endpoint của Agents API.

### remoteApi.auth

<OptionTable
  options={[
    [
      'auth',
      'Object',
      'Cấu hình xác thực cho Remote Agent API.',
      'Supports API key and/or OIDC Bearer token authentication. If omitted, only API key auth is active.',
    ],
  ]}
/>

#### remoteApi.auth.apiKey

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Bật xác thực khóa API cho Remote Agent API.',
      'When true, requests with a valid LibreChat API key are accepted. Can be used alongside or instead of OIDC.',
    ],
  ]}
/>

**Mặc định:** `true`

#### remoteApi.auth.oidc

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Bật xác thực OIDC Bearer token.',
      'When true, the middleware validates Bearer tokens against the configured OIDC issuer via JWKS.',
    ],
    [
      'issuer',
      'String',
      'URL nhà cung cấp OIDC.',
      'The base URL of your OIDC provider, such as a Keycloak realm URL. Used for token issuer validation and JWKS discovery if jwksUri is not set.',
    ],
    [
      'jwksUri',
      'String',
      'URL endpoint JWKS. Tùy chọn.',
      'If omitted, resolved automatically via {issuer}/.well-known/openid-configuration. You can also set OPENID_JWKS_URL as an alternative.',
    ],
    [
      'audience',
      'String',
      'Đối tượng token dự kiến. Bắt buộc khi OIDC auth được bật.',
      'Tokens must contain this value in their aud claim.',
    ],
    [
      'scope',
      'String',
      'Giá trị phạm vi bắt buộc. Tùy chọn.',
      'If set, the token must contain this value in its scp or scope claim. Use this to distinguish token intent across different APIs.',
    ],
  ]}
/>

**Mặc định:** `enabled: false`

**Ví dụ - Chỉ OIDC:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: false
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          audience: my-client-id
```

**Ví dụ - OIDC với dự phòng API key:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: true
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          # jwksUri is optional and auto-discovered if omitted
          jwksUri: https://auth.example.com/realms/myrealm/protocol/openid-connect/certs
          audience: my-client-id
```

<Callout type="info">
  Thứ tự ưu tiên phân giải JWKS URI là `jwksUri` tường minh, sau đó đến `OPENID_JWKS_URL`, và cuối cùng là tự động khám phá thông qua `{issuer}/.well-known/openid-configuration`.
</Callout>

<Callout type="info">
  Việc khớp người dùng OIDC sử dụng claim `sub` làm phương thức tra cứu chính, với phương thức dự phòng là các claim `email`, `preferred_username`, hoặc `upn`. Người dùng được khớp phải đã tồn tại trong LibreChat.
</Callout>

## Subagents [#subagents]

Trường `subagents` kiểm soát các tác nhân con biệt lập mà một tác nhân cha có thể tạo ra khi khả năng `subagents` khả dụng.

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Thêm công cụ tạo subagent vào agent này khi được đặt là true. Mặc định: bị vô hiệu hóa.',
      'enabled: true',
    ],
    [
      'allowSelf',
      'Boolean',
      'Cho phép tác nhân tự khởi tạo trong một ngữ cảnh biệt lập mới. Mặc định: true.',
      'allowSelf: true',
    ],
    [
      'agent_ids',
      'Array/List of Strings',
      'Các agent cụ thể mà agent này có thể tạo ra. Tối đa: 10.',
      'agent_ids: ["agent_researcher"]',
    ],
  ]}
/>

```yaml filename="Agent subagents"
subagents:
  enabled: true
  allowSelf: true
  agent_ids:
    - 'agent_researcher'
    - 'agent_reviewer'
```

Để biết về hành vi và giới hạn đối với người dùng, hãy xem [Subagents](/docs/features/subagents).

## Ghi chú [#notes]

- Không khuyến khích việc vô hiệu hóa giao diện trình tạo (builder interface) trừ khi bạn đang sử dụng [modelSpecs](/docs/configuration/librechat_yaml/object_structure/model_specs) để xác định danh sách các tác nhân (agents) để lựa chọn.
- Cấu hình trích dẫn tệp (`maxCitations`, `maxCitationsPerFile`, `minRelevanceScore`) chỉ áp dụng khi khả năng `file_search` được bật.
- Điểm mức độ liên quan được tính toán bằng cách sử dụng độ tương đồng vector, trong đó 1.0 đại diện cho một kết quả khớp hoàn hảo và 0.0 đại diện cho không có sự tương đồng nào.
- Các giới hạn trích dẫn giúp cân bằng giữa việc truy xuất thông tin toàn diện với chất lượng và hiệu suất phản hồi.
- Khả năng `context` hoạt động mà không cần cấu hình OCR bằng cách sử dụng các phương thức phân tích văn bản. OCR giúp nâng cao chất lượng trích xuất khi được cấu hình.
- Tính năng `ocr` yêu cầu phải cấu hình một dịch vụ OCR (xem [OCR Configuration](/docs/configuration/librechat_yaml/object_structure/ocr)).
