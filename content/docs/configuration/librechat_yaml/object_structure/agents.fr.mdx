---
title: Structure de l'objet endpoint Agents
icon: Bot
---

Cette page s'applique à l'endpoint [`agents`](/docs/features/agents).

## Exemple [#example]

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    recursionLimit: 50
    maxRecursionLimit: 100
    maxToolCallArgBytes: 65536
    maxDeltaEventsPerTurn: 100000
    maxToolCallArgBytesByTool:
      create_file: 131072
    disableBuilder: false
    # (optional) Agent Capabilities available to all users. Omit the ones you wish to exclude. Defaults to list below.
    # Opt-in capabilities: "programmatic_tools", "stateful_code_sessions", "run_in_background", and "tool_intents".
    # capabilities: ["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]
    # (optional) File citation configuration for file_search capability
    maxCitations: 30 # Maximum total citations in responses (1-50)
    maxCitationsPerFile: 7 # Maximum citations from each file (1-10)
    minRelevanceScore: 0.45 # Minimum relevance score threshold (0.0-1.0)
    titleTiming: immediate
    activityLabel: true
    activityModel: gpt-4.1-nano
    activityPhaseLabel: true
    activityPhaseMaxPerRun: 5
    maxSubagents: 10 # Maximum explicit subagents per agent (1-50)
    skills:
      maxCatalogSkills: 20
    toolApproval:
      enabled: true
      mode: default
      deny: ['mcp:*:delete_*']
      ask: ['mcp:*:*']
    checkpointer:
      type: mongo
      ttl: 86400
    remoteApi:
      auth:
        oidc:
          enabled: false
```

> Cette configuration active l'interface de création pour les agents.

## recursionLimit

<OptionTable
  options={[
    [
      'recursionLimit',
      'Number',
      'Définit le nombre par défaut d\'étapes qu\'un agent peut effectuer lors d\'une exécution.',
      'Controls recursion depth to prevent infinite loops. When limit is reached, raises GraphRecursionError. This value can be configured from the UI up to the maxRecursionLimit.',
    ],
  ]}
/>

**Par défaut :** `25`

**Exemple :**

```yaml filename="endpoints / agents / recursionLimit"
recursionLimit: 50
```

Pour plus d'informations sur les étapes de l'agent, consultez [Max Agent Steps](/docs/features/agents#max-agent-steps).

## maxRecursionLimit

<OptionTable
  options={[
    [
      'maxRecursionLimit',
      'Number',
      'Définit le nombre maximal absolu d\'étapes qu\'un agent peut effectuer lors d\'une exécution.',
      'Defines the upper limit for the recursionLimit that can be set from the UI. This prevents users from setting excessively high values.',
    ],
  ]}
/>

**Par défaut :** Si omis, utilise la valeur de recursionLimit ou 50 si recursionLimit est également omis.

**Exemple :**

```yaml filename="endpoints / agents / maxRecursionLimit"
maxRecursionLimit: 100
```

Pour plus d'informations sur les étapes de l'agent, consultez [Max Agent Steps](/docs/features/agents#max-agent-steps).

## Disjoncteurs de flux (Stream circuit breakers) [#stream-circuit-breakers]

Ces paramètres arrêtent l'exécution d'un Agent lorsqu'un flux de fournisseur semble produire des arguments d'outil incontrôlés ou une séquence illimitée d'événements delta.

<OptionTable
  options={[
    [
      'maxToolCallArgBytes',
      'Number',
      'Nombre maximal d\'octets cumulés pour les arguments d\'un appel d\'outil en flux. Définissez sur 0 pour désactiver la protection globale.',
      '65536',
    ],
    [
      'maxDeltaEventsPerTurn',
      'Number',
      'Nombre maximal d\'événements diffusés pour un tour de génération de modèle. Définissez sur 0 ou omettez pour désactiver cette protection.',
      '0 (disabled)',
    ],
    [
      'maxToolCallArgBytesByTool',
      'Object',
      'Remplacements d\'octets d\'argument par outil, indexés par le nom de l\'outil exposé au modèle. Une valeur de 0 désactive la protection pour cet outil.',
      'create_file: 131072',
    ],
  ]}
/>

LibreChat utilise une limite globale d'arguments de 64 KiB et intègre une valeur de remplacement de 128 KiB pour `create_file`, dont les charges utiles légitimes sont souvent plus volumineuses. Les entrées dans `maxToolCallArgBytesByTool` fusionnent par-dessus cette valeur de remplacement intégrée. Si l'une ou l'autre de ces protections est déclenchée, LibreChat interrompt le flux du fournisseur en cours et signale quelle limite a été dépassée.

```yaml filename="endpoints / agents / stream circuit breakers"
maxToolCallArgBytes: 65536
maxDeltaEventsPerTurn: 100000
maxToolCallArgBytesByTool:
  create_file: 262144
  large_import: 524288
```

Pour désactiver toutes les protections de taille d'argument, définissez à la fois la limite globale et la substitution `create_file` fournie sur `0` :

```yaml
maxToolCallArgBytes: 0
maxToolCallArgBytesByTool:
  create_file: 0
```

## titleTiming

<OptionTable
  options={[
    [
      'titleTiming',
      'String',
      'Contrôle le moment où les titres de conversation sont générés pour l\'endpoint agents. Valeurs valides : "immediate" ou "final".',
      'Defaults to "immediate".',
    ],
  ]}
/>

**Par défaut :** `"immediate"`

**Valeurs disponibles :**

- **`"immediate"`** : Génère le titre dès que la requête commence, en parallèle avec la réponse du modèle, en utilisant le premier message de l'utilisateur.
- **`"final"`** : Diffère la génération du titre jusqu'à ce que la réponse complète soit terminée. Cela préserve le comportement hérité.

**Exemple :**

```yaml filename="endpoints / agents / titleTiming"
titleTiming: immediate
```

## activityLabel

Définissez `activityLabel: true` pour regrouper chaque bloc de raisonnement de l'Agent et les appels d'outils sous un en-tête généré sur une ligne. Utilisez `activityModel`, `activityEndpoint`, `activityPrompt`, `activityMaxPerRun` et `activityCharLimit` pour contrôler l'appel de modèle supplémentaire et son coût. Consultez [Agent Activity Groups](/docs/configuration/librechat_yaml/object_structure/shared_endpoint_settings#agent-activity-groups) pour obtenir la référence complète des champs et les règles de priorité.

```yaml filename="endpoints / agents / activity groups"
activityLabel: true
activityEndpoint: openAI
activityModel: gpt-4.1-nano
activityMaxPerRun: 20
activityCharLimit: 600
activityPhaseLabel: true
activityPhaseMaxPerRun: 5
```

`activityPhaseLabel` peut ajouter indépendamment un résumé parent réduit autour de chaque phase d'exécution qui contient au moins deux activités logiques. Utilisez `activityPhaseModel`, `activityPhaseEndpoint`, `activityPhasePrompt` et `activityPhaseMaxPerRun` pour ajuster ces appels ; `activityCharLimit` est partagé avec les étiquettes d'activité enfants.

## disableBuilder

<OptionTable
  options={[
    [
      'disableBuilder',
      'Boolean',
      'Contrôle la visibilité et l\'utilisation de l\'interface de création pour les agents.',
      'When set to `true`, disables the builder interface for the agent, limiting direct manual interaction.',
    ],
  ]}
/>

**Par défaut :** `false`

**Exemple :**

```yaml filename="endpoints / agents / disableBuilder"
disableBuilder: false
```

## allowedProviders

<OptionTable
  options={[
    [
      'allowedProviders',
      'Array/List of Strings',
      'Spécifie une liste de fournisseurs d\'endpoint (par exemple, "openAI", "anthropic", "google") autorisés pour une utilisation avec la fonctionnalité Agents.',
      'If defined, only agents configured with these providers can be initialized. If omitted or empty, all configured providers are allowed.',
    ],
  ]}
/>

**Par défaut :** `[]` (liste vide, tous les fournisseurs sont autorisés)

**Remarque :** Doit être l'un des éléments suivants, ou un nom de endpoint personnalisé tel que défini dans votre [configuration](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#name) : - `openAI, azureOpenAI, google, anthropic, assistants, azureAssistants, bedrock`

**Exemple :**

```yaml filename="endpoints / agents / allowedProviders"
allowedProviders:
  - openAI
  - google
```

## capabilities

<OptionTable
  options={[
    [
      'capabilities',
      'Array/List of Strings',
      'Spécifie les capacités d\'agent disponibles pour tous les utilisateurs pour l\'endpoint agents.',
      'Defines the agent capabilities that are available to all users for the agents endpoint. You can omit the capabilities you wish to exclude from the list.',
    ],
  ]}
/>

**Par défaut :** `["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]`

`programmatic_tools`, `stateful_code_sessions`, `run_in_background` et `tool_intents` sont des options à activer et doivent être ajoutées explicitement. Les outils programmatiques et les sessions avec état nécessitent également `execute_code` et un déploiement de Code Interpreter compatible. L'exécution de code en arrière-plan nécessite `run_in_background`, `execute_code` et Code Interpreter.

**Exemple :**

```yaml filename="endpoints / agents / capabilities"
capabilities:
  - 'deferred_tools'
  # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
  # Requires execute_code and a Code Interpreter deployment with the Tool Call Server component.
  # - 'programmatic_tools'
  - 'execute_code'
  - 'file_search'
  - 'web_search'
  - 'artifacts'
  - 'subagents'
  - 'actions'
  - 'context'
  - 'skills'
  - 'memory'
  - 'ask_user_question'
  - 'tools'
  - 'chain'
  - 'ocr'
  # Optional: reuse one Code Interpreter workspace per conversation.
  # - 'stateful_code_sessions'
  # Optional: allow selected eligible MCP and Code Interpreter tools to run as background tasks.
  # - 'run_in_background'
  # Optional: show live model-written intent labels for native and selected MCP tools.
  # - 'tool_intents'
```

**Remarque :** Ce champ est facultatif. S'il est omis, le comportement par défaut consiste à inclure toutes les capacités listées dans le défaut.

## toolApproval

Contrôle la révision humaine pour les appels d'outils des Agents. Cette fonctionnalité est désactivée par défaut. Lorsqu'elle est activée, les appels correspondants sont mis en pause jusqu'à ce que l'utilisateur soumette une décision autorisée, telle que l'approbation, le rejet ou la modification des arguments.

<OptionTable
  options={[
    ['enabled', 'Boolean', 'Active l\'approbation des outils pour l\'endpoint Agents.', 'false'],
    [
      'mode',
      'String',
      'Définit le comportement pour les appels non correspondants : `default` demande, `dontAsk` refuse, et `bypass` approuve sauf en cas de refus.',
      'default',
    ],
    ['allow', 'Array of strings', 'Modèles glob pour les appels pouvant être exécutés sans invite.', ''],
    ['deny', 'Array of strings', 'Modèles glob pour les appels qui sont toujours refusés. Les règles de refus sont prioritaires.', ''],
    ['ask', 'Array of strings', 'Modèles glob pour les appels nécessitant toujours une révision.', ''],
    [
      'reason',
      'String',
      'Explication facultative affichée dans l\'invite d\'approbation. Utilisez `{tool}` pour insérer le nom de l\'outil.',
      '',
    ],
    [
      'hooks',
      'Array of objects',
      'Modules de politique programmatiques de confiance pour des décisions tenant compte du contexte. Les hooks ne peuvent que renforcer la politique statique.',
      '',
    ],
  ]}
/>

Les modèles utilisent la correspondance glob. Les outils MCP peuvent être délimités avec `mcp:<server>:<tool>` ; par exemple, `mcp:github:*` correspond à tous les outils du serveur `github`. L'évaluation donne la priorité la plus élevée à `deny`, y compris en mode `bypass`.

```yaml filename="endpoints / agents / toolApproval"
toolApproval:
  enabled: true
  mode: default
  allow:
    - 'mcp:analytics:read_*'
  deny:
    - 'mcp:*:delete_*'
  ask:
    - 'mcp:*:*'
  reason: 'Review {tool} before it runs.'
```

Les Hooks sont chargés une fois au démarrage du serveur et s'exécutent dans le processus LibreChat. Chaque entrée accepte un `module`, un `matcher` regex optionnel pour le nom de l'outil, et un objet `options` optionnel transmis au constructeur du module. Ne chargez que du code en lequel vous avez confiance. Les remplacements par rôle ou par locataire ne rechargent pas les modules de hook ; implémentez le comportement contextuel à l'intérieur du hook.

```yaml filename="endpoints / agents / toolApproval / hooks"
toolApproval:
  enabled: true
  hooks:
    - module: '@acme/librechat-approval-hook'
      matcher: '^mcp:production:'
      options:
        requireTicket: true
```

## checkpointer

Configure le stockage pour les exécutions d'Agent mises en pause par l'approbation d'outils ou la fonctionnalité Ask User. Lorsque l'une ou l'autre de ces fonctionnalités nécessite un checkpointer et que ce bloc est omis, LibreChat utilise la base de données MongoDB principale avec une fenêtre de décision de 24 heures.

<OptionTable
  options={[
    [
      'type',
      'String',
      '`mongo` conserve les exécutions en pause entre les redémarrages et les répliques ; `memory` est local au processus et destiné au développement.',
      'mongo',
    ],
    ['ttl', 'Number', 'Durée pendant laquelle une exécution en pause attend une décision, en secondes.', '86400'],
    [
      'checkpointCollectionName',
      'String',
      'Remplacement optionnel du nom de la collection de points de contrôle MongoDB.',
      'agent_checkpoints',
    ],
    [
      'checkpointWritesCollectionName',
      'String',
      'Remplacement optionnel du nom de la collection de points de contrôle d\'écriture MongoDB.',
      'agent_checkpoint_writes',
    ],
  ]}
/>

```yaml filename="endpoints / agents / checkpointer"
checkpointer:
  type: mongo
  ttl: 86400
```

Utilisez `type: memory` uniquement pour un déploiement de développement à processus unique. Une exécution en pause stockée en mémoire ne peut pas reprendre après un redémarrage ou sur une autre réplique.

Les points de contrôle MongoDB enregistrent un avertissement lorsque l'état sérialisé dépasse 8 MiB et rejettent une pause durable au-delà de 15 MiB, laissant une marge sous la limite de document de 16 MiB de MongoDB. Les médias intégrés volumineux ou les sorties d'outils sont des causes fréquentes ; démarrez une nouvelle conversation ou réduisez son contexte si une exécution signale `CHECKPOINT_TOO_LARGE`.

## skills

Contrôle les paramètres de Skills au niveau de l'endpoint pour les agents.

<OptionTable
  options={[
    [
      'skills.maxCatalogSkills',
      'Number',
      'Limite le nombre de Skills accessibles et actifs exposés dans le catalogue visible par le modèle. Doit être compris entre 1 et 100.',
      'maxCatalogSkills: 20',
    ],
  ]}
/>

**Par défaut :** Aucune limite configurée au-delà de la limite du catalogue d'exécution.

**Exemple :**

```yaml filename="endpoints / agents / skills"
skills:
  maxCatalogSkills: 20
```

Ceci ne désactive pas les Skills. Utilisez la capacité `skills` et la définition de portée des skills par agent/Model Specs pour contrôler si les Skills sont disponibles.

## maxSubagents

<OptionTable
  options={[
    [
      'maxSubagents',
      'Number',
      'Nombre maximal de sous-agents explicites qu\'un seul agent peut référencer.',
      'Applies to the `agent_ids` allowlist on agents and on model specs, and to configured subagent `graphs`, which agents support but model specs do not. Must be a whole number between 1 and 50.',
    ],
  ]}
/>

**Par défaut :** `10`

**Plage :** `1-50`

**Exemple :**

```yaml filename="endpoints / agents / maxSubagents"
endpoints:
  agents:
    maxSubagents: 20
```

Augmentez cette valeur lorsqu'un déploiement exécute des agents à forte orchestration qui doivent déléguer à plus de dix enfants. La valeur est lue au démarrage et appliquée à trois endroits :

- Requêtes de création, de mise à jour et de duplication d'agents, pour `subagents.agent_ids` et `subagents.graphs`
- Les listes d'autorisation `subagents.agent_ids` des Model Specs, validées lors de la même passe de configuration
- Le panneau Agent Builder, qui cesse d'accepter de nouvelles entrées de sous-agents à la valeur configurée

Les Model Specs n'acceptent que `enabled`, `allowSelf` et `agent_ids` sous `subagents`. Les `graphs` de sous-agents sont une fonctionnalité au niveau de l'agent : une clé `graphs` dans une Model Spec est ignorée lors de l'analyse de la configuration, sans générer d'erreur.

<Callout type="warning">
  `maxSubagents` doit être un nombre entier compris entre 1 et 50. Une valeur en dehors de cette plage échoue à la validation de `librechat.yaml`, et LibreChat enregistre l'erreur et s'arrête au démarrage au lieu de revenir à la valeur par défaut. Démarrer avec `CONFIG_BYPASS_VALIDATION=true` permet d'éviter l'arrêt, mais la configuration personnalisée entière est ignorée dans ce cas, donc la limite revient à `10`. Pour revenir délibérément à la valeur par défaut, supprimez la clé : un `maxSubagents` omis est résolu à `10` sans erreur.
</Callout>

<Callout type="note">
  La limite s'applique à l'ensemble du processus et provient de `librechat.yaml`. Une substitution dans la base de données par utilisateur ou par groupe de `endpoints.agents.maxSubagents` est reflétée dans la configuration servie, mais n'est pas appliquée par la validation des requêtes.
</Callout>

Augmenter cette valeur modifie uniquement le nombre de sous-agents qu'un agent peut référencer. La profondeur, le nœud de graphe et les limites de configuration d'exécution ne sont pas affectés. Voir [Subagents](/docs/features/subagents#limits).

## maxCitations

<OptionTable
  options={[
    [
      'maxCitations',
      'Number',
      'Contrôle le nombre total maximal de citations pouvant être incluses dans une seule réponse de l\'agent.',
      'When using file_search capability, limits the total number of source citations returned to prevent overwhelming responses while ensuring comprehensive coverage.',
    ],
  ]}
/>

**Par défaut :** `30`

**Plage :** `1-50`

**Exemple :**

```yaml filename="endpoints / agents / maxCitations"
maxCitations: 30
```

## maxCitationsPerFile

<OptionTable
  options={[
    [
      'maxCitationsPerFile',
      'Number',
      'Limite le nombre maximal de citations pouvant être extraites d\'un seul fichier.',
      'Ensures citation diversity by preventing any single file from dominating the citations, encouraging representation from multiple sources.',
    ],
  ]}
/>

**Par défaut :** `7`

**Plage :** `1-10`

**Exemple :**

```yaml filename="endpoints / agents / maxCitationsPerFile"
maxCitationsPerFile: 7
```

## minRelevanceScore

<OptionTable
  options={[
    [
      'minRelevanceScore',
      'Number',
      'Définit le seuil de score de pertinence minimal pour que les sources soient incluses dans les réponses.',
      'Filters out low-quality matches based on vector similarity scores. Higher values (e.g., 0.7) ensure only highly relevant sources are cited, while lower values (e.g., 0.0) include all sources regardless of quality.',
    ],
  ]}
/>

**Par défaut :** `0.45` (seuil de pertinence de 45 %)

**Plage :** `0.0-1.0`

**Exemple :**

```yaml filename="endpoints / agents / minRelevanceScore"
minRelevanceScore: 0.45
```

### Exemples de configuration de citation de fichiers [#file-citation-configuration-examples]

**Configuration par défaut (Équilibrée)**

```yaml
endpoints:
  agents:
    maxCitations: 30
    maxCitationsPerFile: 7
    minRelevanceScore: 0.45
```

Fournit des citations complètes tout en évitant les réponses surchargées et en filtrant les correspondances de faible qualité.

**Configuration stricte (Haute qualité)**

```yaml
endpoints:
  agents:
    maxCitations: 10
    maxCitationsPerFile: 3
    minRelevanceScore: 0.7
```

Inclut uniquement des citations hautement pertinentes avec des limites strictes pour des réponses ciblées.

**Configuration complète (Recherche)**

```yaml
endpoints:
  agents:
    maxCitations: 50
    maxCitationsPerFile: 10
    minRelevanceScore: 0.0
```

Extraction d'informations maximale pour des tâches de recherche exhaustives, incluant toutes les sources indépendamment de leur pertinence.

## Capacités des agents [#agent-capabilities]

Le champ `capabilities` vous permet d'activer ou de désactiver des fonctionnalités spécifiques pour les agents. Les capacités disponibles sont :

- **deferred_tools** : Permet aux agents de découvrir les outils MCP différés au moment de l'exécution au lieu de charger chaque outil dans le contexte au préalable.
- **programmatic_tools** : Active l'appel de programme par outils (Programmatic Tool Calling) pour les outils MCP marqués comme Programmatic dans l'Agent Builder. Nécessite `execute_code` et un déploiement de Code Interpreter avec le composant Tool Call Server. Cette fonctionnalité est optionnelle et n'est pas activée par défaut.
- **execute_code** : Permet à l'agent d'exécuter du code.
- **stateful_code_sessions** : Permet aux agents ayant opté pour cette fonctionnalité de réutiliser un espace de travail Code Interpreter par conversation. Nécessite `execute_code`, un déploiement Code Interpreter compatible et le paramètre avancé par agent. Cette fonctionnalité est hautement expérimentale, n'est pas activée par défaut et peut changer considérablement au cours de l'expérimentation ; ne considérez pas son comportement ou sa configuration actuelle comme un contrat de production stable.
- **file_search** : Permet à l'agent de rechercher et d'interagir avec des fichiers. Lorsqu'il est activé, le comportement des citations est contrôlé par les paramètres `maxCitations`, `maxCitationsPerFile` et `minRelevanceScore`.
- **web_search** : Active la fonctionnalité de recherche web pour les agents, leur permettant de rechercher et de récupérer des informations sur Internet.
- **artifacts** : Permet à l'agent de générer des artefacts interactifs (composants React, HTML, diagrammes Mermaid).
- **subagents** : Permet l'exécution d'agents enfants dans un contexte isolé. Voir [Subagents](/docs/features/subagents).
- **actions**: Permet à l'agent d'effectuer des actions prédéfinies.
- **context** : Active la fonctionnalité « Télécharger en tant que texte » dans le chat, ainsi que le « Contexte de fichier » pour les agents, permettant aux utilisateurs de télécharger des fichiers et de faire extraire leur contenu pour l'inclure directement dans la conversation.
- **skills** : Active les Skills dans le panneau latéral, l'invocation manuelle via `$`, les skills invoqués par le modèle et les listes d'autorisation de skills pour les agents. Voir [Skills](/docs/features/skills).
- **memory** : Permet aux agents d'utiliser `set_memory` et `delete_memory` lorsque la mémoire est configurée et que l'utilisateur y a accès. Activé par défaut ; supprimé au moment de l'exécution lorsque la mémoire est désactivée.
- **ask_user_question** : Permet aux agents de faire une pause pour poser une à quatre questions connexes dans un seul formulaire et de reprendre avec les réponses. Activé par défaut.
- **tools** : Accorde à l'agent l'accès à divers outils.
- **chain** : Active la fonctionnalité bêta pour le chaînage d'agents, également connue sous le nom de flux de travail Mixture-of-Agents (MoA).
- **ocr** : Améliore optionnellement « Téléverser en tant que texte » dans le chat, ainsi que le « Contexte de fichier » pour les agents, permettant aux fichiers d'être téléversés et traités avec l'OCR. **Nécessite la configuration d'un service OCR.**
- **run_in_background** : Rend l'exécution de Code Interpreter et des outils shell éligibles à l'arrière-plan par défaut et permet une option d'activation en arrière-plan par outil pour les outils MCP. Un agent peut explicitement désactiver Code Interpreter. Ce paramètre autorise l'exécution en arrière-plan ; le modèle décide par appel s'il doit l'utiliser. Nécessite `execute_code` et un déploiement Code Interpreter configuré pour le code. Cette fonctionnalité n'est pas activée par défaut.
- **tool_intents** : Ajoute des étiquettes d'intention rédigées en direct par le modèle aux appels d'outils éligibles. Les outils natifs, y compris les outils d'interprétation de code (Code Interpreter), s'activent automatiquement ; les outils MCP peuvent être activés individuellement ou en masse dans le générateur d'agents (Agent Builder). Les Model Specs utilisent `describeIntent`. Cette fonctionnalité n'est pas activée par défaut.

En spécifiant les capacités, vous pouvez contrôler les fonctionnalités disponibles pour les utilisateurs lorsqu'ils interagissent avec des agents.

## Exemple de configuration [#example-configuration]

Voici un exemple de configuration de l'endpoint `agents` avec des capacités personnalisées et des paramètres de citation de fichiers :

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    disableBuilder: false
    # File citation configuration
    maxCitations: 20
    maxCitationsPerFile: 5
    minRelevanceScore: 0.6
    # Custom capabilities
    capabilities:
      # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
      # - 'programmatic_tools'
      - 'execute_code'
      # Optional: makes Code Interpreter background-eligible and enables selected MCP tools.
      # - 'run_in_background'
      # Optional: enables live intent labels for native and selected MCP tools.
      # - 'tool_intents'
      - 'file_search'
      - 'skills'
      - 'subagents'
      - 'actions'
      - 'artifacts'
      - 'context'
      - 'ocr'
      - 'web_search'
```

Dans cet exemple :

- L'interface du constructeur est activée
- Les citations de fichiers sont limitées à 20 au total, avec un maximum de 5 par fichier.
- Seules les sources ayant une pertinence de 60 % ou plus sont incluses
- Les agents LibreChat ont accès à l'exécution de code, à la recherche de fichiers (avec citations), aux Skills, aux Subagents, aux actions, aux artifacts, au contexte de fichier, aux services OCR s'ils sont configurés, ainsi qu'aux capacités de recherche web.
- L'appel d'outils programmatique reste désactivé à moins que vous n'ajoutiez la capacité `programmatic_tools` en plus de `execute_code`
- Les appels d'outils en arrière-plan restent désactivés à moins que vous n'ajoutiez `run_in_background` ; les outils Code Interpreter deviennent alors éligibles par défaut, tandis que les outils MCP nécessitent toujours une sélection outil par outil.
- Les étiquettes d'intention des outils restent désactivées à moins que vous n'ajoutiez `tool_intents` ; les outils natifs s'activent alors automatiquement, tandis que les outils MCP nécessitent toujours une sélection par outil.

## remoteApi

Configuration pour l'authentification de l'API Remote Agent. Contrôle la manière dont les services externes s'authentifient lors de l'appel des endpoints de l'API Agents.

### remoteApi.auth

<OptionTable
  options={[
    [
      'auth',
      'Object',
      'Configuration de l\'authentification pour l\'API Remote Agent.',
      'Supports API key and/or OIDC Bearer token authentication. If omitted, only API key auth is active.',
    ],
  ]}
/>

#### remoteApi.auth.apiKey

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Activer l\'authentification par clé API pour l\'API Remote Agent.',
      'When true, requests with a valid LibreChat API key are accepted. Can be used alongside or instead of OIDC.',
    ],
  ]}
/>

**Par défaut :** `true`

#### remoteApi.auth.oidc

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Activer l\'authentification par jeton Bearer OIDC.',
      'When true, the middleware validates Bearer tokens against the configured OIDC issuer via JWKS.',
    ],
    [
      'issuer',
      'String',
      'URL de l\'émetteur OIDC.',
      'The base URL of your OIDC provider, such as a Keycloak realm URL. Used for token issuer validation and JWKS discovery if jwksUri is not set.',
    ],
    [
      'jwksUri',
      'String',
      'URL du endpoint JWKS. Optionnel.',
      'If omitted, resolved automatically via {issuer}/.well-known/openid-configuration. You can also set OPENID_JWKS_URL as an alternative.',
    ],
    [
      'audience',
      'String',
      'Public cible du jeton attendu. Requis lorsque l\'authentification OIDC est activée.',
      'Tokens must contain this value in their aud claim.',
    ],
    [
      'scope',
      'String',
      'Valeur de portée requise. Optionnel.',
      'If set, the token must contain this value in its scp or scope claim. Use this to distinguish token intent across different APIs.',
    ],
  ]}
/>

**Par défaut :** `enabled: false`

**Exemple - OIDC uniquement :**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: false
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          audience: my-client-id
```

**Exemple - OIDC avec repli sur clé API :**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: true
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          # jwksUri is optional and auto-discovered if omitted
          jwksUri: https://auth.example.com/realms/myrealm/protocol/openid-connect/certs
          audience: my-client-id
```

<Callout type="info">
  La priorité de résolution de l'URI JWKS est l'attribut explicite `jwksUri`, suivi de `OPENID_JWKS_URL`, puis de la découverte automatique via `{issuer}/.well-known/openid-configuration`.
</Callout>

<Callout type="info">
  La correspondance des utilisateurs OIDC utilise la revendication `sub` comme recherche principale, avec une solution de repli vers les revendications `email`, `preferred_username` ou `upn`. L'utilisateur correspondant doit déjà exister dans LibreChat.
</Callout>

## Sous-agents [#subagents]

Le champ `subagents` contrôle quels agents enfants isolés un agent parent peut générer lorsque la fonctionnalité `subagents` est disponible.

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Ajoute l\'outil de génération de sous-agent à cet agent lorsque défini sur true. Par défaut : désactivé.',
      'enabled: true',
    ],
    [
      'allowSelf',
      'Boolean',
      'Permet à l\'agent de se générer lui-même dans un contexte isolé et nouveau. Par défaut : true.',
      'allowSelf: true',
    ],
    [
      'agent_ids',
      'Array/List of Strings',
      'Agents spécifiques que cet agent peut générer. Limité par `maxSubagents`, qui est défini par défaut à 10.',
      'agent_ids: ["agent_researcher"]',
    ],
  ]}
/>

```yaml filename="Agent subagents"
subagents:
  enabled: true
  allowSelf: true
  agent_ids:
    - 'agent_researcher'
    - 'agent_reviewer'
```

Pour le comportement et les limites côté utilisateur, consultez [Subagents](/docs/features/subagents).

## Notes [#notes]

- Il n'est pas recommandé de désactiver l'interface du générateur à moins que vous n'utilisiez [modelSpecs](/docs/configuration/librechat_yaml/object_structure/model_specs) pour définir une liste d'agents parmi lesquels choisir.
- La configuration des citations de fichiers (`maxCitations`, `maxCitationsPerFile`, `minRelevanceScore`) s'applique uniquement lorsque la fonctionnalité `file_search` est activée.
- Le score de pertinence est calculé en utilisant la similarité vectorielle, où 1.0 représente une correspondance parfaite et 0.0 représente aucune similarité.
- Les limites de citation aident à équilibrer la récupération d'informations complètes avec la qualité et la performance des réponses.
- La fonctionnalité `context` fonctionne sans configuration OCR en utilisant des méthodes d'analyse de texte. L'OCR améliore la qualité de l'extraction lorsqu'il est configuré.
- La fonctionnalité `ocr` nécessite la configuration d'un service OCR (voir [Configuration OCR](/docs/configuration/librechat_yaml/object_structure/ocr)).
- `maxSubagents` limite le nombre de sous-agents auxquels un seul agent peut faire référence. Cela ne modifie pas la profondeur ou les limites du graphe documentées sous [Subagents](/docs/features/subagents#limits).
