---
title: 网页搜索配置
icon: Search
---

`webSearch` 配置允许您自定义 LibreChat 中的网络搜索功能，包括搜索提供商、内容抓取工具和结果重排序器。

## 概述 [#overview]

Web search 功能由三个主要组件组成：

1. **搜索提供商 (Search Providers)**：执行初始网络搜索的服务
2. **Scrapers**: 从网页中提取内容的服务
3. **Rerankers**：对搜索结果进行重新排序以提高相关性的服务

## 示例 [#example]

```yaml filename="webSearch"
webSearch:
  # Search Provider Configuration
  serperApiKey: "${SERPER_API_KEY}"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}"
  searchProvider: "serper" # Options: "serper", "searxng", "tavily", "keenable"

  # Optional: query options sent to a SearXNG instance
  searxngSearchOptions:
    engines: "google,bing,startpage" # Default: "google,bing,duckduckgo"
    language: "en" # Default: "all"

  # Tavily Configuration (search and/or scraper)
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"

  # Keenable Configuration (search and/or scraper; all credentials optional)
  keenableApiKey: "${KEENABLE_API_KEY}"
  keenableApiUrl: "${KEENABLE_API_URL}"
  keenableSearchOptions:
    maxResults: 8
    site: "example.com"
    attributionTitle: "LibreChat"
    timeout: 15000
  keenableScraperOptions:
    attributionTitle: "LibreChat"
    timeout: 15000

  # Scraper Configuration
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlApiUrl: "${FIRECRAWL_API_URL}"
  firecrawlVersion: "${FIRECRAWL_VERSION}"
  scraperProvider: "firecrawl" # Options: "firecrawl", "serper", "tavily", "keenable"

  # Reranker Configuration
  jinaApiKey: "${JINA_API_KEY}"
  jinaApiUrl: "${JINA_API_URL}"
  cohereApiKey: "${COHERE_API_KEY}"
  rerankerType: "jina" # Options: "jina", "cohere", "none"

  # General Settings
  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)
  # Required for deliberately private self-hosted provider endpoints
  allowedAddresses:
    - "searxng:8080"
```

## SSRF Protection and Private Providers [#ssrf-protection-and-private-providers]

LibreChat 会验证其为网络搜索、抓取和重排序所建立的每一个连接。私有地址、回环地址、链路本地地址以及云元数据目标地址默认会被拦截，包括重定向跳转以及在连接时解析为私有地址的主机名。

当配置的 SearXNG、Firecrawl、Jina、兼容 Tavily、兼容 Keenable 或相关提供商 endpoint 特意设为私有时，请使用 `webSearch.allowedAddresses`：

```yaml filename="webSearch / allowedAddresses"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  allowedAddresses:
    - "searxng:8080"
    - "10.0.0.5:3002"
    - "[fd00::5]:8080"
```

此字段是一个 SSRF 免除列表，而非严格的允许列表。公共目标地址仍然可用。条目必须是精确的 `host:port`、`private.ip:port` 或 `[ipv6]:port` 对；URL、路径、CIDR 范围、裸主机名/IP 以及公共 IP 字面量将被拒绝。被免除的主机名会信任其在该端口上解析出的任何私有 IP，因此建议优先使用私有 IP 字面量，并仅列出您可控制 DNS 的主机名。

LibreChat 会自动豁免已配置的 HTTP(S) 正向代理端点，以便能够访问该端点。当代理执行请求时，它会解析目标地址并必须自行强制执行目标出口限制。`NO_PROXY` 可以使请求直接发送，此时 LibreChat 的连接防护机制仍然适用。该防护机制涵盖了 LibreChat 向提供商 API 发起的连接；它无法约束第三方抓取工具作为请求数据接收并在其自身基础设施上获取的页面 URL。

## 搜索提供商 [#search-providers]

### searchProvider

<OptionTable
  options={[
    ['searchProvider', 'String', '指定要使用的搜索提供程序。', 'Options: "serper", "searxng", "tavily", "keenable"'],
  ]}
/>

### serperApiKey

<OptionTable
  options={[
    ['serperApiKey', 'String', 'Serper API 密钥的环境变量名称。如果未在 .env 中设置，系统将提示用户通过 UI 提供。', '${SERPER_API_KEY}'],
  ]}
/>

**注意：** 请从 [Serper.dev](https://serper.dev/api-keys) 获取您的 API key。


### searxngInstanceUrl

<OptionTable
  options={[
    ['searxngInstanceUrl', 'String', 'SearXNG 实例 URL 的环境变量名称。如果未在 .env 中设置，系统将提示用户通过 UI 提供。', '${SEARXNG_INSTANCE_URL}'],
  ]}
/>

### searxngApiKey

<OptionTable
  options={[
    ['searxngApiKey', 'String', 'SearXNG API 密钥的环境变量名称。如果未在 .env 中设置，系统将提示用户通过 UI 提供。', '${SEARXNG_API_KEY}'],
  ]}
/>

**注意：** 这是可选配置，仅在您的 SearXNG 实例需要身份验证时才需要。

### searxngSearchOptions

<OptionTable
  options={[
    ['searxngSearchOptions', 'Object', '发送到您的 SearXNG 实例的查询选项。每个子键均为可选，且该代码块仅在 searchProvider 为 "searxng" 时读取。', ''],
  ]}
/>

**子键 (Subkeys):**

<OptionTable
  options={[
    ['engines', 'String or Array of Strings', '您的实例应查询的引擎，将作为 engines 参数发送给 SearXNG。接受逗号分隔的字符串或 YAML 列表；两者都会被标准化为逗号分隔的形式，并去除周围的空格和空条目。修剪后为空的值将被视为未设置。', 'Default: "google,bing,duckduckgo"'],
    ['language', 'String', '结果语言代码，作为 language 参数发送给 SearXNG。请使用您的实例所接受的代码，例如 "en"、"de" 或 "fr"。', 'Default: "all"'],
    ['timeRange', 'String', '按发布日期限制结果，作为 time_range 发送给 SearXNG。未设置时将完全从请求中排除。', 'Options: "day", "month", "year"'],
    ['timeout', 'Number', '调用您的实例时的 HTTP 请求超时时间（以毫秒为单位）。必须是小于或等于 120000 的正整数；0 会被拒绝，因为它会禁用超时。', 'Default: 10000'],
  ]}
/>

<Callout type="info" title="可用性">
`searxngSearchOptions` 通过 `@librechat/agents` 中的搜索工具连接到 SearXNG，并且需要 `@librechat/agents` v3.6.9 或更高版本。针对旧版本构建的版本会忽略该代码块而不是启动失败，因此如果实例持续返回默认结果，可能只是因为运行的是旧版本构建。
</Callout>

**示例：**

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}" # Optional
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeRange: "month"
    timeout: 10000
```

`engines` 也接受 SearXNG 本身使用的逗号分隔格式，因此这与上面的列表等效：

```yaml filename="webSearch"
webSearch:
  searxngSearchOptions:
    engines: "google, bing, startpage, qwant"
```

#### 选择引擎 [#choosing-engines]

引擎名称必须与您实例中启用的引擎相匹配，由此得出以下两点：

- SearXNG 会忽略其无法识别的引擎，而不是报告错误。拼写错误或在您的实例上禁用的引擎只会导致搜索结果减少，而不会导致失败。
- 有效的名称取决于具体的实例。请检查 `https://your-instance/config` 处的已启用列表（该页面会返回包含每个已启用引擎的 JSON），或打开您实例偏好设置页面中的 **Engines** 选项卡。

默认值为 `google,bing,duckduckgo`。SearXNG 会聚合所选搜索引擎返回的所有结果，因此如果某个引擎被屏蔽，您只会损失该引擎提供的部分结果，而不是整个响应；只有当所有选定的引擎都失败或未返回任何内容时，搜索结果才会为空。

DuckDuckGo 是该默认集合中最容易被封锁的引擎，因为它会向大多数自托管实例提供验证码（CAPTCHA）。由于该集合中仅包含三个引擎，如果它与受到速率限制的 Google 或 Bing 同时失效，通常会导致完全无法获取结果。显式设置 `engines`、排除 DuckDuckGo 并列出超过三个引擎，都可以降低这种风险。

响应迅速且能够容纳自托管流量的引擎是最佳的入门选择，例如：

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - brave
      - startpage
      - qwant
```

添加更多的引擎可以扩大覆盖范围，但也会拖慢每次搜索的速度，因为 SearXNG 会在响应前等待集合中最慢的引擎。在增加列表时，请留意 `timeout` 设置。

#### 这些选项无法控制的内容 [#what-these-options-do-not-control]

- **Safe search** 不属于此区块。它来自顶层的 [`safeSearch`](#safesearch) 键，并作为 `safesearch` 转发给 SearXNG。
- **Result categories** 由 LibreChat 根据查询内容自动选择，它会将搜索类型映射为 `general`、`images`、`videos` 或 `news`。此处无法覆盖这些设置。
- **页码和结果格式**是固定的。LibreChat 始终以 JSON 格式请求第 1 页，这就是为什么您的实例必须在 `formats` 下启用 `json` 的原因。

### tavilyApiKey

<OptionTable
  options={[
    ['tavilyApiKey', 'String', 'Tavily API 密钥的环境变量名称。同时用于搜索和抓取。如果未在 .env 中设置，系统将提示用户通过 UI 提供。', '${TAVILY_API_KEY}'],
  ]}
/>

**注意：** 请从 [Tavily](https://app.tavily.com/home) 获取您的 API key。

### tavilySearchUrl

<OptionTable
  options={[
    ['tavilySearchUrl', 'String', '用于自定义 Tavily Search API URL 的环境变量名称。可选；未设置时默认为 Tavily 托管搜索。', '${TAVILY_SEARCH_URL}'],
  ]}
/>

### tavilyExtractUrl

<OptionTable
  options={[
    ['tavilyExtractUrl', 'String', '用于自定义 Tavily Extract API URL 的环境变量名称。可选；未设置时默认为 Tavily 托管的 extract。', '${TAVILY_EXTRACT_URL}'],
  ]}
/>

### tavilySearchOptions

<OptionTable
  options={[
    ['tavilySearchOptions', 'Object', 'Tavily 搜索的配置选项。', ''],
  ]}
/>

**子键 (Subkeys):**

<OptionTable
  options={[
    ['searchDepth', 'String', '控制相关性与延迟之间的权衡。“basic”为每个 URL 返回一个 NLP 摘要。“advanced”为每个 URL 返回多个语义相关的片段（消耗 2 个 API 点数）。“fast”通过片段平衡速度与相关性。“ultra-fast”通过一个 NLP 摘要将延迟降至最低。', 'Options: "basic", "advanced", "fast", "ultra-fast". Default: "basic"'],
    ['maxResults', 'Number', '返回搜索结果的最大数量。', 'Range: 1-20. Default: 5'],
    ['topic', 'String', '搜索的类别。“news”适用于实时更新，“finance”适用于财务数据。', 'Options: "general", "news", "finance". Default: "general"'],
    ['includeImages', 'Boolean', '在响应中包含图像。返回顶层查询图像和每个结果的图像。', 'Default: false'],
    ['includeAnswer', 'Boolean or String', '包含一个 LLM 生成的回答。“basic” 或 true 表示快速回答，“advanced” 表示详细回答。', 'Default: false'],
    ['includeRawContent', 'Boolean or String', '包含经过清理和解析的 HTML 内容。“markdown”或 true 表示 markdown 格式，“text”表示纯文本。', 'Default: false'],
    ['includeDomains', 'Array of Strings', '将搜索限制在特定域名内。最多 300 个域名。', ''],
    ['excludeDomains', 'Array of Strings', '从结果中排除特定域名。最多 150 个域名。', ''],
    ['timeRange', 'String', '基于发布日期或最后更新日期的时间范围筛选。', 'Options: "day", "week", "month", "year"'],
    ['includeImageDescriptions', 'Boolean', '当 includeImages 为 true 时，同时为每张图片添加描述性文本。', 'Default: false'],
    ['includeFavicon', 'Boolean', '为每个搜索结果包含 favicon URL。', 'Default: false'],
    ['chunksPerSource', 'Number', '每个来源的最大相关内容块数量。仅在 searchDepth 为 "advanced" 时可用。', 'Range: 1-3. Default: 3'],
    ['safeSearch', 'Boolean', 'Tavily Search 请求的可选 Tavily safe_search 覆盖设置。默认省略；设置为 true 可能需要 Tavily Enterprise。', 'Default: omitted'],
    ['timeout', 'Number', '客户端 HTTP 请求超时时间（以毫秒为单位）。控制在放弃之前等待 Tavily API 响应的时长。', 'Default: 15000'],
  ]}
/>

### keenableApiKey

<OptionTable
  options={[
    ['keenableApiKey', 'String', '可选 Keenable API 密钥的环境变量名称。Keenable 无需密钥即可工作；配置密钥可提高公共速率限制。', '${KEENABLE_API_KEY}'],
  ]}
/>

### keenableApiUrl

<OptionTable
  options={[
    ['keenableApiUrl', 'String', '用于自定义 Keenable 搜索 API URL 的环境变量名称。这不会更改页面获取的 endpoint。', '${KEENABLE_API_URL}'],
  ]}
/>

page-fetch endpoint 是通过仅限环境变量的 `KEENABLE_FETCH_URL` 变量进行单独配置的。

### keenableSearchOptions

<OptionTable
  options={[
    ['keenableSearchOptions', 'Object', 'Keenable 搜索的配置选项。', ''],
  ]}
/>

<OptionTable
  options={[
    ['maxResults', 'Number', '搜索结果的最大数量。', 'Range: 1-20. Default: 8'],
    ['site', 'String', '将结果限制为一个域名。', ''],
    ['attributionTitle', 'String', '在 X-Keenable-Title 归因标头中发送的值。', ''],
    ['timeout', 'Number', '客户端 HTTP 请求超时时间（以毫秒为单位）。', 'Range: 0-120000. Default: 15000'],
  ]}
/>

## Scrapers [#scrapers]

### firecrawlApiKey

<OptionTable
  options={[
    ['firecrawlApiKey', 'String', 'Firecrawl API 密钥的环境变量名称。如果未在 .env 中设置，系统将提示用户通过 UI 提供。', '${FIRECRAWL_API_KEY}'],
  ]}
/>

**注意：** 请从 [Firecrawl.dev](https://docs.firecrawl.dev/introduction#api-key) 获取您的 API key。

### firecrawlApiUrl

<OptionTable
  options={[
    ['firecrawlApiUrl', 'String', 'Firecrawl API URL 的环境变量名称。如果未在 .env 中设置，系统将提示用户通过 UI 提供。', '${FIRECRAWL_API_URL}'],
  ]}
/>

**注意：** 这是可选配置，仅在您使用自定义 Firecrawl 实例时才需要。

### firecrawlVersion

<OptionTable
  options={[
    ['firecrawlVersion', 'String', 'Firecrawl API 版本（v0 或 v1）的环境变量名称。', '${FIRECRAWL_VERSION}'],
  ]}
/>

### scraperProvider

<OptionTable
  options={[
    ['scraperProvider', 'String', '指定要使用的爬虫服务。', 'Options: "firecrawl", "serper", "tavily", "keenable"'],
  ]}
/>

### firecrawlOptions

<OptionTable
  options={[
    ['firecrawlOptions', 'Object', 'Firecrawl 爬虫的高级配置选项。', ''],
  ]}
/>

**子键 (Subkeys):**

#### formats

<OptionTable
  options={[
    ['formats', 'Array of Strings', '要在输出中包含的格式。', ''],
  ]}
/>

#### includeTags

<OptionTable
  options={[
    ['includeTags', 'Array of Strings', '输出中包含的标签。', ''],
  ]}
/>

#### excludeTags

<OptionTable
  options={[
    ['excludeTags', 'Array of Strings', '要从输出中排除的标签。', ''],
  ]}
/>

#### headers

<OptionTable
  options={[
    ['headers', 'Object', '发送请求时携带的请求头。可用于发送 cookies、user-agent 等。', ''],
  ]}
/>

#### waitFor

<OptionTable
  options={[
    ['waitFor', 'Number', '指定获取内容前的延迟时间（以毫秒为单位），以便页面有足够的时间加载。', ''],
  ]}
/>

#### timeout

<OptionTable
  options={[
    ['timeout', 'Integer', '抓取请求的超时时间（以毫秒为单位）。必须是非负整数。', 'Default: 7500'],
  ]}
/>

#### maxAge

<OptionTable
  options={[
    ['maxAge', 'Number', '如果页面缓存的存活时间（以毫秒为单位）小于此值，则返回缓存版本。如果缓存的页面版本超过此值，则会重新抓取页面。', ''],
  ]}
/>

**注意：** 如果您不需要极新的数据，启用此功能可以将抓取速度提高 500%。

#### mobile

<OptionTable
  options={[
    ['mobile', 'Boolean', '模拟从移动设备进行抓取。', ''],
  ]}
/>

#### skipTlsVerification

<OptionTable
  options={[
    ['skipTlsVerification', 'Boolean', '在发起请求时跳过 TLS 证书验证。', ''],
  ]}
/>

#### blockAds

<OptionTable
  options={[
    ['blockAds', 'Boolean', '启用广告拦截和 Cookie 弹窗拦截。', ''],
  ]}
/>

#### removeBase64Images

<OptionTable
  options={[
    ['removeBase64Images', 'Boolean', '从输出中移除所有 base 64 图像，因为它们可能会导致输出过长。图像的 alt 文本将保留在输出中，但 URL 会被替换为占位符。', ''],
  ]}
/>

#### parsePDF

<OptionTable
  options={[
    ['parsePDF', 'Boolean', '控制 PDF 文件在抓取过程中的处理方式。', ''],
  ]}
/>

#### storeInCache

<OptionTable
  options={[
    ['storeInCache', 'Boolean', '如果设为 true，页面将被存储在 Firecrawl 索引和缓存中。如果您的抓取活动可能涉及数据保护问题，将其设为 false 会很有用。使用某些与敏感抓取相关的参数（如 headers）将强制此参数设为 false。', ''],
  ]}
/>

#### zeroDataRetention

<OptionTable
  options={[
    ['zeroDataRetention', 'Boolean', '如果设为 true，将为此抓取启用零数据保留（需要预先在 Firecrawl 上进行设置）。', ''],
  ]}
/>

#### location

<OptionTable
  options={[
    ['location', 'Object', '用于抓取的地理位置和语言设置。', ''],
  ]}
/>

#### onlyMainContent

<OptionTable
  options={[
    ['onlyMainContent', 'Boolean', '仅返回页面主要内容，不包含页眉、导航、页脚等。', ''],
  ]}
/>

#### changeTrackingOptions

<OptionTable
  options={[
    ['changeTrackingOptions', 'Object', '用于跟踪抓取内容更改的配置。', ''],
  ]}
/>

**示例：**
```yaml filename="webSearch"
webSearch:
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlOptions:
    formats: ["markdown", "rawHtml"]
    includeTags: ["main", "article", ".content"]
    excludeTags: ["nav", "footer", ".ads"]
    waitFor: 2000
    timeout: 10000
    mobile: false
    blockAds: true
    onlyMainContent: true
    location:
      country: "US"
      languages: ["en"]
```

**注意：** 有关 Firecrawl 爬虫选项和默认值的详细信息，请参阅 [Firecrawl API Documentation](https://docs.firecrawl.dev/api-reference/endpoint/scrape)。

### tavilyScraperOptions

<OptionTable
  options={[
    ['tavilyScraperOptions', 'Object', 'Tavily Extract (抓取工具) 的配置选项。', ''],
  ]}
/>

**子键 (Subkeys):**

<OptionTable
  options={[
    ['extractDepth', 'String', '提取过程的深度。“advanced”可检索包括表格和嵌入内容在内的更多数据，成功率更高，但可能会增加延迟。“basic”每 5 个成功 URL 消耗 1 个积分，“advanced”每 5 个成功 URL 消耗 2 个积分。', 'Options: "basic", "advanced". Default: "basic"'],
    ['includeImages', 'Boolean', '在响应中包含从 URL 提取的图像列表。', 'Default: false'],
    ['includeFavicon', 'Boolean', '为每个提取的结果包含 favicon URL。', 'Default: false'],
    ['format', 'String', '提取的网页内容格式。“markdown”以 markdown 格式返回内容。“text”返回纯文本，可能会增加延迟。', 'Options: "markdown", "text". Default: "markdown"'],
    ['timeout', 'Number', '超时时间（以毫秒为单位）。控制客户端 HTTP 超时。设置后，它还会向 Tavily 发送一个转换为秒并限制在 1-60 秒之间的服务端提取超时时间。', 'Default: 15000 for basic, 30000 for advanced'],
  ]}
/>

**示例：**
```yaml filename="webSearch"
webSearch:
  searchProvider: tavily
  scraperProvider: tavily
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  # tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  # tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"
  tavilySearchOptions:
    searchDepth: basic
    maxResults: 5
    topic: general
  tavilyScraperOptions:
    extractDepth: basic
```

**注意：** 有关 Tavily API 选项的详细信息，请参阅 [Tavily API Documentation](https://docs.tavily.com)。

### keenableScraperOptions

<OptionTable
  options={[
    ['keenableScraperOptions', 'Object', 'Keenable 页面抓取的配置选项。', ''],
  ]}
/>

<OptionTable
  options={[
    ['attributionTitle', 'String', '在 X-Keenable-Title 归因标头中发送的值。', ''],
    ['timeout', 'Number', '客户端 HTTP 请求超时时间（以毫秒为单位）。', 'Range: 0-120000. Default: 15000'],
  ]}
/>

Keenable 可以提供搜索、页面抓取或两者兼有。其公共 endpoint 无需密钥即可工作；可选的 `keenableApiKey` 可以提高速率限制。将 `rerankerType: "none"` 设置为可避免需要重排序器（reranker）密钥：

```yaml filename="webSearch"
webSearch:
  searchProvider: keenable
  scraperProvider: keenable
  rerankerType: none
  # keenableApiKey: "${KEENABLE_API_KEY}" # Optional
  # keenableApiUrl: "${KEENABLE_API_URL}" # Optional search endpoint
  keenableSearchOptions:
    maxResults: 8
    # site: example.com
  keenableScraperOptions:
    timeout: 15000
```

## Rerankers [#rerankers]

### jinaApiKey

<OptionTable
  options={[
    ['jinaApiKey', 'String', 'Jina API 密钥的环境变量名称。如果未在 .env 中设置，系统将提示用户通过 UI 提供。', '${JINA_API_KEY}'],
  ]}
/>

**注意：** 请从 [Jina.ai](https://jina.ai/api-dashboard/) 获取您的 API key。

### jinaApiUrl

<OptionTable
  options={[
    ['jinaApiUrl', 'String', 'Jina API URL 的环境变量名称。如果未在 .env 中设置，系统将提示用户通过 UI 提供。', '${JINA_API_URL}'],
  ]}
/>

**注意：** 这是可选配置，仅在您使用自定义 Jina 实例时才需要。

### cohereApiKey

<OptionTable
  options={[
    ['cohereApiKey', 'String', 'Cohere API 密钥的环境变量名称。如果未在 .env 中设置，系统将提示用户通过 UI 提供。', '${COHERE_API_KEY}'],
  ]}
/>

**注意：** 请从 [Cohere Dashboard](https://dashboard.cohere.com/welcome/login) 获取您的 API key。

### rerankerType

<OptionTable
  options={[
    ['rerankerType', 'String', '指定要使用的重排序服务。设置为 "none" 以跳过重排序。', 'Options: "jina", "cohere", "none"'],
  ]}
/>

## 常规设置 [#general-settings]

### scraperTimeout

<OptionTable
  options={[
    ['scraperTimeout', 'Integer', '爬虫请求的超时时间（以毫秒为单位）。必须是非负整数。', 'Default: 7500'],
  ]}
/>

### safeSearch

<OptionTable
  options={[
    ['safeSearch', 'Number', '安全搜索过滤级别。0 = 关闭（无过滤），1 = 中等（默认），2 = 严格（最大程度过滤）。', 'Default: 1 (MODERATE)'],
  ]}
/>

**注意：** 安全搜索级别与标准搜索 API 约定保持一致。默认启用 MODERATE 过滤，以在保持搜索有效性的同时提供合理的内容过滤。Tavily 默认不会继承此全局设置；仅当您的 Tavily 账户支持 `safe_search` 时，才使用 `tavilySearchOptions.safeSearch`。

## 注意事项 [#notes]

- API keys 可以通过两种方式进行配置：
  1. 设置 YAML 配置中指定的环境变量
  2. 如果未设置环境变量，系统将提示用户通过 UI 提供 API 密钥。
- 该配置支持每个组件（providers、scrapers、rerankers）的多种服务。
- 如果未指定特定的服务类型，系统将尝试该类别下的所有可用服务。
- 安全搜索提供三个级别的内容过滤：OFF (0)、MODERATE (1) 和 STRICT (2)
- Tavily 默认不会继承全局安全搜索设置；仅当您的 Tavily 账户支持 `safe_search` 时，才显式设置 `tavilySearchOptions.safeSearch`。
- Keenable 支持无密钥搜索和页面抓取；其可选密钥可提高公共速率限制，且其抓取 URL 覆盖仅限环境配置。
- SearXNG 查询行为（引擎、语言、时间范围、请求超时）在 `searxngSearchOptions` 下进行配置；如果搜索结果为空，请参阅 [Choosing engines](#choosing-engines)。
- 切勿在 YAML 配置中放入真实的 API 密钥 - 仅使用环境变量名称

## 设置 SearXNG [#setting-up-searxng]

SearXNG 是一个注重隐私的元搜索引擎，您可以自行托管。
有关更多信息，请参阅 [SearXNG 官方文档](https://docs.searxng.org/)。

以下是为 LibreChat 设置您自己的 SearXNG 实例的步骤：

### 使用 Docker Desktop [#using-docker-desktop]

1. **搜索官方 SearXNG 镜像**
   - 打开 Docker Desktop
   - 在 **Images** 选项卡中搜索 `searxng/searxng`
   - 点击官方镜像上的 **Run**，即可自动拉取并运行该镜像的容器。

2. **运行容器**
   - 在下载完成后出现的后续面板中，展开 **Optional Settings** 下拉菜单
   - 设置您所需的配置详情（端口号、容器名称等）
   - 点击 **Run** 以启动容器

3. **配置 LibreChat 的 SearXNG**
   - 在 Docker Desktop 中导航至 `Files` 选项卡
   - 前往 `/etc/searxng/settings.yaml`
   - 打开文件编辑器
   - 导航至 `formats` 部分
   - 添加 `json` 作为可接受的格式，以便 LibreChat 能够与您的实例进行通信
   - 保存文件

4. **重启容器**
   - 重启容器以使更改生效

#### 视频指南： [#video-guide]

这是一个视频，将引导您在大约一分钟内完成从开始到结束的整个过程：

     <Video src="/videos/min_searxng_docker_setup_hq.mp4" title="SearXNG Docker 安装指南" />

**Note:** In this example, the instance URL is http://localhost:55011 (port number found under the container name in the top left at the end of the video)

### 配置 LibreChat 以使用 SearXNG [#configuring-librechat-to-use-searxng]

您可以在 LibreChat 的 UI 中或通过 `librechat.yaml` 配置 SearXNG。

#### UI 配置 [#ui-configuration]

1. **打开聊天输入栏中的工具下拉菜单**
![工具配置按钮](/images/web-search/tools_highlight.png)

2. **点击 Web Search 旁边的齿轮图标**
![Tools configuration section](/images/web-search/tools_config_highlight.png)

3. **从搜索提供商 (Search Provider) 下拉菜单中选择 SearXNG**
![SearXNG dropdown selection](/images/web-search/searxng_dropdown_highlight.png)

4. **输入您的配置详情（例如实例 URL、抓取工具类型等）并点击保存**
![保存网页搜索配置](/images/web-search/web_search_save.png)

5. **点击工具下拉菜单中的 Web Search 选项**
![Web search badge in chat interface](/images/web-search/websearch_highlight.png)

6. **Web Search 徽章现在应该已启用，这意味着您的查询现在可以使用网页搜索功能**
![Web search badge confirmation](/images/web-search/search_badge_confirm.png)

#### YAML 配置 [#yaml-configuration]

若要为实例上的所有用户配置 SearXNG，而不是按用户单独配置，请在 `librechat.yaml` 中设置相同的值：

```yaml filename="librechat.yaml"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  # searxngApiKey: "${SEARXNG_API_KEY}" # Only if your instance requires authentication
  # Required: a self-hosted instance is a private destination, so exempt it from the connection guard
  allowedAddresses:
    - "localhost:55011"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeout: 10000
```

URL 和密钥仍然从环境变量中读取，因此请将上述配置与以下内容配对：

```bash filename=".env"
SEARXNG_INSTANCE_URL=http://localhost:55011
# SEARXNG_API_KEY=your_api_key
```

<Callout type="warning" title="私有实例需要一个 allowedAddresses 条目">
`localhost:55011` 是一个回环地址，LibreChat 的 [SSRF protection](#ssrf-protection-and-private-providers) 会在连接时对其进行拦截。如果没有匹配的 `allowedAddresses` 条目，即使 SearXNG 运行正常，搜索也会失败。该条目必须与您实例 URL 中的 `host:port` 完全一致。

当 LibreChat 本身在 Docker 中运行时，`localhost` 指的是 LibreChat 容器，而不是 SearXNG 容器。请使用容器可以访问的主机名（例如 compose 服务名称），并改为豁免该主机名：

```yaml filename="librechat.yaml"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}" # SEARXNG_INSTANCE_URL=http://searxng:8080
  allowedAddresses:
    - "searxng:8080"
```
</Callout>

请参阅 [`searxngSearchOptions`](#searxngsearchoptions) 以了解此代码块支持的所有子键。

#### 排查空结果问题 [#troubleshooting-empty-results]

如果 SearXNG 搜索未返回任何结果，几乎总是因为请求未到达可用的实例，而不是 LibreChat 丢弃了结果。请按顺序排查以下内容：

1. **该实例是一个私有地址，且没有 `allowedAddresses` 条目。** LibreChat 会在连接时拦截回环地址和私有目标地址，因此自托管实例需要在 [`allowedAddresses`](#ssrf-protection-and-private-providers) 下列出其确切的 `host:port`。这会直接阻止请求，而不是返回空结果集。
2. **`json` 在 `formats` 中缺失。** LibreChat 要求以 JSON 格式返回结果。如果您的实例配置文件中的 `formats` 部分未列出 `json`，则实例会返回错误页面，且每次搜索的结果都为空。这是上述 Docker 设置的第 3 步。
3. **所有选定的引擎均失败。** SearXNG 会合并那些有响应的引擎，因此返回空结果意味着没有任何引擎返回任何内容。DuckDuckGo 包含在默认集合（`google,bing,duckduckgo`）中，但它会向大多数自托管实例提供验证码，这导致只剩下两个引擎来执行搜索。请显式设置 `searxngSearchOptions.engines`，移除 DuckDuckGo，并列出更多引擎。
4. **您的实例上不存在该引擎名称。** SearXNG 会静默跳过它无法识别的引擎，因此拼写错误只会导致搜索结果减少。请将您的列表与 `https://your-instance/config` 进行对比。
5. **该实例的响应速度慢于超时限制。** 如果您的实例查询了多个引擎或处于较慢的网络路径中，请将 `searxngSearchOptions.timeout` 的值调高至默认的 `10000` 以上。

您可以通过直接调用实例来重现 LibreChat 发送的内容：

```bash
curl -s "http://localhost:55011/search?q=librechat&format=json&engines=google,bing,startpage" | head -c 500
```

这会在您自己的机器上运行，因此它完全绕过了 LibreChat 的连接保护。如果此处返回一个空的 `results` 数组，则确认问题出在 SearXNG 一侧；如果此处有结果而 LibreChat 仍然找不到任何内容，则说明问题指向 `allowedAddresses`。
