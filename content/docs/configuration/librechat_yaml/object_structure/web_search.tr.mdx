---
title: Web Arama Yapılandırması
icon: Search
---

`webSearch` yapılandırması, arama sağlayıcıları, içerik kazıyıcılar ve sonuç yeniden sıralayıcılar dahil olmak üzere LibreChat içindeki web arama işlevini özelleştirmenize olanak tanır.

## Genel Bakış [#overview]

Web arama özelliği üç ana bileşenden oluşur:

1. **Arama Sağlayıcıları**: İlk web aramasını gerçekleştiren hizmetler
2. **Scrapers**: Web sayfalarından içerik çıkaran servisler
3. **Rerankers**: Arama sonuçlarını daha iyi alaka düzeyi için yeniden sıralayan hizmetler

## Örnek [#example]

```yaml filename="webSearch"
webSearch:
  # Search Provider Configuration
  serperApiKey: "${SERPER_API_KEY}"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}"
  searchProvider: "serper" # Options: "serper", "searxng", "tavily", "keenable"

  # Optional: query options sent to a SearXNG instance
  searxngSearchOptions:
    engines: "google,bing,startpage" # Default: "google,bing,duckduckgo"
    language: "en" # Default: "all"

  # Tavily Configuration (search and/or scraper)
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"

  # Keenable Configuration (search and/or scraper; all credentials optional)
  keenableApiKey: "${KEENABLE_API_KEY}"
  keenableApiUrl: "${KEENABLE_API_URL}"
  keenableSearchOptions:
    maxResults: 8
    site: "example.com"
    attributionTitle: "LibreChat"
    timeout: 15000
  keenableScraperOptions:
    attributionTitle: "LibreChat"
    timeout: 15000

  # Scraper Configuration
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlApiUrl: "${FIRECRAWL_API_URL}"
  firecrawlVersion: "${FIRECRAWL_VERSION}"
  scraperProvider: "firecrawl" # Options: "firecrawl", "serper", "tavily", "keenable"

  # Reranker Configuration
  jinaApiKey: "${JINA_API_KEY}"
  jinaApiUrl: "${JINA_API_URL}"
  cohereApiKey: "${COHERE_API_KEY}"
  rerankerType: "jina" # Options: "jina", "cohere", "none"

  # General Settings
  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)
  # Required for deliberately private self-hosted provider endpoints
  allowedAddresses:
    - "searxng:8080"
```

## SSRF Protection and Private Providers [#ssrf-protection-and-private-providers]

LibreChat, web araması, veri kazıma ve yeniden sıralama için açtığı her bağlantıyı doğrular. Özel, loopback, link-local ve bulut-meta veri hedefleri, yönlendirme sıçramaları ve bağlantı anında özel bir adrese çözümlenen ana bilgisayar adları da dahil olmak üzere varsayılan olarak engellenir.

Yapılandırılmış bir SearXNG, Firecrawl, Jina, Tavily uyumlu, Keenable uyumlu veya ilgili sağlayıcı endpoint'i kasıtlı olarak özel (private) olduğunda `webSearch.allowedAddresses` kullanın:

```yaml filename="webSearch / allowedAddresses"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  allowedAddresses:
    - "searxng:8080"
    - "10.0.0.5:3002"
    - "[fd00::5]:8080"
```

Bu alan bir SSRF muafiyet listesidir, katı bir izin listesi değildir. Genel hedefler erişilebilir kalmaya devam eder. Girdiler tam olarak `host:port`, `private.ip:port` veya `[ipv6]:port` çiftleri olmalıdır; URL'ler, yollar, CIDR aralıkları, çıplak ana bilgisayarlar/IP'ler ve genel IP değişmezleri reddedilir. Muaf tutulan bir ana bilgisayar adı, o portta çözümlendiği özel IP'ye güvenir, bu nedenle özel bir IP değişmezi tercih edin ve yalnızca DNS'ini kontrol ettiğiniz adları listeleyin.

LibreChat, yapılandırılmış bir HTTP(S) ileri vekil (forward-proxy) uç noktasını otomatik olarak muaf tutar, böylece bu uç noktaya erişilebilir. Vekil sunucu bir isteği taşıdığında, hedefi çözer ve hedef çıkış trafiğini kendisi zorunlu kılmalıdır. `NO_PROXY`, bir isteği doğrudan hale getirebilir; bu durumda LibreChat'in bağlantı koruması (connection guard) yine de geçerli olur. Bu koruma, LibreChat'in sağlayıcı API'lerine açtığı bağlantıları kapsar; üçüncü taraf bir kazıyıcının (scraper) istek verisi olarak aldığı ve kendi altyapısında getirdiği bir sayfa URL'sini kısıtlayamaz.

## Arama Sağlayıcıları [#search-providers]

### searchProvider

<OptionTable
  options={[
    ['searchProvider', 'String', 'Hangi arama sağlayıcısının kullanılacağını belirtir.', 'Options: "serper", "searxng", "tavily", "keenable"'],
  ]}
/>

### serperApiKey

<OptionTable
  options={[
    ['serperApiKey', 'String', 'Serper API anahtarı için ortam değişkeni adı. .env dosyasında ayarlanmazsa, kullanıcılardan bunu arayüz üzerinden sağlamaları istenecektir.', '${SERPER_API_KEY}'],
  ]}
/>

**Not:** API anahtarınızı [Serper.dev](https://serper.dev/api-keys) adresinden alın.


### searxngInstanceUrl

<OptionTable
  options={[
    ['searxngInstanceUrl', 'String', 'SearXNG örneği URL\'si için ortam değişkeni adı. .env dosyasında ayarlanmazsa, kullanıcılardan bunu arayüz üzerinden sağlamaları istenecektir.', '${SEARXNG_INSTANCE_URL}'],
  ]}
/>

### searxngApiKey

<OptionTable
  options={[
    ['searxngApiKey', 'String', 'SearXNG API anahtarı için ortam değişkeni adı. .env dosyasında ayarlanmazsa, kullanıcılardan bunu arayüz üzerinden sağlamaları istenecektir.', '${SEARXNG_API_KEY}'],
  ]}
/>

**Not:** Bu isteğe bağlıdır ve yalnızca SearXNG örneğiniz kimlik doğrulaması gerektiriyorsa gereklidir.

### searxngSearchOptions

<OptionTable
  options={[
    ['searxngSearchOptions', 'Object', 'Her aramada SearXNG örneğinize gönderilen sorgu seçenekleri. Her alt anahtar isteğe bağlıdır ve blok yalnızca searchProvider "searxng" olduğunda okunur.', ''],
  ]}
/>

**Alt anahtarlar:**

<OptionTable
  options={[
    ['engines', 'String or Array of Strings', 'Örneğinizin sorgulaması gereken ve SearXNG\'ye engines parametresi olarak gönderilen motorlar. Virgülle ayrılmış bir dizeyi veya bir YAML listesini kabul eder; her ikisi de, çevreleyen boşluklar ve boş girdiler kaldırılarak virgülle ayrılmış forma dönüştürülür. Kırpıldıktan sonra boş olan bir değer, ayarlanmamış olarak kabul edilir.', 'Default: "google,bing,duckduckgo"'],
    ['language', 'String', 'SearXNG\'ye language parametresi olarak gönderilen sonuç dili kodu. Örneğinizin kabul ettiği "en", "de" veya "fr" gibi bir kod kullanın.', 'Default: "all"'],
    ['timeRange', 'String', 'Sonuçları yayınlanma tarihine göre kısıtlar, SearXNG\'ye time_range olarak gönderilir. Ayarlanmadığında istekten tamamen çıkarılır.', 'Options: "day", "month", "year"'],
    ['timeout', 'Number', 'Örneğinize yapılan çağrılar için milisaniye cinsinden HTTP istek zaman aşımı. 120000\'den büyük olmayan pozitif bir tam sayı olmalıdır; 0, zaman aşımını devre dışı bırakacağı için reddedilir.', 'Default: 10000'],
  ]}
/>

<Callout type="info" title="Kullanılabilirlik">
`searxngSearchOptions`, `@librechat/agents` içindeki arama aracı aracılığıyla SearXNG'ye ulaşır ve `@librechat/agents` v3.6.9 veya sonraki bir sürümünü gerektirir. Daha eski bir sürüme göre oluşturulan sürümler, başlatma sırasında hata vermek yerine bloğu yok sayar; bu nedenle varsayılan sonuçları döndürmeye devam eden bir örnek, sadece daha eski bir yapı çalıştırıyor olabilir.
</Callout>

**Örnek:**

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}" # Optional
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeRange: "month"
    timeout: 10000
```

`engines` aynı zamanda SearXNG'nin kendi kullandığı virgülle ayrılmış biçimi de kabul eder, bu nedenle bu, yukarıdaki listeye eşdeğerdir:

```yaml filename="webSearch"
webSearch:
  searxngSearchOptions:
    engines: "google, bing, startpage, qwant"
```

#### Motor seçimi [#choosing-engines]

Motor adları, kendi örneğinizde (instance) etkinleştirilmiş motorlarla eşleşmelidir ve bundan iki sonuç çıkar:

- SearXNG, tanımadığı bir motoru hata bildirmek yerine görmezden gelir. Bir yazım hatası veya örneğinizde devre dışı bırakılmış bir motor, başarısızlık yerine daha az sonuç olarak görünür.
- Geçerli adlar örneğe özeldir. Etkinleştirilmiş her motoru içeren JSON döndüren `https://your-instance/config` adresindeki etkin listeyi kontrol edin veya örnek tercihler sayfanızın **Engines** sekmesini açın.

Varsayılan değer `google,bing,duckduckgo` şeklindedir. SearXNG, seçilen motorların döndürdüğü her şeyi toplar; bu nedenle engellenen bir motor, tüm yanıt yerine yalnızca kendi sonuç payının kaybına neden olur ve bir arama ancak seçilen tüm motorlar başarısız olduğunda veya hiçbir şey döndürmediğinde boş sonuç verir.

DuckDuckGo, varsayılan kümede engellenme olasılığı en yüksek olan motordur, çünkü çoğu self-hosted örneğe CAPTCHA sunar. Kümede sadece üç motor varken, onu hız sınırlamasına takılan Google veya Bing ile aynı anda kaybetmek, hiçbir sonuç alamamanın yaygın bir yoludur. `engines` ayarını açıkça yapmak, DuckDuckGo'yu dışarıda bırakmak ve üçten fazla motor listelemek bu riski azaltır.

Hızlı yanıt veren ve kendi kendine barındırılan trafiği tolere edebilen motorlar en iyi başlangıç setini oluşturur, örneğin:

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - brave
      - startpage
      - qwant
```

Daha fazla motor eklemek kapsamı genişletir ancak SearXNG yanıt vermeden önce kümedeki en yavaş motoru beklediği için her aramayı yavaşlatır. Listeyi büyütürken `timeout` değerini göz önünde bulundurun.

#### Bu seçeneklerin kontrol etmedikleri [#what-these-options-do-not-control]

- **Safe search** bu bloğun bir parçası değildir. En üst düzey [`safeSearch`](#safesearch) anahtarından gelir ve SearXNG'ye `safesearch` olarak iletilir.
- **Result categories**, LibreChat tarafından sorgu başına seçilir ve arama türünü `general`, `images`, `videos` veya `news` olarak eşleştirir. Bunlar buradan geçersiz kılınamaz.
- **Sayfa numarası ve sonuç formatı** sabittir. LibreChat her zaman JSON formatında 1. sayfayı talep eder, bu nedenle örneğinizin `formats` altında `json` özelliğinin etkinleştirilmiş olması gerekir.

### tavilyApiKey

<OptionTable
  options={[
    ['tavilyApiKey', 'String', 'Tavily API anahtarı için ortam değişkeni adı. Hem arama hem de kazıyıcı için kullanılır. .env dosyasında ayarlanmazsa, kullanıcılardan bunu arayüz üzerinden sağlamaları istenecektir.', '${TAVILY_API_KEY}'],
  ]}
/>

**Not:** API anahtarınızı [Tavily](https://app.tavily.com/home) adresinden alın.

### tavilySearchUrl

<OptionTable
  options={[
    ['tavilySearchUrl', 'String', 'Özel bir Tavily Search API URL\'si için ortam değişkeni adı. İsteğe bağlıdır; ayarlanmadığında varsayılan olarak Tavily barındırmalı arama kullanılır.', '${TAVILY_SEARCH_URL}'],
  ]}
/>

### tavilyExtractUrl

<OptionTable
  options={[
    ['tavilyExtractUrl', 'String', 'Özel bir Tavily Extract API URL\'si için ortam değişkeni adı. İsteğe bağlıdır; ayarlanmadığında varsayılan olarak Tavily barındırmalı extract kullanılır.', '${TAVILY_EXTRACT_URL}'],
  ]}
/>

### tavilySearchOptions

<OptionTable
  options={[
    ['tavilySearchOptions', 'Object', 'Tavily arama için yapılandırma seçenekleri.', ''],
  ]}
/>

**Alt anahtarlar:**

<OptionTable
  options={[
    ['searchDepth', 'String', 'Alaka düzeyi ile gecikme arasındaki dengeyi kontrol eder. "basic", URL başına bir NLP özeti döndürür. "advanced", URL başına anlamsal olarak ilgili birden fazla kesit döndürür (2 API kredisi). "fast", hız ve alaka düzeyini kesitlerle dengeler. "ultra-fast", tek bir NLP özeti ile gecikmeyi en aza indirir.', 'Options: "basic", "advanced", "fast", "ultra-fast". Default: "basic"'],
    ['maxResults', 'Number', 'Döndürülecek maksimum arama sonucu sayısı.', 'Range: 1-20. Default: 5'],
    ['topic', 'String', 'Aramanın kategorisi. "news" gerçek zamanlı güncellemeler için, "finance" ise finansal veriler için kullanışlıdır.', 'Options: "general", "news", "finance". Default: "general"'],
    ['includeImages', 'Boolean', 'Yanıta görselleri dahil eder. Hem üst düzey sorgu görsellerini hem de sonuç başına düşen görselleri döndürür.', 'Default: false'],
    ['includeAnswer', 'Boolean or String', 'LLM tarafından oluşturulan bir yanıt ekleyin. Hızlı bir yanıt için "basic" veya true, ayrıntılı bir yanıt için "advanced" değerini kullanın.', 'Default: false'],
    ['includeRawContent', 'Boolean or String', 'Temizlenmiş ve ayrıştırılmış HTML içeriğini dahil edin. Markdown formatı için "markdown" veya true, düz metin için "text" kullanın.', 'Default: false'],
    ['includeDomains', 'Array of Strings', 'Aramayı belirli alan adlarıyla sınırlandırın. Maksimum 300 alan adı.', ''],
    ['excludeDomains', 'Array of Strings', 'Sonuçlardan belirli alan adlarını hariç tutun. Maksimum 150 alan adı.', ''],
    ['timeRange', 'String', 'Yayınlanma veya son güncelleme tarihine dayalı zaman aralığı filtresi.', 'Options: "day", "week", "month", "year"'],
    ['includeImageDescriptions', 'Boolean', 'includeImages true olduğunda, her görsel için açıklayıcı bir metin de ekleyin.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Her arama sonucu için favicon URL\'sini dahil et.', 'Default: false'],
    ['chunksPerSource', 'Number', 'Kaynak başına maksimum ilgili içerik parçası sayısı. Yalnızca searchDepth "advanced" olarak ayarlandığında kullanılabilir.', 'Range: 1-3. Default: 3'],
    ['safeSearch', 'Boolean', 'Tavily Search istekleri için isteğe bağlı Tavily safe_search geçersiz kılma ayarı. Varsayılan olarak atlanmıştır; true değeri Tavily Enterprise gerektirebilir.', 'Default: omitted'],
    ['timeout', 'Number', 'Milisaniye cinsinden istemci tarafı HTTP istek zaman aşımı. Tavily API\'sinin yanıt vermesi için vazgeçmeden önce ne kadar bekleneceğini kontrol eder.', 'Default: 15000'],
  ]}
/>

### keenableApiKey

<OptionTable
  options={[
    ['keenableApiKey', 'String', 'İsteğe bağlı Keenable API anahtarı için ortam değişkeni adı. Keenable anahtar olmadan çalışır; bir anahtar yapılandırmak genel hız sınırlarını yükseltir.', '${KEENABLE_API_KEY}'],
  ]}
/>

### keenableApiUrl

<OptionTable
  options={[
    ['keenableApiUrl', 'String', 'Özel bir Keenable arama API URL\'si için ortam değişkeni adı. Bu, sayfa getirme endpoint\'ini değiştirmez.', '${KEENABLE_API_URL}'],
  ]}
/>

page-fetch endpoint'i, yalnızca ortam değişkeni olan `KEENABLE_FETCH_URL` ile ayrı olarak yapılandırılır.

### keenableSearchOptions

<OptionTable
  options={[
    ['keenableSearchOptions', 'Object', 'Keenable araması için yapılandırma seçenekleri.', ''],
  ]}
/>

<OptionTable
  options={[
    ['maxResults', 'Number', 'Maksimum arama sonucu sayısı.', 'Range: 1-20. Default: 8'],
    ['site', 'String', 'Sonuçları tek bir alan adıyla kısıtlar.', ''],
    ['attributionTitle', 'String', 'X-Keenable-Title attribution başlığında gönderilen değer.', ''],
    ['timeout', 'Number', 'İstemci tarafı HTTP isteği zaman aşımı (milisaniye cinsinden).', 'Range: 0-120000. Default: 15000'],
  ]}
/>

## Scrapers [#scrapers]

### firecrawlApiKey

<OptionTable
  options={[
    ['firecrawlApiKey', 'String', 'Firecrawl API anahtarı için ortam değişkeni adı. .env dosyasında ayarlanmazsa, kullanıcılardan bunu arayüz üzerinden sağlamaları istenecektir.', '${FIRECRAWL_API_KEY}'],
  ]}
/>

**Not:** API anahtarınızı [Firecrawl.dev](https://docs.firecrawl.dev/introduction#api-key) adresinden alın.

### firecrawlApiUrl

<OptionTable
  options={[
    ['firecrawlApiUrl', 'String', 'Firecrawl API URL\'si için ortam değişkeni adı. .env dosyasında ayarlanmazsa, kullanıcılardan bunu arayüz üzerinden sağlamaları istenecektir.', '${FIRECRAWL_API_URL}'],
  ]}
/>

**Not:** Bu isteğe bağlıdır ve yalnızca özel bir Firecrawl örneği kullanıyorsanız gereklidir.

### firecrawlVersion

<OptionTable
  options={[
    ['firecrawlVersion', 'String', 'Firecrawl API sürümü (v0 veya v1) için ortam değişkeni adı.', '${FIRECRAWL_VERSION}'],
  ]}
/>

### scraperProvider

<OptionTable
  options={[
    ['scraperProvider', 'String', 'Hangi scraper servisinin kullanılacağını belirtir.', 'Options: "firecrawl", "serper", "tavily", "keenable"'],
  ]}
/>

### firecrawlOptions

<OptionTable
  options={[
    ['firecrawlOptions', 'Object', 'Firecrawl kazıyıcı için gelişmiş yapılandırma seçenekleri.', ''],
  ]}
/>

**Alt anahtarlar:**

#### formats

<OptionTable
  options={[
    ['formats', 'Array of Strings', 'Çıktıya dahil edilecek formatlar.', ''],
  ]}
/>

#### includeTags

<OptionTable
  options={[
    ['includeTags', 'Array of Strings', 'Çıktıya dahil edilecek etiketler.', ''],
  ]}
/>

#### excludeTags

<OptionTable
  options={[
    ['excludeTags', 'Array of Strings', 'Çıktıdan hariç tutulacak etiketler.', ''],
  ]}
/>

#### headers

<OptionTable
  options={[
    ['headers', 'Object', 'İstekle birlikte gönderilecek başlıklar. Çerezleri, user-agent bilgisini vb. göndermek için kullanılabilir.', ''],
  ]}
/>

#### waitFor

<OptionTable
  options={[
    ['waitFor', 'Number', 'İçeriği getirmeden önce milisaniye cinsinden bir gecikme belirterek sayfanın yüklenmesi için yeterli zaman tanıyın.', ''],
  ]}
/>

#### timeout

<OptionTable
  options={[
    ['timeout', 'Integer', 'Kazıma isteği için milisaniye cinsinden zaman aşımı süresi. Negatif olmayan bir tam sayı olmalıdır.', 'Default: 7500'],
  ]}
/>

#### maxAge

<OptionTable
  options={[
    ['maxAge', 'Number', 'Sayfa bu yaştan (milisaniye cinsinden) daha yeniyse sayfanın önbelleğe alınmış bir sürümünü döndürür. Sayfanın önbelleğe alınmış sürümü bu değerden daha eskiyse sayfa yeniden taranacaktır.', ''],
  ]}
/>

**Not:** Çok güncel verilere ihtiyacınız yoksa, bunu etkinleştirmek tarama işlemlerinizi %500 oranında hızlandırabilir.

#### mobile

<OptionTable
  options={[
    ['mobile', 'Boolean', 'Mobil cihazdan kazıma işlemini taklit et.', ''],
  ]}
/>

#### skipTlsVerification

<OptionTable
  options={[
    ['skipTlsVerification', 'Boolean', 'İstekleri yaparken TLS sertifika doğrulamasını atla.', ''],
  ]}
/>

#### blockAds

<OptionTable
  options={[
    ['blockAds', 'Boolean', 'Reklam engellemeyi ve çerez açılır pencere engellemeyi etkinleştirir.', ''],
  ]}
/>

#### removeBase64Images

<OptionTable
  options={[
    ['removeBase64Images', 'Boolean', 'Çıktıdan, aşırı uzun olabilen tüm base 64 görselleri kaldırır. Görselin alt metni çıktıda kalır, ancak URL bir yer tutucu ile değiştirilir.', ''],
  ]}
/>

#### parsePDF

<OptionTable
  options={[
    ['parsePDF', 'Boolean', 'PDF dosyalarının tarama sırasında nasıl işleneceğini kontrol eder.', ''],
  ]}
/>

#### storeInCache

<OptionTable
  options={[
    ['storeInCache', 'Boolean', 'True olarak ayarlanırsa, sayfa Firecrawl dizininde ve önbelleğinde saklanır. Bunu false olarak ayarlamak, tarama etkinliğinizin veri koruma endişeleri taşıyabileceği durumlarda yararlıdır. Hassas tarama ile ilişkili bazı parametrelerin (başlıklar) kullanılması, bu parametreyi zorunlu olarak false yapacaktır.', ''],
  ]}
/>

#### zeroDataRetention

<OptionTable
  options={[
    ['zeroDataRetention', 'Boolean', 'Eğer true olarak ayarlanırsa, bu tarama için sıfır veri saklama etkinleştirilecektir (Firecrawl üzerinde önceden kurulum gerektirir).', ''],
  ]}
/>

#### location

<OptionTable
  options={[
    ['location', 'Object', 'Kazıma işlemleri için coğrafi konum ve dil ayarları.', ''],
  ]}
/>

#### onlyMainContent

<OptionTable
  options={[
    ['onlyMainContent', 'Boolean', 'Sayfanın yalnızca ana içeriğini döndürün; üstbilgiler, gezinme çubukları, altbilgiler vb. hariç tutulacaktır.', ''],
  ]}
/>

#### changeTrackingOptions

<OptionTable
  options={[
    ['changeTrackingOptions', 'Object', 'Kazınmış içerikteki değişiklikleri izlemek için yapılandırma.', ''],
  ]}
/>

**Örnek:**
```yaml filename="webSearch"
webSearch:
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlOptions:
    formats: ["markdown", "rawHtml"]
    includeTags: ["main", "article", ".content"]
    excludeTags: ["nav", "footer", ".ads"]
    waitFor: 2000
    timeout: 10000
    mobile: false
    blockAds: true
    onlyMainContent: true
    location:
      country: "US"
      languages: ["en"]
```

**Not:** Firecrawl kazıyıcı seçenekleri ve varsayılanları hakkında ayrıntılı bilgi için [Firecrawl API Documentation](https://docs.firecrawl.dev/api-reference/endpoint/scrape) sayfasına bakın.

### tavilyScraperOptions

<OptionTable
  options={[
    ['tavilyScraperOptions', 'Object', 'Tavily Extract (kazıyıcı) için yapılandırma seçenekleri.', ''],
  ]}
/>

**Alt anahtarlar:**

<OptionTable
  options={[
    ['extractDepth', 'String', 'Çıkarma işleminin derinliği. "advanced", tablolar ve gömülü içerikler dahil olmak üzere daha fazla veriyi daha yüksek başarı oranıyla alır ancak gecikmeyi artırabilir. "basic" 5 başarılı URL için 1 kredi, "advanced" ise 5 başarılı URL için 2 kredi tüketir.', 'Options: "basic", "advanced". Default: "basic"'],
    ['includeImages', 'Boolean', 'Yanıta URL\'lerden çıkarılan görsellerin bir listesini dahil edin.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Çıkarılan her sonuç için favicon URL\'sini dahil et.', 'Default: false'],
    ['format', 'String', 'Ayıklanan web sayfası içeriğinin formatı. "markdown", içeriği markdown formatında döndürür. "text", düz metin döndürür ve gecikmeyi artırabilir.', 'Options: "markdown", "text". Default: "markdown"'],
    ['timeout', 'Number', 'Milisaniye cinsinden zaman aşımı. İstemci tarafındaki HTTP zaman aşımını kontrol eder. Ayarlandığında, ayrıca Tavily\'ye saniyeye dönüştürülmüş ve 1-60 saniye aralığına sabitlenmiş bir sunucu tarafı çıkarma zaman aşımı gönderir.', 'Default: 15000 for basic, 30000 for advanced'],
  ]}
/>

**Örnek:**
```yaml filename="webSearch"
webSearch:
  searchProvider: tavily
  scraperProvider: tavily
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  # tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  # tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"
  tavilySearchOptions:
    searchDepth: basic
    maxResults: 5
    topic: general
  tavilyScraperOptions:
    extractDepth: basic
```

**Not:** Tavily API seçenekleri hakkında ayrıntılı bilgi için [Tavily API Documentation](https://docs.tavily.com) sayfasına bakın.

### keenableScraperOptions

<OptionTable
  options={[
    ['keenableScraperOptions', 'Object', 'Keenable sayfa getirme için yapılandırma seçenekleri.', ''],
  ]}
/>

<OptionTable
  options={[
    ['attributionTitle', 'String', 'X-Keenable-Title attribution başlığında gönderilen değer.', ''],
    ['timeout', 'Number', 'İstemci tarafı HTTP isteği zaman aşımı (milisaniye cinsinden).', 'Range: 0-120000. Default: 15000'],
  ]}
/>

Keenable arama, sayfa getirme veya her ikisini birden sağlayabilir. Genel uç noktaları anahtar olmadan çalışır; isteğe bağlı `keenableApiKey` hız sınırlarını yükseltir. Bir yeniden sıralayıcı (reranker) anahtarı gerektirmemek için `rerankerType: "none"` olarak ayarlayın:

```yaml filename="webSearch"
webSearch:
  searchProvider: keenable
  scraperProvider: keenable
  rerankerType: none
  # keenableApiKey: "${KEENABLE_API_KEY}" # Optional
  # keenableApiUrl: "${KEENABLE_API_URL}" # Optional search endpoint
  keenableSearchOptions:
    maxResults: 8
    # site: example.com
  keenableScraperOptions:
    timeout: 15000
```

## Rerankers [#rerankers]

### jinaApiKey

<OptionTable
  options={[
    ['jinaApiKey', 'String', 'Jina API anahtarı için ortam değişkeni adı. .env dosyasında ayarlanmazsa, kullanıcılardan bunu arayüz üzerinden sağlamaları istenecektir.', '${JINA_API_KEY}'],
  ]}
/>

**Not:** API anahtarınızı [Jina.ai](https://jina.ai/api-dashboard/) adresinden alın.

### jinaApiUrl

<OptionTable
  options={[
    ['jinaApiUrl', 'String', 'Jina API URL\'si için ortam değişkeni adı. .env dosyasında ayarlanmazsa, kullanıcılardan bunu arayüz üzerinden sağlamaları istenecektir.', '${JINA_API_URL}'],
  ]}
/>

**Not:** Bu isteğe bağlıdır ve yalnızca özel bir Jina örneği kullanıyorsanız gereklidir.

### cohereApiKey

<OptionTable
  options={[
    ['cohereApiKey', 'String', 'Cohere API anahtarı için ortam değişkeni adı. .env dosyasında ayarlanmazsa, kullanıcılardan bunu arayüz üzerinden sağlamaları istenecektir.', '${COHERE_API_KEY}'],
  ]}
/>

**Not:** API anahtarınızı [Cohere Dashboard](https://dashboard.cohere.com/welcome/login) adresinden alın.

### rerankerType

<OptionTable
  options={[
    ['rerankerType', 'String', 'Hangi reranker servisinin kullanılacağını belirtir. Yeniden sıralamayı (reranking) atlamak için "none" olarak ayarlayın.', 'Options: "jina", "cohere", "none"'],
  ]}
/>

## Genel Ayarlar [#general-settings]

### scraperTimeout

<OptionTable
  options={[
    ['scraperTimeout', 'Integer', 'Kazıyıcı istekleri için milisaniye cinsinden zaman aşımı süresi. Negatif olmayan bir tam sayı olmalıdır.', 'Default: 7500'],
  ]}
/>

### safeSearch

<OptionTable
  options={[
    ['safeSearch', 'Number', 'Güvenli arama filtreleme seviyesi. 0 = KAPALI (filtreleme yok), 1 = ORTA (varsayılan), 2 = SIKI (maksimum filtreleme).', 'Default: 1 (MODERATE)'],
  ]}
/>

**Not:** Güvenli arama seviyeleri, standart arama API'si kurallarıyla uyumludur. Makul bir içerik filtrelemesi sağlarken arama etkinliğini korumak için MODERATE filtreleme varsayılan olarak etkindir. Tavily, bu genel ayarı varsayılan olarak devralmaz; `tavilySearchOptions.safeSearch` öğesini yalnızca Tavily hesabınız `safe_search` özelliğini destekliyorsa kullanın.

## Notlar [#notes]

- API anahtarları iki şekilde yapılandırılabilir:
  1. YAML yapılandırmasında belirtilen ortam değişkenlerini ayarlayın
  2. Ortam değişkenleri ayarlanmadıysa, kullanıcılardan API anahtarlarını arayüz (UI) aracılığıyla sağlamaları istenecektir.
- Yapılandırma, her bileşen (providers, scrapers, rerankers) için birden fazla hizmeti destekler.
- Belirli bir hizmet türü belirtilmemişse, sistem o kategorideki tüm mevcut hizmetleri deneyecektir.
- Güvenli arama, üç seviyeli içerik filtreleme sağlar: OFF (0), MODERATE (1) ve STRICT (2)
- Tavily, varsayılan olarak genel güvenli arama ayarını devralmaz; `tavilySearchOptions.safeSearch` değerini yalnızca Tavily hesabınız `safe_search` özelliğini desteklediğinde açıkça ayarlayın.
- Keenable, anahtarsız arama ve sayfa getirmeyi destekler; isteğe bağlı anahtarı genel hız sınırlarını yükseltir ve getirme URL'si geçersiz kılma işlemi yalnızca ortam (environment) bazlıdır.
- SearXNG sorgu davranışı (motorlar, dil, zaman aralığı, istek zaman aşımı) `searxngSearchOptions` altında yapılandırılır; aramalar boş sonuç döndürürse [Choosing engines](#choosing-engines) bölümüne bakın.
- Asla gerçek API anahtarlarını YAML yapılandırmasına koymayın - yalnızca ortam değişkeni adlarını kullanın

## SearXNG Kurulumu [#setting-up-searxng]

SearXNG, kendi sunucunuzda barındırabileceğiniz, gizlilik odaklı bir meta arama motorudur. 
Daha fazla bilgi için [resmi SearXNG belgelerine](https://docs.searxng.org/) bakabilirsiniz.

LibreChat ile kullanmak üzere kendi SearXNG örneğinizi kurmanız için gereken adımlar şunlardır:

### Docker Desktop Kullanımı [#using-docker-desktop]

1. **Resmi SearXNG imajını arayın**
   - Docker Desktop'ı açın
   - **Görseller** (Images) sekmesinde `searxng/searxng` için arama yapın
   - Resmi görüntünün üzerinde **Run** düğmesine tıklayarak görüntünün bir konteynerini otomatik olarak çekin ve çalıştırın

2. **Kapsayıcıyı çalıştırma**
   - İndirme işlemi tamamlandığında açılan paneldeki **Optional Settings** açılır menüsünü genişletin
   - İstediğiniz yapılandırma ayrıntılarını (bağlantı noktası numarası, kapsayıcı adı vb.) ayarlayın
   - Konteyneri başlatmak için **Run** düğmesine tıklayın

3. **LibreChat için SearXNG yapılandırması**
   - Docker Desktop'ta `Files` sekmesine gidin
   - `/etc/searxng/settings.yaml` dosyasına gidin
   - Dosya düzenleyiciyi aç
   - `formats` bölümüne gidin
   - LibreChat'in örneğinizle iletişim kurabilmesi için kabul edilebilir bir format olarak `json` ekleyin
   - Dosyayı kaydet

4. **Kapsayıcıyı yeniden başlatın**
   - Değişikliklerin geçerli olması için konteyneri yeniden başlatın

#### Video Rehberi: [#video-guide]

Süreci baştan sona yaklaşık bir dakika içinde anlatan rehber videoyu aşağıda bulabilirsiniz:

     <Video src="/videos/min_searxng_docker_setup_hq.mp4" title="SearXNG Docker kurulum kılavuzu" />

**Not:** Bu örnekte, örnek URL'si http://localhost:55011 şeklindedir (bağlantı noktası numarası, videonun sol üst köşesindeki konteyner adının sonunda bulunabilir)

### LibreChat'i SearXNG kullanacak şekilde yapılandırma [#configuring-librechat-to-use-searxng]

SearXNG'yi LibreChat içinde arayüz üzerinden veya `librechat.yaml` aracılığıyla yapılandırabilirsiniz.

#### UI Yapılandırması [#ui-configuration]

1. **Sohbet giriş çubuğundaki araçlar açılır menüsünü açın**
![Araçlar yapılandırma düğmesi](/images/web-search/tools_highlight.png)

2. **Web Search yanındaki dişli simgesine tıklayın**
![Tools configuration section](/images/web-search/tools_config_highlight.png)

3. **Arama Sağlayıcısı açılır menüsünden SearXNG'yi seçin**
![SearXNG açılır menü seçimi](/images/web-search/searxng_dropdown_highlight.png)

4. **Yapılandırma ayrıntılarınızı (örneğin instance URL, scraper türü vb.) girin ve kaydet'e tıklayın**
![Web araması yapılandırmasını kaydet](/images/web-search/web_search_save.png)

5. **Araçlar açılır menüsündeki Web Search seçeneğine tıklayın**
![Sohbet arayüzündeki Web search rozeti](/images/web-search/websearch_highlight.png)

6. **Web Arama rozeti artık etkinleştirilmiş olmalıdır, bu da sorgularınızın artık web arama işlevini kullanabileceği anlamına gelir**
![Web arama rozeti onayı](/images/web-search/search_badge_confirm.png)

#### YAML Yapılandırması [#yaml-configuration]

SearXNG'yi kullanıcı bazlı yerine örnek (instance) üzerindeki herkes için yapılandırmak istiyorsanız, `librechat.yaml` dosyasında aynı değerleri ayarlayın:

```yaml filename="librechat.yaml"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  # searxngApiKey: "${SEARXNG_API_KEY}" # Only if your instance requires authentication
  # Required: a self-hosted instance is a private destination, so exempt it from the connection guard
  allowedAddresses:
    - "localhost:55011"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeout: 10000
```

URL ve anahtar hala ortamdan okunur, bu nedenle yukarıdaki yapılandırmayı şununla eşleştirin:

```bash filename=".env"
SEARXNG_INSTANCE_URL=http://localhost:55011
# SEARXNG_API_KEY=your_api_key
```

<Callout type="warning" title="Özel örneklerin bir allowedAddresses girdisine ihtiyacı vardır">
`localhost:55011` bir loopback adresidir ve LibreChat'in [SSRF protection](#ssrf-protection-and-private-providers) özelliği bu adresi bağlantı anında engeller. Eşleşen bir `allowedAddresses` girdisi olmadan, SearXNG sağlıklı olsa bile arama başarısız olur. Girdi, örnek URL'nizdeki tam `host:port` çifti olmalıdır.

LibreChat Docker içinde çalıştığında, `localhost` LibreChat konteyneridir, SearXNG konteyneri değildir. Konteynerin erişebileceği bir ana bilgisayar adı (hostname) kullanın, örneğin compose servis adı, ve bunun yerine onu muaf tutun:

```yaml filename="librechat.yaml"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}" # SEARXNG_INSTANCE_URL=http://searxng:8080
  allowedAddresses:
    - "searxng:8080"
```
</Callout>

Bu bloğun kabul ettiği tüm alt anahtarlar için [`searxngSearchOptions`](#searxngsearchoptions) kısmına bakın.

#### Boş sonuçlarda sorun giderme [#troubleshooting-empty-results]

Hiçbir sonuç döndürmeyen bir SearXNG araması, neredeyse her zaman LibreChat'in sonuçları düşürmesinden ziyade isteğin çalışan bir örneğe hiç ulaşmamasından kaynaklanır. Bunları sırasıyla inceleyin:

1. **Örnek, `allowedAddresses` girdisi olmayan özel bir adrestir.** LibreChat, bağlantı sırasında loopback ve özel hedefleri engeller, bu nedenle kendi kendine barındırılan bir örneğin tam `host:port` bilgisinin [`allowedAddresses`](#ssrf-protection-and-private-providers) altında listelenmesi gerekir. Bu, boş bir sonuç kümesi döndürmek yerine isteği doğrudan engeller.
2. **`json` `formats` içinde eksik.** LibreChat sonuçları JSON olarak talep eder. Eğer örnek ayar dosyanızın `formats` bölümünde `json` listelenmemişse, örnek bir hata sayfasıyla yanıt verir ve her arama boş döner. Bu, yukarıdaki Docker kurulumunun 3. adımıdır.
3. **Seçilen tüm motorlar başarısız oldu.** SearXNG, yanıt veren motorları birleştirir, bu nedenle boş bir yanıt, hiçbirinin bir şey döndürmediği anlamına gelir. DuckDuckGo varsayılan kümede (`google,bing,duckduckgo`) yer alır ve çoğu kendi kendine barındırılan (self-hosted) örneğe CAPTCHA sunar, bu da aramayı gerçekleştirmek için sadece iki motorun kalmasına neden olur. `searxngSearchOptions.engines` ayarını açıkça belirtin, DuckDuckGo'yu dışarıda bırakın ve birkaç motor daha ekleyin.
4. **Örneğinizde bir motor adı mevcut değil.** SearXNG, tanımadığı motorları sessizce atlar, bu nedenle yapılan bir yazım hatası sadece daha az sonuç alacağınız anlamına gelir. Listenizi `https://your-instance/config` adresiyle karşılaştırın.
5. **Örnek, zaman aşımı süresinden daha yavaş.** Eğer örneğiniz birçok motoru sorguluyorsa veya yavaş bir ağ yolunun arkasında yer alıyorsa `searxngSearchOptions.timeout` değerini varsayılan `10000` değerinin üzerine çıkarın.

LibreChat'in ne gönderdiğini, örneği doğrudan çağırarak yeniden oluşturabilirsiniz:

```bash
curl -s "http://localhost:55011/search?q=librechat&format=json&engines=google,bing,startpage" | head -c 500
```

Bu, kendi makinenizden çalışır, bu nedenle LibreChat'in bağlantı korumasını tamamen devre dışı bırakır. Buradaki boş bir `results` dizisi, sorunun SearXNG tarafında olduğunu doğrular; burada sonuçlar varken LibreChat hala hiçbir şey bulamıyorsa, sorun `allowedAddresses` kısmına işaret ediyor demektir.
