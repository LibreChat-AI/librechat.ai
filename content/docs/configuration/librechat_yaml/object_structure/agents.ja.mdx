---
title: Agents エンドポイントオブジェクト構造
icon: Bot
---

このページは [`agents`](/docs/features/agents) endpoint に適用されます。

## 例 [#example]

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    recursionLimit: 50
    maxRecursionLimit: 100
    maxToolCallArgBytes: 65536
    maxDeltaEventsPerTurn: 100000
    maxToolCallArgBytesByTool:
      create_file: 131072
    disableBuilder: false
    # (optional) Agent Capabilities available to all users. Omit the ones you wish to exclude. Defaults to list below.
    # Opt-in capabilities: "programmatic_tools", "stateful_code_sessions", "run_in_background", and "tool_intents".
    # capabilities: ["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]
    # (optional) File citation configuration for file_search capability
    maxCitations: 30 # Maximum total citations in responses (1-50)
    maxCitationsPerFile: 7 # Maximum citations from each file (1-10)
    minRelevanceScore: 0.45 # Minimum relevance score threshold (0.0-1.0)
    titleTiming: immediate
    activityLabel: true
    activityModel: gpt-4.1-nano
    activityPhaseLabel: true
    activityPhaseMaxPerRun: 5
    maxSubagents: 10 # Maximum explicit subagents per agent (1-50)
    skills:
      maxCatalogSkills: 20
    toolApproval:
      enabled: true
      mode: default
      deny: ['mcp:*:delete_*']
      ask: ['mcp:*:*']
    checkpointer:
      type: mongo
      ttl: 86400
    remoteApi:
      auth:
        oidc:
          enabled: false
```

> この設定により、エージェント用のビルダーインターフェースが有効になります。

## recursionLimit

<OptionTable
  options={[
    [
      'recursionLimit',
      'Number',
      'エージェントが実行時に取ることができるステップのデフォルト数を設定します。',
      'Controls recursion depth to prevent infinite loops. When limit is reached, raises GraphRecursionError. This value can be configured from the UI up to the maxRecursionLimit.',
    ],
  ]}
/>

**デフォルト:** `25`

**例:**

```yaml filename="endpoints / agents / recursionLimit"
recursionLimit: 50
```

エージェントのステップに関する詳細については、[Max Agent Steps](/docs/features/agents#max-agent-steps) を参照してください。

## maxRecursionLimit

<OptionTable
  options={[
    [
      'maxRecursionLimit',
      'Number',
      'エージェントが1回の実行で取れるステップの絶対最大数を設定します。',
      'Defines the upper limit for the recursionLimit that can be set from the UI. This prevents users from setting excessively high values.',
    ],
  ]}
/>

**デフォルト:** 省略された場合、recursionLimitの値、またはrecursionLimitも省略されている場合は50がデフォルト値となります。

**例:**

```yaml filename="endpoints / agents / maxRecursionLimit"
maxRecursionLimit: 100
```

エージェントのステップに関する詳細については、[Max Agent Steps](/docs/features/agents#max-agent-steps) を参照してください。

## ストリームサーキットブレーカー [#stream-circuit-breakers]

これらの設定は、プロバイダーのストリームが暴走したツール引数や制限のないデルタイベントのシーケンスを生成していると思われる場合に、Agentの実行を停止します。

<OptionTable
  options={[
    [
      'maxToolCallArgBytes',
      'Number',
      '1回のストリーミングツール呼び出しの引数に対する累積バイト数の上限。0に設定するとグローバルガードが無効になります。',
      '65536',
    ],
    [
      'maxDeltaEventsPerTurn',
      'Number',
      '1回のモデル生成ターンにおける最大ストリーミングイベント数。0に設定するか省略すると、このガードは無効になります。',
      '0 (disabled)',
    ],
    [
      'maxToolCallArgBytesByTool',
      'Object',
      'モデル向けのツール名でキー指定された、ツールごとの引数バイト数のオーバーライド。値を0にすると、そのツールのガードが無効になります。',
      'create_file: 131072',
    ],
  ]}
/>

LibreChatは64 KiBのグローバル引数制限を使用しており、正当なペイロードがこれより大きくなることが多い`create_file`に対しては128 KiBのオーバーライドを提供しています。`maxToolCallArgBytesByTool`内のエントリは、その組み込みオーバーライドにマージされます。いずれかのガードが作動した場合、LibreChatは実行中のプロバイダストリームを中断し、どの制限を超過したかを報告します。

```yaml filename="endpoints / agents / stream circuit breakers"
maxToolCallArgBytes: 65536
maxDeltaEventsPerTurn: 100000
maxToolCallArgBytesByTool:
  create_file: 262144
  large_import: 524288
```

すべての引数サイズのガードを無効にするには、グローバル制限と同梱されている `create_file` オーバーライドの両方を `0` に設定します。

```yaml
maxToolCallArgBytes: 0
maxToolCallArgBytesByTool:
  create_file: 0
```

## titleTiming

<OptionTable
  options={[
    [
      'titleTiming',
      'String',
      'agents endpointの会話タイトルが生成されるタイミングを制御します。有効な値: "immediate" または "final"。',
      'Defaults to "immediate".',
    ],
  ]}
/>

**デフォルト:** `"immediate"`

**利用可能な値:**

- **`"immediate"`**: リクエストが開始されると同時に、ユーザーの最初のメッセージを使用して、モデルの応答と並行してタイトルを生成します。
- **`"final"`**: タイトルの生成を完全な応答が完了するまで延期します。これは従来の動作を維持します。

**例:**

```yaml filename="endpoints / agents / titleTiming"
titleTiming: immediate
```

## activityLabel

`activityLabel: true` を設定すると、エージェントの推論とツール呼び出しの各ブロックが、生成された1行のヘッダーの下にグループ化されます。追加のモデル呼び出しとそのコストを制御するには、`activityModel`、`activityEndpoint`、`activityPrompt`、`activityMaxPerRun`、および `activityCharLimit` を使用します。フィールドの完全なリファレンスと優先順位のルールについては、[Agent Activity Groups](/docs/configuration/librechat_yaml/object_structure/shared_endpoint_settings#agent-activity-groups) を参照してください。

```yaml filename="endpoints / agents / activity groups"
activityLabel: true
activityEndpoint: openAI
activityModel: gpt-4.1-nano
activityMaxPerRun: 20
activityCharLimit: 600
activityPhaseLabel: true
activityPhaseMaxPerRun: 5
```

`activityPhaseLabel` は、少なくとも2つの論理的なアクティビティを含む各実行フェーズの周囲に、折りたたみ可能な親サマリーを個別に付加できます。これらの呼び出しを調整するには `activityPhaseModel`、`activityPhaseEndpoint`、`activityPhasePrompt`、および `activityPhaseMaxPerRun` を使用してください。`activityCharLimit` は子アクティビティラベルと共有されます。

## disableBuilder

<OptionTable
  options={[
    [
      'disableBuilder',
      'Boolean',
      'エージェント用ビルダーインターフェースの表示と使用を制御します。',
      'When set to `true`, disables the builder interface for the agent, limiting direct manual interaction.',
    ],
  ]}
/>

**デフォルト:** `false`

**例:**

```yaml filename="endpoints / agents / disableBuilder"
disableBuilder: false
```

## allowedProviders

<OptionTable
  options={[
    [
      'allowedProviders',
      'Array/List of Strings',
      'Agents機能で使用を許可するendpointプロバイダー（例: "openAI", "anthropic", "google"）のリストを指定します。',
      'If defined, only agents configured with these providers can be initialized. If omitted or empty, all configured providers are allowed.',
    ],
  ]}
/>

**デフォルト:** `[]` (空のリスト、すべてのプロバイダーが許可されます)

**注意:** 以下のいずれか、または [configuration](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#name) で定義されたカスタム endpoint 名である必要があります: - `openAI, azureOpenAI, google, anthropic, assistants, azureAssistants, bedrock`

**例:**

```yaml filename="endpoints / agents / allowedProviders"
allowedProviders:
  - openAI
  - google
```

## capabilities

<OptionTable
  options={[
    [
      'capabilities',
      'Array/List of Strings',
      'agents endpointのすべてのユーザーが利用可能なエージェント機能を指定します。',
      'Defines the agent capabilities that are available to all users for the agents endpoint. You can omit the capabilities you wish to exclude from the list.',
    ],
  ]}
/>

**デフォルト:** `["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]`

`programmatic_tools`、`stateful_code_sessions`、`run_in_background`、および `tool_intents` はオプトイン機能であり、明示的に追加する必要があります。Programmatic tools（プログラムツール）と Stateful sessions（ステートフルセッション）には、`execute_code` および互換性のある Code Interpreter デプロイメントも必要です。バックグラウンドでのコード実行には、`run_in_background`、`execute_code`、および Code Interpreter が必要です。

**例:**

```yaml filename="endpoints / agents / capabilities"
capabilities:
  - 'deferred_tools'
  # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
  # Requires execute_code and a Code Interpreter deployment with the Tool Call Server component.
  # - 'programmatic_tools'
  - 'execute_code'
  - 'file_search'
  - 'web_search'
  - 'artifacts'
  - 'subagents'
  - 'actions'
  - 'context'
  - 'skills'
  - 'memory'
  - 'ask_user_question'
  - 'tools'
  - 'chain'
  - 'ocr'
  # Optional: reuse one Code Interpreter workspace per conversation.
  # - 'stateful_code_sessions'
  # Optional: allow selected eligible MCP and Code Interpreter tools to run as background tasks.
  # - 'run_in_background'
  # Optional: show live model-written intent labels for native and selected MCP tools.
  # - 'tool_intents'
```

**注:** このフィールドは任意です。省略した場合、デフォルトの動作として、デフォルトでリストされているすべての機能が含まれます。

## toolApproval

エージェントのツール呼び出しに対する人間によるレビューを制御します。この機能はデフォルトで無効になっています。有効にすると、一致する呼び出しは、ユーザーが承認、拒否、または引数の編集といった許可の決定を送信するまで一時停止します。

<OptionTable
  options={[
    ['enabled', 'Boolean', 'Agents endpointのツール承認を有効にします。', 'false'],
    [
      'mode',
      'String',
      '一致しない呼び出しの動作を設定します：`default` は確認し、`dontAsk` は拒否し、`bypass` は拒否されない限り承認します。',
      'default',
    ],
    ['allow', 'Array of strings', 'プロンプトなしで実行可能な呼び出しのためのGlobパターン。', ''],
    ['deny', 'Array of strings', '常に拒否される呼び出しのGlobパターン。拒否ルールが優先されます。', ''],
    ['ask', 'Array of strings', '常にレビューを必要とする呼び出しのGlobパターン。', ''],
    [
      'reason',
      'String',
      'Optional explanation shown in the approval prompt. Use `{tool}` to insert the tool name.',
      '',
    ],
    [
      'hooks',
      'Array of objects',
      'コンテキストを認識した意思決定のための信頼されたプログラム可能なポリシーモジュール。フックは静的ポリシーを強化することのみ可能です。',
      '',
    ],
  ]}
/>

パターンはglobマッチングを使用します。MCPツールは `mcp:<server>:<tool>` でスコープを指定できます。例えば、`mcp:github:*` は `github` サーバーのすべてのツールにマッチします。評価では `deny` が最も高い優先度を持ち、これは `bypass` モードにおいても同様です。

```yaml filename="endpoints / agents / toolApproval"
toolApproval:
  enabled: true
  mode: default
  allow:
    - 'mcp:analytics:read_*'
  deny:
    - 'mcp:*:delete_*'
  ask:
    - 'mcp:*:*'
  reason: 'Review {tool} before it runs.'
```

Hooksはサーバー起動時に一度だけ読み込まれ、LibreChatプロセス内で実行されます。各エントリは `module`、オプションのツール名 `matcher` 正規表現、およびモジュールのビルダーに渡されるオプションの `options` オブジェクトを受け入れます。信頼できるコードのみを読み込んでください。ロールごとやテナントごとのオーバーライドではフックモジュールは再読み込みされません。コンテキストに応じた動作はフック内部で実装してください。

```yaml filename="endpoints / agents / toolApproval / hooks"
toolApproval:
  enabled: true
  hooks:
    - module: '@acme/librechat-approval-hook'
      matcher: '^mcp:production:'
      options:
        requireTicket: true
```

## checkpointer

ツール承認または「ユーザーに尋ねる (Ask User)」によって一時停止されたエージェント実行のストレージを設定します。いずれかの機能でチェックポインターが必要な場合にこのブロックが省略されると、LibreChatは24時間の決定ウィンドウを持つプライマリのMongoDBを使用します。

<OptionTable
  options={[
    [
      'type',
      'String',
      '`mongo` は再起動やレプリカをまたいで一時停止中の実行を保持しますが、`memory` はプロセスローカルであり、開発用を意図しています。',
      'mongo',
    ],
    ['ttl', 'Number', '一時停止された実行が決定を待機する時間（秒単位）。', '86400'],
    [
      'checkpointCollectionName',
      'String',
      'オプションのMongoDBチェックポイントコレクション名のオーバーライド。',
      'agent_checkpoints',
    ],
    [
      'checkpointWritesCollectionName',
      'String',
      'オプションのMongoDBチェックポイント書き込み用コレクション名のオーバーライド。',
      'agent_checkpoint_writes',
    ],
  ]}
/>

```yaml filename="endpoints / agents / checkpointer"
checkpointer:
  type: mongo
  ttl: 86400
```

`type: memory` は、単一プロセスの開発環境でのデプロイメントにのみ使用してください。メモリ内に保存された一時停止中の実行状態は、再起動後や別のレプリカ上では再開できません。

MongoDBのチェックポイントは、シリアル化された状態が8 MiBを超えると警告をログに記録し、15 MiBを超えると永続的な一時停止を拒否します。これにより、MongoDBの16 MiBのドキュメント制限を下回る余裕を確保しています。インライン化された大きなメディアやツール出力が主な原因となるため、実行時に `CHECKPOINT_TOO_LARGE` が報告された場合は、新しい会話を開始するか、コンテキストを削減してください。

## skills

エージェントのendpointレベルのSkills設定を制御します。

<OptionTable
  options={[
    [
      'skills.maxCatalogSkills',
      'Number',
      'モデルから参照可能なカタログに公開される、アクティブでアクセス可能なSkillsの数に上限を設定します。1から100の間で指定する必要があります。',
      'maxCatalogSkills: 20',
    ],
  ]}
/>

**デフォルト:** ランタイムカタログの制限を超える設定された上限はありません。

**例:**

```yaml filename="endpoints / agents / skills"
skills:
  maxCatalogSkills: 20
```

これはSkillsを無効にするものではありません。Skillsが利用可能かどうかを制御するには、`skills`機能およびエージェントごと/Model Specsごとのスキルスコープ設定を使用してください。

## maxSubagents

<OptionTable
  options={[
    [
      'maxSubagents',
      'Number',
      '単一のエージェントが参照できる明示的なサブエージェントの最大数。',
      'Applies to the `agent_ids` allowlist on agents and on model specs, and to configured subagent `graphs`, which agents support but model specs do not. Must be a whole number between 1 and 50.',
    ],
  ]}
/>

**デフォルト:** `10`

**範囲:** `1-50`

**例:**

```yaml filename="endpoints / agents / maxSubagents"
endpoints:
  agents:
    maxSubagents: 20
```

デプロイメントで10個を超える子エージェントへの委任が必要な、オーケストレーションを多用するエージェントを実行する場合にこれを引き上げてください。この値は起動時に読み取られ、以下の3箇所で適用されます：

- `subagents.agent_ids` および `subagents.graphs` に対する、エージェントの作成、更新、および複製リクエスト
- Model spec `subagents.agent_ids` の許可リスト。同じ設定パスで検証されます。
- 設定された値で新しいサブエージェントの入力を受け付けなくなる、Agent Builderパネル

Model specsは、`subagents`配下において`enabled`、`allowSelf`、および`agent_ids`のみを受け付けます。Subagentの`graphs`はエージェントレベルの機能であり、model spec上の`graphs`キーは設定が解析される際にエラーなしで破棄されます。

<Callout type="warning">
  `maxSubagents` は1から50の間の整数である必要があります。この範囲外の値は `librechat.yaml` のバリデーションで失敗し、LibreChatはデフォルト値にフォールバックする代わりにエラーをログに記録して起動時に終了します。`CONFIG_BYPASS_VALIDATION=true` を指定して起動すると終了は回避されますが、その場合はカスタム設定全体が破棄されるため、上限は `10` に戻ります。意図的にデフォルトに戻すには、キーを削除してください。`maxSubagents` を省略すると、エラーなしで `10` に解決されます。
</Callout>

<Callout type="note">
  この上限はプロセス全体に適用されるもので、`librechat.yaml` から読み込まれます。ユーザー単位またはグループ単位のデータベースによる `endpoints.agents.maxSubagents` の上書きは、提供される設定には反映されますが、リクエストの検証には適用されません。
</Callout>

これを上げても、1つのエージェントが参照できるサブエージェントの数のみが変更されます。深さ、グラフノード、および実行構成の制限には影響しません。詳細は [Subagents](/docs/features/subagents#limits) を参照してください。

## maxCitations

<OptionTable
  options={[
    [
      'maxCitations',
      'Number',
      '単一のエージェント応答に含めることができる引用の最大合計数を制御します。',
      'When using file_search capability, limits the total number of source citations returned to prevent overwhelming responses while ensuring comprehensive coverage.',
    ],
  ]}
/>

**デフォルト:** `30`

**範囲:** `1-50`

**例:**

```yaml filename="endpoints / agents / maxCitations"
maxCitations: 30
```

## maxCitationsPerFile

<OptionTable
  options={[
    [
      'maxCitationsPerFile',
      'Number',
      '1つのファイルから抽出できる引用の最大数を制限します。',
      'Ensures citation diversity by preventing any single file from dominating the citations, encouraging representation from multiple sources.',
    ],
  ]}
/>

**デフォルト:** `7`

**範囲:** `1-10`

**例:**

```yaml filename="endpoints / agents / maxCitationsPerFile"
maxCitationsPerFile: 7
```

## minRelevanceScore

<OptionTable
  options={[
    [
      'minRelevanceScore',
      'Number',
      '回答に含めるソースの最小関連度スコアのしきい値を設定します。',
      'Filters out low-quality matches based on vector similarity scores. Higher values (e.g., 0.7) ensure only highly relevant sources are cited, while lower values (e.g., 0.0) include all sources regardless of quality.',
    ],
  ]}
/>

**デフォルト:** `0.45` (関連性しきい値 45%)

**範囲:** `0.0-1.0`

**例:**

```yaml filename="endpoints / agents / minRelevanceScore"
minRelevanceScore: 0.45
```

### File Citation Configuration Examples [#file-citation-configuration-examples]

**デフォルト設定 (バランス型)**

```yaml
endpoints:
  agents:
    maxCitations: 30
    maxCitationsPerFile: 7
    minRelevanceScore: 0.45
```

包括的な引用を提供しつつ、過剰な応答を防ぎ、低品質な一致を除外します。

**厳格な設定 (高品質)**

```yaml
endpoints:
  agents:
    maxCitations: 10
    maxCitationsPerFile: 3
    minRelevanceScore: 0.7
```

焦点を絞った回答のために、厳格な制限を設けて関連性の高い引用のみを含めます。

**包括的な設定（リサーチ）**

```yaml
endpoints:
  agents:
    maxCitations: 50
    maxCitationsPerFile: 10
    minRelevanceScore: 0.0
```

網羅的なリサーチタスクのための最大限の情報抽出。関連性に関わらず、すべてのソースを含みます。

## エージェントの機能 [#agent-capabilities]

`capabilities` フィールドを使用すると、エージェントの特定の機能を有効または無効にできます。利用可能な機能は以下の通りです：

- **deferred_tools**: エージェントが実行時に MCP ツールを遅延検出できるようにし、すべてのツールを事前にコンテキストへ読み込む必要をなくします。
- **programmatic_tools**: Agent BuilderでProgrammaticとマークされたMCPツールに対して、Programmatic Tool Callingを有効にします。これには `execute_code` およびTool Call Serverコンポーネントを備えたCode Interpreterのデプロイメントが必要です。この機能はオプトインであり、デフォルトでは有効になっていません。
- **execute_code**: エージェントがコードを実行できるようにします。
- **stateful_code_sessions**: オプトインしたエージェントが、1つの会話につき1つのCode Interpreterワークスペースを再利用できるようにします。これには `execute_code`、互換性のあるCode Interpreterのデプロイメント、およびエージェントごとの詳細設定が必要です。この機能は非常に実験的なものであり、デフォルトでは有効になっておらず、実験中に大幅に変更される可能性があります。現在の動作や構成を安定した本番環境の契約として扱わないでください。
- **file_search**: エージェントがファイルを検索し、操作できるようにします。有効にすると、引用の動作は `maxCitations`、`maxCitationsPerFile`、および `minRelevanceScore` の設定によって制御されます。
- **web_search**: エージェントのウェブ検索機能を有効にし、インターネット上の情報を検索および取得できるようにします。
- **artifacts**: エージェントがインタラクティブなアーティファクト（Reactコンポーネント、HTML、Mermaidダイアグラム）を生成できるようにします。
- **subagents**: 隔離されたコンテキストでの子エージェントの実行を有効にします。詳細は [Subagents](/docs/features/subagents) を参照してください。
- **actions**: エージェントが定義済みの操作を実行することを許可します。
- **context**: チャットでの「テキストとしてアップロード」機能およびエージェントの「ファイルコンテキスト」を有効にします。これにより、ユーザーはファイルをアップロードし、その内容を抽出して会話に直接含めることができるようになります。
- **skills**: サイドパネルでのSkillsの有効化、手動での `$` 呼び出し、モデルによるSkillsの呼び出し、およびエージェントのSkill許可リストを有効にします。詳細は [Skills](/docs/features/skills) を参照してください。
- **memory**: メモリが設定されており、ユーザーがアクセス権を持っている場合に、エージェントが `set_memory` および `delete_memory` を使用できるようにします。デフォルトで有効になっており、メモリが無効な場合は実行時に削除されます。
- **ask_user_question**: エージェントが一時停止し、単一のフォームで1〜4つの関連する質問を行い、回答を得てから再開できるようにします。デフォルトで有効になっています。
- **tools**: エージェントに様々なツールへのアクセス権を付与します。
- **chain**: エージェントチェーン（Mixture-of-Agents (MoA) ワークフローとも呼ばれます）のベータ機能を有効にします。
- **ocr**: チャットでの「テキストとしてアップロード」およびエージェントの「ファイルコンテキスト」をオプションで強化し、ファイルをアップロードしてOCRで処理できるようにします。**OCRサービスの設定が必要です。**
- **run_in_background**: Code Interpreterの実行およびシェルツールをデフォルトでバックグラウンド実行の対象とし、MCPツールに対してはツールごとのバックグラウンド実行のオプトインを有効にします。エージェントはCode Interpreterを明示的に除外することができます。この設定はバックグラウンド実行を許可するものであり、実際に使用するかどうかはモデルが呼び出しごとに判断します。コードを実行するには `execute_code` および設定済みのCode Interpreterデプロイメントが必要です。この機能はデフォルトでは有効になっていません。
- **tool_intents**: 適切なツール呼び出しに対して、モデルが記述したライブインテントラベルを追加します。Code Interpreterツールを含むネイティブツールは自動的にオプトインされます。MCPツールは、Agent Builderで個別または一括で有効にできます。Model Specsでは `describeIntent` を使用します。この機能はデフォルトでは有効になっていません。

capabilitiesを指定することで、エージェントと対話する際にユーザーが利用できる機能を制御できます。

## 設定例 [#example-configuration]

以下は、カスタム機能とファイル引用設定を使用して `agents` endpoint を構成する例です：

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    disableBuilder: false
    # File citation configuration
    maxCitations: 20
    maxCitationsPerFile: 5
    minRelevanceScore: 0.6
    # Custom capabilities
    capabilities:
      # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
      # - 'programmatic_tools'
      - 'execute_code'
      # Optional: makes Code Interpreter background-eligible and enables selected MCP tools.
      # - 'run_in_background'
      # Optional: enables live intent labels for native and selected MCP tools.
      # - 'tool_intents'
      - 'file_search'
      - 'skills'
      - 'subagents'
      - 'actions'
      - 'artifacts'
      - 'context'
      - 'ocr'
      - 'web_search'
```

この例では：

- ビルダーインターフェースが有効になっています
- ファイル引用は合計20件まで、1ファイルにつき最大5件までとなります。
- 関連度が60%以上のソースのみが含まれます
- LibreChat Agentsは、コード実行、ファイル検索（引用付き）、Skills、Subagents、actions、artifacts、ファイルコンテキスト、設定済みの場合はOCRサービス、およびウェブ検索機能にアクセスできます。
- `execute_code` と併せて `programmatic_tools` 機能を有効にしない限り、Programmatic Tool Calling は無効のままとなります。
- バックグラウンドツール呼び出しは、`run_in_background` を追加しない限り無効のままです。Code Interpreterツールはデフォルトで有効になりますが、MCPツールは引き続きツールごとの選択が必要です。
- `tool_intents` を追加しない限り、ツールインテントのラベルは無効のままです。ネイティブツールは自動的にオプトインされますが、MCPツールは引き続きツールごとの選択が必要です。

## remoteApi

Remote Agent API認証の設定。外部サービスがAgents APIエンドポイントを呼び出す際の認証方法を制御します。

### remoteApi.auth

<OptionTable
  options={[
    [
      'auth',
      'Object',
      'Remote Agent APIの認証設定。',
      'Supports API key and/or OIDC Bearer token authentication. If omitted, only API key auth is active.',
    ],
  ]}
/>

#### remoteApi.auth.apiKey

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Remote Agent APIのAPIキー認証を有効にします。',
      'When true, requests with a valid LibreChat API key are accepted. Can be used alongside or instead of OIDC.',
    ],
  ]}
/>

**デフォルト:** `true`

#### remoteApi.auth.oidc

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'OIDC Bearerトークン認証を有効にする。',
      'When true, the middleware validates Bearer tokens against the configured OIDC issuer via JWKS.',
    ],
    [
      'issuer',
      'String',
      'OIDC発行者URL',
      'The base URL of your OIDC provider, such as a Keycloak realm URL. Used for token issuer validation and JWKS discovery if jwksUri is not set.',
    ],
    [
      'jwksUri',
      'String',
      'JWKS endpoint URL。オプション。',
      'If omitted, resolved automatically via {issuer}/.well-known/openid-configuration. You can also set OPENID_JWKS_URL as an alternative.',
    ],
    [
      'audience',
      'String',
      '期待されるトークンオーディエンス。OIDC認証が有効な場合に必須です。',
      'Tokens must contain this value in their aud claim.',
    ],
    [
      'scope',
      'String',
      '必須のスコープ値。オプション。',
      'If set, the token must contain this value in its scp or scope claim. Use this to distinguish token intent across different APIs.',
    ],
  ]}
/>

**デフォルト:** `enabled: false`

**例 - OIDCのみ:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: false
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          audience: my-client-id
```

**例 - APIキーフォールバック付きOIDC:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: true
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          # jwksUri is optional and auto-discovered if omitted
          jwksUri: https://auth.example.com/realms/myrealm/protocol/openid-connect/certs
          audience: my-client-id
```

<Callout type="info">
  JWKS URIの解決優先順位は、明示的な `jwksUri`、次に `OPENID_JWKS_URL`、最後に `{issuer}/.well-known/openid-configuration` を介した自動検出の順となります。
</Callout>

<Callout type="info">
  OIDCのユーザー照合では、`sub` クレームをプライマリの検索キーとして使用し、フォールバックとして `email`、`preferred_username`、または `upn` クレームを使用します。照合されるユーザーは、LibreChat内に既に存在している必要があります。
</Callout>

## Subagents [#subagents]

`subagents` フィールドは、`subagents` 機能が利用可能な場合に、親エージェントが生成できる分離された子エージェントを制御します。

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'trueに設定すると、このエージェントにサブエージェント生成ツールを追加します。デフォルト：無効。',
      'enabled: true',
    ],
    [
      'allowSelf',
      'Boolean',
      'エージェントが新しい隔離されたコンテキストで自身を生成できるようにします。デフォルト: true。',
      'allowSelf: true',
    ],
    [
      'agent_ids',
      'Array/List of Strings',
      'このエージェントが生成可能な特定のエージェント。`maxSubagents`（デフォルトは10）によって上限が設定されます。',
      'agent_ids: ["agent_researcher"]',
    ],
  ]}
/>

```yaml filename="Agent subagents"
subagents:
  enabled: true
  allowSelf: true
  agent_ids:
    - 'agent_researcher'
    - 'agent_reviewer'
```

ユーザー向けの動作や制限については、[Subagents](/docs/features/subagents) を参照してください。

## 注記 [#notes]

- [modelSpecs](/docs/configuration/librechat_yaml/object_structure/model_specs) を使用して選択可能なエージェントのリストを定義している場合を除き、ビルダーインターフェースを無効にすることは推奨されません。
- ファイル引用設定（`maxCitations`、`maxCitationsPerFile`、`minRelevanceScore`）は、`file_search`機能が有効な場合にのみ適用されます。
- 関連性スコアはベクトル類似性を使用して計算され、1.0は完全一致、0.0は類似性がないことを表します。
- 引用制限は、包括的な情報検索と応答の品質およびパフォーマンスのバランスを取るのに役立ちます。
- `context`機能は、OCR設定なしでもテキスト解析メソッドを使用して動作します。OCRを設定すると、抽出品質が向上します。
- `ocr` 機能を使用するには、OCR サービスの設定が必要です（[OCR Configuration](/docs/configuration/librechat_yaml/object_structure/ocr) を参照してください）。
- `maxSubagents` は、単一のエージェントが参照できるサブエージェントの数を制限します。これは、[Subagents](/docs/features/subagents#limits) で文書化されている深さやグラフの制限を変更するものではありません。
