---
title: Konfiguracja wyszukiwania w sieci
icon: Search
---

Konfiguracja `webSearch` pozwala na dostosowanie funkcjonalności wyszukiwania w sieci w ramach LibreChat, w tym dostawców wyszukiwania, skraperów treści oraz mechanizmów zmiany rangi wyników (rerankers).

## Przegląd [#overview]

Funkcja wyszukiwania w sieci składa się z trzech głównych komponentów:

1. **Dostawcy wyszukiwania**: Usługi, które wykonują wstępne wyszukiwanie w sieci
2. **Scrapers**: Usługi, które wyodrębniają zawartość ze stron internetowych
3. **Rerankers**: Usługi, które zmieniają kolejność wyników wyszukiwania w celu uzyskania lepszej trafności

## Przykład [#example]

```yaml filename="webSearch"
webSearch:
  # Search Provider Configuration
  serperApiKey: "${SERPER_API_KEY}"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}"
  searchProvider: "serper" # Options: "serper", "searxng", "tavily", "keenable"

  # Optional: query options sent to a SearXNG instance
  searxngSearchOptions:
    engines: "google,bing,startpage" # Default: "google,bing,duckduckgo"
    language: "en" # Default: "all"

  # Tavily Configuration (search and/or scraper)
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"

  # Keenable Configuration (search and/or scraper; all credentials optional)
  keenableApiKey: "${KEENABLE_API_KEY}"
  keenableApiUrl: "${KEENABLE_API_URL}"
  keenableSearchOptions:
    maxResults: 8
    site: "example.com"
    attributionTitle: "LibreChat"
    timeout: 15000
  keenableScraperOptions:
    attributionTitle: "LibreChat"
    timeout: 15000

  # Scraper Configuration
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlApiUrl: "${FIRECRAWL_API_URL}"
  firecrawlVersion: "${FIRECRAWL_VERSION}"
  scraperProvider: "firecrawl" # Options: "firecrawl", "serper", "tavily", "keenable"

  # Reranker Configuration
  jinaApiKey: "${JINA_API_KEY}"
  jinaApiUrl: "${JINA_API_URL}"
  cohereApiKey: "${COHERE_API_KEY}"
  rerankerType: "jina" # Options: "jina", "cohere", "none"

  # General Settings
  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)
  # Required for deliberately private self-hosted provider endpoints
  allowedAddresses:
    - "searxng:8080"
```

## Ochrona przed SSRF i prywatni dostawcy [#ssrf-protection-and-private-providers]

LibreChat weryfikuje każde połączenie, które otwiera w celu wyszukiwania w sieci, scrapingu oraz rerankingu. Miejsca docelowe typu private, loopback, link-local oraz cloud-metadata są domyślnie blokowane, wliczając w to przekierowania (redirect hops) oraz nazwy hostów, które w momencie łączenia są rozpoznawane jako adresy prywatne.

Użyj `webSearch.allowedAddresses`, gdy skonfigurowany punkt końcowy SearXNG, Firecrawl, Jina, kompatybilny z Tavily, kompatybilny z Keenable lub powiązany dostawca jest celowo prywatny:

```yaml filename="webSearch / allowedAddresses"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  allowedAddresses:
    - "searxng:8080"
    - "10.0.0.5:3002"
    - "[fd00::5]:8080"
```

To pole jest listą wyjątków SSRF, a nie ścisłą listą dozwolonych adresów (allowlist). Publiczne miejsca docelowe pozostają dostępne. Wpisy muszą być dokładnymi parami `host:port`, `private.ip:port` lub `[ipv6]:port`; adresy URL, ścieżki, zakresy CIDR, same nazwy hostów/adresy IP oraz publiczne literały IP są odrzucane. Wykluczona nazwa hosta ufa każdemu prywatnemu adresowi IP, na który rozwiązuje się na danym porcie, dlatego preferuj używanie literałów prywatnych adresów IP i wymieniaj tylko te nazwy, których DNS kontrolujesz.

LibreChat automatycznie wyklucza skonfigurowany punkt końcowy forward-proxy HTTP(S), aby zapewnić możliwość połączenia z nim. Gdy proxy obsługuje żądanie, rozwiązuje ono adres docelowy i musi samodzielnie wymusić egresję (ruch wychodzący) do miejsca docelowego. `NO_PROXY` może sprawić, że żądanie zostanie wysłane bezpośrednio, gdzie nadal obowiązuje mechanizm ochrony połączeń LibreChat. Ochrona ta obejmuje połączenia otwierane przez LibreChat do API dostawców; nie może ona ograniczać adresu URL strony, który zewnętrzny scraper otrzymuje jako dane żądania i pobiera na własnej infrastrukturze.

## Dostawcy wyszukiwania [#search-providers]

### searchProvider

<OptionTable
  options={[
    ['searchProvider', 'String', 'Określa, którego dostawcę wyszukiwania użyć.', 'Options: "serper", "searxng", "tavily", "keenable"'],
  ]}
/>

### serperApiKey

<OptionTable
  options={[
    ['serperApiKey', 'String', 'Nazwa zmiennej środowiskowej dla klucza API Serper. Jeśli nie zostanie ustawiona w .env, użytkownicy zostaną poproszeni o jej podanie przez interfejs użytkownika.', '${SERPER_API_KEY}'],
  ]}
/>

**Uwaga:** Pobierz swój klucz API z [Serper.dev](https://serper.dev/api-keys)


### searxngInstanceUrl

<OptionTable
  options={[
    ['searxngInstanceUrl', 'String', 'Nazwa zmiennej środowiskowej dla adresu URL instancji SearXNG. Jeśli nie zostanie ustawiona w .env, użytkownicy zostaną poproszeni o jej podanie za pośrednictwem interfejsu użytkownika.', '${SEARXNG_INSTANCE_URL}'],
  ]}
/>

### searxngApiKey

<OptionTable
  options={[
    ['searxngApiKey', 'String', 'Nazwa zmiennej środowiskowej dla klucza API SearXNG. Jeśli nie zostanie ustawiona w .env, użytkownicy zostaną poproszeni o podanie jej przez interfejs użytkownika.', '${SEARXNG_API_KEY}'],
  ]}
/>

**Uwaga:** Jest to opcjonalne i wymagane tylko wtedy, gdy Twoja instancja SearXNG wymaga uwierzytelnienia.

### searxngSearchOptions

<OptionTable
  options={[
    ['searxngSearchOptions', 'Object', 'Opcje zapytania wysyłane do Twojej instancji SearXNG przy każdym wyszukiwaniu. Każdy podklucz jest opcjonalny, a blok jest odczytywany tylko wtedy, gdy searchProvider ma wartość "searxng".', ''],
  ]}
/>

**Podklucze:**

<OptionTable
  options={[
    ['engines', 'String or Array of Strings', 'Silniki, które Twoja instancja powinna odpytywać, wysyłane do SearXNG jako parametr engines. Akceptuje ciąg znaków rozdzielony przecinkami lub listę YAML; oba formaty są normalizowane do postaci rozdzielonej przecinkami, przy czym zbędne białe znaki oraz puste wpisy są usuwane. Wartość, która po przycięciu jest pusta, jest traktowana jako nieustawiona.', 'Default: "google,bing,duckduckgo"'],
    ['language', 'String', 'Kod języka wyniku, wysyłany do SearXNG jako parametr języka. Użyj kodu akceptowanego przez Twoją instancję, takiego jak "en", "de" lub "fr".', 'Default: "all"'],
    ['timeRange', 'String', 'Ogranicza wyniki według daty publikacji, przesyłane do SearXNG jako time_range. Pomijane w żądaniu, jeśli nie ustawiono.', 'Options: "day", "month", "year"'],
    ['timeout', 'Number', 'Limit czasu żądania HTTP w milisekundach dla wywołań do Twojej instancji. Musi być liczbą całkowitą dodatnią nie większą niż 120000; wartość 0 jest odrzucana, ponieważ wyłączyłaby limit czasu.', 'Default: 10000'],
  ]}
/>

<Callout type="info" title="Dostępność">
`searxngSearchOptions` łączy się z SearXNG za pośrednictwem narzędzia wyszukiwania w `@librechat/agents` i wymaga wersji `@librechat/agents` v3.6.9 lub nowszej. Wydania zbudowane w oparciu o wcześniejszą wersję ignorują ten blok zamiast kończyć się błędem przy uruchomieniu, więc instancja, która ciągle zwraca domyślne wyniki, może po prostu korzystać ze starszej kompilacji.
</Callout>

**Przykład:**

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}" # Optional
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeRange: "month"
    timeout: 10000
```

`engines` akceptuje również format rozdzielany przecinkami, którego używa samo SearXNG, więc jest to odpowiednik powyższej listy:

```yaml filename="webSearch"
webSearch:
  searxngSearchOptions:
    engines: "google, bing, startpage, qwant"
```

#### Wybór silników [#choosing-engines]

Nazwy silników muszą być zgodne z silnikami włączonymi w Twojej własnej instancji, co pociąga za sobą dwie następujące kwestie:

- SearXNG ignoruje silnik, którego nie rozpoznaje, zamiast zgłaszać błąd. Literówka lub silnik wyłączony w Twojej instancji skutkuje mniejszą liczbą wyników zamiast awarią.
- Prawidłowe nazwy są specyficzne dla danej instancji. Sprawdź listę włączonych silników pod adresem `https://your-instance/config`, który zwraca plik JSON zawierający każdy włączony silnik, lub otwórz kartę **Engines** na stronie preferencji swojej instancji.

Domyślną wartością jest `google,bing,duckduckgo`. SearXNG agreguje wszystko, co zwracają wybrane wyszukiwarki, więc zablokowanie jednej z nich powoduje jedynie utratę jej części wyników, a nie całej odpowiedzi. Wyszukiwanie kończy się niepowodzeniem tylko wtedy, gdy każda z wybranych wyszukiwarek zawiedzie lub nie zwróci żadnych wyników.

DuckDuckGo jest silnikiem w tym domyślnym zestawie, który najprawdopodobniej zostanie zablokowany, ponieważ wyświetla CAPTCHA większości instancji hostowanych samodzielnie. Przy zaledwie trzech silnikach w zestawie, utrata go w tym samym czasie co ograniczonego limitami Google lub Bing jest częstym powodem braku jakichkolwiek wyników. Jawne ustawienie `engines`, pominięcie DuckDuckGo oraz wymienienie więcej niż trzech silników zmniejsza to ryzyko.

Silniki, które odpowiadają szybko i tolerują ruch z własnych serwerów (self-hosted), stanowią najlepszy zestaw startowy, na przykład:

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - brave
      - startpage
      - qwant
```

Dodanie większej liczby silników zwiększa zasięg, ale jednocześnie spowalnia każde wyszukiwanie, ponieważ SearXNG czeka na najwolniejszy silnik w zestawie przed udzieleniem odpowiedzi. Pamiętaj o parametrze `timeout`, gdy powiększasz listę.

#### Czego te opcje nie kontrolują [#what-these-options-do-not-control]

- **Safe search** nie jest częścią tego bloku. Pochodzi on z klucza najwyższego poziomu [`safeSearch`](#safesearch) i jest przekazywany do SearXNG jako `safesearch`.
- **Kategorie wyników** są wybierane dla każdego zapytania przez LibreChat, który przypisuje typ wyszukiwania do `general`, `images`, `videos` lub `news`. Nie można ich tutaj nadpisać.
- **Numer strony i format wyniku** są stałe. LibreChat zawsze żąda strony 1 w formacie JSON, dlatego Twoja instancja musi mieć włączony `json` w sekcji `formats`.

### tavilyApiKey

<OptionTable
  options={[
    ['tavilyApiKey', 'String', 'Nazwa zmiennej środowiskowej dla klucza API Tavily. Używana zarówno do wyszukiwania, jak i scrapowania. Jeśli nie zostanie ustawiona w .env, użytkownicy zostaną poproszeni o jej podanie przez interfejs użytkownika.', '${TAVILY_API_KEY}'],
  ]}
/>

**Uwaga:** Pobierz swój klucz API z [Tavily](https://app.tavily.com/home)

### tavilySearchUrl

<OptionTable
  options={[
    ['tavilySearchUrl', 'String', 'Nazwa zmiennej środowiskowej dla niestandardowego adresu URL Tavily Search API. Opcjonalna; jeśli nie zostanie ustawiona, domyślnie używane jest wyszukiwanie hostowane przez Tavily.', '${TAVILY_SEARCH_URL}'],
  ]}
/>

### tavilyExtractUrl

<OptionTable
  options={[
    ['tavilyExtractUrl', 'String', 'Nazwa zmiennej środowiskowej dla niestandardowego adresu URL API Tavily Extract. Opcjonalne; jeśli nie zostanie ustawione, domyślnie używany jest hostowany ekstraktor Tavily.', '${TAVILY_EXTRACT_URL}'],
  ]}
/>

### tavilySearchOptions

<OptionTable
  options={[
    ['tavilySearchOptions', 'Object', 'Opcje konfiguracji wyszukiwania Tavily.', ''],
  ]}
/>

**Podklucze:**

<OptionTable
  options={[
    ['searchDepth', 'String', 'Kontroluje kompromis między trafnością a opóźnieniem. "basic" zwraca jedno podsumowanie NLP na adres URL. "advanced" zwraca wiele semantycznie istotnych fragmentów na adres URL (2 kredyty API). "fast" równoważy szybkość i trafność za pomocą fragmentów. "ultra-fast" minimalizuje opóźnienia za pomocą jednego podsumowania NLP.', 'Options: "basic", "advanced", "fast", "ultra-fast". Default: "basic"'],
    ['maxResults', 'Number', 'Maksymalna liczba zwracanych wyników wyszukiwania.', 'Range: 1-20. Default: 5'],
    ['topic', 'String', 'Kategoria wyszukiwania. "news" jest przydatne do aktualizacji w czasie rzeczywistym. "finance" do danych finansowych.', 'Options: "general", "news", "finance". Default: "general"'],
    ['includeImages', 'Boolean', 'Dołącz obrazy w odpowiedzi. Zwraca zarówno obrazy z zapytania najwyższego poziomu, jak i obrazy dla poszczególnych wyników.', 'Default: false'],
    ['includeAnswer', 'Boolean or String', 'Dołącz odpowiedź wygenerowaną przez LLM. "basic" lub true dla szybkiej odpowiedzi, "advanced" dla szczegółowej odpowiedzi.', 'Default: false'],
    ['includeRawContent', 'Boolean or String', 'Dołącz oczyszczoną i przetworzoną zawartość HTML. "markdown" lub true dla formatu markdown, "text" dla zwykłego tekstu.', 'Default: false'],
    ['includeDomains', 'Array of Strings', 'Ogranicz wyszukiwanie do określonych domen. Maksymalnie 300 domen.', ''],
    ['excludeDomains', 'Array of Strings', 'Wyklucz określone domeny z wyników. Maksymalnie 150 domen.', ''],
    ['timeRange', 'String', 'Filtr zakresu czasu oparty na dacie publikacji lub ostatniej aktualizacji.', 'Options: "day", "week", "month", "year"'],
    ['includeImageDescriptions', 'Boolean', 'Gdy includeImages jest ustawione na true, dodaj również opisowy tekst dla każdego obrazu.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Dołącz adres URL favicon dla każdego wyniku wyszukiwania.', 'Default: false'],
    ['chunksPerSource', 'Number', 'Maksymalna liczba fragmentów istotnej treści na źródło. Dostępne tylko, gdy searchDepth jest ustawione na "advanced".', 'Range: 1-3. Default: 3'],
    ['safeSearch', 'Boolean', 'Opcjonalne zastąpienie safe_search dla żądań Tavily Search. Domyślnie pominięte; wartość true może wymagać planu Tavily Enterprise.', 'Default: omitted'],
    ['timeout', 'Number', 'Limit czasu żądania HTTP po stronie klienta w milisekundach. Określa, jak długo czekać na odpowiedź API Tavily przed przerwaniem operacji.', 'Default: 15000'],
  ]}
/>

### keenableApiKey

<OptionTable
  options={[
    ['keenableApiKey', 'String', 'Nazwa zmiennej środowiskowej dla opcjonalnego klucza API Keenable. Keenable działa bez klucza; skonfigurowanie go zwiększa publiczne limity zapytań.', '${KEENABLE_API_KEY}'],
  ]}
/>

### keenableApiUrl

<OptionTable
  options={[
    ['keenableApiUrl', 'String', 'Nazwa zmiennej środowiskowej dla niestandardowego adresu URL API wyszukiwania Keenable. Nie zmienia to endpointu pobierania strony.', '${KEENABLE_API_URL}'],
  ]}
/>

Punkt końcowy page-fetch jest konfigurowany oddzielnie za pomocą zmiennej środowiskowej `KEENABLE_FETCH_URL`.

### keenableSearchOptions

<OptionTable
  options={[
    ['keenableSearchOptions', 'Object', 'Opcje konfiguracji dla wyszukiwania Keenable.', ''],
  ]}
/>

<OptionTable
  options={[
    ['maxResults', 'Number', 'Maksymalna liczba wyników wyszukiwania.', 'Range: 1-20. Default: 8'],
    ['site', 'String', 'Ogranicza wyniki do jednej domeny.', ''],
    ['attributionTitle', 'String', 'Wartość wysyłana w nagłówku atrybucji X-Keenable-Title.', ''],
    ['timeout', 'Number', 'Limit czasu żądania HTTP po stronie klienta w milisekundach.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

## Scrapers [#scrapers]

### firecrawlApiKey

<OptionTable
  options={[
    ['firecrawlApiKey', 'String', 'Nazwa zmiennej środowiskowej dla klucza API Firecrawl. Jeśli nie zostanie ustawiona w .env, użytkownicy będą proszeni o podanie jej przez UI.', '${FIRECRAWL_API_KEY}'],
  ]}
/>

**Uwaga:** Pobierz swój klucz API z [Firecrawl.dev](https://docs.firecrawl.dev/introduction#api-key)

### firecrawlApiUrl

<OptionTable
  options={[
    ['firecrawlApiUrl', 'String', 'Nazwa zmiennej środowiskowej dla adresu URL API Firecrawl. Jeśli nie zostanie ustawiona w .env, użytkownicy zostaną poproszeni o podanie jej przez interfejs użytkownika.', '${FIRECRAWL_API_URL}'],
  ]}
/>

**Uwaga:** Jest to opcjonalne i wymagane tylko w przypadku korzystania z własnej instancji Firecrawl.

### firecrawlVersion

<OptionTable
  options={[
    ['firecrawlVersion', 'String', 'Nazwa zmiennej środowiskowej dla wersji API Firecrawl (v0 lub v1).', '${FIRECRAWL_VERSION}'],
  ]}
/>

### scraperProvider

<OptionTable
  options={[
    ['scraperProvider', 'String', 'Określa, której usługi scrapera użyć.', 'Options: "firecrawl", "serper", "tavily", "keenable"'],
  ]}
/>

### firecrawlOptions

<OptionTable
  options={[
    ['firecrawlOptions', 'Object', 'Zaawansowane opcje konfiguracji dla scrapera Firecrawl.', ''],
  ]}
/>

**Podklucze:**

#### formats

<OptionTable
  options={[
    ['formats', 'Array of Strings', 'Formaty do uwzględnienia w danych wyjściowych.', ''],
  ]}
/>

#### includeTags

<OptionTable
  options={[
    ['includeTags', 'Array of Strings', 'Tagi do uwzględnienia w danych wyjściowych.', ''],
  ]}
/>

#### excludeTags

<OptionTable
  options={[
    ['excludeTags', 'Array of Strings', 'Tagi do wykluczenia z danych wyjściowych.', ''],
  ]}
/>

#### headers

<OptionTable
  options={[
    ['headers', 'Object', 'Nagłówki do wysłania wraz z żądaniem. Mogą być używane do wysyłania plików cookie, user-agent itp.', ''],
  ]}
/>

#### waitFor

<OptionTable
  options={[
    ['waitFor', 'Number', 'Określ opóźnienie w milisekundach przed pobraniem zawartości, aby zapewnić stronie wystarczająco dużo czasu na załadowanie.', ''],
  ]}
/>

#### timeout

<OptionTable
  options={[
    ['timeout', 'Integer', 'Limit czasu w milisekundach dla żądania scrapowania. Musi być nieujemną liczbą całkowitą.', 'Default: 7500'],
  ]}
/>

#### maxAge

<OptionTable
  options={[
    ['maxAge', 'Number', 'Zwraca zbuforowaną wersję strony, jeśli jest ona młodsza niż ten czas w milisekundach. Jeśli zbuforowana wersja strony jest starsza niż ta wartość, strona zostanie pobrana ponownie.', ''],
  ]}
/>

**Uwaga:** Jeśli nie potrzebujesz bardzo świeżych danych, włączenie tej opcji może przyspieszyć pobieranie danych (scrapes) o 500%.

#### mobile

<OptionTable
  options={[
    ['mobile', 'Boolean', 'Emuluj scrapowanie z urządzenia mobilnego.', ''],
  ]}
/>

#### skipTlsVerification

<OptionTable
  options={[
    ['skipTlsVerification', 'Boolean', 'Pomiń weryfikację certyfikatu TLS podczas wykonywania żądań.', ''],
  ]}
/>

#### blockAds

<OptionTable
  options={[
    ['blockAds', 'Boolean', 'Włącza blokowanie reklam oraz wyskakujących okienek z plikami cookie.', ''],
  ]}
/>

#### removeBase64Images

<OptionTable
  options={[
    ['removeBase64Images', 'Boolean', 'Usuwa wszystkie obrazy base 64 z danych wyjściowych, które mogą być nadmiernie długie. Tekst alternatywny obrazu pozostaje w danych wyjściowych, ale adres URL zostaje zastąpiony symbolem zastępczym.', ''],
  ]}
/>

#### parsePDF

<OptionTable
  options={[
    ['parsePDF', 'Boolean', 'Określa sposób przetwarzania plików PDF podczas scrapowania.', ''],
  ]}
/>

#### storeInCache

<OptionTable
  options={[
    ['storeInCache', 'Boolean', 'Jeśli ustawiono na true, strona zostanie zapisana w indeksie i pamięci podręcznej Firecrawl. Ustawienie tej wartości na false jest przydatne, jeśli działania związane ze scrapowaniem mogą budzić obawy dotyczące ochrony danych. Użycie niektórych parametrów związanych z wrażliwym scrapowaniem (nagłówki) wymusi ustawienie tego parametru na false.', ''],
  ]}
/>

#### zeroDataRetention

<OptionTable
  options={[
    ['zeroDataRetention', 'Boolean', 'Jeśli ustawiono na true, włączy to zerową retencję danych dla tego scrapowania (wymaga wcześniejszej konfiguracji w Firecrawl).', ''],
  ]}
/>

#### location

<OptionTable
  options={[
    ['location', 'Object', 'Lokalizacja geograficzna i ustawienia języka dla scrapingu.', ''],
  ]}
/>

#### onlyMainContent

<OptionTable
  options={[
    ['onlyMainContent', 'Boolean', 'Zwracaj tylko główną zawartość strony, z wyłączeniem nagłówków, nawigacji, stopek itp.', ''],
  ]}
/>

#### changeTrackingOptions

<OptionTable
  options={[
    ['changeTrackingOptions', 'Object', 'Konfiguracja śledzenia zmian w pobranych treściach.', ''],
  ]}
/>

**Przykład:**
```yaml filename="webSearch"
webSearch:
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlOptions:
    formats: ["markdown", "rawHtml"]
    includeTags: ["main", "article", ".content"]
    excludeTags: ["nav", "footer", ".ads"]
    waitFor: 2000
    timeout: 10000
    mobile: false
    blockAds: true
    onlyMainContent: true
    location:
      country: "US"
      languages: ["en"]
```

**Uwaga:** Szczegółowe informacje na temat opcji i ustawień domyślnych narzędzia Firecrawl scraper można znaleźć w [dokumentacji API Firecrawl](https://docs.firecrawl.dev/api-reference/endpoint/scrape).

### tavilyScraperOptions

<OptionTable
  options={[
    ['tavilyScraperOptions', 'Object', 'Opcje konfiguracji dla Tavily Extract (scraper).', ''],
  ]}
/>

**Podklucze:**

<OptionTable
  options={[
    ['extractDepth', 'String', 'Głębokość procesu ekstrakcji. "advanced" pobiera więcej danych, w tym tabele i osadzone treści z wyższą skutecznością, ale może zwiększyć opóźnienia. "basic" kosztuje 1 kredyt za każde 5 pomyślnych adresów URL, "advanced" kosztuje 2 kredyty za każde 5 pomyślnych adresów URL.', 'Options: "basic", "advanced". Default: "basic"'],
    ['includeImages', 'Boolean', 'Dołącz listę obrazów wyodrębnionych z adresów URL w odpowiedzi.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Dołącz adres URL favicon dla każdego wyodrębnionego wyniku.', 'Default: false'],
    ['format', 'String', 'Format wyodrębnionej zawartości strony internetowej. "markdown" zwraca zawartość w formacie markdown. "text" zwraca zwykły tekst i może zwiększyć opóźnienie.', 'Options: "markdown", "text". Default: "markdown"'],
    ['timeout', 'Number', 'Limit czasu w milisekundach. Kontroluje limit czasu HTTP po stronie klienta. Po ustawieniu wysyła również do Tavily limit czasu ekstrakcji po stronie serwera, przeliczony na sekundy i ograniczony do zakresu 1-60 s.', 'Default: 15000 for basic, 30000 for advanced'],
  ]}
/>

**Przykład:**
```yaml filename="webSearch"
webSearch:
  searchProvider: tavily
  scraperProvider: tavily
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  # tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  # tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"
  tavilySearchOptions:
    searchDepth: basic
    maxResults: 5
    topic: general
  tavilyScraperOptions:
    extractDepth: basic
```

**Uwaga:** Szczegółowe informacje na temat opcji Tavily API znajdują się w [Tavily API Documentation](https://docs.tavily.com).

### keenableScraperOptions

<OptionTable
  options={[
    ['keenableScraperOptions', 'Object', 'Opcje konfiguracji dla pobierania stron Keenable.', ''],
  ]}
/>

<OptionTable
  options={[
    ['attributionTitle', 'String', 'Wartość wysyłana w nagłówku atrybucji X-Keenable-Title.', ''],
    ['timeout', 'Number', 'Limit czasu żądania HTTP po stronie klienta w milisekundach.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

Keenable może zapewniać wyszukiwanie, pobieranie stron lub obie te funkcje. Jego publiczne endpoint działają bez klucza; opcjonalny `keenableApiKey` zwiększa limity zapytań. Ustaw `rerankerType: "none"`, aby uniknąć konieczności posiadania klucza rerankera:

```yaml filename="webSearch"
webSearch:
  searchProvider: keenable
  scraperProvider: keenable
  rerankerType: none
  # keenableApiKey: "${KEENABLE_API_KEY}" # Optional
  # keenableApiUrl: "${KEENABLE_API_URL}" # Optional search endpoint
  keenableSearchOptions:
    maxResults: 8
    # site: example.com
  keenableScraperOptions:
    timeout: 15000
```

## Rerankers [#rerankers]

### jinaApiKey

<OptionTable
  options={[
    ['jinaApiKey', 'String', 'Nazwa zmiennej środowiskowej dla klucza API Jina. Jeśli nie zostanie ustawiona w .env, użytkownicy zostaną poproszeni o jej podanie przez interfejs użytkownika.', '${JINA_API_KEY}'],
  ]}
/>

**Uwaga:** Pobierz swój klucz API z [Jina.ai](https://jina.ai/api-dashboard/)

### jinaApiUrl

<OptionTable
  options={[
    ['jinaApiUrl', 'String', 'Nazwa zmiennej środowiskowej dla adresu URL API Jina. Jeśli nie zostanie ustawiona w .env, użytkownicy zostaną poproszeni o podanie jej przez interfejs użytkownika.', '${JINA_API_URL}'],
  ]}
/>

**Uwaga:** Jest to opcjonalne i wymagane tylko w przypadku korzystania z własnej instancji Jina.

### cohereApiKey

<OptionTable
  options={[
    ['cohereApiKey', 'String', 'Nazwa zmiennej środowiskowej dla klucza API Cohere. Jeśli nie zostanie ustawiona w .env, użytkownicy zostaną poproszeni o podanie jej przez interfejs użytkownika.', '${COHERE_API_KEY}'],
  ]}
/>

**Uwaga:** Pobierz swój klucz API z [Cohere Dashboard](https://dashboard.cohere.com/welcome/login)

### rerankerType

<OptionTable
  options={[
    ['rerankerType', 'String', 'Określa, której usługi rerankera użyć. Ustaw na "none", aby pominąć reranking.', 'Options: "jina", "cohere", "none"'],
  ]}
/>

## Ustawienia ogólne [#general-settings]

### scraperTimeout

<OptionTable
  options={[
    ['scraperTimeout', 'Integer', 'Limit czasu w milisekundach dla żądań scrapera. Musi być nieujemną liczbą całkowitą.', 'Default: 7500'],
  ]}
/>

### safeSearch

<OptionTable
  options={[
    ['safeSearch', 'Number', 'Poziom filtrowania bezpiecznego wyszukiwania. 0 = WYŁĄCZONE (brak filtrowania), 1 = UMIARKOWANY (domyślnie), 2 = ŚCISŁY (maksymalne filtrowanie).', 'Default: 1 (MODERATE)'],
  ]}
/>

**Uwaga:** Poziomy bezpiecznego wyszukiwania są zgodne ze standardowymi konwencjami API wyszukiwania. Filtrowanie MODERATE jest domyślnie włączone, aby zapewnić rozsądne filtrowanie treści przy jednoczesnym zachowaniu skuteczności wyszukiwania. Tavily domyślnie nie dziedziczy tego globalnego ustawienia; użyj `tavilySearchOptions.safeSearch` tylko wtedy, gdy Twoje konto Tavily obsługuje `safe_search`.

## Uwagi [#notes]

- Klucze API można skonfigurować na dwa sposoby:
  1. Ustaw zmienne środowiskowe określone w konfiguracji YAML
  2. Jeśli zmienne środowiskowe nie są ustawione, użytkownicy zostaną poproszeni o podanie kluczy API za pośrednictwem interfejsu użytkownika.
- Konfiguracja obsługuje wiele usług dla każdego komponentu (dostawców, scraperów, rerankerów)
- Jeśli określony typ usługi nie zostanie wskazany, system spróbuje użyć wszystkich dostępnych usług w tej kategorii
- Safe search zapewnia trzy poziomy filtrowania treści: OFF (0), MODERATE (1) oraz STRICT (2)
- Tavily domyślnie nie dziedziczy globalnego ustawienia bezpiecznego wyszukiwania; ustaw `tavilySearchOptions.safeSearch` jawnie tylko wtedy, gdy Twoje konto Tavily obsługuje `safe_search`.
- Keenable obsługuje wyszukiwanie bezkluczowe i pobieranie stron; jego opcjonalny klucz zwiększa publiczne limity zapytań, a jego nadpisanie adresu URL pobierania jest dostępne tylko w środowisku.
- Zachowanie zapytań SearXNG (silniki, język, zakres czasu, limit czasu żądania) jest konfigurowane w `searxngSearchOptions`; zobacz [Wybieranie silników](#choosing-engines), jeśli wyniki wyszukiwania są puste.
- Nigdy nie umieszczaj rzeczywistych kluczy API w konfiguracji YAML - używaj wyłącznie nazw zmiennych środowiskowych

## Konfiguracja SearXNG [#setting-up-searxng]

SearXNG to zorientowana na prywatność metawyszukiwarka, którą możesz hostować samodzielnie. 
Więcej informacji znajdziesz w [oficjalnej dokumentacji SearXNG](https://docs.searxng.org/).

Oto kroki, które należy wykonać, aby skonfigurować własną instancję SearXNG do użytku z LibreChat:

### Używanie Docker Desktop [#using-docker-desktop]

1. **Wyszukaj oficjalny obraz SearXNG**
   - Otwórz Docker Desktop
   - Wyszukaj `searxng/searxng` w zakładce **Images**.
   - Kliknij **Run** przy oficjalnym obrazie, aby automatycznie pobrać i uruchomić kontener z tym obrazem

2. **Uruchamianie kontenera**
   - Rozwiń listę rozwijaną **Optional Settings** w panelu, który pojawi się po zakończeniu pobierania.
   - Ustaw żądane szczegóły konfiguracji (numer portu, nazwę kontenera itp.)
   - Kliknij **Run**, aby uruchomić kontener

3. **Konfiguracja SearXNG dla LibreChat**
   - Przejdź do karty `Files` w Docker Desktop
   - Przejdź do `/etc/searxng/settings.yaml`
   - Otwórz edytor plików
   - Przejdź do sekcji `formats`
   - Dodaj `json` jako akceptowalny format, aby LibreChat mógł komunikować się z Twoją instancją
   - Zapisz plik

4. **Zrestartuj kontener**
   - Zrestartuj kontener, aby zmiany weszły w życie

#### Przewodnik wideo: [#video-guide]

Oto film, który przeprowadzi Cię przez cały proces od początku do końca w około minutę:

     <Video src="/videos/min_searxng_docker_setup_hq.mp4" title="Przewodnik konfiguracji SearXNG w Dockerze" />

**Uwaga:** W tym przykładzie URL instancji to http://localhost:55011 (numer portu znajduje się pod nazwą kontenera w lewym górnym rogu pod koniec filmu)

### Konfiguracja LibreChat do używania SearXNG [#configuring-librechat-to-use-searxng]

Możesz skonfigurować SearXNG w LibreChat z poziomu interfejsu użytkownika lub poprzez `librechat.yaml`.

#### Konfiguracja UI [#ui-configuration]

1. **Otwórz menu rozwijane narzędzi na pasku wprowadzania czatu**
![Przycisk konfiguracji narzędzi](/images/web-search/tools_highlight.png)

2. **Kliknij ikonę koła zębatego obok Web Search**
![Tools configuration section](/images/web-search/tools_config_highlight.png)

3. **Wybierz SearXNG z rozwijanej listy Dostawcy wyszukiwania (Search Provider)**
![SearXNG dropdown selection](/images/web-search/searxng_dropdown_highlight.png)

4. **Wprowadź szczegóły konfiguracji (np. adres URL instancji, typ scrapera itp.) i kliknij zapisz**
![Zapisz konfigurację wyszukiwania w sieci](/images/web-search/web_search_save.png)

5. **Kliknij opcję Web Search w menu rozwijanym narzędzi**
![Odznaka Web search w interfejsie czatu](/images/web-search/websearch_highlight.png)

6. **Odznaka wyszukiwania w sieci (Web Search) powinna być teraz włączona, co oznacza, że Twoje zapytania mogą teraz korzystać z funkcji wyszukiwania w sieci**
![Potwierdzenie odznaki wyszukiwania w sieci](/images/web-search/search_badge_confirm.png)

#### Konfiguracja YAML [#yaml-configuration]

Aby skonfigurować SearXNG dla wszystkich użytkowników instancji zamiast dla każdego z osobna, ustaw te same wartości w `librechat.yaml`:

```yaml filename="librechat.yaml"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  # searxngApiKey: "${SEARXNG_API_KEY}" # Only if your instance requires authentication
  # Required: a self-hosted instance is a private destination, so exempt it from the connection guard
  allowedAddresses:
    - "localhost:55011"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeout: 10000
```

Adres URL oraz klucz są nadal odczytywane ze środowiska, więc połącz powyższą konfigurację z:

```bash filename=".env"
SEARXNG_INSTANCE_URL=http://localhost:55011
# SEARXNG_API_KEY=your_api_key
```

<Callout type="warning" title="Prywatne instancje wymagają wpisu allowedAddresses">
`localhost:55011` to adres zwrotny (loopback), który LibreChat blokuje w momencie łączenia w ramach [ochrony SSRF](#ssrf-protection-and-private-providers). Bez pasującego wpisu `allowedAddresses` wyszukiwanie zakończy się niepowodzeniem, nawet jeśli SearXNG działa poprawnie. Wpis musi być dokładną parą `host:port` z adresu URL Twojej instancji.

Kiedy LibreChat działa w Docker, `localhost` odnosi się do kontenera LibreChat, a nie SearXNG. Użyj nazwy hosta, do której kontener ma dostęp, takiej jak nazwa usługi w compose, i zamiast tego wyklucz ją:

```yaml filename="librechat.yaml"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}" # SEARXNG_INSTANCE_URL=http://searxng:8080
  allowedAddresses:
    - "searxng:8080"
```
</Callout>

Zobacz [`searxngSearchOptions`](#searxngsearchoptions), aby poznać wszystkie podklucze akceptowane przez ten blok.

#### Rozwiązywanie problemów z pustymi wynikami [#troubleshooting-empty-results]

Wyszukiwanie SearXNG, które nie zwraca żadnych wyników, prawie zawsze oznacza, że żądanie nie dociera do działającej instancji, a nie że LibreChat odrzuca wyniki. Przeanalizuj poniższe punkty w podanej kolejności:

1. **Instancja jest adresem prywatnym bez wpisu `allowedAddresses`.** LibreChat blokuje adresy zwrotne (loopback) oraz prywatne miejsca docelowe w momencie nawiązywania połączenia, dlatego samodzielnie hostowana instancja musi mieć swój dokładny `host:port` wymieniony w sekcji [`allowedAddresses`](#ssrf-protection-and-private-providers). Blokuje to żądanie całkowicie, zamiast zwracać pusty zestaw wyników.
2. **`json` brakuje w `formats`.** LibreChat żąda wyników w formacie JSON. Jeśli sekcja `formats` w pliku ustawień instancji nie zawiera `json`, instancja odpowiada stroną z błędem, a każde wyszukiwanie kończy się pustym wynikiem. Jest to krok 3 powyższej konfiguracji Docker.
3. **Każdy wybrany silnik zawiódł.** SearXNG łączy wyniki z silników, które odpowiedziały, więc pusta odpowiedź oznacza, że żaden z nich nic nie zwrócił. DuckDuckGo znajduje się w domyślnym zestawie (`google,bing,duckduckgo`) i wyświetla CAPTCHA większości instancji self-hosted, co pozostawia tylko dwa silniki do obsługi wyszukiwania. Ustaw `searxngSearchOptions.engines` jawnie, pomiń DuckDuckGo i wymień kilka innych silników.
4. **Nazwa silnika nie istnieje w Twojej instancji.** SearXNG pomija po cichu silniki, których nie rozpoznaje, więc literówka oznacza po prostu mniej wyników. Porównaj swoją listę z `https://your-instance/config`.
5. **Instancja jest wolniejsza niż limit czasu.** Zwiększ `searxngSearchOptions.timeout` powyżej domyślnej wartości `10000`, jeśli Twoja instancja odpytuje wiele silników lub znajduje się za wolnym łączem sieciowym.

Możesz odtworzyć to, co wysyła LibreChat, wywołując instancję bezpośrednio:

```bash
curl -s "http://localhost:55011/search?q=librechat&format=json&engines=google,bing,startpage" | head -c 500
```

Uruchamia się to z Twojej własnej maszyny, więc całkowicie omija mechanizm ochrony połączeń LibreChat. Pusta tablica `results` w tym miejscu potwierdza, że problem leży po stronie SearXNG; jeśli wyniki się pojawiają, a LibreChat nadal nic nie znajduje, wskazuje to na `allowedAddresses`.
