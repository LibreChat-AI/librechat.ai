---
title: Configuração de Pesquisa na Web
icon: Search
---

A configuração `webSearch` permite que você personalize a funcionalidade de pesquisa na web dentro do LibreChat, incluindo provedores de pesquisa, scrapers de conteúdo e rerankers de resultados.

## Visão geral [#overview]

O recurso de pesquisa na web consiste em três componentes principais:

1. **Provedores de pesquisa**: Serviços que realizam a pesquisa inicial na web
2. **Scrapers**: Serviços que extraem conteúdo de páginas da web
3. **Rerankers**: Serviços que reordenam resultados de pesquisa para melhor relevância

## Exemplo [#example]

```yaml filename="webSearch"
webSearch:
  # Search Provider Configuration
  serperApiKey: "${SERPER_API_KEY}"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}"
  searchProvider: "serper" # Options: "serper", "searxng", "tavily", "keenable"

  # Optional: query options sent to a SearXNG instance
  searxngSearchOptions:
    engines: "google,bing,startpage" # Default: "google,bing,duckduckgo"
    language: "en" # Default: "all"

  # Tavily Configuration (search and/or scraper)
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"

  # Keenable Configuration (search and/or scraper; all credentials optional)
  keenableApiKey: "${KEENABLE_API_KEY}"
  keenableApiUrl: "${KEENABLE_API_URL}"
  keenableSearchOptions:
    maxResults: 8
    site: "example.com"
    attributionTitle: "LibreChat"
    timeout: 15000
  keenableScraperOptions:
    attributionTitle: "LibreChat"
    timeout: 15000

  # Scraper Configuration
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlApiUrl: "${FIRECRAWL_API_URL}"
  firecrawlVersion: "${FIRECRAWL_VERSION}"
  scraperProvider: "firecrawl" # Options: "firecrawl", "serper", "tavily", "keenable"

  # Reranker Configuration
  jinaApiKey: "${JINA_API_KEY}"
  jinaApiUrl: "${JINA_API_URL}"
  cohereApiKey: "${COHERE_API_KEY}"
  rerankerType: "jina" # Options: "jina", "cohere", "none"

  # General Settings
  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)
  # Required for deliberately private self-hosted provider endpoints
  allowedAddresses:
    - "searxng:8080"
```

## Proteção contra SSRF e Provedores Privados [#ssrf-protection-and-private-providers]

O LibreChat valida cada conexão que abre para pesquisa na web, scraping e reranking. Destinos privados, loopback, link-local e de metadados de nuvem são bloqueados por padrão, incluindo saltos de redirecionamento e nomes de host que resolvem para um endereço privado no momento da conexão.

Use `webSearch.allowedAddresses` quando um endpoint configurado de SearXNG, Firecrawl, Jina, compatível com Tavily, compatível com Keenable ou provedor relacionado for deliberadamente privado:

```yaml filename="webSearch / allowedAddresses"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  allowedAddresses:
    - "searxng:8080"
    - "10.0.0.5:3002"
    - "[fd00::5]:8080"
```

Este campo é uma lista de exceção de SSRF, não uma lista de permissões estrita. Destinos públicos permanecem disponíveis. As entradas devem ser pares exatos de `host:port`, `private.ip:port` ou `[ipv6]:port`; URLs, caminhos, intervalos CIDR, hosts/IPs simples e literais de IP público são rejeitados. Um nome de host isento confia em qualquer IP privado para o qual ele seja resolvido naquela porta, portanto, prefira um literal de IP privado e liste apenas nomes cujo DNS você controla.

O LibreChat isenta automaticamente um endpoint de proxy de encaminhamento HTTP(S) configurado para que ele possa ser alcançado. Quando o proxy realiza uma solicitação, ele resolve o destino e deve aplicar a saída (egress) de destino por conta própria. `NO_PROXY` pode tornar uma solicitação direta, onde o guardião de conexão do LibreChat ainda se aplica. O guardião cobre conexões que o LibreChat abre para APIs de provedores; ele não pode restringir uma URL de página que um scraper de terceiros recebe como dados de solicitação e busca em sua própria infraestrutura.

## Provedores de Busca [#search-providers]

### searchProvider

<OptionTable
  options={[
    ['searchProvider', 'String', 'Especifica qual provedor de busca utilizar.', 'Options: "serper", "searxng", "tavily", "keenable"'],
  ]}
/>

### serperApiKey

<OptionTable
  options={[
    ['serperApiKey', 'String', 'Nome da variável de ambiente para a chave de API do Serper. Se não for definida no .env, os usuários serão solicitados a fornecê-la via interface.', '${SERPER_API_KEY}'],
  ]}
/>

**Nota:** Obtenha sua chave de API em [Serper.dev](https://serper.dev/api-keys)


### searxngInstanceUrl

<OptionTable
  options={[
    ['searxngInstanceUrl', 'String', 'Nome da variável de ambiente para a URL da instância do SearXNG. Se não for definida no .env, os usuários serão solicitados a fornecê-la via interface.', '${SEARXNG_INSTANCE_URL}'],
  ]}
/>

### searxngApiKey

<OptionTable
  options={[
    ['searxngApiKey', 'String', 'Nome da variável de ambiente para a chave de API do SearXNG. Se não for definida no .env, os usuários serão solicitados a fornecê-la via interface.', '${SEARXNG_API_KEY}'],
  ]}
/>

**Nota:** Isso é opcional e necessário apenas se a sua instância do SearXNG exigir autenticação.

### searxngSearchOptions

<OptionTable
  options={[
    ['searxngSearchOptions', 'Object', 'Opções de consulta enviadas para sua instância do SearXNG em cada pesquisa. Cada subchave é opcional, e o bloco só é lido quando searchProvider for "searxng".', ''],
  ]}
/>

**Subchaves:**

<OptionTable
  options={[
    ['engines', 'String or Array of Strings', 'Motores que sua instância deve consultar, enviados ao SearXNG como o parâmetro engines. Aceita uma string separada por vírgulas ou uma lista YAML; ambos são normalizados para a forma separada por vírgulas, com espaços em branco ao redor e entradas vazias removidas. Um valor que fica vazio após o corte é tratado como não definido.', 'Default: "google,bing,duckduckgo"'],
    ['language', 'String', 'Código de idioma do resultado, enviado ao SearXNG como o parâmetro de idioma. Use um código que sua instância aceite, como "en", "de" ou "fr".', 'Default: "all"'],
    ['timeRange', 'String', 'Restringe os resultados pela data de publicação, enviado ao SearXNG como time_range. Omitido da requisição quando não definido.', 'Options: "day", "month", "year"'],
    ['timeout', 'Number', 'Tempo limite da requisição HTTP em milissegundos para chamadas à sua instância. Deve ser um número inteiro positivo não superior a 120000; 0 é rejeitado porque desativaria o tempo limite.', 'Default: 10000'],
  ]}
/>

<Callout type="info" title="Disponibilidade">
`searxngSearchOptions` alcança o SearXNG através da ferramenta de busca em `@librechat/agents` e requer o `@librechat/agents` v3.6.9 ou superior. Versões compiladas com uma versão anterior ignoram o bloco em vez de falhar ao iniciar, portanto, uma instância que continua retornando resultados padrão pode simplesmente estar executando uma versão antiga.
</Callout>

**Exemplo:**

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}" # Optional
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeRange: "month"
    timeout: 10000
```

`engines` também aceita o formato separado por vírgulas que o próprio SearXNG utiliza, portanto, isto é equivalente à lista acima:

```yaml filename="webSearch"
webSearch:
  searxngSearchOptions:
    engines: "google, bing, startpage, qwant"
```

#### Escolhendo engines [#choosing-engines]

Os nomes dos engines devem corresponder aos engines que estão habilitados na sua própria instância, e duas coisas decorrem disso:

- O SearXNG ignora um mecanismo que ele não reconhece em vez de relatar um erro. Um erro de digitação, ou um mecanismo que está desativado em sua instância, resulta em menos resultados em vez de uma falha.
- Os nomes válidos são específicos da instância. Verifique a lista habilitada em `https://your-instance/config`, que retorna um JSON incluindo cada engine habilitada, ou abra a aba **Engines** na página de preferências da sua instância.

O padrão é `google,bing,duckduckgo`. O SearXNG agrega o que quer que os mecanismos selecionados retornem, portanto, um mecanismo bloqueado custa apenas a sua parcela dos resultados em vez da resposta inteira, e uma pesquisa só retorna vazia quando todos os mecanismos selecionados falham ou não retornam nada.

DuckDuckGo é o mecanismo no conjunto padrão com maior probabilidade de ser bloqueado, uma vez que ele apresenta CAPTCHAs para a maioria das instâncias auto-hospedadas. Com apenas três mecanismos no conjunto, perdê-lo ao mesmo tempo que o Google ou o Bing, devido a limites de taxa, é uma forma comum de acabar sem nenhum resultado. Definir `engines` explicitamente, deixando o DuckDuckGo de fora e listando mais de três mecanismos, reduz esse risco.

Motores que respondem rapidamente e toleram tráfego auto-hospedado compõem o melhor conjunto inicial, por exemplo:

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - brave
      - startpage
      - qwant
```

Adicionar mais mecanismos amplia a cobertura, mas também torna cada pesquisa mais lenta, já que o SearXNG aguarda o mecanismo mais lento do conjunto antes de responder. Tenha o `timeout` em mente ao aumentar a lista.

#### O que estas opções não controlam [#what-these-options-do-not-control]

- **Safe search** não faz parte deste bloco. Ele vem da chave de nível superior [`safeSearch`](#safesearch) e é encaminhado para o SearXNG como `safesearch`.
- **Result categories** são escolhidas por consulta pelo LibreChat, que mapeia o tipo de pesquisa para `general`, `images`, `videos` ou `news`. Elas não podem ser substituídas aqui.
- **O número da página e o formato do resultado** são fixos. O LibreChat sempre solicita a página 1 no formato JSON, e é por isso que sua instância deve ter `json` habilitado em `formats`.

### tavilyApiKey

<OptionTable
  options={[
    ['tavilyApiKey', 'String', 'Nome da variável de ambiente para a chave de API do Tavily. Usada tanto para pesquisa quanto para scraper. Se não for definida no .env, os usuários serão solicitados a fornecê-la via interface.', '${TAVILY_API_KEY}'],
  ]}
/>

**Nota:** Obtenha sua chave de API em [Tavily](https://app.tavily.com/home)

### tavilySearchUrl

<OptionTable
  options={[
    ['tavilySearchUrl', 'String', 'Nome da variável de ambiente para uma URL personalizada da API Tavily Search. Opcional; o padrão é a busca hospedada pelo Tavily quando não definida.', '${TAVILY_SEARCH_URL}'],
  ]}
/>

### tavilyExtractUrl

<OptionTable
  options={[
    ['tavilyExtractUrl', 'String', 'Nome da variável de ambiente para uma URL personalizada da API Tavily Extract. Opcional; o padrão é o extract hospedado pela Tavily quando não definido.', '${TAVILY_EXTRACT_URL}'],
  ]}
/>

### tavilySearchOptions

<OptionTable
  options={[
    ['tavilySearchOptions', 'Object', 'Opções de configuração para a pesquisa Tavily.', ''],
  ]}
/>

**Subchaves:**

<OptionTable
  options={[
    ['searchDepth', 'String', 'Controla o equilíbrio entre relevância e latência. "basic" retorna um resumo de PLN por URL. "advanced" retorna múltiplos trechos semanticamente relevantes por URL (2 créditos de API). "fast" equilibra velocidade e relevância com trechos. "ultra-fast" minimiza a latência com um resumo de PLN.', 'Options: "basic", "advanced", "fast", "ultra-fast". Default: "basic"'],
    ['maxResults', 'Number', 'O número máximo de resultados de pesquisa a serem retornados.', 'Range: 1-20. Default: 5'],
    ['topic', 'String', 'A categoria da pesquisa. "news" é útil para atualizações em tempo real. "finance" para dados financeiros.', 'Options: "general", "news", "finance". Default: "general"'],
    ['includeImages', 'Boolean', 'Incluir imagens na resposta. Retorna tanto imagens da consulta de nível superior quanto imagens por resultado.', 'Default: false'],
    ['includeAnswer', 'Boolean or String', 'Inclua uma resposta gerada por LLM. "basic" ou true para uma resposta rápida, "advanced" para uma resposta detalhada.', 'Default: false'],
    ['includeRawContent', 'Boolean or String', 'Incluir conteúdo HTML limpo e analisado. "markdown" ou true para formato markdown, "text" para texto simples.', 'Default: false'],
    ['includeDomains', 'Array of Strings', 'Restrinja a pesquisa a domínios específicos. Máximo de 300 domínios.', ''],
    ['excludeDomains', 'Array of Strings', 'Excluir domínios específicos dos resultados. Máximo de 150 domínios.', ''],
    ['timeRange', 'String', 'Filtro de intervalo de tempo baseado na data de publicação ou última atualização.', 'Options: "day", "week", "month", "year"'],
    ['includeImageDescriptions', 'Boolean', 'Quando includeImages for true, adicione também um texto descritivo para cada imagem.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Inclua a URL do favicon para cada resultado de pesquisa.', 'Default: false'],
    ['chunksPerSource', 'Number', 'Número máximo de blocos de conteúdo relevante por fonte. Disponível apenas quando searchDepth for "advanced".', 'Range: 1-3. Default: 3'],
    ['safeSearch', 'Boolean', 'Substituição opcional safe_search do Tavily para solicitações de pesquisa do Tavily. Omitido por padrão; true pode exigir o Tavily Enterprise.', 'Default: omitted'],
    ['timeout', 'Number', 'Tempo limite da solicitação HTTP do lado do cliente em milissegundos. Controla por quanto tempo aguardar a resposta da API Tavily antes de desistir.', 'Default: 15000'],
  ]}
/>

### keenableApiKey

<OptionTable
  options={[
    ['keenableApiKey', 'String', 'Nome da variável de ambiente para a chave de API opcional da Keenable. O Keenable funciona sem uma chave; configurar uma aumenta os limites de taxa públicos.', '${KEENABLE_API_KEY}'],
  ]}
/>

### keenableApiUrl

<OptionTable
  options={[
    ['keenableApiUrl', 'String', 'Nome da variável de ambiente para uma URL de API de busca Keenable personalizada. Isso não altera o endpoint de busca de página.', '${KEENABLE_API_URL}'],
  ]}
/>

O endpoint page-fetch é configurado separadamente com a variável exclusiva de ambiente `KEENABLE_FETCH_URL`.

### keenableSearchOptions

<OptionTable
  options={[
    ['keenableSearchOptions', 'Object', 'Opções de configuração para a pesquisa Keenable.', ''],
  ]}
/>

<OptionTable
  options={[
    ['maxResults', 'Number', 'Número máximo de resultados de pesquisa.', 'Range: 1-20. Default: 8'],
    ['site', 'String', 'Restringe os resultados a um domínio.', ''],
    ['attributionTitle', 'String', 'Valor enviado no cabeçalho de atribuição X-Keenable-Title.', ''],
    ['timeout', 'Number', 'Tempo limite da requisição HTTP do lado do cliente em milissegundos.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

## Scrapers [#scrapers]

### firecrawlApiKey

<OptionTable
  options={[
    ['firecrawlApiKey', 'String', 'Nome da variável de ambiente para a chave de API do Firecrawl. Se não for definida no .env, os usuários serão solicitados a fornecê-la via interface.', '${FIRECRAWL_API_KEY}'],
  ]}
/>

**Nota:** Obtenha sua chave de API em [Firecrawl.dev](https://docs.firecrawl.dev/introduction#api-key)

### firecrawlApiUrl

<OptionTable
  options={[
    ['firecrawlApiUrl', 'String', 'Nome da variável de ambiente para a URL da API do Firecrawl. Se não for definida no .env, os usuários serão solicitados a fornecê-la via interface.', '${FIRECRAWL_API_URL}'],
  ]}
/>

**Nota:** Isso é opcional e necessário apenas se você estiver usando uma instância personalizada do Firecrawl.

### firecrawlVersion

<OptionTable
  options={[
    ['firecrawlVersion', 'String', 'Nome da variável de ambiente para a versão da API do Firecrawl (v0 ou v1).', '${FIRECRAWL_VERSION}'],
  ]}
/>

### scraperProvider

<OptionTable
  options={[
    ['scraperProvider', 'String', 'Especifica qual serviço de scraper utilizar.', 'Options: "firecrawl", "serper", "tavily", "keenable"'],
  ]}
/>

### firecrawlOptions

<OptionTable
  options={[
    ['firecrawlOptions', 'Object', 'Opções de configuração avançadas para o scraper Firecrawl.', ''],
  ]}
/>

**Subchaves:**

#### formats

<OptionTable
  options={[
    ['formats', 'Array of Strings', 'Formatos a incluir na saída.', ''],
  ]}
/>

#### includeTags

<OptionTable
  options={[
    ['includeTags', 'Array of Strings', 'Tags para incluir na saída.', ''],
  ]}
/>

#### excludeTags

<OptionTable
  options={[
    ['excludeTags', 'Array of Strings', 'Tags a serem excluídas da saída.', ''],
  ]}
/>

#### headers

<OptionTable
  options={[
    ['headers', 'Object', 'Cabeçalhos a serem enviados com a requisição. Podem ser usados para enviar cookies, user-agent, etc.', ''],
  ]}
/>

#### waitFor

<OptionTable
  options={[
    ['waitFor', 'Number', 'Especifique um atraso em milissegundos antes de buscar o conteúdo, permitindo tempo suficiente para a página carregar.', ''],
  ]}
/>

#### timeout

<OptionTable
  options={[
    ['timeout', 'Integer', 'Tempo limite em milissegundos para a solicitação de scraping. Deve ser um número inteiro não negativo.', 'Default: 7500'],
  ]}
/>

#### maxAge

<OptionTable
  options={[
    ['maxAge', 'Number', 'Retorna uma versão em cache da página se ela for mais recente do que esta idade em milissegundos. Se uma versão em cache da página for mais antiga do que este valor, a página será extraída (scraped).', ''],
  ]}
/>

**Nota:** Se você não precisa de dados extremamente recentes, habilitar isso pode acelerar suas capturas (scrapes) em 500%.

#### mobile

<OptionTable
  options={[
    ['mobile', 'Boolean', 'Emular scraping a partir de um dispositivo móvel.', ''],
  ]}
/>

#### skipTlsVerification

<OptionTable
  options={[
    ['skipTlsVerification', 'Boolean', 'Ignorar a verificação de certificado TLS ao fazer solicitações.', ''],
  ]}
/>

#### blockAds

<OptionTable
  options={[
    ['blockAds', 'Boolean', 'Ativa o bloqueio de anúncios e o bloqueio de pop-ups de cookies.', ''],
  ]}
/>

#### removeBase64Images

<OptionTable
  options={[
    ['removeBase64Images', 'Boolean', 'Remove todas as imagens em base 64 da saída, que podem ser excessivamente longas. O texto alternativo da imagem permanece na saída, mas a URL é substituída por um espaço reservado.', ''],
  ]}
/>

#### parsePDF

<OptionTable
  options={[
    ['parsePDF', 'Boolean', 'Controla como os arquivos PDF são processados durante a extração.', ''],
  ]}
/>

#### storeInCache

<OptionTable
  options={[
    ['storeInCache', 'Boolean', 'Se definido como true, a página será armazenada no índice e no cache do Firecrawl. Definir isso como false é útil se sua atividade de scraping puder envolver preocupações com proteção de dados. O uso de alguns parâmetros associados a scraping sensível (headers) forçará este parâmetro a ser false.', ''],
  ]}
/>

#### zeroDataRetention

<OptionTable
  options={[
    ['zeroDataRetention', 'Boolean', 'Se verdadeiro, isso habilitará a retenção zero de dados para esta raspagem (requer configuração prévia no Firecrawl).', ''],
  ]}
/>

#### location

<OptionTable
  options={[
    ['location', 'Object', 'Localização geográfica e configurações de idioma para scraping.', ''],
  ]}
/>

#### onlyMainContent

<OptionTable
  options={[
    ['onlyMainContent', 'Boolean', 'Retorne apenas o conteúdo principal da página, excluindo cabeçalhos, navegação, rodapés, etc.', ''],
  ]}
/>

#### changeTrackingOptions

<OptionTable
  options={[
    ['changeTrackingOptions', 'Object', 'Configuração para rastrear alterações em conteúdo extraído.', ''],
  ]}
/>

**Exemplo:**
```yaml filename="webSearch"
webSearch:
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlOptions:
    formats: ["markdown", "rawHtml"]
    includeTags: ["main", "article", ".content"]
    excludeTags: ["nav", "footer", ".ads"]
    waitFor: 2000
    timeout: 10000
    mobile: false
    blockAds: true
    onlyMainContent: true
    location:
      country: "US"
      languages: ["en"]
```

**Nota:** Para informações detalhadas sobre as opções e padrões do scraper Firecrawl, consulte a [Documentação da API do Firecrawl](https://docs.firecrawl.dev/api-reference/endpoint/scrape).

### tavilyScraperOptions

<OptionTable
  options={[
    ['tavilyScraperOptions', 'Object', 'Opções de configuração para o Tavily Extract (scraper).', ''],
  ]}
/>

**Subchaves:**

<OptionTable
  options={[
    ['extractDepth', 'String', 'A profundidade do processo de extração. "advanced" recupera mais dados, incluindo tabelas e conteúdo incorporado com maior sucesso, mas pode aumentar a latência. "basic" custa 1 crédito por 5 URLs bem-sucedidas, "advanced" custa 2 créditos por 5 URLs bem-sucedidas.', 'Options: "basic", "advanced". Default: "basic"'],
    ['includeImages', 'Boolean', 'Inclua uma lista de imagens extraídas das URLs na resposta.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Inclua a URL do favicon para cada resultado extraído.', 'Default: false'],
    ['format', 'String', 'O formato do conteúdo da página web extraído. "markdown" retorna o conteúdo em formato markdown. "text" retorna texto simples e pode aumentar a latência.', 'Options: "markdown", "text". Default: "markdown"'],
    ['timeout', 'Number', 'Tempo limite em milissegundos. Controla o tempo limite de HTTP do lado do cliente. Quando definido, também envia ao Tavily um tempo limite de extração do lado do servidor convertido em segundos e limitado a 1-60s.', 'Default: 15000 for basic, 30000 for advanced'],
  ]}
/>

**Exemplo:**
```yaml filename="webSearch"
webSearch:
  searchProvider: tavily
  scraperProvider: tavily
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  # tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  # tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"
  tavilySearchOptions:
    searchDepth: basic
    maxResults: 5
    topic: general
  tavilyScraperOptions:
    extractDepth: basic
```

**Nota:** Para informações detalhadas sobre as opções da API Tavily, consulte a [Documentação da API Tavily](https://docs.tavily.com).

### keenableScraperOptions

<OptionTable
  options={[
    ['keenableScraperOptions', 'Object', 'Opções de configuração para a busca de páginas do Keenable.', ''],
  ]}
/>

<OptionTable
  options={[
    ['attributionTitle', 'String', 'Valor enviado no cabeçalho de atribuição X-Keenable-Title.', ''],
    ['timeout', 'Number', 'Tempo limite da requisição HTTP do lado do cliente em milissegundos.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

O Keenable pode fornecer pesquisa, busca de páginas ou ambos. Seus endpoints públicos funcionam sem uma chave; o `keenableApiKey` opcional aumenta os limites de taxa. Defina `rerankerType: "none"` para evitar a necessidade de uma chave de reranker:

```yaml filename="webSearch"
webSearch:
  searchProvider: keenable
  scraperProvider: keenable
  rerankerType: none
  # keenableApiKey: "${KEENABLE_API_KEY}" # Optional
  # keenableApiUrl: "${KEENABLE_API_URL}" # Optional search endpoint
  keenableSearchOptions:
    maxResults: 8
    # site: example.com
  keenableScraperOptions:
    timeout: 15000
```

## Rerankers [#rerankers]

### jinaApiKey

<OptionTable
  options={[
    ['jinaApiKey', 'String', 'Nome da variável de ambiente para a chave de API da Jina. Se não for definida no .env, os usuários serão solicitados a fornecê-la via interface.', '${JINA_API_KEY}'],
  ]}
/>

**Nota:** Obtenha sua chave de API em [Jina.ai](https://jina.ai/api-dashboard/)

### jinaApiUrl

<OptionTable
  options={[
    ['jinaApiUrl', 'String', 'Nome da variável de ambiente para a URL da API Jina. Se não for definida no .env, os usuários serão solicitados a fornecê-la via interface.', '${JINA_API_URL}'],
  ]}
/>

**Nota:** Isso é opcional e necessário apenas se você estiver usando uma instância personalizada do Jina.

### cohereApiKey

<OptionTable
  options={[
    ['cohereApiKey', 'String', 'Nome da variável de ambiente para a chave de API da Cohere. Se não for definida no .env, os usuários serão solicitados a fornecê-la via interface.', '${COHERE_API_KEY}'],
  ]}
/>

**Nota:** Obtenha sua chave de API no [Cohere Dashboard](https://dashboard.cohere.com/welcome/login)

### rerankerType

<OptionTable
  options={[
    ['rerankerType', 'String', 'Especifica qual serviço de reranker utilizar. Defina como "none" para pular o reranking.', 'Options: "jina", "cohere", "none"'],
  ]}
/>

## Configurações Gerais [#general-settings]

### scraperTimeout

<OptionTable
  options={[
    ['scraperTimeout', 'Integer', 'Tempo limite em milissegundos para solicitações do scraper. Deve ser um número inteiro não negativo.', 'Default: 7500'],
  ]}
/>

### safeSearch

<OptionTable
  options={[
    ['safeSearch', 'Number', 'Nível de filtragem de pesquisa segura. 0 = DESLIGADO (sem filtragem), 1 = MODERADO (padrão), 2 = ESTRITO (filtragem máxima).', 'Default: 1 (MODERATE)'],
  ]}
/>

**Nota:** Os níveis de pesquisa segura estão alinhados com as convenções padrão da API de pesquisa. A filtragem MODERATE (moderada) é ativada por padrão para fornecer uma filtragem de conteúdo razoável, mantendo a eficácia da pesquisa. O Tavily não herda essa configuração global por padrão; use `tavilySearchOptions.safeSearch` apenas se sua conta Tavily oferecer suporte a `safe_search`.

## Notas [#notes]

- As chaves de API podem ser configuradas de duas maneiras:
  1. Defina as variáveis de ambiente especificadas na configuração YAML
  2. Se as variáveis de ambiente não estiverem definidas, os usuários serão solicitados a fornecer as chaves de API através da interface.
- A configuração suporta múltiplos serviços para cada componente (providers, scrapers, rerankers)
- Se um tipo de serviço específico não for especificado, o sistema tentará todos os serviços disponíveis nessa categoria
- O Safe search oferece três níveis de filtragem de conteúdo: OFF (0), MODERATE (1) e STRICT (2)
- O Tavily não herda a configuração global de pesquisa segura por padrão; defina `tavilySearchOptions.safeSearch` explicitamente apenas quando sua conta Tavily oferecer suporte a `safe_search`.
- O Keenable oferece suporte a busca sem chave e busca de página; sua chave opcional aumenta os limites de taxa públicos, e sua substituição de URL de busca é exclusiva do ambiente.
- O comportamento de consulta do SearXNG (engines, idioma, intervalo de tempo, tempo limite da solicitação) é configurado em `searxngSearchOptions`; veja [Escolhendo engines](#choosing-engines) caso as pesquisas retornem vazias
- Nunca coloque chaves de API reais na configuração YAML - use apenas nomes de variáveis de ambiente

## Configurando o SearXNG [#setting-up-searxng]

SearXNG é um meta mecanismo de busca focado em privacidade que você pode auto-hospedar. 
Para mais informações, consulte a [documentação oficial do SearXNG](https://docs.searxng.org/).

Aqui estão os passos para configurar sua própria instância do SearXNG para uso com o LibreChat:

### Usando o Docker Desktop [#using-docker-desktop]

1. **Procure pela imagem oficial do SearXNG**
   - Abra o Docker Desktop
   - Pesquise por `searxng/searxng` na aba **Images**
   - Clique em **Run** na imagem oficial para baixar automaticamente e executar um container da imagem

2. **Executando o container**
   - Expanda o menu suspenso **Optional Settings** no painel subsequente que aparece assim que o download for concluído
   - Defina os detalhes de configuração desejados (número da porta, nome do container, etc.)
   - Clique em **Run** para iniciar o container

3. **Configurar o SearXNG para o LibreChat**
   - Navegue até a aba `Files` no Docker Desktop
   - Vá para `/etc/searxng/settings.yaml`
   - Abra o editor de arquivos
   - Navegue até a seção `formats`
   - Adicione `json` como um formato aceitável para que o LibreChat possa se comunicar com sua instância
   - Salve o arquivo

4. **Reinicie o container**
   - Reinicie o container para que as alterações entrem em vigor

#### Guia em vídeo: [#video-guide]

Aqui está um vídeo para guiá-lo pelo processo do início ao fim em cerca de um minuto:

     <Video src="/videos/min_searxng_docker_setup_hq.mp4" title="Passo a passo da configuração do SearXNG no Docker" />

**Nota:** Neste exemplo, a URL da instância é http://localhost:55011 (o número da porta encontrado abaixo do nome do container no canto superior esquerdo ao final do vídeo)

### Configurando o LibreChat para usar o SearXNG [#configuring-librechat-to-use-searxng]

Você pode configurar o SearXNG no LibreChat através da interface do usuário ou pelo `librechat.yaml`.

#### Configuração da UI [#ui-configuration]

1. **Abra o menu suspenso de ferramentas na barra de entrada do chat**
![Botão de configuração de ferramentas](/images/web-search/tools_highlight.png)

2. **Clique no ícone de engrenagem ao lado de Web Search**
![Tools configuration section](/images/web-search/tools_config_highlight.png)

3. **Selecione SearXNG no menu suspenso Search Provider**
![SearXNG dropdown selection](/images/web-search/searxng_dropdown_highlight.png)

4. **Insira os detalhes da sua configuração (por exemplo, URL da instância, tipo de scraper, etc.) e clique em salvar**
![Salvar configuração de pesquisa na web](/images/web-search/web_search_save.png)

5. **Clique na opção Web Search no menu suspenso de ferramentas**
![Web search badge in chat interface](/images/web-search/websearch_highlight.png)

6. **O selo de Web Search agora deve estar ativado, o que significa que suas consultas podem utilizar a funcionalidade de pesquisa na web**
![Confirmação do selo de Web Search](/images/web-search/search_badge_confirm.png)

#### Configuração YAML [#yaml-configuration]

Para configurar o SearXNG para todos na instância em vez de por usuário, defina os mesmos valores no `librechat.yaml`:

```yaml filename="librechat.yaml"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  # searxngApiKey: "${SEARXNG_API_KEY}" # Only if your instance requires authentication
  # Required: a self-hosted instance is a private destination, so exempt it from the connection guard
  allowedAddresses:
    - "localhost:55011"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeout: 10000
```

A URL e a chave ainda são lidas a partir do ambiente, portanto, combine a configuração acima com:

```bash filename=".env"
SEARXNG_INSTANCE_URL=http://localhost:55011
# SEARXNG_API_KEY=your_api_key
```

<Callout type="warning" title="Instâncias privadas precisam de uma entrada allowedAddresses">
`localhost:55011` é um endereço de loopback, o qual a [proteção SSRF](#ssrf-protection-and-private-providers) do LibreChat bloqueia no momento da conexão. Sem a entrada correspondente em `allowedAddresses`, a pesquisa falha mesmo quando o SearXNG está saudável. A entrada deve ser o par `host:port` exato da URL da sua instância.

Quando o LibreChat é executado no Docker, `localhost` refere-se ao contêiner do LibreChat, não ao do SearXNG. Use um nome de host que o contêiner consiga alcançar, como o nome do serviço no compose, e adicione-o à lista de exceções:

```yaml filename="librechat.yaml"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}" # SEARXNG_INSTANCE_URL=http://searxng:8080
  allowedAddresses:
    - "searxng:8080"
```
</Callout>

Veja [`searxngSearchOptions`](#searxngsearchoptions) para todas as subchaves que este bloco aceita.

#### Solução de problemas de resultados vazios [#troubleshooting-empty-results]

Uma pesquisa no SearXNG que não retorna nada quase sempre significa que a solicitação nunca chegou a uma instância funcional, em vez de o LibreChat descartar os resultados. Siga estes passos em ordem:

1. **A instância é um endereço privado sem nenhuma entrada em `allowedAddresses`.** O LibreChat bloqueia destinos de loopback e privados no momento da conexão, portanto, uma instância auto-hospedada precisa que seu `host:port` exato esteja listado em [`allowedAddresses`](#ssrf-protection-and-private-providers). Isso bloqueia a solicitação imediatamente em vez de retornar um conjunto de resultados vazio.
2. **`json` está faltando em `formats`.** O LibreChat solicita resultados como JSON. Se a seção `formats` do seu arquivo de configurações da instância não listar `json`, a instância responderá com uma página de erro e todas as pesquisas retornarão vazias. Este é o passo 3 da configuração do Docker acima.
3. **Every selected engine failed.** O SearXNG mescla os mecanismos que responderam, portanto, uma resposta vazia significa que nenhum deles retornou nada. O DuckDuckGo está no conjunto padrão (`google,bing,duckduckgo`) e apresenta CAPTCHAs para a maioria das instâncias auto-hospedadas, o que deixa apenas dois mecanismos para realizar a busca. Defina `searxngSearchOptions.engines` explicitamente, remova o DuckDuckGo e liste alguns outros mecanismos.
4. **Um nome de engine não existe na sua instância.** O SearXNG ignora silenciosamente engines que ele não reconhece, portanto, um erro de digitação significa apenas menos resultados. Compare sua lista com `https://your-instance/config`.
5. **A instância está mais lenta que o tempo limite.** Aumente `searxngSearchOptions.timeout` acima do padrão `10000` se sua instância consultar muitos mecanismos ou estiver atrás de um caminho de rede lento.

Você pode reproduzir o que o LibreChat envia chamando a instância diretamente:

```bash
curl -s "http://localhost:55011/search?q=librechat&format=json&engines=google,bing,startpage" | head -c 500
```

Isso é executado a partir da sua própria máquina, portanto, ignora completamente o guardião de conexão do LibreChat. Um array `results` vazio aqui confirma que o problema está no lado do SearXNG; resultados aqui enquanto o LibreChat ainda não encontra nada apontam de volta para `allowedAddresses`.
