---
title: Cấu trúc đối tượng đăng ký
icon: UserPlus
---

## Ví dụ [#example]

```yaml filename="Registration Object Structure"
# Example Registration Object Structure
registration:
  socialLogins: ["google", "facebook", "github", "discord", "openid"]
  allowedDomains:
    - "gmail.com"
    - "protonmail.com"
```

## socialLogins

**Khóa:**
<OptionTable
  options={[
    ['socialLogins', 'Array of Strings', 'Xác định các nhà cung cấp đăng nhập mạng xã hội khả dụng và thứ tự hiển thị của chúng.', 'The order of the providers in the list determines their appearance order on the login/registration page. Each provider listed must be properly configured within the system to be active and available for users. This configuration allows for a tailored authentication experience, emphasizing the most relevant or preferred social login options for your user base.'],
  ]}
/>


**Ví dụ:**
```yaml filename="registration / socialLogins"
socialLogins: ["google", "facebook", "github", "discord", "openid"]
```

## allowedDomains

**Khóa:**
<OptionTable
  options={[
    ['allowedDomains', 'Array of Strings', 'Danh sách chỉ định các tên miền email được phép đăng ký.', 'Users with email domains not listed will be restricted from registering.'],
  ]}
/>

**Bắt buộc**

**Ví dụ:**
```yaml filename="registration / allowedDomains"
allowedDomains:
  - "gmail.com"
  - "protonmail.com"
```
