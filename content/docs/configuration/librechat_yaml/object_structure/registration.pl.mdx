---
title: Struktura obiektu rejestracji
icon: UserPlus
---

## Przykład [#example]

```yaml filename="Registration Object Structure"
# Example Registration Object Structure
registration:
  socialLogins: ["google", "facebook", "github", "discord", "openid"]
  allowedDomains:
    - "gmail.com"
    - "protonmail.com"
```

## socialLogins

**Klucz:**
<OptionTable
  options={[
    ['socialLogins', 'Array of Strings', 'Definiuje dostępne metody logowania społecznościowego oraz ich kolejność wyświetlania.', 'The order of the providers in the list determines their appearance order on the login/registration page. Each provider listed must be properly configured within the system to be active and available for users. This configuration allows for a tailored authentication experience, emphasizing the most relevant or preferred social login options for your user base.'],
  ]}
/>


**Przykład:**
```yaml filename="registration / socialLogins"
socialLogins: ["google", "facebook", "github", "discord", "openid"]
```

## allowedDomains

**Klucz:**
<OptionTable
  options={[
    ['allowedDomains', 'Array of Strings', 'Lista określająca dozwolone domeny e-mail dla rejestracji.', 'Users with email domains not listed will be restricted from registering.'],
  ]}
/>

**Wymagane**

**Przykład:**
```yaml filename="registration / allowedDomains"
allowedDomains:
  - "gmail.com"
  - "protonmail.com"
```
