---
title: Struktur Objek endpoint Agents
icon: Bot
---

Halaman ini berlaku untuk endpoint [`agents`](/docs/features/agents).

## Contoh [#example]

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    recursionLimit: 50
    maxRecursionLimit: 100
    maxSubagents: 20
    maxToolCallArgBytes: 65536
    maxDeltaEventsPerTurn: 100000
    maxToolCallArgBytesByTool:
      create_file: 131072
    disableBuilder: false
    # (optional) Agent Capabilities available to all users. Omit the ones you wish to exclude. Defaults to list below.
    # Opt-in capabilities: "programmatic_tools", "stateful_code_sessions", "run_in_background", and "tool_intents".
    # capabilities: ["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]
    # (optional) File citation configuration for file_search capability
    maxCitations: 30 # Maximum total citations in responses (1-50)
    maxCitationsPerFile: 7 # Maximum citations from each file (1-10)
    minRelevanceScore: 0.45 # Minimum relevance score threshold (0.0-1.0)
    titleTiming: immediate
    activityLabel: true
    activityModel: gpt-4.1-nano
    activityPhaseLabel: true
    activityPhaseMaxPerRun: 5
    skills:
      maxCatalogSkills: 20
    statefulCodeSessions:
      allowedEnvironments: ['user', 'agent-user', 'conversation']
      environments:
        - id: managed-default
          name: Managed Code API
          type: managed
          baseURL: https://code.example.com/v1
          default: true
        - id: engineering-vm
          name: Engineering VM
          type: attached
          baseURL: https://code-bridge.example.com/v1
          pairing:
            workerId: engineering-vm
            tokenEnv: CODE_BRIDGE_ADMIN_TOKEN
        - id: personal-workers
          name: Personal Code Workers
          type: attached
          baseURL: https://code-bridge.example.com/v1
          pairing:
            allowPrincipalWorkers: true
            tokenEnv: CODE_BRIDGE_ADMIN_TOKEN
    # eventDriven:
    #   selfUrl: https://librechat.internal
    backgroundTasks:
      completionWakeups: true
    toolApproval:
      enabled: true
      mode: default
      deny: ['mcp:*:delete_*']
      ask: ['mcp:*:*']
    checkpointer:
      type: mongo
      ttl: 86400
    remoteApi:
      auth:
        oidc:
          enabled: false
```

> Konfigurasi ini mengaktifkan antarmuka pembuat untuk agen.

## recursionLimit

<OptionTable
  options={[
    [
      'recursionLimit',
      'Number',
      'Mengatur jumlah langkah default yang dapat diambil agen dalam satu proses eksekusi.',
      'Controls recursion depth to prevent infinite loops. When the limit is reached, LibreChat preserves the partial turn and offers Keep going or Answer now. This value can be configured from the UI up to maxRecursionLimit.',
    ],
  ]}
/>

**Default:** `25`

**Contoh:**

```yaml filename="endpoints / agents / recursionLimit"
recursionLimit: 50
```

Untuk informasi lebih lanjut mengenai langkah-langkah agen, lihat [Max Agent Steps](/docs/features/agents#max-agent-steps).

## maxRecursionLimit

<OptionTable
  options={[
    [
      'maxRecursionLimit',
      'Number',
      'Menetapkan jumlah langkah maksimum absolut yang dapat diambil agen dalam satu kali eksekusi.',
      'Defines the upper limit for the recursionLimit that can be set from the UI. This prevents users from setting excessively high values.',
    ],
  ]}
/>

**Default:** Jika dihilangkan, secara default akan menggunakan nilai recursionLimit atau 50 jika recursionLimit juga dihilangkan.

**Contoh:**

```yaml filename="endpoints / agents / maxRecursionLimit"
maxRecursionLimit: 100
```

Untuk informasi lebih lanjut mengenai langkah-langkah agen, lihat [Max Agent Steps](/docs/features/agents#max-agent-steps).

## Pemutus arus stream [#stream-circuit-breakers]

Pengaturan ini menghentikan eksekusi Agent ketika aliran (stream) penyedia tampak menghasilkan argumen alat yang tidak terkendali atau urutan peristiwa delta yang tidak terbatas.

<OptionTable
  options={[
    [
      'maxToolCallArgBytes',
      'Number',
      'Batas maksimum akumulasi byte untuk argumen satu panggilan tool yang di-stream. Atur ke 0 untuk menonaktifkan pelindung global.',
      '65536',
    ],
    [
      'maxDeltaEventsPerTurn',
      'Number',
      'Jumlah maksimum peristiwa yang dialirkan untuk satu giliran pembuatan model. Atur ke 0 atau abaikan untuk menonaktifkan pelindung ini.',
      '0 (disabled)',
    ],
    [
      'maxToolCallArgBytesByTool',
      'Object',
      'Penggantian byte argumen per-alat yang dikunci berdasarkan nama alat yang menghadap model. Nilai 0 menonaktifkan pelindung untuk alat tersebut.',
      'create_file: 131072',
    ],
  ]}
/>

LibreChat menggunakan batas argumen global 64 KiB dan menyertakan penggantian (override) 128 KiB untuk `create_file`, yang payload sahnya sering kali lebih besar. Entri dalam `maxToolCallArgBytesByTool` akan menggabungkan penggantian bawaan tersebut. Jika salah satu batasan terlampaui, LibreChat akan membatalkan stream penyedia yang sedang berjalan dan melaporkan batas mana yang terlampaui.

```yaml filename="endpoints / agents / stream circuit breakers"
maxToolCallArgBytes: 65536
maxDeltaEventsPerTurn: 100000
maxToolCallArgBytesByTool:
  create_file: 262144
  large_import: 524288
```

Untuk menonaktifkan setiap penjaga ukuran argumen, atur batas global dan penggantian `create_file` bawaan ke `0`:

```yaml
maxToolCallArgBytes: 0
maxToolCallArgBytesByTool:
  create_file: 0
```

## titleTiming

<OptionTable
  options={[
    [
      'titleTiming',
      'String',
      'Mengontrol kapan judul percakapan dibuat untuk endpoint agents. Nilai yang valid: "immediate" atau "final".',
      'Defaults to "immediate".',
    ],
  ]}
/>

**Default:** `"immediate"`

**Nilai yang Tersedia:**

- **`"immediate"`**: Menghasilkan judul segera setelah permintaan dimulai, secara paralel dengan respons model, menggunakan pesan pertama pengguna.
- **`"final"`**: Menunda pembuatan judul hingga respons penuh selesai. Ini mempertahankan perilaku lama.

**Contoh:**

```yaml filename="endpoints / agents / titleTiming"
titleTiming: immediate
```

## activityLabel

Atur `activityLabel: true` untuk mengelompokkan setiap blok penalaran Agen dan panggilan alat di bawah satu header satu baris yang dibuat secara otomatis. Gunakan `activityModel`, `activityEndpoint`, `activityPrompt`, `activityMaxPerRun`, dan `activityCharLimit` untuk mengontrol panggilan model tambahan beserta biayanya. Lihat [Agent Activity Groups](/docs/configuration/librechat_yaml/object_structure/shared_endpoint_settings#agent-activity-groups) untuk referensi bidang lengkap dan aturan prioritas.

```yaml filename="endpoints / agents / activity groups"
activityLabel: true
activityEndpoint: openAI
activityModel: gpt-4.1-nano
activityMaxPerRun: 20
activityCharLimit: 600
activityPhaseLabel: true
activityPhaseMaxPerRun: 5
```

`activityPhaseLabel` dapat secara independen menambahkan satu ringkasan induk yang diciutkan di sekitar setiap fase proses yang berisi setidaknya dua aktivitas logis. Gunakan `activityPhaseModel`, `activityPhaseEndpoint`, `activityPhasePrompt`, dan `activityPhaseMaxPerRun` untuk menyesuaikan panggilan tersebut; `activityCharLimit` digunakan bersama dengan label aktivitas anak.

## disableBuilder

<OptionTable
  options={[
    [
      'disableBuilder',
      'Boolean',
      'Mengontrol visibilitas dan penggunaan antarmuka pembuat untuk agen.',
      'When set to `true`, disables the builder interface for the agent, limiting direct manual interaction.',
    ],
  ]}
/>

**Default:** `false`

**Contoh:**

```yaml filename="endpoints / agents / disableBuilder"
disableBuilder: false
```

## allowedProviders

<OptionTable
  options={[
    [
      'allowedProviders',
      'Array/List of Strings',
      'Menentukan daftar penyedia endpoint (misalnya, "openAI", "anthropic", "google") yang diizinkan untuk digunakan dengan fitur Agents.',
      'If defined, only agents configured with these providers can be initialized. If omitted or empty, all configured providers are allowed.',
    ],
  ]}
/>

**Default:** `[]` (daftar kosong, semua penyedia diizinkan)

**Catatan:** Harus berupa salah satu dari berikut ini, atau nama endpoint kustom sebagaimana didefinisikan dalam [konfigurasi](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#name) Anda: - `openAI, azureOpenAI, google, anthropic, assistants, azureAssistants, bedrock`

**Contoh:**

```yaml filename="endpoints / agents / allowedProviders"
allowedProviders:
  - openAI
  - google
```

## capabilities

<OptionTable
  options={[
    [
      'capabilities',
      'Array/List of Strings',
      'Menentukan kapabilitas agen yang tersedia bagi semua pengguna untuk endpoint agents.',
      'Defines the agent capabilities that are available to all users for the agents endpoint. You can omit the capabilities you wish to exclude from the list.',
    ],
  ]}
/>

**Default:** `["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]`

`programmatic_tools`, `stateful_code_sessions`, `run_in_background`, dan `tool_intents` bersifat opsional dan harus ditambahkan secara eksplisit. Programmatic tools dan stateful sessions juga memerlukan `execute_code` dan deployment Code Interpreter yang kompatibel. Agent Builder menonaktifkan pilihan Programmatic MCP tanpa Code Interpreter, dan server akan menghapus opsi pemanggil programatik yang kedaluwarsa jika eksekusi kode tidak tersedia. `run_in_background` membuat alat Code Interpreter memenuhi syarat secara default dan memungkinkan pemilihan opsional per-alat untuk alat MCP, Plugin, dan Action yang memenuhi syarat; eksekusi kode di latar belakang juga memerlukan `execute_code` dan Code Interpreter.

`stateful_code_sessions` masih sangat eksperimental. Fitur ini memerlukan rute Code Interpreter dengan profil `stateful` terpisah yang dikonfigurasi dengan [`LIBRECHAT_CODE_BASEURL_STATEFUL`](/docs/configuration/dotenv#stateful-code-interpreter-endpoint) atau [`statefulCodeSessions.environments`](#statefulcodesessions). Permintaan stateful akan gagal secara tertutup (fail closed) alih-alih beralih ke layanan stateless normal. Di dalam Agent Builder, setiap Agen yang diaktifkan memilih cakupan ruang kerja pengguna (user), agen-dan-pengguna (agent-and-user), atau percakapan (conversation) dan, jika backend bernama dikonfigurasi, lingkungan eksekusi. Agen baru dimulai dengan cakupan default pribadi pengguna dan lingkungan eksekusi default penyebaran.

**Contoh:**

```yaml filename="endpoints / agents / capabilities"
capabilities:
  - 'deferred_tools'
  # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
  # Requires execute_code and a Code Interpreter deployment with the Tool Call Server component.
  # - 'programmatic_tools'
  - 'execute_code'
  - 'file_search'
  - 'web_search'
  - 'artifacts'
  - 'subagents'
  - 'actions'
  - 'context'
  - 'skills'
  - 'memory'
  - 'ask_user_question'
  - 'tools'
  - 'chain'
  - 'ocr'
  # Optional and highly experimental: reuse a user-, agent-and-user-, or
  # conversation-scoped workspace. Requires LIBRECHAT_CODE_BASEURL_STATEFUL
  # or a named statefulCodeSessions environment.
  # - 'stateful_code_sessions'
  # Optional: allow eligible Code Interpreter, MCP, Plugin, and Action tools to run as background tasks.
  # - 'run_in_background'
  # Optional: show live model-written intent labels for native and selected MCP tools.
  # - 'tool_intents'
```

**Catatan:** Bidang ini bersifat opsional. Jika diabaikan, perilaku defaultnya adalah menyertakan semua kapabilitas yang tercantum dalam default.

## statefulCodeSessions

Mengonfigurasi cakupan berbagi dan backend eksekusi opsional untuk ruang kerja Code Interpreter stateful yang sangat eksperimental.

<Callout type="warning" title="Sangat eksperimental">
  Stateful Code Sessions, named execution environments, attached workers, dan pairing saat ini berada dalam tahap eksperimen awal. Perilaku, konfigurasi, karakteristik persistensi, dan protokol integrasinya dapat berubah secara substansial.
</Callout>

<OptionTable
  options={[
    [
      'allowedEnvironments',
      'Array/List of Strings',
      'Cakupan ruang kerja stateful yang diizinkan: `user`, `agent-user`, dan/atau `conversation`.',
      '["user", "agent-user", "percakapan"]',
    ],
    [
      'environments',
      'Array of Objects',
      'Lingkungan Code API terkelola atau terlampir bernama opsional yang ditampilkan di Agent Builder.',
      'Unset; LIBRECHAT_CODE_BASEURL_STATEFUL remains the stateful endpoint',
    ],
  ]}
/>

```yaml filename="endpoints / agents / statefulCodeSessions"
statefulCodeSessions:
  allowedEnvironments:
    - 'user'
    - 'agent-user'
    - 'conversation'
  environments:
    - id: managed-default
      name: Managed Code API
      type: managed
      baseURL: https://code.example.com/v1
      default: true
    - id: engineering-vm
      name: Engineering VM
      type: attached
      baseURL: https://code-bridge.example.com/v1
      owner: deployment
      pairing:
        workerId: engineering-vm
        tokenEnv: CODE_BRIDGE_ADMIN_TOKEN
    - id: personal-workers
      name: Personal Code Workers
      type: attached
      baseURL: https://code-bridge.example.com/v1
      owner: deployment
      configSchema:
        permissions:
          fileWrite:
            allowed: [allow, ask, deny]
            default: ask
          commandExecution:
            allowed: [ask, deny]
            default: ask
      pairing:
        allowPrincipalWorkers: true
        tokenEnv: CODE_BRIDGE_ADMIN_TOKEN
```

`allowedEnvironments` wajib ada kapan pun blok tersebut disertakan dan harus mencakup setidaknya satu cakupan ruang kerja (workspace scope). Hilangkan seluruh blok untuk mengizinkan ketiga cakupan tersebut dan terus merutekan pekerjaan stateful melalui [`LIBRECHAT_CODE_BASEURL_STATEFUL`](/docs/configuration/dotenv#stateful-code-interpreter-endpoint). Default pribadi dan Agent Builder hanya mengizinkan nilai yang diperbolehkan. Jika administrator memperketat kebijakan, default pribadi yang tersimpan akan tetap terlihat namun dinonaktifkan, dan Agen baru akan menggunakan cakupan pertama yang diperbolehkan. Agen yang sudah diaktifkan akan tetap mempertahankan cakupan yang tersimpan, namun cakupan yang kini tidak diperbolehkan akan gagal secara tertutup (fails closed) saat runtime hingga Agen dikonfigurasi ulang.

Ketika `environments` tidak kosong, setiap entri memerlukan `id` yang unik, `name` tampilan, `type` berupa `managed` atau `attached`, serta `baseURL` HTTP(S) tanpa string kueri atau fragmen. Jika daftar tersebut berisi lingkungan yang dapat dieksekusi, tepat satu dari entri tersebut harus menetapkan `default: true`. Agent Builder kemudian akan menambahkan pemilih **Execution environment**; membiarkannya pada **Deployment default** akan menggunakan entri default tersebut. Daftar yang hanya berisi control plane pemasangan swalayan tidak memerlukan default. Agen yang menamai lingkungan yang kemudian dihapus atau tidak lagi dapat diakses akan gagal secara tertutup (fails closed) alih-alih beralih ke backend lain.

- `managed` mengarah langsung ke Code API stateful yang dikelola operator.
- `attached` mengarah ke backend `remote-bridge` Code API yang kompatibel yang menyewakan pekerjaan ke worker `@librechat/code` outbound. Worker tersebut tidak memerlukan port publik inbound, tetapi endpoint eksekusi lokalnya tetap memerlukan isolasi yang sesuai.
- `workerId` secara opsional merutekan lingkungan yang terlampir ke satu worker keluar. Ini hanya untuk sisi server dan tidak diekspos melalui konfigurasi startup klien.
- `owner` secara default adalah `deployment`. Lingkungan yang dimiliki oleh principal dibuat melalui API Code Environments LibreChat yang terautentikasi, dilindungi oleh ACL, dan digabungkan hanya untuk permintaan yang diotorisasi; jangan letakkan URL yang disediakan klien di dalam YAML deployment.
- `pairing` bersifat opsional dan hanya valid untuk entri `attached` yang dimiliki oleh deployment. `pairing.workerId` mengidentifikasi worker yang dikelola operator untuk didaftarkan, sementara `pairing.allowPrincipalWorkers: true` memungkinkan pengguna yang berwenang untuk mendaftarkan worker yang terikat dengan pemilik ke control plane tersebut. Setidaknya salah satu dari kolom tersebut wajib diisi jika `pairing` disertakan.
- `pairing.tokenEnv` menamai variabel lingkungan yang berisi token administrator bridge; variabel ini tidak pernah berisi token itu sendiri. Pairing memerlukan HTTPS kecuali untuk pengembangan loopback.
- `configSchema.permissions` secara opsional memungkinkan pemilik yang berwenang untuk memilih kebijakan `fileWrite` dan `commandExecution` untuk lingkungan pribadi di bawah **Settings > Code environments**. Setiap kolom memerlukan daftar `allowed` yang tidak kosong yang berisi `allow`, `ask`, dan/atau `deny`; `default` adalah `ask` jika dihilangkan dan harus muncul di dalam `allowed`. LibreChat akan menolak nilai tersimpan yang berada di luar daftar administrator.
- Jangan konfigurasikan `settings` di dalam deployment YAML. LibreChat menyediakan field dengan cakupan permintaan (request-scoped) tersebut dari preferensi pemilik yang terautentikasi dan tervalidasi oleh server.
- Hanya kebijakan penulisan file dan eksekusi perintah yang dapat dikonfigurasi oleh pengguna. Isolasi, jaringan, pemasangan (mounts), eksekusi istimewa (privileged execution), ingress, egress, dan rahasia (secrets) tetap dikendalikan oleh administrator.
- Jika `workerId` tingkat atas dan `pairing.workerId` keduanya ada, keduanya harus cocok. Entri dengan `allowPrincipalWorkers: true` dan tidak ada ID pekerja yang bersifat khusus-pasangan (pairing-only): entri tersebut dihilangkan dari pemilih eksekusi dan tidak dapat menetapkan `default: true`.

Eksekusi terlampir secara otomatis mengaktifkan garis dasar persetujuan yang aman: penulisan file serta eksekusi perintah atau kode akan meminta konfirmasi, sementara operasi baca-saja dan pencarian tetap berjalan di bawah kebijakan alat reguler. Garis dasar ini dicakupkan ke Agen yang menggunakan lingkungan terlampir. Jika tidak ada pengaturan pengguna yang tersedia, LibreChat menggunakan `ask`; [`toolApproval.enabled: false`](#toolapproval) adalah penggantian darurat administrator yang menonaktifkan garis dasar lingkungan terlampir ini. Pemanggil tanpa dukungan persetujuan dan resume akan gagal secara tertutup (fail closed) kecuali penggantian eksplisit tersebut diatur.

Eksekusi terlampir memerlukan build remote-bridge Code Interpreter eksperimental yang cocok. Penyandingan mengautentikasi koneksi worker keluar; ini tidak menggantikan isolasi sandbox atau VM. Pengguna dengan izin manajemen Code Environment melakukan penyandingan, mengonfigurasi kebijakan alat yang diizinkan, dan mencabut worker pribadi di bawah **Settings > Code environments**. Lihat [Stateful Code Sessions](/docs/features/code_interpreter#stateful-code-sessions).

## eventDriven

Mengonfigurasi origin internal tepercaya yang digunakan oleh pengiriman event Agent. Sebagian besar deployment harus mengabaikan blok ini dan menggunakan listener yang terikat pada proses saat ini.

<OptionTable
  options={[
    [
      'selfUrl',
      'URL',
      'Mengganti listener terikat proses saat ini untuk penerimaan pemicu internal tepercaya. Gunakan hanya jika pengiriman harus melewati origin HTTP tepercaya lainnya.',
      '',
    ],
  ]}
/>

```yaml filename="endpoints / agents / eventDriven"
endpoints:
  agents:
    eventDriven:
      selfUrl: https://librechat.internal
```

Kelanjutan turunan terikat bersifat otomatis. Bidang `childTurns`, `completionWakeups`, `coalescing`, `actorMailbox`, `checkpointForks`, dan `durableReceipts` yang lebih lama di bawah `eventDriven` bukan lagi merupakan sakelar konfigurasi dan harus dihapus. Runtime sekarang menerapkan jaminan pengiriman tersebut secara langsung. Bidang terpisah [`backgroundTasks.completionWakeups`](#backgroundtasks) mengontrol pengiriman percakapan untuk alat latar belakang yang telah selesai dan Subagent yang terlepas.

Biarkan `selfUrl` tidak disetel untuk jalur pendengar terikat (bound-listener) normal. Setel hanya jika penerimaan pemicu internal tepercaya harus melintasi origin HTTP(S) lain, seperti pintu depan TLS. `AGENT_TRIGGERS_SELF_URL` tetap menjadi cadangan kompatibilitas saat kolom ini dihilangkan. Lihat [Peristiwa API Agen](/docs/features/agents_api#agent-events) dan [Kelanjutan Induk Otomatis](/docs/features/subagents#automatic-parent-continuation).

Penyelesaian Action yang terlepas (detached) dari Event Actor dipilih secara otomatis dari penyimpanan generasi bawaan. Eksekusi dalam memori bersifat lokal terhadap proses; stream generasi Redis menambahkan pemulihan restart yang tahan lama dan serah terima replika. Tidak diperlukan switch `librechat.yaml` atau flag fitur environment. Lihat [Agent Event Runtime](/docs/configuration/dotenv#agent-event-runtime).

## backgroundTasks

Mengontrol apakah alat latar belakang yang telah selesai dan Subagent yang terlepas yang didukung secara otomatis melanjutkan Agen induk mereka yang tersimpan.

<OptionTable
  options={[
    [
      'completionWakeups',
      'Boolean',
      'Secara otomatis mengirimkan penyelesaian tugas latar belakang yang didukung sebagai kelanjutan. Atur ke false untuk perilaku poll-only.',
      'true',
    ],
  ]}
/>

```yaml filename="endpoints / agents / backgroundTasks"
backgroundTasks:
  completionWakeups: true
```

Pengiriman penyelesaian otomatis diaktifkan bahkan ketika blok ini dihilangkan. Tetapkan `completionWakeups: false` untuk mengharuskan Agen mengumpulkan setiap hasil melalui `check_background_task`.

Eksekusi alat latar belakang biasa tetap bersifat lokal terhadap proses dan tidak bertahan jika proses pekerja tersebut hilang. Setelah hasil terminal yang hanya berisi konten disimpan, pengirimannya bersifat permanen dan dapat berlanjut di replika lain. Tugas dengan artefak langsung masih memerlukan polling pada proses yang memilikinya. Subagent yang terlepas (Detached Subagents) tetap mempertahankan transkrip permanen dan jalur hasil terminal mereka sendiri. Polling manual tetap tersedia untuk status, kontrol, dan pemulihan dalam mode mana pun. Lihat [Background Tool Calls](/docs/features/agents#background-tool-calls) dan [Automatic Parent Continuation](/docs/features/subagents#automatic-parent-continuation).

## toolApproval

Mengontrol tinjauan manusia untuk pemanggilan alat Agen. Kebijakan di seluruh penyebaran dinonaktifkan jika blok ini dihilangkan. Agen yang menggunakan lingkungan Kode terlampir adalah pengecualian: LibreChat mengaktifkan kebijakan keamanan *ask-by-default* yang dicakup kecuali administrator secara eksplisit menetapkan `enabled: false`. Ketika tinjauan berlaku, pemanggilan yang cocok akan dijeda hingga pengguna mengirimkan keputusan yang diizinkan, seperti menyetujui, menolak, atau mengedit argumen.

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Mengaktifkan persetujuan alat di seluruh deployment. Nilai false secara eksplisit juga menonaktifkan baseline keamanan lingkungan Code yang terlampir.',
      'Omitted',
    ],
    [
      'mode',
      'String',
      'Mengatur perilaku panggilan yang tidak cocok: `default` akan bertanya, `dontAsk` akan menolak, dan `bypass` akan menyetujui kecuali jika ditolak.',
      'default',
    ],
    ['allow', 'Array of strings', 'Pola glob untuk panggilan yang dapat dijalankan tanpa meminta konfirmasi.', ''],
    ['deny', 'Array of strings', 'Pola glob untuk panggilan yang selalu ditolak. Aturan penolakan lebih diutamakan.', ''],
    ['ask', 'Array of strings', 'Pola glob untuk panggilan yang selalu memerlukan peninjauan.', ''],
    [
      'reason',
      'String',
      'Penjelasan opsional yang ditampilkan dalam prompt persetujuan. Gunakan `{tool}` untuk menyisipkan nama alat.',
      '',
    ],
    [
      'hooks',
      'Array of objects',
      'Modul kebijakan terprogram tepercaya untuk keputusan yang sadar konteks. Hooks hanya dapat memperketat kebijakan statis.',
      '',
    ],
  ]}
/>

Pola menggunakan pencocokan glob. Alat MCP dapat dicakup dengan `mcp:<server>:<tool>`; sebagai contoh, `mcp:github:*` mencocokkan setiap alat dari server `github`. LibreChat menerapkan aturan statis dan pencocok hook ke nama runtime MCP dan alias yang menghadap model, termasuk alat yang dapat dijangkau melalui Subagent bersarang. Aturan statis dievaluasi dalam urutan `deny`, `ask`, kemudian `allow` sebelum mode yang dipilih menyediakan fallback. Oleh karena itu, aturan deny selalu menang, termasuk dalam mode `bypass`.

```yaml filename="endpoints / agents / toolApproval"
toolApproval:
  enabled: true
  mode: default
  allow:
    - 'mcp:analytics:read_*'
  deny:
    - 'mcp:*:delete_*'
  ask:
    - 'mcp:*:*'
  reason: 'Review {tool} before it runs.'
```

Hooks dimuat sekali saat server dimulai dan dieksekusi dalam proses LibreChat. Setiap entri menerima `module`, regex `matcher` nama-alat opsional, dan objek `options` opsional yang diteruskan ke builder modul tersebut. Hanya muat kode yang Anda percayai. Penggantian (override) per-peran atau per-penyewa tidak memuat ulang modul hook; implementasikan perilaku kontekstual di dalam hook tersebut.

```yaml filename="endpoints / agents / toolApproval / hooks"
toolApproval:
  enabled: true
  hooks:
    - module: '@acme/librechat-approval-hook'
      matcher: '^mcp:production:'
      options:
        requireTicket: true
```

## checkpointer

Mengonfigurasi penyimpanan untuk eksekusi Agent yang dijeda oleh persetujuan alat (tool approval) atau Ask User. Jika salah satu fitur memerlukan checkpointer dan blok ini dihilangkan, LibreChat akan menggunakan MongoDB utama dengan jendela keputusan 24 jam.

<OptionTable
  options={[
    [
      'type',
      'String',
      '`mongo` mempertahankan eksekusi yang dijeda di seluruh proses mulai ulang dan replika; `memory` bersifat lokal terhadap proses dan ditujukan untuk pengembangan.',
      'mongo',
    ],
    ['ttl', 'Number', 'Berapa lama proses yang dijeda menunggu keputusan, dalam detik.', '86400'],
    [
      'checkpointCollectionName',
      'String',
      'Penggantian nama koleksi checkpoint MongoDB opsional.',
      'agent_checkpoints',
    ],
    [
      'checkpointWritesCollectionName',
      'String',
      'Penggantian nama koleksi checkpoint-write MongoDB opsional.',
      'agent_checkpoint_writes',
    ],
  ]}
/>

```yaml filename="endpoints / agents / checkpointer"
checkpointer:
  type: mongo
  ttl: 86400
```

Gunakan `type: memory` hanya untuk deployment pengembangan proses tunggal. Run yang dijeda dan disimpan di dalam memori tidak dapat dilanjutkan setelah restart atau pada replika lain.

Checkpoint MongoDB mencatat peringatan saat status yang diserialisasi melebihi 8 MiB dan menolak jeda persisten di atas 15 MiB, menyisakan ruang di bawah batas dokumen 16 MiB MongoDB. Media yang disisipkan (inlined) atau output alat yang besar adalah penyebab umum; mulailah percakapan baru atau kurangi konteksnya jika sebuah proses melaporkan `CHECKPOINT_TOO_LARGE`.

## skills

Mengontrol pengaturan Skills tingkat endpoint untuk agen.

<OptionTable
  options={[
    [
      'skills.maxCatalogSkills',
      'Number',
      'Membatasi jumlah Skill aktif yang dapat diakses dan ditampilkan dalam katalog yang terlihat oleh model. Harus bernilai antara 1 dan 100.',
      'maxCatalogSkills: 20',
    ],
  ]}
/>

**Default:** Tidak ada batas yang dikonfigurasi selain batas katalog runtime.

**Contoh:**

```yaml filename="endpoints / agents / skills"
skills:
  maxCatalogSkills: 20
```

Ini tidak menonaktifkan Skills. Gunakan kapabilitas `skills` dan cakupan skill per-agent/model-spec untuk mengontrol apakah Skills tersedia atau tidak.

## maxSubagents

<OptionTable
  options={[
    [
      'maxSubagents',
      'Number',
      'Jumlah maksimum subagen eksplisit yang dapat dirujuk oleh satu agen.',
      'Applies to the `agent_ids` allowlist on agents and on model specs, and to configured subagent `graphs`, which agents support but model specs do not. Must be a whole number between 1 and 50.',
    ],
  ]}
/>

**Default:** `10`

**Rentang:** `1-50`

**Contoh:**

```yaml filename="endpoints / agents / maxSubagents"
endpoints:
  agents:
    maxSubagents: 20
```

Tingkatkan nilai ini saat deployment menjalankan agen yang berat dalam orkestrasi yang perlu mendelegasikan ke lebih dari sepuluh anak. Nilai ini dibaca saat startup dan diberlakukan di tiga tempat:

- Permintaan pembuatan, pembaruan, dan duplikasi agen, untuk `subagents.agent_ids` dan `subagents.graphs`
- Model spec `subagents.agent_ids` allowlists, divalidasi dalam pass konfigurasi yang sama
- Panel Agent Builder, yang berhenti menerima entri subagent baru pada nilai yang dikonfigurasi

Model specs hanya menerima `enabled`, `allowSelf`, dan `agent_ids` di bawah `subagents`. `graphs` subagent adalah fitur tingkat agen: kunci `graphs` pada model spec akan diabaikan saat konfigurasi diurai, tanpa memunculkan error.

<Callout type="warning">
  `maxSubagents` harus berupa bilangan bulat antara 1 dan 50. Nilai di luar rentang tersebut akan menyebabkan kegagalan validasi `librechat.yaml`, dan LibreChat akan mencatat kesalahan tersebut serta berhenti saat startup alih-alih kembali ke nilai default. Memulai dengan `CONFIG_BYPASS_VALIDATION=true` akan melewati proses penghentian tersebut, namun seluruh konfigurasi kustom akan diabaikan dalam kasus ini, sehingga batasnya kembali ke `10`. Untuk kembali ke nilai default secara sengaja, hapus kunci tersebut: `maxSubagents` yang dihilangkan akan bernilai `10` tanpa kesalahan.
</Callout>

<Callout type="note">
  Batas ini bersifat menyeluruh untuk proses dan berasal dari `librechat.yaml`. Penggantian basis data per pengguna atau per grup untuk `endpoints.agents.maxSubagents` tercermin dalam konfigurasi yang disajikan, namun tidak diterapkan oleh validasi permintaan.
</Callout>

Meningkatkan ini hanya mengubah berapa banyak subagen yang dapat dirujuk oleh satu agen. Batas kedalaman, node grafik, dan konfigurasi proses tidak terpengaruh. Lihat [Subagents](/docs/features/subagents#limits).

## maxCitations

<OptionTable
  options={[
    [
      'maxCitations',
      'Number',
      'Mengontrol jumlah total maksimum sitasi yang dapat disertakan dalam satu respons agen.',
      'When using file_search capability, limits the total number of source citations returned to prevent overwhelming responses while ensuring comprehensive coverage.',
    ],
  ]}
/>

**Default:** `30`

**Rentang:** `1-50`

**Contoh:**

```yaml filename="endpoints / agents / maxCitations"
maxCitations: 30
```

## maxCitationsPerFile

<OptionTable
  options={[
    [
      'maxCitationsPerFile',
      'Number',
      'Membatasi jumlah maksimum sitasi yang dapat diekstrak dari satu file.',
      'Ensures citation diversity by preventing any single file from dominating the citations, encouraging representation from multiple sources.',
    ],
  ]}
/>

**Default:** `7`

**Rentang:** `1-10`

**Contoh:**

```yaml filename="endpoints / agents / maxCitationsPerFile"
maxCitationsPerFile: 7
```

## minRelevanceScore

<OptionTable
  options={[
    [
      'minRelevanceScore',
      'Number',
      'Menetapkan ambang batas skor relevansi minimum agar sumber disertakan dalam respons.',
      'Filters out low-quality matches based on vector similarity scores. Higher values (e.g., 0.7) ensure only highly relevant sources are cited, while lower values (e.g., 0.0) include all sources regardless of quality.',
    ],
  ]}
/>

**Default:** `0.45` (ambang batas relevansi 45%)

**Rentang:** `0.0-1.0`

**Contoh:**

```yaml filename="endpoints / agents / minRelevanceScore"
minRelevanceScore: 0.45
```

### Contoh Konfigurasi Sitasi File [#file-citation-configuration-examples]

**Konfigurasi Default (Seimbang)**

```yaml
endpoints:
  agents:
    maxCitations: 30
    maxCitationsPerFile: 7
    minRelevanceScore: 0.45
```

Menyediakan sitasi yang komprehensif sekaligus mencegah respons yang berlebihan dan memfilter kecocokan berkualitas rendah.

**Konfigurasi Ketat (Kualitas Tinggi)**

```yaml
endpoints:
  agents:
    maxCitations: 10
    maxCitationsPerFile: 3
    minRelevanceScore: 0.7
```

Hanya menyertakan kutipan yang sangat relevan dengan batasan ketat untuk respons yang terfokus.

**Konfigurasi Komprehensif (Penelitian)**

```yaml
endpoints:
  agents:
    maxCitations: 50
    maxCitationsPerFile: 10
    minRelevanceScore: 0.0
```

Ekstraksi informasi maksimal untuk tugas penelitian yang mendalam, mencakup semua sumber tanpa memandang relevansinya.

## Kemampuan Agen [#agent-capabilities]

Kolom `capabilities` memungkinkan Anda untuk mengaktifkan atau menonaktifkan fungsionalitas tertentu untuk agen. Kapabilitas yang tersedia adalah:

- **deferred_tools**: Memungkinkan agen untuk menemukan MCP tools yang ditangguhkan (deferred) saat runtime alih-alih memuat setiap tool ke dalam konteks di awal.
- **programmatic_tools**: Mengaktifkan Pemanggilan Alat Programatis (Programmatic Tool Calling) untuk alat MCP yang ditandai sebagai Programmatic di Agent Builder. Memerlukan `execute_code` dan deployment Code Interpreter dengan komponen Tool Call Server. Kemampuan ini bersifat opt-in dan tidak diaktifkan secara default.
- **execute_code**: Mengizinkan agen untuk mengeksekusi kode.
- **stateful_code_sessions**: Memungkinkan agen yang memilih untuk ikut serta menggunakan kembali satu ruang kerja Code Interpreter pada cakupan pengguna, agen-dan-pengguna, atau percakapan. Memerlukan `execute_code`, deployment Code Interpreter yang kompatibel, dan pengaturan Lanjutan per-agen. Agen baru menggunakan cakupan pilihan pengguna yang telah masuk. Kemampuan ini sangat eksperimental, tidak diaktifkan secara default, dan dapat berubah secara substansial selama eksperimen; jangan anggap perilaku atau konfigurasinya saat ini sebagai kontrak produksi yang stabil.
- **file_search**: Memungkinkan agen untuk mencari dan berinteraksi dengan file. Saat diaktifkan, perilaku sitasi dikendalikan oleh pengaturan `maxCitations`, `maxCitationsPerFile`, dan `minRelevanceScore`.
- **web_search**: Mengaktifkan fungsionalitas pencarian web untuk agen, memungkinkan mereka untuk mencari dan mengambil informasi dari internet.
- **artifacts**: Memungkinkan agen untuk menghasilkan artefak interaktif (komponen React, HTML, diagram Mermaid).
- **subagents**: Mengaktifkan eksekusi agen anak dengan konteks terisolasi. Lihat [Subagents](/docs/features/subagents).
- **actions**: Mengizinkan agen untuk melakukan tindakan yang telah ditentukan sebelumnya.
- **context**: Mengaktifkan fungsionalitas "Upload as Text" dalam obrolan, dan "File Context" untuk agen, yang memungkinkan pengguna untuk mengunggah file dan kontennya diekstraksi serta disertakan secara langsung ke dalam percakapan.
- **skills**: Mengaktifkan Skills di panel samping, pemanggilan manual `$`, skills yang dipanggil model, dan daftar izin (allowlist) skill agen. Lihat [Skills](/docs/features/skills).
- **memory**: Memungkinkan agen menggunakan `set_memory` dan `delete_memory` saat memory dikonfigurasi dan pengguna memiliki akses. Diaktifkan secara default; dihapus saat runtime ketika memory dinonaktifkan.
- **ask_user_question**: Memungkinkan agen untuk menjeda guna mengajukan satu hingga empat pertanyaan terkait dalam satu formulir dan melanjutkannya dengan jawaban tersebut. Diaktifkan secara default.
- **tools**: Memberikan agen akses ke berbagai tools.
- **chain**: Mengaktifkan fitur Beta untuk perantaian agen (agent chaining), yang juga dikenal sebagai alur kerja Mixture-of-Agents (MoA).
- **ocr**: Secara opsional meningkatkan fitur "Upload as Text" dalam obrolan, dan "File Context" untuk agen, yang memungkinkan file diunggah dan diproses dengan OCR. **Memerlukan layanan OCR untuk dikonfigurasi.**
- **run_in_background**: Membuat eksekusi Code Interpreter dan alat shell memenuhi syarat untuk berjalan di latar belakang secara default dan mengaktifkan keikutsertaan latar belakang per-alat untuk alat MCP, Plugin, dan Action yang memenuhi syarat. Tombol tingkat Action akan mengikutsertakan setiap operasi yang memenuhi syarat; Action OAuth dan operasi yang telah menentukan parameter `run_in_background` dikecualikan. Agen dapat secara eksplisit mengeluarkan Code Interpreter dari fitur ini. Pengaturan ini mengizinkan eksekusi latar belakang; model memutuskan per panggilan apakah akan menggunakannya atau tidak. Memerlukan `execute_code` dan deployment Code Interpreter yang telah dikonfigurasi untuk kode. Kemampuan ini tidak diaktifkan secara default.
- **tool_intents**: Menambahkan label intent yang ditulis oleh model secara langsung ke panggilan tool yang memenuhi syarat. Tool bawaan, termasuk tool Code Interpreter, ikut serta secara otomatis; tool MCP dapat diaktifkan satu per satu atau secara massal di Agent Builder. Model specs menggunakan `describeIntent`. Kemampuan ini tidak diaktifkan secara default.

Dengan menentukan kapabilitas, Anda dapat mengontrol fitur yang tersedia bagi pengguna saat berinteraksi dengan agen.

## Contoh Konfigurasi [#example-configuration]

Berikut adalah contoh konfigurasi endpoint `agents` dengan kemampuan kustom dan pengaturan sitasi file:

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    disableBuilder: false
    # File citation configuration
    maxCitations: 20
    maxCitationsPerFile: 5
    minRelevanceScore: 0.6
    # Custom capabilities
    capabilities:
      # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
      # - 'programmatic_tools'
      - 'execute_code'
      # Optional: makes Code Interpreter background-eligible and enables selected MCP, Plugin, and Action tools.
      # - 'run_in_background'
      # Optional: enables live intent labels for native and selected MCP tools.
      # - 'tool_intents'
      - 'file_search'
      - 'skills'
      - 'subagents'
      - 'actions'
      - 'artifacts'
      - 'context'
      - 'ocr'
      - 'web_search'
```

Dalam contoh ini:

- Antarmuka builder telah diaktifkan
- Kutipan file dibatasi hingga total 20, dengan maksimal 5 per file
- Hanya sumber dengan relevansi 60%+ yang disertakan
- Agen LibreChat memiliki akses ke eksekusi kode, pencarian file (dengan sitasi), Skills, Subagents, actions, artifacts, konteks file, layanan ocr jika dikonfigurasi, dan kemampuan pencarian web.
- Programmatic Tool Calling tetap dinonaktifkan kecuali Anda menambahkan kapabilitas `programmatic_tools` bersama dengan `execute_code`
- Panggilan alat latar belakang tetap dinonaktifkan kecuali Anda menambahkan `run_in_background`; alat Code Interpreter kemudian akan memenuhi syarat secara default, sementara alat MCP, Plugin, dan Action yang memenuhi syarat masih memerlukan pemilihan.
- Label intent alat tetap dinonaktifkan kecuali Anda menambahkan `tool_intents`; alat bawaan kemudian akan ikut serta secara otomatis, sementara alat MCP masih memerlukan pemilihan per alat.

## remoteApi

Konfigurasi untuk autentikasi API Remote Agent. Mengontrol bagaimana layanan eksternal melakukan autentikasi saat memanggil endpoint Agents API.

### remoteApi.auth

<OptionTable
  options={[
    [
      'auth',
      'Object',
      'Konfigurasi autentikasi untuk API Remote Agent.',
      'Supports API key and/or OIDC Bearer token authentication. If omitted, only API key auth is active.',
    ],
  ]}
/>

#### remoteApi.auth.apiKey

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Aktifkan autentikasi kunci API untuk Remote Agent API.',
      'When true, requests with a valid LibreChat API key are accepted. Can be used alongside or instead of OIDC.',
    ],
  ]}
/>

**Default:** `true`

#### remoteApi.auth.oidc

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Aktifkan autentikasi token Bearer OIDC.',
      'When true, the middleware validates Bearer tokens against the configured OIDC issuer via JWKS.',
    ],
    [
      'issuer',
      'String',
      'URL penerbit OIDC.',
      'The base URL of your OIDC provider, such as a Keycloak realm URL. Used for token issuer validation and JWKS discovery if jwksUri is not set.',
    ],
    [
      'jwksUri',
      'String',
      'URL endpoint JWKS. Opsional.',
      'If omitted, resolved automatically via {issuer}/.well-known/openid-configuration. You can also set OPENID_JWKS_URL as an alternative.',
    ],
    [
      'audience',
      'String',
      'Audiens token yang diharapkan. Diperlukan saat autentikasi OIDC diaktifkan.',
      'Tokens must contain this value in their aud claim.',
    ],
    [
      'scope',
      'String',
      'Nilai scope yang diperlukan. Opsional.',
      'If set, the token must contain this value in its scp or scope claim. Use this to distinguish token intent across different APIs.',
    ],
  ]}
/>

**Default:** `enabled: false`

**Contoh - Hanya OIDC:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: false
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          audience: my-client-id
```

**Contoh - OIDC dengan fallback API key:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: true
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          # jwksUri is optional and auto-discovered if omitted
          jwksUri: https://auth.example.com/realms/myrealm/protocol/openid-connect/certs
          audience: my-client-id
```

<Callout type="info">
  Prioritas resolusi JWKS URI adalah `jwksUri` eksplisit, kemudian `OPENID_JWKS_URL`, lalu penemuan otomatis melalui `{issuer}/.well-known/openid-configuration`.
</Callout>

<Callout type="info">
  Pencocokan pengguna OIDC menggunakan klaim `sub` sebagai pencarian utama, dengan cadangan ke klaim `email`, `preferred_username`, atau `upn`. Pengguna yang dicocokkan harus sudah ada di LibreChat.
</Callout>

## Subagents [#subagents]

Kolom `subagents` mengontrol agen anak terisolasi mana yang dapat dibuat oleh agen induk saat kapabilitas `subagents` tersedia.

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Menambahkan alat spawn subagent ke agen ini jika bernilai true. Default: dinonaktifkan.',
      'enabled: true',
    ],
    [
      'allowSelf',
      'Boolean',
      'Mengizinkan agen untuk memunculkan dirinya sendiri dalam konteks terisolasi yang baru. Default: true.',
      'allowSelf: true',
    ],
    [
      'agent_ids',
      'Array/List of Strings',
      'Agen spesifik yang dapat dibuat oleh agen ini. Dibatasi oleh `maxSubagents`, yang secara default bernilai 10.',
      'agent_ids: ["agent_researcher"]',
    ],
    [
      'graphs',
      'Array of Objects',
      'Tim Agen tersimpan yang dapat berjalan sebagai satu grafik anak yang terisolasi. Daftar ini dibatasi oleh `maxSubagents` (default: 10); setiap tim dapat berisi hingga 32 anggota.',
      '',
    ],
  ]}
/>

```yaml filename="Agent subagents"
subagents:
  enabled: true
  allowSelf: true
  agent_ids:
    - 'agent_researcher'
    - 'agent_reviewer'
```

Setiap entri `graphs` memerlukan `type`, `name`, `description`, `agent_ids`, `edges` langsung, `entry_agent_id`, dan `result_agent_id` yang unik. Definisi tim harus membentuk graf asiklik berarah yang terhubung, setiap anggota harus terlihat oleh pengguna, dan total konfigurasi dibatasi hingga 50 target Agent unik dan 100 konfigurasi eksekusi yang diperluas. Lihat [contoh saved-team dan perilaku runtime](/docs/features/subagents#configure-an-agent).

Untuk perilaku dan batasan yang dihadapi pengguna, lihat [Subagents](/docs/features/subagents).

## Catatan [#notes]

- Tidak disarankan untuk menonaktifkan antarmuka builder kecuali Anda menggunakan [modelSpecs](/docs/configuration/librechat_yaml/object_structure/model_specs) untuk menentukan daftar agen yang dapat dipilih.
- Konfigurasi sitasi file (`maxCitations`, `maxCitationsPerFile`, `minRelevanceScore`) hanya berlaku ketika kapabilitas `file_search` diaktifkan.
- Skor relevansi dihitung menggunakan kesamaan vektor, di mana 1.0 merepresentasikan kecocokan sempurna dan 0.0 merepresentasikan tidak ada kesamaan.
- Batas sitasi membantu menyeimbangkan pengambilan informasi yang komprehensif dengan kualitas dan performa respons.
- Kemampuan `context` berfungsi tanpa konfigurasi OCR menggunakan metode penguraian teks. OCR meningkatkan kualitas ekstraksi saat dikonfigurasi.
- Kemampuan `ocr` memerlukan layanan OCR untuk dikonfigurasi (lihat [Konfigurasi OCR](/docs/configuration/librechat_yaml/object_structure/ocr)).
- `maxSubagents` membatasi berapa banyak subagent yang dapat dirujuk oleh satu agen. Pengaturan ini tidak mengubah kedalaman atau batas grafik yang didokumentasikan di bawah [Subagents](/docs/features/subagents#limits).
