---
title: 语音配置
icon: Mic
---

## 概述 [#overview]

`speech` 对象允许您直接在 `librechat.yaml` 配置文件中配置文本转语音 (TTS) 和语音转文本 (STT) 提供商。这使得无需用户配置自己的 API 密钥即可启用服务器端语音服务。

**`speech` 下的字段：**

- `tts` - Text-to-Speech 提供商配置
- `stt` - Speech-to-Text 提供商配置
- `speechTab` - 语音功能的默认 UI 设置

**注意：**

- 可以同时配置多个提供商
- 用户可以从可用选项中选择他们偏好的提供商
- 配置文件中的 API keys 应使用环境变量引用以确保安全

## 示例 [#example]

```yaml filename="speech"
speech:
  tts:
    allowedAddresses: ["tts.internal:8080"]
    openai:
      apiKey: "${TTS_API_KEY}"
      model: "tts-1"
      voices: ["alloy", "echo", "fable", "onyx", "nova", "shimmer"]
    elevenlabs:
      apiKey: "${ELEVENLABS_API_KEY}"
      model: "eleven_multilingual_v2"
      voices: ["voice-id-1", "voice-id-2"]
  stt:
    allowedAddresses: ["stt.internal:8080"]
    openai:
      apiKey: "${STT_API_KEY}"
      model: "whisper-1"
  speechTab:
    conversationMode: true
    advancedMode: false
    speechToText: true
    textToSpeech: true
```

---

## SSRF 防护 [#ssrf-protection]

LibreChat 会在连接时保护运营商提供的 STT 和 TTS URL。私有地址、回环地址、链路本地地址以及云元数据目标地址默认会被拦截，且语音请求的重定向功能已被禁用。

`speech.tts.allowedAddresses` 和 `speech.stt.allowedAddresses` 是独立的豁免列表，而非严格的白名单。仅当该部分必须访问受信任的自托管服务时，才添加精确的私有 `host:port`、`private.ip:port` 或 `[ipv6]:port`。请勿使用 URL、路径、CIDR 范围、裸主机名/IP 或公网 IP 字面量。默认端口会被标准化，因此未指定端口的 HTTPS URL 将被检查为 `443` 端口。

被豁免的主机名会信任其在该端口上解析到的任何私有地址。请尽可能优先使用私有 IP 字面量，并仅列出您拥有 DNS 控制权且无法被攻击者重定向的主机名。

当正向代理携带主机名请求时，代理会执行目标 DNS 解析和出口流量转发，因此代理必须强制执行其自身的 SSRF 策略。在代理处理之前，字面意义上的私有目标地址仍会被拦截，除非其确切的地址和端口已被豁免。

---

## tts

`tts` 对象用于配置 Text-to-Speech 提供商。可以配置多个提供商，用户可以选择使用其中任意一个。

<OptionTable
  options={[
    [
      'allowedAddresses',
      'Array of Strings',
      '用于 TTS 连接时 SSRF 检查的受信任私有 host:port 例外。公共目标保持可用。',
      'allowedAddresses: ["tts.internal:8080"]',
    ],
  ]}
/>

### openai

使用 `tts-1` 或 `tts-1-hd` 等模型进行 OpenAI TTS 配置。

<OptionTable
  options={[
    ['url', 'String', '自定义 API URL（可选）。用于兼容 OpenAI 的 endpoint。', ''],
    ['apiKey', 'String', 'OpenAI API 密钥。使用环境变量引用。', 'Required'],
    ['model', 'String', '要使用的 TTS 模型（例如 "tts-1", "tts-1-hd"）。', 'Required'],
    ['voices', 'Array of Strings', '用户可选择的可用语音选项。', 'Required'],
  ]}
/>

**示例：**
```yaml filename="speech / tts / openai"
tts:
  openai:
    apiKey: "${TTS_API_KEY}"
    model: "tts-1"
    voices: ["alloy", "echo", "fable", "onyx", "nova", "shimmer"]
```

### azureOpenAI

Azure OpenAI TTS 配置。

<OptionTable
  options={[
    ['instanceName', 'String', 'Azure OpenAI 实例名称。', 'Required'],
    ['apiKey', 'String', 'Azure OpenAI API 密钥。', 'Required'],
    ['deploymentName', 'String', 'TTS 模型的部署名称。', 'Required'],
    ['apiVersion', 'String', 'Azure OpenAI API 版本。', 'Required'],
    ['model', 'String', 'TTS 模型标识符。', 'Required'],
    ['voices', 'Array of Strings', '可用的语音选项。', 'Required'],
  ]}
/>

**示例：**
```yaml filename="speech / tts / azureOpenAI"
tts:
  azureOpenAI:
    instanceName: "my-azure-instance"
    apiKey: "${AZURE_TTS_API_KEY}"
    deploymentName: "tts-deployment"
    apiVersion: "2024-02-15-preview"
    model: "tts-1"
    voices: ["alloy", "echo", "nova"]
```

### elevenlabs

用于高质量语音合成的 ElevenLabs TTS 配置。

<OptionTable
  options={[
    ['url', 'String', '自定义 API URL（可选）。', ''],
    ['websocketUrl', 'String', '用于流式传输的 WebSocket URL（可选）。', ''],
    ['apiKey', 'String', 'ElevenLabs API key', 'Required'],
    ['model', 'String', 'ElevenLabs 模型（例如 "eleven_multilingual_v2"）。', 'Required'],
    ['voices', 'Array of Strings', '可供选择的 Voice ID。', 'Required'],
    ['voice_settings', 'Object', '语音自定义设置（可选）。', ''],
    ['pronunciation_dictionary_locators', 'Array of Strings', '发音词典 ID（可选）。', ''],
  ]}
/>

**voice_settings 子键：**
<OptionTable
  options={[
    ['similarity_boost', 'Number', '语音相似度增强 (0-1)。', ''],
    ['stability', 'Number', '语音稳定性 (0-1)。', ''],
    ['style', 'Number', '风格夸张程度 (0-1)。', ''],
    ['use_speaker_boost', 'Boolean', '启用扬声器增强', ''],
  ]}
/>

**示例：**
```yaml filename="speech / tts / elevenlabs"
tts:
  elevenlabs:
    apiKey: "${ELEVENLABS_API_KEY}"
    model: "eleven_multilingual_v2"
    voices: ["21m00Tcm4TlvDq8ikWAM", "AZnzlk1XvdvUeBnXmlld"]
    voice_settings:
      stability: 0.5
      similarity_boost: 0.75
      use_speaker_boost: true
```

### localai

用于自托管语音合成的 LocalAI TTS 配置。

<OptionTable
  options={[
    ['url', 'String', 'LocalAI 服务器 URL。', 'Required'],
    ['apiKey', 'String', '如果启用了身份验证，则为 API key（可选）。', ''],
    ['voices', 'Array of Strings', '可用的语音模型。', 'Required'],
    ['backend', 'String', '要使用的 TTS 后端（例如 "piper"）。', 'Required'],
  ]}
/>

**示例：**
```yaml filename="speech / tts / localai"
tts:
  localai:
    url: "http://localhost:8080"
    voices: ["en-us-amy-low", "en-us-danny-low"]
    backend: "piper"
```

---

## stt

`stt` 对象用于配置语音转文字（Speech-to-Text）提供商。

<OptionTable
  options={[
    [
      'allowedAddresses',
      'Array of Strings',
      '用于 STT 连接时 SSRF 检查的受信任私有 host:port 例外。公共目标保持可用。',
      'allowedAddresses: ["stt.internal:8080"]',
    ],
  ]}
/>

### openai [#openai-1]

OpenAI Whisper STT 配置。

<OptionTable
  options={[
    ['url', 'String', '自定义 API URL（可选）。用于兼容 OpenAI 的 endpoint。', ''],
    ['apiKey', 'String', 'OpenAI API 密钥。使用环境变量引用。', 'Required'],
    ['model', 'String', '要使用的 STT 模型（例如 "whisper-1"）。', 'Required'],
  ]}
/>

**示例：**
```yaml filename="speech / stt / openai"
stt:
  openai:
    apiKey: "${STT_API_KEY}"
    model: "whisper-1"
```

### azureOpenAI [#azureopenai-1]

Azure OpenAI Whisper STT 配置。

<OptionTable
  options={[
    ['instanceName', 'String', 'Azure OpenAI 实例名称。', 'Required'],
    ['apiKey', 'String', 'Azure OpenAI API 密钥。', 'Required'],
    ['deploymentName', 'String', 'Whisper 模型的部署名称。', 'Required'],
    ['apiVersion', 'String', 'Azure OpenAI API 版本。', 'Required'],
  ]}
/>

**示例：**
```yaml filename="speech / stt / azureOpenAI"
stt:
  azureOpenAI:
    instanceName: "my-azure-instance"
    apiKey: "${AZURE_STT_API_KEY}"
    deploymentName: "whisper-deployment"
    apiVersion: "2024-02-15-preview"
```

---

## speechTab

`speechTab` 对象用于配置语音功能的默认 UI 设置。这些设置控制用户在语音设置面板中默认看到的内容。

<OptionTable
  options={[
    ['conversationMode', 'Boolean', '默认启用对话模式。', 'false'],
    ['advancedMode', 'Boolean', '默认显示高级语音设置。', 'false'],
    ['speechToText', 'Boolean or Object', '默认启用 STT，或配置详细的 STT 设置。', 'false'],
    ['textToSpeech', 'Boolean or Object', '默认启用 TTS，或配置详细的 TTS 设置。', 'false'],
  ]}
/>

### speechToText (Object format) [#speechtotext-object-format]

当使用对象而非布尔值时：

<OptionTable
  options={[
    [
      'engineSTT',
      'String',
      '默认 STT 引擎。使用 `"browser"` 或 `"external"`。旧版 `"openai"` 和 `"azureOpenAI"` 值仍可接受，并会被标准化为 `"external"`。',
      'browser',
    ],
    ['languageSTT', 'String', 'STT 的默认语言。', ''],
    ['autoTranscribeAudio', 'Boolean', '保持麦克风持续监听，而不是在第一次停顿时停止。使用外部引擎时，它还会根据 `decibelValue` 开启静音检测。', ''],
    ['decibelValue', 'Number', '用于静音检测的分贝阈值。范围 -100 到 -30，默认值为 -45。', ''],
    ['autoSendText', 'Number', '转录后自动发送前的等待秒数。`0` 表示立即发送；`-1` 表示禁用自动发送。', ''],
  ]}
/>

### textToSpeech (对象格式) [#texttospeech-object-format]

当使用对象而非布尔值时：

<OptionTable
  options={[
    [
      'engineTTS',
      'String',
      '默认 TTS 引擎。使用 `"browser"` 或 `"external"`。旧版提供商的值会被接受并标准化为 `"external"`。',
      'browser',
    ],
    ['voice', 'String', '默认语音选择。', ''],
    ['languageTTS', 'String', 'TTS 的默认语言。', ''],
    ['automaticPlayback', 'Boolean', '自动播放 TTS 响应。', ''],
    ['playbackRate', 'Number', '默认播放速度（1.0 = 正常）。范围：0.25–4.0。', ''],
    ['cacheTTS', 'Boolean', '缓存 TTS 音频以供重复播放。', ''],
  ]}
/>

**示例：**
```yaml filename="speech / speechTab"
speechTab:
  conversationMode: false
  advancedMode: false
  speechToText:
    engineSTT: "external"
    autoTranscribeAudio: true
    decibelValue: -45
  textToSpeech:
    engineTTS: "external"
    voice: "nova"
    automaticPlayback: false
    playbackRate: 1.0
    cacheTTS: true
```

---

## 完整示例 [#complete-example]

```yaml filename="librechat.yaml"
version: 1.3.15
cache: true

speech:
  tts:
    openai:
      apiKey: "${TTS_API_KEY}"
      model: "tts-1-hd"
      voices: ["alloy", "echo", "fable", "onyx", "nova", "shimmer"]
    elevenlabs:
      apiKey: "${ELEVENLABS_API_KEY}"
      model: "eleven_multilingual_v2"
      voices: ["21m00Tcm4TlvDq8ikWAM", "AZnzlk1XvdvUeBnXmlld"]
      voice_settings:
        stability: 0.5
        similarity_boost: 0.75
  stt:
    openai:
      apiKey: "${STT_API_KEY}"
      model: "whisper-1"
  speechTab:
    conversationMode: false
    advancedMode: false
    speechToText: true
    textToSpeech:
      engineTTS: "external"
      voice: "nova"
      automaticPlayback: false
```

---

## 注意事项 [#notes]

- 在配置文件中，请始终使用环境变量引用（例如 `${API_KEY}`）来设置 API 密钥。
- 可以配置多个 TTS 提供商；用户可以在 UI 中选择其首选选项。
- `speechTab` 设置定义了用户可以在其个人设置中覆盖的默认值
- `browser` 使用浏览器内置的语音支持；`external` 使用在 `speech.stt` 或 `speech.tts` 下配置的服务器端提供商。
- 现有的 `openai` 和 `azureOpenAI` STT 默认配置，以及 `openai`、`azureOpenAI`、`elevenlabs` 和 `localai` TTS 默认配置，已迁移至 `external`；若外部引擎不可用，则会回退至 `browser`。
- 有关详细的功能文档，请参阅 [Speech to Text & Text to Speech](/docs/configuration/stt_tts)
