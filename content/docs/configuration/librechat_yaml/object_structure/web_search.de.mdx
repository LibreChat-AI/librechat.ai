---
title: Konfiguration der Websuche
icon: Search
---

Die `webSearch`-Konfiguration ermöglicht es Ihnen, die Websuchfunktion innerhalb von LibreChat anzupassen, einschließlich Suchanbietern, Content-Scrapern und Ergebnis-Rerankern.

## Übersicht [#overview]

Die Websuchfunktion besteht aus drei Hauptkomponenten:

1. **Suchanbieter**: Dienste, die die anfängliche Websuche durchführen
2. **Scrapers**: Dienste, die Inhalte von Webseiten extrahieren
3. **Rerankers**: Dienste, die Suchergebnisse für eine bessere Relevanz neu sortieren

## Beispiel [#example]

```yaml filename="webSearch"
webSearch:
  # Search Provider Configuration
  serperApiKey: "${SERPER_API_KEY}"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}"
  searchProvider: "serper" # Options: "serper", "searxng", "tavily", "keenable"

  # Optional: query options sent to a SearXNG instance
  searxngSearchOptions:
    engines: "google,bing,startpage" # Default: "google,bing,duckduckgo"
    language: "en" # Default: "all"

  # Tavily Configuration (search and/or scraper)
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"

  # Keenable Configuration (search and/or scraper; all credentials optional)
  keenableApiKey: "${KEENABLE_API_KEY}"
  keenableApiUrl: "${KEENABLE_API_URL}"
  keenableSearchOptions:
    maxResults: 8
    site: "example.com"
    attributionTitle: "LibreChat"
    timeout: 15000
  keenableScraperOptions:
    attributionTitle: "LibreChat"
    timeout: 15000

  # Scraper Configuration
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlApiUrl: "${FIRECRAWL_API_URL}"
  firecrawlVersion: "${FIRECRAWL_VERSION}"
  scraperProvider: "firecrawl" # Options: "firecrawl", "serper", "tavily", "keenable"

  # Reranker Configuration
  jinaApiKey: "${JINA_API_KEY}"
  jinaApiUrl: "${JINA_API_URL}"
  cohereApiKey: "${COHERE_API_KEY}"
  rerankerType: "jina" # Options: "jina", "cohere", "none"

  # General Settings
  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)
  # Required for deliberately private self-hosted provider endpoints
  allowedAddresses:
    - "searxng:8080"
```

## SSRF-Schutz und private Anbieter [#ssrf-protection-and-private-providers]

LibreChat validiert jede Verbindung, die es für die Websuche, das Scraping und das Reranking öffnet. Private, Loopback-, Link-Local- und Cloud-Metadata-Ziele sind standardmäßig blockiert, einschließlich Redirect-Hops und Hostnamen, die zum Zeitpunkt der Verbindung in eine private Adresse aufgelöst werden.

Verwenden Sie `webSearch.allowedAddresses`, wenn ein konfigurierter SearXNG-, Firecrawl-, Jina-, Tavily-kompatibler, Keenable-kompatibler oder ein verwandter Provider-endpoint bewusst privat ist:

```yaml filename="webSearch / allowedAddresses"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  allowedAddresses:
    - "searxng:8080"
    - "10.0.0.5:3002"
    - "[fd00::5]:8080"
```

Dieses Feld ist eine SSRF-Ausnahmeliste, keine strikte Positivliste. Öffentliche Ziele bleiben erreichbar. Einträge müssen exakte `host:port`-, `private.ip:port`- oder `[ipv6]:port`-Paare sein; URLs, Pfade, CIDR-Bereiche, reine Hosts/IPs und öffentliche IP-Literale werden abgelehnt. Ein ausgenommener Hostname vertraut der privaten IP, zu der er an diesem Port aufgelöst wird. Bevorzugen Sie daher ein privates IP-Literal und listen Sie nur Namen auf, deren DNS Sie kontrollieren.

LibreChat nimmt einen konfigurierten HTTP(S)-Forward-Proxy-Endpunkt automatisch aus, damit dieser erreicht werden kann. Wenn der Proxy eine Anfrage weiterleitet, löst er das Ziel auf und muss die Ziel-Egress-Beschränkungen selbst durchsetzen. `NO_PROXY` kann eine Anfrage direkt stellen, wobei der Verbindungsschutz von LibreChat weiterhin greift. Der Schutz deckt Verbindungen ab, die LibreChat zu Anbieter-APIs öffnet; er kann keine Seiten-URL einschränken, die ein Drittanbieter-Scraper als Anfragedaten erhält und auf seiner eigenen Infrastruktur abruft.

## Suchanbieter [#search-providers]

### searchProvider

<OptionTable
  options={[
    ['searchProvider', 'String', 'Gibt an, welcher Suchanbieter verwendet werden soll.', 'Options: "serper", "searxng", "tavily", "keenable"'],
  ]}
/>

### serperApiKey

<OptionTable
  options={[
    ['serperApiKey', 'String', 'Umgebungsvariablenname für den Serper API-Schlüssel. Falls dieser nicht in der .env gesetzt ist, werden Benutzer aufgefordert, ihn über die UI einzugeben.', '${SERPER_API_KEY}'],
  ]}
/>

**Hinweis:** Holen Sie sich Ihren API-Schlüssel von [Serper.dev](https://serper.dev/api-keys)


### searxngInstanceUrl

<OptionTable
  options={[
    ['searxngInstanceUrl', 'String', 'Umgebungsvariablenname für die SearXNG-Instanz-URL. Wenn dieser nicht in der .env gesetzt ist, werden Benutzer aufgefordert, ihn über die UI anzugeben.', '${SEARXNG_INSTANCE_URL}'],
  ]}
/>

### searxngApiKey

<OptionTable
  options={[
    ['searxngApiKey', 'String', 'Umgebungsvariablenname für den SearXNG API-Schlüssel. Falls nicht in der .env gesetzt, werden Benutzer aufgefordert, diesen über die UI einzugeben.', '${SEARXNG_API_KEY}'],
  ]}
/>

**Hinweis:** Dies ist optional und nur erforderlich, wenn Ihre SearXNG-Instanz eine Authentifizierung erfordert.

### searxngSearchOptions

<OptionTable
  options={[
    ['searxngSearchOptions', 'Object', 'Abfrageoptionen, die bei jeder Suche an Ihre SearXNG-Instanz gesendet werden. Jeder Unterpunkt ist optional, und der Block wird nur gelesen, wenn searchProvider auf "searxng" gesetzt ist.', ''],
  ]}
/>

**Untergeordnete Schlüssel:**

<OptionTable
  options={[
    ['engines', 'String or Array of Strings', 'Engines, die Ihre Instanz abfragen soll; werden als engines-Parameter an SearXNG gesendet. Akzeptiert eine durch Kommas getrennte Zeichenfolge oder eine YAML-Liste; beide werden in die durch Kommas getrennte Form normalisiert, wobei umgebende Leerzeichen und leere Einträge entfernt werden. Ein Wert, der nach dem Trimmen leer ist, wird als nicht gesetzt behandelt.', 'Default: "google,bing,duckduckgo"'],
    ['language', 'String', 'Ergebnis-Sprachcode, der als language-Parameter an SearXNG gesendet wird. Verwenden Sie einen Code, den Ihre Instanz akzeptiert, wie z. B. "en", "de" oder "fr".', 'Default: "all"'],
    ['timeRange', 'String', 'Schränkt die Ergebnisse nach Veröffentlichungsdatum ein, gesendet an SearXNG als time_range. Wird bei Nichtfestlegung vollständig aus der Anfrage weggelassen.', 'Options: "day", "month", "year"'],
    ['timeout', 'Number', 'HTTP-Request-Timeout in Millisekunden für Aufrufe an Ihre Instanz. Muss eine positive Ganzzahl von maximal 120000 sein; 0 wird abgelehnt, da dies das Timeout deaktivieren würde.', 'Default: 10000'],
  ]}
/>

<Callout type="info" title="Verfügbarkeit">
`searxngSearchOptions` erreicht SearXNG über das Such-Tool in `@librechat/agents` und erfordert `@librechat/agents` v3.6.9 oder neuer. Releases, die mit einer früheren Version erstellt wurden, ignorieren den Block, anstatt den Start zu verweigern. Eine Instanz, die weiterhin nur Standardergebnisse liefert, verwendet daher möglicherweise einfach einen älteren Build.
</Callout>

**Beispiel:**

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}" # Optional
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeRange: "month"
    timeout: 10000
```

`engines` akzeptiert auch die durch Kommas getrennte Form, die SearXNG selbst verwendet, daher ist dies äquivalent zur obigen Liste:

```yaml filename="webSearch"
webSearch:
  searxngSearchOptions:
    engines: "google, bing, startpage, qwant"
```

#### Auswahl von Engines [#choosing-engines]

Die Namen der Engines müssen mit den Engines übereinstimmen, die auf Ihrer eigenen Instanz aktiviert sind, und daraus ergeben sich zwei Dinge:

- SearXNG ignoriert eine Engine, die es nicht erkennt, anstatt einen Fehler zu melden. Ein Tippfehler oder eine Engine, die auf Ihrer Instanz deaktiviert ist, führt zu weniger Ergebnissen anstatt zu einem Fehler.
- Die gültigen Namen sind instanzspezifisch. Überprüfen Sie die Liste der aktivierten Engines unter `https://your-instance/config`, welche ein JSON mit jeder aktivierten Engine zurückgibt, oder öffnen Sie den Tab **Engines** auf der Einstellungsseite Ihrer Instanz.

Der Standardwert ist `google,bing,duckduckgo`. SearXNG aggregiert die Ergebnisse aller ausgewählten Suchmaschinen. Wenn also eine Suchmaschine blockiert ist, entfällt lediglich deren Anteil an den Ergebnissen, anstatt die gesamte Antwort zu beeinträchtigen. Eine Suche liefert nur dann keine Ergebnisse, wenn jede ausgewählte Suchmaschine fehlschlägt oder nichts zurückgibt.

DuckDuckGo ist die Engine in diesem Standard-Set, die am wahrscheinlichsten blockiert wird, da sie für die meisten selbst gehosteten Instanzen CAPTCHAs ausgibt. Da das Set nur drei Engines enthält, führt der gleichzeitige Ausfall von DuckDuckGo und einer ratenbegrenzten Google- oder Bing-Suche häufig dazu, dass überhaupt keine Ergebnisse mehr erzielt werden. Das explizite Festlegen von `engines`, das Weglassen von DuckDuckGo und das Auflisten von mehr als drei Engines verringern dieses Risiko.

Engines, die schnell antworten und selbst gehosteten Traffic tolerieren, bilden das beste Start-Set, zum Beispiel:

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - brave
      - startpage
      - qwant
```

Das Hinzufügen weiterer Engines erweitert die Abdeckung, verlangsamt jedoch auch jede Suche, da SearXNG auf die langsamste Engine im Set wartet, bevor eine Antwort erfolgt. Beachten Sie den `timeout`, wenn Sie die Liste erweitern.

#### Was diese Optionen nicht steuern [#what-these-options-do-not-control]

- **Safe search** ist nicht Teil dieses Blocks. Es stammt vom [`safeSearch`](#safesearch)-Schlüssel auf oberster Ebene und wird als `safesearch` an SearXNG weitergeleitet.
- **Ergebniskategorien** werden pro Abfrage von LibreChat ausgewählt, das den Suchtyp auf `general`, `images`, `videos` oder `news` abbildet. Sie können hier nicht überschrieben werden.
- **Seitenzahl und Ergebnisformat** sind festgelegt. LibreChat fordert immer Seite 1 im JSON-Format an, weshalb Ihre Instanz `json` unter `formats` aktiviert haben muss.

### tavilyApiKey

<OptionTable
  options={[
    ['tavilyApiKey', 'String', 'Umgebungsvariablenname für den Tavily API-Schlüssel. Wird sowohl für die Suche als auch für den Scraper verwendet. Falls nicht in der .env gesetzt, werden Benutzer aufgefordert, diesen über die UI einzugeben.', '${TAVILY_API_KEY}'],
  ]}
/>

**Hinweis:** Holen Sie sich Ihren API-Schlüssel von [Tavily](https://app.tavily.com/home)

### tavilySearchUrl

<OptionTable
  options={[
    ['tavilySearchUrl', 'String', 'Umgebungsvariablenname für eine benutzerdefinierte Tavily Search API-URL. Optional; standardmäßig wird die von Tavily gehostete Suche verwendet, wenn keine Angabe erfolgt.', '${TAVILY_SEARCH_URL}'],
  ]}
/>

### tavilyExtractUrl

<OptionTable
  options={[
    ['tavilyExtractUrl', 'String', 'Umgebungsvariablenname für eine benutzerdefinierte Tavily Extract API-URL. Optional; standardmäßig wird das von Tavily gehostete Extract verwendet, wenn keine Angabe erfolgt.', '${TAVILY_EXTRACT_URL}'],
  ]}
/>

### tavilySearchOptions

<OptionTable
  options={[
    ['tavilySearchOptions', 'Object', 'Konfigurationsoptionen für die Tavily-Suche.', ''],
  ]}
/>

**Untergeordnete Schlüssel:**

<OptionTable
  options={[
    ['searchDepth', 'String', 'Steuert das Verhältnis zwischen Relevanz und Latenz. "basic" gibt eine NLP-Zusammenfassung pro URL zurück. "advanced" gibt mehrere semantisch relevante Snippets pro URL zurück (2 API-Credits). "fast" gleicht Geschwindigkeit und Relevanz mit Snippets aus. "ultra-fast" minimiert die Latenz mit einer NLP-Zusammenfassung.', 'Options: "basic", "advanced", "fast", "ultra-fast". Default: "basic"'],
    ['maxResults', 'Number', 'Die maximale Anzahl der zurückzugebenden Suchergebnisse.', 'Range: 1-20. Default: 5'],
    ['topic', 'String', 'Die Kategorie der Suche. "news" ist nützlich für Echtzeit-Updates. "finance" für Finanzdaten.', 'Options: "general", "news", "finance". Default: "general"'],
    ['includeImages', 'Boolean', 'Bilder in die Antwort einbeziehen. Gibt sowohl Bilder der obersten Abfrage als auch Bilder pro Ergebnis zurück.', 'Default: false'],
    ['includeAnswer', 'Boolean or String', 'Füge eine LLM-generierte Antwort hinzu. "basic" oder true für eine schnelle Antwort, "advanced" für eine detaillierte Antwort.', 'Default: false'],
    ['includeRawContent', 'Boolean or String', 'Inklusive bereinigtem und geparstem HTML-Inhalt. „markdown“ oder true für das Markdown-Format, „text“ für reinen Text.', 'Default: false'],
    ['includeDomains', 'Array of Strings', 'Suche auf bestimmte Domains beschränken. Maximal 300 Domains.', ''],
    ['excludeDomains', 'Array of Strings', 'Bestimmte Domains von den Ergebnissen ausschließen. Maximal 150 Domains.', ''],
    ['timeRange', 'String', 'Zeitbereichsfilter basierend auf dem Veröffentlichungs- oder letzten Aktualisierungsdatum.', 'Options: "day", "week", "month", "year"'],
    ['includeImageDescriptions', 'Boolean', 'Wenn includeImages auf true gesetzt ist, fügen Sie zusätzlich einen beschreibenden Text für jedes Bild hinzu.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Füge die Favicon-URL für jedes Suchergebnis hinzu.', 'Default: false'],
    ['chunksPerSource', 'Number', 'Maximale Anzahl relevanter Inhaltsabschnitte pro Quelle. Nur verfügbar, wenn searchDepth auf "advanced" gesetzt ist.', 'Range: 1-3. Default: 3'],
    ['safeSearch', 'Boolean', 'Optionale Tavily safe_search-Überschreibung für Tavily Search-Anfragen. Standardmäßig weggelassen; true erfordert möglicherweise Tavily Enterprise.', 'Default: omitted'],
    ['timeout', 'Number', 'Clientseitiges HTTP-Anfrage-Timeout in Millisekunden. Steuert, wie lange auf eine Antwort der Tavily API gewartet wird, bevor der Vorgang abgebrochen wird.', 'Default: 15000'],
  ]}
/>

### keenableApiKey

<OptionTable
  options={[
    ['keenableApiKey', 'String', 'Umgebungsvariablenname für den optionalen Keenable API-Schlüssel. Keenable funktioniert ohne Schlüssel; die Konfiguration eines Schlüssels erhöht die öffentlichen Ratenbegrenzungen.', '${KEENABLE_API_KEY}'],
  ]}
/>

### keenableApiUrl

<OptionTable
  options={[
    ['keenableApiUrl', 'String', 'Umgebungsvariablenname für eine benutzerdefinierte Keenable-Such-API-URL. Dies ändert nicht den page-fetch endpoint.', '${KEENABLE_API_URL}'],
  ]}
/>

Der page-fetch endpoint wird separat mit der reinen Umgebungsvariablen `KEENABLE_FETCH_URL` konfiguriert.

### keenableSearchOptions

<OptionTable
  options={[
    ['keenableSearchOptions', 'Object', 'Konfigurationsoptionen für die Keenable-Suche.', ''],
  ]}
/>

<OptionTable
  options={[
    ['maxResults', 'Number', 'Maximale Anzahl an Suchergebnissen.', 'Range: 1-20. Default: 8'],
    ['site', 'String', 'Beschränkt die Ergebnisse auf eine Domain.', ''],
    ['attributionTitle', 'String', 'Wert, der im X-Keenable-Title Attribution-Header gesendet wird.', ''],
    ['timeout', 'Number', 'Clientseitiges HTTP-Anfrage-Timeout in Millisekunden.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

## Scraper [#scrapers]

### firecrawlApiKey

<OptionTable
  options={[
    ['firecrawlApiKey', 'String', 'Umgebungsvariablenname für den Firecrawl API-Schlüssel. Wenn dieser nicht in der .env gesetzt ist, werden Benutzer aufgefordert, ihn über die UI einzugeben.', '${FIRECRAWL_API_KEY}'],
  ]}
/>

**Hinweis:** Holen Sie sich Ihren API-Schlüssel von [Firecrawl.dev](https://docs.firecrawl.dev/introduction#api-key)

### firecrawlApiUrl

<OptionTable
  options={[
    ['firecrawlApiUrl', 'String', 'Umgebungsvariablenname für die Firecrawl API-URL. Wenn dieser nicht in der .env gesetzt ist, werden Benutzer aufgefordert, ihn über die UI anzugeben.', '${FIRECRAWL_API_URL}'],
  ]}
/>

**Hinweis:** Dies ist optional und nur erforderlich, wenn Sie eine benutzerdefinierte Firecrawl-Instanz verwenden.

### firecrawlVersion

<OptionTable
  options={[
    ['firecrawlVersion', 'String', 'Umgebungsvariablenname für die Firecrawl API-Version (v0 oder v1).', '${FIRECRAWL_VERSION}'],
  ]}
/>

### scraperProvider

<OptionTable
  options={[
    ['scraperProvider', 'String', 'Gibt an, welcher Scraper-Dienst verwendet werden soll.', 'Options: "firecrawl", "serper", "tavily", "keenable"'],
  ]}
/>

### firecrawlOptions

<OptionTable
  options={[
    ['firecrawlOptions', 'Object', 'Erweiterte Konfigurationsoptionen für den Firecrawl-Scraper.', ''],
  ]}
/>

**Untergeordnete Schlüssel:**

#### formats

<OptionTable
  options={[
    ['formats', 'Array of Strings', 'Formate, die in die Ausgabe aufgenommen werden sollen.', ''],
  ]}
/>

#### includeTags

<OptionTable
  options={[
    ['includeTags', 'Array of Strings', 'Tags, die in die Ausgabe aufgenommen werden sollen.', ''],
  ]}
/>

#### excludeTags

<OptionTable
  options={[
    ['excludeTags', 'Array of Strings', 'Tags, die von der Ausgabe ausgeschlossen werden sollen.', ''],
  ]}
/>

#### headers

<OptionTable
  options={[
    ['headers', 'Object', 'Header, die mit der Anfrage gesendet werden sollen. Können verwendet werden, um Cookies, User-Agent usw. zu senden.', ''],
  ]}
/>

#### waitFor

<OptionTable
  options={[
    ['waitFor', 'Number', 'Geben Sie eine Verzögerung in Millisekunden an, bevor der Inhalt abgerufen wird, damit die Seite ausreichend Zeit zum Laden hat.', ''],
  ]}
/>

#### timeout

<OptionTable
  options={[
    ['timeout', 'Integer', 'Timeout in Millisekunden für die Scraping-Anfrage. Muss eine nicht-negative Ganzzahl sein.', 'Default: 7500'],
  ]}
/>

#### maxAge

<OptionTable
  options={[
    ['maxAge', 'Number', 'Gibt eine zwischengespeicherte Version der Seite zurück, wenn diese jünger als dieser Wert in Millisekunden ist. Wenn eine zwischengespeicherte Version der Seite älter als dieser Wert ist, wird die Seite neu abgerufen.', ''],
  ]}
/>

**Hinweis:** Wenn Sie keine extrem aktuellen Daten benötigen, kann die Aktivierung dieser Option Ihre Scrapes um 500 % beschleunigen.

#### mobile

<OptionTable
  options={[
    ['mobile', 'Boolean', 'Emulieren des Scrapings von einem Mobilgerät.', ''],
  ]}
/>

#### skipTlsVerification

<OptionTable
  options={[
    ['skipTlsVerification', 'Boolean', 'Überspringen der TLS-Zertifikatsüberprüfung bei Anfragen.', ''],
  ]}
/>

#### blockAds

<OptionTable
  options={[
    ['blockAds', 'Boolean', 'Aktiviert Werbeblocker und das Blockieren von Cookie-Popups.', ''],
  ]}
/>

#### removeBase64Images

<OptionTable
  options={[
    ['removeBase64Images', 'Boolean', 'Entfernt alle Base64-Bilder aus der Ausgabe, die extrem lang sein können. Der Alt-Text des Bildes bleibt in der Ausgabe erhalten, aber die URL wird durch einen Platzhalter ersetzt.', ''],
  ]}
/>

#### parsePDF

<OptionTable
  options={[
    ['parsePDF', 'Boolean', 'Steuert, wie PDF-Dateien während des Scrapings verarbeitet werden.', ''],
  ]}
/>

#### storeInCache

<OptionTable
  options={[
    ['storeInCache', 'Boolean', 'Wenn auf true gesetzt, wird die Seite im Firecrawl-Index und Cache gespeichert. Dies auf false zu setzen ist nützlich, wenn Ihre Scraping-Aktivitäten Datenschutzbedenken aufwerfen könnten. Die Verwendung einiger Parameter, die mit sensiblem Scraping verbunden sind (Header), erzwingt, dass dieser Parameter auf false gesetzt wird.', ''],
  ]}
/>

#### zeroDataRetention

<OptionTable
  options={[
    ['zeroDataRetention', 'Boolean', 'Wenn auf true gesetzt, wird die Null-Datenaufbewahrung für diesen Scrape aktiviert (erfordert eine vorherige Einrichtung bei Firecrawl).', ''],
  ]}
/>

#### location

<OptionTable
  options={[
    ['location', 'Object', 'Geografischer Standort und Spracheinstellungen für das Scraping.', ''],
  ]}
/>

#### onlyMainContent

<OptionTable
  options={[
    ['onlyMainContent', 'Boolean', 'Geben Sie nur den Hauptinhalt der Seite zurück, ohne Header, Navigation, Footer usw.', ''],
  ]}
/>

#### changeTrackingOptions

<OptionTable
  options={[
    ['changeTrackingOptions', 'Object', 'Konfiguration zur Nachverfolgung von Änderungen in gescrapten Inhalten.', ''],
  ]}
/>

**Beispiel:**
```yaml filename="webSearch"
webSearch:
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlOptions:
    formats: ["markdown", "rawHtml"]
    includeTags: ["main", "article", ".content"]
    excludeTags: ["nav", "footer", ".ads"]
    waitFor: 2000
    timeout: 10000
    mobile: false
    blockAds: true
    onlyMainContent: true
    location:
      country: "US"
      languages: ["en"]
```

**Hinweis:** Detaillierte Informationen zu den Optionen und Standardeinstellungen des Firecrawl-Scrapers finden Sie in der [Firecrawl API Documentation](https://docs.firecrawl.dev/api-reference/endpoint/scrape).

### tavilyScraperOptions

<OptionTable
  options={[
    ['tavilyScraperOptions', 'Object', 'Konfigurationsoptionen für Tavily Extract (Scraper).', ''],
  ]}
/>

**Untergeordnete Schlüssel:**

<OptionTable
  options={[
    ['extractDepth', 'String', 'Die Tiefe des Extraktionsprozesses. "advanced" ruft mehr Daten ab, einschließlich Tabellen und eingebetteter Inhalte mit höherer Erfolgsquote, kann jedoch die Latenz erhöhen. "basic" kostet 1 Credit pro 5 erfolgreiche URLs, "advanced" kostet 2 Credits pro 5 erfolgreiche URLs.', 'Options: "basic", "advanced". Default: "basic"'],
    ['includeImages', 'Boolean', 'Fügen Sie eine Liste der aus den URLs extrahierten Bilder in die Antwort ein.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Füge die Favicon-URL für jedes extrahierte Ergebnis hinzu.', 'Default: false'],
    ['format', 'String', 'Das Format des extrahierten Webseiteninhalts. "markdown" gibt den Inhalt im Markdown-Format zurück. "text" gibt reinen Text zurück und kann die Latenz erhöhen.', 'Options: "markdown", "text". Default: "markdown"'],
    ['timeout', 'Number', 'Timeout in Millisekunden. Steuert das clientseitige HTTP-Timeout. Wenn festgelegt, wird zudem ein serverseitiges Extraktions-Timeout an Tavily gesendet, das in Sekunden umgerechnet und auf einen Bereich von 1-60s begrenzt wird.', 'Default: 15000 for basic, 30000 for advanced'],
  ]}
/>

**Beispiel:**
```yaml filename="webSearch"
webSearch:
  searchProvider: tavily
  scraperProvider: tavily
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  # tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  # tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"
  tavilySearchOptions:
    searchDepth: basic
    maxResults: 5
    topic: general
  tavilyScraperOptions:
    extractDepth: basic
```

**Hinweis:** Detaillierte Informationen zu den Optionen der Tavily API finden Sie in der [Tavily API Documentation](https://docs.tavily.com).

### keenableScraperOptions

<OptionTable
  options={[
    ['keenableScraperOptions', 'Object', 'Konfigurationsoptionen für den Keenable-Seitenabruf.', ''],
  ]}
/>

<OptionTable
  options={[
    ['attributionTitle', 'String', 'Wert, der im X-Keenable-Title Attribution-Header gesendet wird.', ''],
    ['timeout', 'Number', 'Clientseitiges HTTP-Anfrage-Timeout in Millisekunden.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

Keenable kann Suchen, Seitenabrufe oder beides bereitstellen. Die öffentlichen Endpoints funktionieren ohne Key; der optionale `keenableApiKey` erhöht die Ratenbegrenzungen. Setzen Sie `rerankerType: "none"`, um zu vermeiden, dass ein Reranker-Key erforderlich ist:

```yaml filename="webSearch"
webSearch:
  searchProvider: keenable
  scraperProvider: keenable
  rerankerType: none
  # keenableApiKey: "${KEENABLE_API_KEY}" # Optional
  # keenableApiUrl: "${KEENABLE_API_URL}" # Optional search endpoint
  keenableSearchOptions:
    maxResults: 8
    # site: example.com
  keenableScraperOptions:
    timeout: 15000
```

## Rerankers [#rerankers]

### jinaApiKey

<OptionTable
  options={[
    ['jinaApiKey', 'String', 'Umgebungsvariablenname für den Jina API-Schlüssel. Falls dieser nicht in der .env gesetzt ist, werden Benutzer aufgefordert, ihn über die UI einzugeben.', '${JINA_API_KEY}'],
  ]}
/>

**Hinweis:** Holen Sie sich Ihren API-Schlüssel von [Jina.ai](https://jina.ai/api-dashboard/)

### jinaApiUrl

<OptionTable
  options={[
    ['jinaApiUrl', 'String', 'Umgebungsvariablenname für die Jina API-URL. Falls nicht in der .env gesetzt, werden Benutzer aufgefordert, diese über die UI anzugeben.', '${JINA_API_URL}'],
  ]}
/>

**Hinweis:** Dies ist optional und nur erforderlich, wenn Sie eine benutzerdefinierte Jina-Instanz verwenden.

### cohereApiKey

<OptionTable
  options={[
    ['cohereApiKey', 'String', 'Umgebungsvariablenname für den Cohere API-Schlüssel. Wenn dieser nicht in der .env gesetzt ist, werden Benutzer aufgefordert, ihn über die UI einzugeben.', '${COHERE_API_KEY}'],
  ]}
/>

**Hinweis:** Holen Sie sich Ihren API-Schlüssel vom [Cohere Dashboard](https://dashboard.cohere.com/welcome/login)

### rerankerType

<OptionTable
  options={[
    ['rerankerType', 'String', 'Gibt an, welcher Reranker-Dienst verwendet werden soll. Auf "none" setzen, um das Reranking zu überspringen.', 'Options: "jina", "cohere", "none"'],
  ]}
/>

## Allgemeine Einstellungen [#general-settings]

### scraperTimeout

<OptionTable
  options={[
    ['scraperTimeout', 'Integer', 'Timeout in Millisekunden für Scraper-Anfragen. Muss eine nicht-negative Ganzzahl sein.', 'Default: 7500'],
  ]}
/>

### safeSearch

<OptionTable
  options={[
    ['safeSearch', 'Number', 'Sicherheitsfilter-Stufe für die Suche. 0 = AUS (keine Filterung), 1 = MODERAT (Standard), 2 = STRENG (maximale Filterung).', 'Default: 1 (MODERATE)'],
  ]}
/>

**Hinweis:** Die Stufen der sicheren Suche (Safe Search) entsprechen den gängigen Konventionen der Such-API. Die Filterung MODERATE ist standardmäßig aktiviert, um eine angemessene Inhaltsfilterung bei gleichzeitiger Aufrechterhaltung der Sucheffektivität zu gewährleisten. Tavily übernimmt diese globale Einstellung standardmäßig nicht; verwenden Sie `tavilySearchOptions.safeSearch` nur, wenn Ihr Tavily-Konto `safe_search` unterstützt.

## Hinweise [#notes]

- API-Schlüssel können auf zwei Arten konfiguriert werden:
  1. Legen Sie die in der YAML-Konfiguration angegebenen Umgebungsvariablen fest
  2. Wenn Umgebungsvariablen nicht gesetzt sind, werden Benutzer dazu aufgefordert, die API-Schlüssel über die UI einzugeben.
- Die Konfiguration unterstützt mehrere Dienste für jede Komponente (Anbieter, Scraper, Reranker)
- Wenn kein spezifischer Diensttyp angegeben ist, versucht das System, alle verfügbaren Dienste in dieser Kategorie zu verwenden.
- Safe Search bietet drei Stufen der Inhaltsfilterung: OFF (0), MODERATE (1) und STRICT (2)
- Tavily übernimmt standardmäßig nicht die globale Einstellung für die sichere Suche (Safe Search); setzen Sie `tavilySearchOptions.safeSearch` nur dann explizit, wenn Ihr Tavily-Konto `safe_search` unterstützt.
- Keenable unterstützt schlüssellose Suche und Seitenabruf; der optionale Schlüssel erhöht die öffentlichen Ratenbegrenzungen, und die URL-Überschreibung für den Abruf ist nur über die Umgebung konfigurierbar.
- Das Suchverhalten von SearXNG (Engines, Sprache, Zeitbereich, Request-Timeout) wird unter `searxngSearchOptions` konfiguriert; siehe [Choosing engines](#choosing-engines), falls Suchanfragen keine Ergebnisse liefern.
- Geben Sie niemals echte API-Schlüssel in die YAML-Konfiguration ein – verwenden Sie nur die Namen der Umgebungsvariablen.

## Einrichten von SearXNG [#setting-up-searxng]

SearXNG ist eine datenschutzorientierte Metasuchmaschine, die Sie selbst hosten können. 
Weitere Informationen finden Sie in der [offiziellen SearXNG-Dokumentation](https://docs.searxng.org/).

Hier sind die Schritte zur Einrichtung Ihrer eigenen SearXNG-Instanz zur Verwendung mit LibreChat:

### Verwendung von Docker Desktop [#using-docker-desktop]

1. **Suche nach dem offiziellen SearXNG-Image**
   - Öffnen Sie Docker Desktop
   - Suchen Sie nach `searxng/searxng` im Tab **Images**
   - Klicken Sie auf **Run** beim offiziellen Image, um automatisch einen Container des Images herunterzuladen und auszuführen.

2. **Ausführen des Containers**
   - Erweitern Sie das Dropdown-Menü **Optional Settings** im darauf folgenden Bereich, der erscheint, sobald der Download abgeschlossen ist.
   - Legen Sie Ihre gewünschten Konfigurationsdetails fest (Portnummer, Container-Name usw.)
   - Klicken Sie auf **Run**, um den Container zu starten

3. **Konfiguration von SearXNG für LibreChat**
   - Navigieren Sie zum Tab `Files` in Docker Desktop
   - Gehe zu `/etc/searxng/settings.yaml`
   - Öffnen Sie den Datei-Editor
   - Navigieren Sie zum Abschnitt `formats`
   - Fügen Sie `json` als akzeptables Format hinzu, damit LibreChat mit Ihrer Instanz kommunizieren kann.
   - Speichere die Datei

4. **Starten Sie den Container neu**
   - Starten Sie den Container neu, damit die Änderungen wirksam werden.

#### Video-Anleitung: [#video-guide]

Hier ist ein Video, das Sie in etwa einer Minute von Anfang bis Ende durch den Prozess führt:

     <Video src="/videos/min_searxng_docker_setup_hq.mp4" title="Anleitung zur SearXNG Docker-Einrichtung" />

**Hinweis:** In diesem Beispiel lautet die Instanz-URL http://localhost:55011 (die Portnummer befindet sich oben links unter dem Containernamen am Ende des Videos)

### Konfiguration von LibreChat zur Verwendung von SearXNG [#configuring-librechat-to-use-searxng]

Sie können SearXNG in LibreChat innerhalb der Benutzeroberfläche oder über `librechat.yaml` konfigurieren.

#### UI-Konfiguration [#ui-configuration]

1. **Öffnen Sie das Tools-Dropdown-Menü in der Chat-Eingabeleiste**
![Tools configuration button](/images/web-search/tools_highlight.png)

2. **Klicken Sie auf das Zahnradsymbol neben Web Search**
![Tools configuration section](/images/web-search/tools_config_highlight.png)

3. **Wählen Sie SearXNG aus dem Dropdown-Menü für Suchanbieter**
![SearXNG Dropdown-Auswahl](/images/web-search/searxng_dropdown_highlight.png)

4. **Geben Sie Ihre Konfigurationsdetails ein (z. B. Instanz-URL, Scraper-Typ usw.) und klicken Sie auf Speichern**
![Websuche-Konfiguration speichern](/images/web-search/web_search_save.png)

5. **Klicken Sie auf die Option Web Search im Dropdown-Menü für Tools**
![Web-Search-Badge in der Chat-Oberfläche](/images/web-search/websearch_highlight.png)

6. **Das Web-Search-Badge sollte nun aktiviert sein, was bedeutet, dass Ihre Anfragen jetzt die Websuchfunktion nutzen können.**
![Bestätigung des Web-Search-Badges](/images/web-search/search_badge_confirm.png)

#### YAML-Konfiguration [#yaml-configuration]

Um SearXNG für alle auf der Instanz anstatt pro Benutzer zu konfigurieren, legen Sie dieselben Werte in der `librechat.yaml` fest:

```yaml filename="librechat.yaml"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  # searxngApiKey: "${SEARXNG_API_KEY}" # Only if your instance requires authentication
  # Required: a self-hosted instance is a private destination, so exempt it from the connection guard
  allowedAddresses:
    - "localhost:55011"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeout: 10000
```

Die URL und der Schlüssel werden weiterhin aus der Umgebung gelesen, kombinieren Sie also die obige Konfiguration mit:

```bash filename=".env"
SEARXNG_INSTANCE_URL=http://localhost:55011
# SEARXNG_API_KEY=your_api_key
```

<Callout type="warning" title="Private Instanzen benötigen einen allowedAddresses-Eintrag">
`localhost:55011` ist eine Loopback-Adresse, die von LibreChat beim Verbindungsaufbau durch den [SSRF-Schutz](#ssrf-protection-and-private-providers) blockiert wird. Ohne den passenden `allowedAddresses`-Eintrag schlägt die Suche fehl, selbst wenn SearXNG einwandfrei funktioniert. Der Eintrag muss exakt dem `host:port`-Paar Ihrer Instanz-URL entsprechen.

Wenn LibreChat selbst in Docker ausgeführt wird, ist `localhost` der LibreChat-Container, nicht der von SearXNG. Verwenden Sie einen Hostnamen, den der Container erreichen kann, wie zum Beispiel den Namen des Compose-Dienstes, und schließen Sie stattdessen diesen aus:

```yaml filename="librechat.yaml"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}" # SEARXNG_INSTANCE_URL=http://searxng:8080
  allowedAddresses:
    - "searxng:8080"
```
</Callout>

Siehe [`searxngSearchOptions`](#searxngsearchoptions) für jeden Unter-Key, den dieser Block akzeptiert.

#### Fehlerbehebung bei leeren Ergebnissen [#troubleshooting-empty-results]

Eine SearXNG-Suche, die keine Ergebnisse liefert, liegt fast immer daran, dass die Anfrage keine funktionierende Instanz erreicht, anstatt dass LibreChat Ergebnisse verwirft. Gehen Sie diese Punkte der Reihe nach durch:

1. **Die Instanz ist eine private Adresse ohne `allowedAddresses`-Eintrag.** LibreChat blockiert Loopback- und private Ziele zum Zeitpunkt der Verbindung. Daher muss eine selbst gehostete Instanz mit ihrem genauen `host:port` unter [`allowedAddresses`](#ssrf-protection-and-private-providers) aufgeführt sein. Dies blockiert die Anfrage vollständig, anstatt ein leeres Ergebnisset zurückzugeben.
2. **`json` fehlt in `formats`.** LibreChat fordert Ergebnisse als JSON an. Wenn der Abschnitt `formats` in Ihrer Instanz-Konfigurationsdatei `json` nicht auflistet, antwortet die Instanz mit einer Fehlerseite und jede Suche liefert keine Ergebnisse. Dies ist Schritt 3 der oben genannten Docker-Einrichtung.
3. **Jede ausgewählte Engine ist fehlgeschlagen.** SearXNG führt die Engines zusammen, die geantwortet haben; eine leere Antwort bedeutet also, dass keine von ihnen etwas zurückgegeben hat. DuckDuckGo ist im Standard-Set (`google,bing,duckduckgo`) enthalten und liefert für die meisten selbst gehosteten Instanzen CAPTCHAs aus, wodurch nur zwei Engines für die Suche übrig bleiben. Legen Sie `searxngSearchOptions.engines` explizit fest, lassen Sie DuckDuckGo weg und listen Sie einige weitere Engines auf.
4. **Ein Engine-Name existiert auf Ihrer Instanz nicht.** SearXNG überspringt Engines, die es nicht erkennt, stillschweigend; ein Tippfehler führt also lediglich zu weniger Ergebnissen. Vergleichen Sie Ihre Liste mit `https://your-instance/config`.
5. **Die Instanz ist langsamer als das Timeout.** Erhöhen Sie `searxngSearchOptions.timeout` über den Standardwert `10000`, wenn Ihre Instanz viele Engines abfragt oder sich hinter einer langsamen Netzwerkverbindung befindet.

Sie können reproduzieren, was LibreChat sendet, indem Sie die Instanz direkt aufrufen:

```bash
curl -s "http://localhost:55011/search?q=librechat&format=json&engines=google,bing,startpage" | head -c 500
```

Dies läuft von Ihrem eigenen Rechner aus, umgeht also den Verbindungsschutz von LibreChat vollständig. Ein leeres `results`-Array an dieser Stelle bestätigt, dass das Problem auf der Seite von SearXNG liegt; Ergebnisse hier, während LibreChat weiterhin nichts findet, deuten auf `allowedAddresses` hin.
