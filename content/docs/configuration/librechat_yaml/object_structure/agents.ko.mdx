---
title: Agents 엔드포인트 객체 구조
icon: Bot
---

이 페이지는 [`agents`](/docs/features/agents) endpoint에 적용됩니다.

## 예시 [#example]

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    recursionLimit: 50
    maxRecursionLimit: 100
    maxToolCallArgBytes: 65536
    maxDeltaEventsPerTurn: 100000
    maxToolCallArgBytesByTool:
      create_file: 131072
    disableBuilder: false
    # (optional) Agent Capabilities available to all users. Omit the ones you wish to exclude. Defaults to list below.
    # Opt-in capabilities: "programmatic_tools", "stateful_code_sessions", "run_in_background", and "tool_intents".
    # capabilities: ["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]
    # (optional) File citation configuration for file_search capability
    maxCitations: 30 # Maximum total citations in responses (1-50)
    maxCitationsPerFile: 7 # Maximum citations from each file (1-10)
    minRelevanceScore: 0.45 # Minimum relevance score threshold (0.0-1.0)
    titleTiming: immediate
    activityLabel: true
    activityModel: gpt-4.1-nano
    activityPhaseLabel: true
    activityPhaseMaxPerRun: 5
    maxSubagents: 10 # Maximum explicit subagents per agent (1-50)
    skills:
      maxCatalogSkills: 20
    toolApproval:
      enabled: true
      mode: default
      deny: ['mcp:*:delete_*']
      ask: ['mcp:*:*']
    checkpointer:
      type: mongo
      ttl: 86400
    remoteApi:
      auth:
        oidc:
          enabled: false
```

> 이 구성은 에이전트를 위한 빌더 인터페이스를 활성화합니다.

## recursionLimit

<OptionTable
  options={[
    [
      'recursionLimit',
      'Number',
      '에이전트가 실행 중에 취할 수 있는 기본 단계 수를 설정합니다.',
      'Controls recursion depth to prevent infinite loops. When limit is reached, raises GraphRecursionError. This value can be configured from the UI up to the maxRecursionLimit.',
    ],
  ]}
/>

**기본값:** `25`

**예시:**

```yaml filename="endpoints / agents / recursionLimit"
recursionLimit: 50
```

에이전트 단계에 대한 자세한 내용은 [Max Agent Steps](/docs/features/agents#max-agent-steps)를 참조하세요.

## maxRecursionLimit

<OptionTable
  options={[
    [
      'maxRecursionLimit',
      'Number',
      '에이전트가 실행 중에 취할 수 있는 절대적인 최대 단계 수를 설정합니다.',
      'Defines the upper limit for the recursionLimit that can be set from the UI. This prevents users from setting excessively high values.',
    ],
  ]}
/>

**기본값:** 생략할 경우, recursionLimit의 값으로 설정되거나 recursionLimit 또한 생략된 경우 50으로 설정됩니다.

**예시:**

```yaml filename="endpoints / agents / maxRecursionLimit"
maxRecursionLimit: 100
```

에이전트 단계에 대한 자세한 내용은 [Max Agent Steps](/docs/features/agents#max-agent-steps)를 참조하세요.

## 스트림 서킷 브레이커(Stream circuit breakers) [#stream-circuit-breakers]

이 설정들은 공급자 스트림이 제어할 수 없는 도구 인수를 생성하거나 제한 없는 델타 이벤트 시퀀스를 생성하는 것으로 보일 때 Agent 실행을 중단합니다.

<OptionTable
  options={[
    [
      'maxToolCallArgBytes',
      'Number',
      '한 번의 스트리밍된 도구 호출 인수에 대한 최대 누적 바이트 수입니다. 전역 가드를 비활성화하려면 0으로 설정하세요.',
      '65536',
    ],
    [
      'maxDeltaEventsPerTurn',
      'Number',
      '한 번의 모델 생성 턴에 대한 최대 스트리밍 이벤트 수입니다. 이 가드를 비활성화하려면 0으로 설정하거나 생략하세요.',
      '0 (disabled)',
    ],
    [
      'maxToolCallArgBytesByTool',
      'Object',
      '모델이 인식하는 도구 이름을 키로 사용하는 도구별 인수 바이트(argument-byte) 재정의입니다. 0으로 설정하면 해당 도구에 대한 가드가 비활성화됩니다.',
      'create_file: 131072',
    ],
  ]}
/>

LibreChat은 64 KiB의 전역 인수 제한을 사용하며, 합법적인 페이로드가 더 큰 경우가 많은 `create_file`에 대해 128 KiB의 오버라이드를 기본으로 제공합니다. `maxToolCallArgBytesByTool`의 항목은 해당 내장 오버라이드에 병합됩니다. 두 제한 중 하나라도 초과되면 LibreChat은 진행 중인 공급자 스트림을 중단하고 어떤 제한이 초과되었는지 보고합니다.

```yaml filename="endpoints / agents / stream circuit breakers"
maxToolCallArgBytes: 65536
maxDeltaEventsPerTurn: 100000
maxToolCallArgBytesByTool:
  create_file: 262144
  large_import: 524288
```

모든 인수 크기 제한(argument-size guard)을 비활성화하려면, 전역 제한과 기본 제공 `create_file` 재정의를 모두 `0`으로 설정하세요:

```yaml
maxToolCallArgBytes: 0
maxToolCallArgBytesByTool:
  create_file: 0
```

## titleTiming

<OptionTable
  options={[
    [
      'titleTiming',
      'String',
      'agents endpoint에 대한 대화 제목이 생성되는 시점을 제어합니다. 유효한 값: "immediate" 또는 "final".',
      'Defaults to "immediate".',
    ],
  ]}
/>

**기본값:** `"immediate"`

**사용 가능한 값:**

- **`"immediate"`**: 사용자의 첫 번째 메시지를 사용하여 모델 응답과 병렬로 요청이 시작되자마자 제목을 생성합니다.
- **`"final"`**: 전체 응답이 완료될 때까지 제목 생성을 지연합니다. 이는 기존의 레거시 동작을 유지합니다.

**예시:**

```yaml filename="endpoints / agents / titleTiming"
titleTiming: immediate
```

## activityLabel

`activityLabel: true`를 설정하여 각 에이전트 추론 및 도구 호출 블록을 생성된 한 줄 헤더 아래로 그룹화합니다. `activityModel`, `activityEndpoint`, `activityPrompt`, `activityMaxPerRun` 및 `activityCharLimit`를 사용하여 추가 모델 호출과 그 비용을 제어하세요. 전체 필드 참조 및 우선순위 규칙은 [Agent Activity Groups](/docs/configuration/librechat_yaml/object_structure/shared_endpoint_settings#agent-activity-groups)를 참조하세요.

```yaml filename="endpoints / agents / activity groups"
activityLabel: true
activityEndpoint: openAI
activityModel: gpt-4.1-nano
activityMaxPerRun: 20
activityCharLimit: 600
activityPhaseLabel: true
activityPhaseMaxPerRun: 5
```

`activityPhaseLabel`은 최소 두 개의 논리적 활동을 포함하는 각 실행 단계 주위에 독립적으로 하나의 접힌 상위 요약을 추가할 수 있습니다. 해당 호출을 조정하려면 `activityPhaseModel`, `activityPhaseEndpoint`, `activityPhasePrompt` 및 `activityPhaseMaxPerRun`을 사용하세요. `activityCharLimit`은 하위 활동 레이블과 공유됩니다.

## disableBuilder

<OptionTable
  options={[
    [
      'disableBuilder',
      'Boolean',
      '에이전트용 빌더 인터페이스의 표시 여부와 사용을 제어합니다.',
      'When set to `true`, disables the builder interface for the agent, limiting direct manual interaction.',
    ],
  ]}
/>

**기본값:** `false`

**예시:**

```yaml filename="endpoints / agents / disableBuilder"
disableBuilder: false
```

## allowedProviders

<OptionTable
  options={[
    [
      'allowedProviders',
      'Array/List of Strings',
      'Agents 기능에서 사용이 허용된 endpoint 제공자(예: "openAI", "anthropic", "google") 목록을 지정합니다.',
      'If defined, only agents configured with these providers can be initialized. If omitted or empty, all configured providers are allowed.',
    ],
  ]}
/>

**기본값:** `[]` (빈 목록, 모든 제공자 허용)

**참고:** 다음 중 하나여야 하며, [configuration](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#name)에 정의된 사용자 지정 endpoint 이름일 수도 있습니다: - `openAI, azureOpenAI, google, anthropic, assistants, azureAssistants, bedrock`

**예시:**

```yaml filename="endpoints / agents / allowedProviders"
allowedProviders:
  - openAI
  - google
```

## capabilities

<OptionTable
  options={[
    [
      'capabilities',
      'Array/List of Strings',
      'agents 엔드포인트에 대해 모든 사용자에게 제공되는 에이전트 기능을 지정합니다.',
      'Defines the agent capabilities that are available to all users for the agents endpoint. You can omit the capabilities you wish to exclude from the list.',
    ],
  ]}
/>

**기본값:** `["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]`

`programmatic_tools`, `stateful_code_sessions`, `run_in_background` 및 `tool_intents`는 옵트인(opt-in) 방식이며 명시적으로 추가해야 합니다. Programmatic tools 및 Stateful sessions를 사용하려면 `execute_code`와 호환되는 Code Interpreter 배포가 필요합니다. 백그라운드 코드 실행을 위해서는 `run_in_background`, `execute_code` 및 Code Interpreter가 필요합니다.

**예시:**

```yaml filename="endpoints / agents / capabilities"
capabilities:
  - 'deferred_tools'
  # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
  # Requires execute_code and a Code Interpreter deployment with the Tool Call Server component.
  # - 'programmatic_tools'
  - 'execute_code'
  - 'file_search'
  - 'web_search'
  - 'artifacts'
  - 'subagents'
  - 'actions'
  - 'context'
  - 'skills'
  - 'memory'
  - 'ask_user_question'
  - 'tools'
  - 'chain'
  - 'ocr'
  # Optional: reuse one Code Interpreter workspace per conversation.
  # - 'stateful_code_sessions'
  # Optional: allow selected eligible MCP and Code Interpreter tools to run as background tasks.
  # - 'run_in_background'
  # Optional: show live model-written intent labels for native and selected MCP tools.
  # - 'tool_intents'
```

**참고:** 이 필드는 선택 사항입니다. 생략할 경우, 기본 동작은 기본값에 나열된 모든 기능을 포함하는 것입니다.

## toolApproval

에이전트 도구 호출에 대한 사람의 검토를 제어합니다. 이 기능은 기본적으로 비활성화되어 있습니다. 활성화되면, 일치하는 호출은 사용자가 승인, 거부 또는 인수 편집과 같은 허용 결정을 제출할 때까지 일시 중지됩니다.

<OptionTable
  options={[
    ['enabled', 'Boolean', 'Agents endpoint에 대한 도구 승인을 활성화합니다.', 'false'],
    [
      'mode',
      'String',
      '일치하지 않는 호출 동작을 설정합니다: `default`는 질문하고, `dontAsk`는 거부하며, `bypass`는 거부되지 않는 한 승인합니다.',
      'default',
    ],
    ['allow', 'Array of strings', '프롬프트 없이 실행할 수 있는 호출을 위한 Glob 패턴.', ''],
    ['deny', 'Array of strings', '항상 거부되는 호출에 대한 Glob 패턴입니다. 거부 규칙이 우선합니다.', ''],
    ['ask', 'Array of strings', '항상 검토가 필요한 호출에 대한 Glob 패턴.', ''],
    [
      'reason',
      'String',
      '승인 프롬프트에 표시되는 선택적 설명입니다. 도구 이름을 삽입하려면 `{tool}`을 사용하세요.',
      '',
    ],
    [
      'hooks',
      'Array of objects',
      '컨텍스트 인식 결정을 위한 신뢰할 수 있는 프로그래밍 방식의 정책 모듈입니다. Hook은 정적 정책을 강화하는 데에만 사용할 수 있습니다.',
      '',
    ],
  ]}
/>

패턴은 glob 매칭을 사용합니다. MCP 도구는 `mcp:<server>:<tool>`로 범위를 지정할 수 있습니다. 예를 들어, `mcp:github:*`는 `github` 서버의 모든 도구와 일치합니다. 평가는 `bypass` 모드를 포함하여 `deny`에 가장 높은 우선순위를 부여합니다.

```yaml filename="endpoints / agents / toolApproval"
toolApproval:
  enabled: true
  mode: default
  allow:
    - 'mcp:analytics:read_*'
  deny:
    - 'mcp:*:delete_*'
  ask:
    - 'mcp:*:*'
  reason: 'Review {tool} before it runs.'
```

Hooks는 서버 시작 시 한 번 로드되며 LibreChat 프로세스 내에서 실행됩니다. 각 항목은 `module`, 선택 사항인 도구 이름 `matcher` 정덱스(regex), 그리고 모듈의 빌더로 전달되는 선택 사항인 `options` 객체를 허용합니다. 신뢰할 수 있는 코드만 로드하십시오. 역할별 또는 테넌트별 재정의는 hook 모듈을 다시 로드하지 않으므로, hook 내부에서 상황에 맞는 동작을 구현하십시오.

```yaml filename="endpoints / agents / toolApproval / hooks"
toolApproval:
  enabled: true
  hooks:
    - module: '@acme/librechat-approval-hook'
      matcher: '^mcp:production:'
      options:
        requireTicket: true
```

## checkpointer

도구 승인 또는 사용자에게 질문(Ask User) 기능으로 인해 일시 중지된 Agent 실행을 위한 저장소를 구성합니다. 두 기능 중 하나라도 체크포인터(checkpointer)가 필요하지만 이 블록이 생략된 경우, LibreChat은 24시간 결정 유예 기간을 가진 기본 MongoDB를 사용합니다.

<OptionTable
  options={[
    [
      'type',
      'String',
      '`mongo`는 재시작 및 복제본 전반에 걸쳐 일시 중지된 실행을 유지하며, `memory`는 프로세스 로컬 방식으로서 개발 용도로 의도되었습니다.',
      'mongo',
    ],
    ['ttl', 'Number', '일시 중지된 실행이 결정을 기다리는 시간(초 단위)입니다.', '86400'],
    [
      'checkpointCollectionName',
      'String',
      '선택 사항인 MongoDB 체크포인트 컬렉션 이름 재정의입니다.',
      'agent_checkpoints',
    ],
    [
      'checkpointWritesCollectionName',
      'String',
      '선택적 MongoDB 체크포인트 쓰기 컬렉션 이름 재정의.',
      'agent_checkpoint_writes',
    ],
  ]}
/>

```yaml filename="endpoints / agents / checkpointer"
checkpointer:
  type: mongo
  ttl: 86400
```

`type: memory`는 단일 프로세스 개발 배포 환경에서만 사용하세요. 메모리에 저장된 일시 중지된 실행은 재시작 후나 다른 복제본(replica)에서 재개할 수 없습니다.

MongoDB 체크포인트는 직렬화된 상태가 8 MiB를 초과하면 경고를 기록하고, 15 MiB를 초과하는 지속적 일시 중지(durable pause)는 거부하여 MongoDB의 16 MiB 문서 제한 내에 여유 공간을 확보합니다. 인라인 처리된 대용량 미디어 또는 도구 출력값이 이러한 문제의 일반적인 원인입니다. 실행 시 `CHECKPOINT_TOO_LARGE` 오류가 발생하면 새 대화를 시작하거나 컨텍스트를 줄이십시오.

## skills

에이전트에 대한 엔드포인트 수준의 Skills 설정을 제어합니다.

<OptionTable
  options={[
    [
      'skills.maxCatalogSkills',
      'Number',
      '모델이 볼 수 있는 카탈로그에 노출되는 활성 접근 가능 Skill의 수를 제한합니다. 1에서 100 사이여야 합니다.',
      'maxCatalogSkills: 20',
    ],
  ]}
/>

**기본값:** 런타임 카탈로그 제한을 초과하는 구성된 제한 없음.

**예시:**

```yaml filename="endpoints / agents / skills"
skills:
  maxCatalogSkills: 20
```

이 설정은 Skills를 비활성화하지 않습니다. Skills의 사용 가능 여부를 제어하려면 `skills` 기능과 에이전트/Model Specs별 스킬 범위 지정을 사용하세요.

## maxSubagents

<OptionTable
  options={[
    [
      'maxSubagents',
      'Number',
      '단일 에이전트가 참조할 수 있는 명시적 하위 에이전트의 최대 개수입니다.',
      'Applies to the `agent_ids` allowlist on agents and on model specs, and to configured subagent `graphs`, which agents support but model specs do not. Must be a whole number between 1 and 50.',
    ],
  ]}
/>

**기본값:** `10`

**범위:** `1-50`

**예시:**

```yaml filename="endpoints / agents / maxSubagents"
endpoints:
  agents:
    maxSubagents: 20
```

배포 환경에서 10개 이상의 하위 에이전트에게 작업을 위임해야 하는 오케스트레이션 중심의 에이전트를 실행할 때 이 값을 높이십시오. 이 값은 시작 시 읽히며 다음 세 곳에서 적용됩니다:

- `subagents.agent_ids` 및 `subagents.graphs`에 대한 에이전트 생성, 업데이트 및 복제 요청
- Model spec `subagents.agent_ids` 허용 목록은 동일한 구성 단계에서 유효성이 검사됩니다.
- 설정된 값에서 새로운 하위 에이전트(subagent) 항목 입력을 중단하는 Agent Builder 패널

Model specs는 `subagents` 하위에서 `enabled`, `allowSelf`, `agent_ids`만 허용합니다. Subagent `graphs`는 에이전트 수준의 기능입니다. 따라서 model spec의 `graphs` 키는 설정이 파싱될 때 오류 없이 삭제됩니다.

<Callout type="warning">
  `maxSubagents`는 1에서 50 사이의 정수여야 합니다. 해당 범위를 벗어난 값은 `librechat.yaml` 유효성 검사를 통과하지 못하며, LibreChat은 오류를 기록하고 기본값으로 대체하는 대신 시작 시 종료됩니다. `CONFIG_BYPASS_VALIDATION=true`로 시작하면 종료를 건너뛸 수 있지만, 이 경우 사용자 지정 설정 전체가 폐기되므로 제한은 `10`으로 돌아갑니다. 의도적으로 기본값으로 되돌리려면 해당 키를 제거하십시오. `maxSubagents`를 생략하면 오류 없이 `10`으로 설정됩니다.
</Callout>

<Callout type="note">
  이 제한은 프로세스 전체에 적용되며 `librechat.yaml`에서 설정됩니다. `endpoints.agents.maxSubagents`에 대한 사용자별 또는 그룹별 데이터베이스 재정의는 제공되는 구성에는 반영되지만, 요청 유효성 검사에는 적용되지 않습니다.
</Callout>

이 값을 높이는 것은 하나의 에이전트가 참조할 수 있는 하위 에이전트의 수만 변경합니다. 깊이, 그래프 노드 및 실행 구성 제한은 영향을 받지 않습니다. [Subagents](/docs/features/subagents#limits)를 참조하세요.

## maxCitations

<OptionTable
  options={[
    [
      'maxCitations',
      'Number',
      '단일 에이전트 응답에 포함될 수 있는 최대 총 인용 횟수를 제어합니다.',
      'When using file_search capability, limits the total number of source citations returned to prevent overwhelming responses while ensuring comprehensive coverage.',
    ],
  ]}
/>

**기본값:** `30`

**범위:** `1-50`

**예시:**

```yaml filename="endpoints / agents / maxCitations"
maxCitations: 30
```

## maxCitationsPerFile

<OptionTable
  options={[
    [
      'maxCitationsPerFile',
      'Number',
      '단일 파일에서 추출할 수 있는 최대 인용 횟수를 제한합니다.',
      'Ensures citation diversity by preventing any single file from dominating the citations, encouraging representation from multiple sources.',
    ],
  ]}
/>

**기본값:** `7`

**범위:** `1-10`

**예시:**

```yaml filename="endpoints / agents / maxCitationsPerFile"
maxCitationsPerFile: 7
```

## minRelevanceScore

<OptionTable
  options={[
    [
      'minRelevanceScore',
      'Number',
      '응답에 포함될 소스에 대한 최소 관련성 점수 임계값을 설정합니다.',
      'Filters out low-quality matches based on vector similarity scores. Higher values (e.g., 0.7) ensure only highly relevant sources are cited, while lower values (e.g., 0.0) include all sources regardless of quality.',
    ],
  ]}
/>

**기본값:** `0.45` (45% 관련성 임계값)

**범위:** `0.0-1.0`

**예시:**

```yaml filename="endpoints / agents / minRelevanceScore"
minRelevanceScore: 0.45
```

### 파일 인용 설정 예시 [#file-citation-configuration-examples]

**기본 구성 (균형 잡힘)**

```yaml
endpoints:
  agents:
    maxCitations: 30
    maxCitationsPerFile: 7
    minRelevanceScore: 0.45
```

압도적인 응답을 방지하고 품질이 낮은 일치 항목을 필터링하면서 포괄적인 출처를 제공합니다.

**엄격한 구성 (고품질)**

```yaml
endpoints:
  agents:
    maxCitations: 10
    maxCitationsPerFile: 3
    minRelevanceScore: 0.7
```

집중된 응답을 위해 엄격한 제한을 두어 관련성이 매우 높은 인용문만 포함합니다.

**포괄적 구성 (연구)**

```yaml
endpoints:
  agents:
    maxCitations: 50
    maxCitationsPerFile: 10
    minRelevanceScore: 0.0
```

관련성 여부와 관계없이 모든 출처를 포함하여, 철저한 연구 작업을 위한 최대 정보 추출.

## 에이전트 기능 (Agent Capabilities) [#agent-capabilities]

`capabilities` 필드를 사용하면 에이전트에 대한 특정 기능을 활성화하거나 비활성화할 수 있습니다. 사용 가능한 기능은 다음과 같습니다:

- **deferred_tools**: 에이전트가 모든 도구를 사전에 컨텍스트로 로드하는 대신 런타임에 지연된 MCP 도구를 검색할 수 있도록 합니다.
- **programmatic_tools**: 에이전트 빌더(Agent Builder)에서 Programmatic으로 표시된 MCP 도구에 대해 프로그래밍 방식의 도구 호출(Programmatic Tool Calling)을 활성화합니다. `execute_code` 및 Tool Call Server 구성 요소가 포함된 코드 인터프리터(Code Interpreter) 배포가 필요합니다. 이 기능은 선택 사항이며 기본적으로 활성화되어 있지 않습니다.
- **execute_code**: 에이전트가 코드를 실행할 수 있도록 허용합니다.
- **stateful_code_sessions**: 옵트인(opted-in)된 에이전트가 대화당 하나의 Code Interpreter 작업 공간을 재사용할 수 있도록 합니다. `execute_code`, 호환되는 Code Interpreter 배포, 그리고 에이전트별 고급(Advanced) 설정이 필요합니다. 이 기능은 매우 실험적인 단계이며 기본적으로 활성화되어 있지 않고, 실험 과정에서 크게 변경될 수 있습니다. 현재의 동작이나 구성을 안정적인 프로덕션 계약으로 간주하지 마십시오.
- **file_search**: 에이전트가 파일을 검색하고 상호작용할 수 있도록 합니다. 활성화되면 인용 동작은 `maxCitations`, `maxCitationsPerFile` 및 `minRelevanceScore` 설정에 의해 제어됩니다.
- **web_search**: 에이전트가 인터넷을 검색하고 정보를 가져올 수 있도록 웹 검색 기능을 활성화합니다.
- **artifacts**: 에이전트가 대화형 아티팩트(React 컴포넌트, HTML, Mermaid 다이어그램)를 생성할 수 있도록 합니다.
- **subagents**: 격리된 컨텍스트의 하위 에이전트 실행을 활성화합니다. [Subagents](/docs/features/subagents)를 참조하세요.
- **actions**: 에이전트가 미리 정의된 작업을 수행할 수 있도록 허용합니다.
- **context**: 채팅에서 "텍스트로 업로드(Upload as Text)" 기능과 에이전트를 위한 "파일 컨텍스트(File Context)"를 활성화하여, 사용자가 파일을 업로드하고 해당 내용을 추출하여 대화에 직접 포함할 수 있도록 합니다.
- **skills**: 사이드 패널에서 Skills를 활성화하고, 수동 `$` 호출, 모델 호출 Skills, 그리고 에이전트 skill 허용 목록을 지원합니다. [Skills](/docs/features/skills)를 참조하세요.
- **memory**: 메모리가 구성되어 있고 사용자에게 접근 권한이 있을 때 에이전트가 `set_memory` 및 `delete_memory`를 사용할 수 있도록 합니다. 기본적으로 활성화되어 있으며, 메모리가 비활성화되면 런타임에 제거됩니다.
- **ask_user_question**: 에이전트가 일시 중지하고 단일 양식으로 1~4개의 관련 질문을 한 뒤 답변을 받아 재개할 수 있도록 합니다. 기본적으로 활성화되어 있습니다.
- **tools**: 에이전트에게 다양한 도구에 대한 액세스 권한을 부여합니다.
- **chain**: 에이전트 체이닝(Mixture-of-Agents (MoA) 워크플로우라고도 함)을 위한 베타 기능을 활성화합니다.
- **ocr**: 채팅에서 "텍스트로 업로드(Upload as Text)" 기능과 에이전트를 위한 "파일 컨텍스트(File Context)"를 선택적으로 향상시켜, 파일을 업로드하고 OCR로 처리할 수 있게 합니다. **OCR 서비스를 구성해야 합니다.**
- **run_in_background**: Code Interpreter 실행 및 셸 도구를 기본적으로 백그라운드 실행이 가능하도록 설정하며, MCP 도구에 대한 도구별 백그라운드 옵트인(opt-in)을 활성화합니다. 에이전트는 Code Interpreter를 명시적으로 제외할 수 있습니다. 이 설정은 백그라운드 실행을 허용하며, 모델이 호출할 때마다 이를 사용할지 여부를 결정합니다. 코드를 실행하려면 `execute_code`와 구성된 Code Interpreter 배포가 필요합니다. 이 기능은 기본적으로 활성화되어 있지 않습니다.
- **tool_intents**: 적격한 도구 호출에 모델이 직접 작성한 인텐트(intent) 레이블을 추가합니다. 코드 인터프리터 도구를 포함한 기본 도구는 자동으로 옵트인(opt-in)되며, MCP 도구는 Agent Builder에서 개별적으로 또는 일괄적으로 활성화할 수 있습니다. Model Specs는 `describeIntent`를 사용합니다. 이 기능은 기본적으로 활성화되어 있지 않습니다.

기능(capabilities)을 지정하여 사용자가 에이전트와 상호작용할 때 사용할 수 있는 기능을 제어할 수 있습니다.

## 예시 구성 [#example-configuration]

다음은 사용자 지정 기능 및 파일 인용 설정을 사용하여 `agents` endpoint를 구성하는 예시입니다:

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    disableBuilder: false
    # File citation configuration
    maxCitations: 20
    maxCitationsPerFile: 5
    minRelevanceScore: 0.6
    # Custom capabilities
    capabilities:
      # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
      # - 'programmatic_tools'
      - 'execute_code'
      # Optional: makes Code Interpreter background-eligible and enables selected MCP tools.
      # - 'run_in_background'
      # Optional: enables live intent labels for native and selected MCP tools.
      # - 'tool_intents'
      - 'file_search'
      - 'skills'
      - 'subagents'
      - 'actions'
      - 'artifacts'
      - 'context'
      - 'ocr'
      - 'web_search'
```

이 예시에서:

- 빌더 인터페이스가 활성화되었습니다
- 파일 인용은 총 20개로 제한되며, 파일당 최대 5개까지 가능합니다.
- 60% 이상의 관련성을 가진 소스만 포함됩니다
- LibreChat 에이전트는 코드 실행, 파일 검색(출처 포함), Skills, Subagents, actions, artifacts, 파일 컨텍스트, 구성된 경우 OCR 서비스, 그리고 웹 검색 기능에 액세스할 수 있습니다.
- `execute_code`와 함께 `programmatic_tools` 기능을 추가하지 않으면 Programmatic Tool Calling은 비활성화된 상태로 유지됩니다.
- `run_in_background`를 추가하지 않으면 백그라운드 도구 호출은 비활성화된 상태로 유지됩니다. 이 경우 Code Interpreter 도구는 기본적으로 사용 가능해지지만, MCP 도구는 여전히 도구별로 선택해야 합니다.
- `tool_intents`를 추가하지 않으면 도구 의도(tool intent) 레이블은 비활성화된 상태로 유지됩니다. 기본 도구(native tools)는 자동으로 옵트인(opt in)되지만, MCP 도구는 여전히 도구별로 선택해야 합니다.

## remoteApi

Remote Agent API 인증을 위한 설정입니다. 외부 서비스가 Agents API 엔드포인트를 호출할 때 인증하는 방식을 제어합니다.

### remoteApi.auth

<OptionTable
  options={[
    [
      'auth',
      'Object',
      'Remote Agent API를 위한 인증 구성.',
      'Supports API key and/or OIDC Bearer token authentication. If omitted, only API key auth is active.',
    ],
  ]}
/>

#### remoteApi.auth.apiKey

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Remote Agent API에 대한 API 키 인증을 활성화합니다.',
      'When true, requests with a valid LibreChat API key are accepted. Can be used alongside or instead of OIDC.',
    ],
  ]}
/>

**기본값:** `true`

#### remoteApi.auth.oidc

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'OIDC Bearer 토큰 인증을 활성화합니다.',
      'When true, the middleware validates Bearer tokens against the configured OIDC issuer via JWKS.',
    ],
    [
      'issuer',
      'String',
      'OIDC 발급자 URL.',
      'The base URL of your OIDC provider, such as a Keycloak realm URL. Used for token issuer validation and JWKS discovery if jwksUri is not set.',
    ],
    [
      'jwksUri',
      'String',
      'JWKS endpoint URL. 선택 사항입니다.',
      'If omitted, resolved automatically via {issuer}/.well-known/openid-configuration. You can also set OPENID_JWKS_URL as an alternative.',
    ],
    [
      'audience',
      'String',
      '예상 토큰 대상(audience)입니다. OIDC 인증이 활성화된 경우 필수입니다.',
      'Tokens must contain this value in their aud claim.',
    ],
    [
      'scope',
      'String',
      '필수 범위 값. 선택 사항.',
      'If set, the token must contain this value in its scp or scope claim. Use this to distinguish token intent across different APIs.',
    ],
  ]}
/>

**기본값:** `enabled: false`

**예시 - OIDC 전용:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: false
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          audience: my-client-id
```

**예시 - API 키 폴백을 사용하는 OIDC:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: true
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          # jwksUri is optional and auto-discovered if omitted
          jwksUri: https://auth.example.com/realms/myrealm/protocol/openid-connect/certs
          audience: my-client-id
```

<Callout type="info">
  JWKS URI 확인 우선순위는 명시적 `jwksUri`, 그 다음 `OPENID_JWKS_URL`, 마지막으로 `{issuer}/.well-known/openid-configuration`을 통한 자동 검색 순입니다.
</Callout>

<Callout type="info">
  OIDC 사용자 매칭은 `sub` 클레임을 기본 조회 값으로 사용하며, `email`, `preferred_username` 또는 `upn` 클레임을 대체 값으로 사용합니다. 매칭되는 사용자는 LibreChat에 이미 존재해야 합니다.
</Callout>

## Subagents [#subagents]

`subagents` 필드는 `subagents` 기능을 사용할 수 있을 때 부모 에이전트가 생성할 수 있는 격리된 하위 에이전트를 제어합니다.

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'true일 경우 이 에이전트에 하위 에이전트 생성 도구를 추가합니다. 기본값: 비활성화됨.',
      'enabled: true',
    ],
    [
      'allowSelf',
      'Boolean',
      '에이전트가 새롭고 격리된 컨텍스트에서 스스로를 생성할 수 있도록 허용합니다. 기본값: true.',
      'allowSelf: true',
    ],
    [
      'agent_ids',
      'Array/List of Strings',
      '이 에이전트가 생성할 수 있는 특정 에이전트입니다. 기본값이 10인 `maxSubagents`에 의해 제한됩니다.',
      'agent_ids: ["agent_researcher"]',
    ],
  ]}
/>

```yaml filename="Agent subagents"
subagents:
  enabled: true
  allowSelf: true
  agent_ids:
    - 'agent_researcher'
    - 'agent_reviewer'
```

사용자 대상 동작 및 제한 사항은 [Subagents](/docs/features/subagents)를 참조하세요.

## 참고 사항 [#notes]

- [modelSpecs](/docs/configuration/librechat_yaml/object_structure/model_specs)를 사용하여 선택할 에이전트 목록을 정의하는 경우가 아니라면, 빌더 인터페이스를 비활성화하는 것은 권장되지 않습니다.
- 파일 인용 설정(`maxCitations`, `maxCitationsPerFile`, `minRelevanceScore`)은 `file_search` 기능이 활성화된 경우에만 적용됩니다.
- 관련성 점수는 벡터 유사도를 사용하여 계산되며, 1.0은 완벽한 일치를, 0.0은 유사성이 없음을 나타냅니다.
- 인용 제한은 포괄적인 정보 검색과 응답 품질 및 성능 사이의 균형을 맞추는 데 도움을 줍니다.
- `context` 기능은 OCR 설정 없이도 텍스트 파싱 방식을 사용하여 작동합니다. OCR을 설정하면 추출 품질이 향상됩니다.
- `ocr` 기능을 사용하려면 OCR 서비스를 구성해야 합니다 ([OCR 구성](/docs/configuration/librechat_yaml/object_structure/ocr) 참조).
- `maxSubagents`는 단일 에이전트가 참조할 수 있는 하위 에이전트의 수를 제한합니다. 이는 [Subagents](/docs/features/subagents#limits) 문서에 명시된 깊이 또는 그래프 제한을 변경하지 않습니다.
