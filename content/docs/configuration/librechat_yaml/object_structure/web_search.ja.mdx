---
title: Web検索の設定
icon: Search
---

`webSearch` 設定では、LibreChat 内のウェブ検索機能（検索プロバイダー、コンテンツスクレイパー、結果の再ランキング機能など）をカスタマイズできます。

## 概要 [#overview]

ウェブ検索機能は、主に3つのコンポーネントで構成されています。

1. **Search Providers**: 初期ウェブ検索を実行するサービス
2. **Scrapers**: Webページからコンテンツを抽出するサービス
3. **Rerankers**: 検索結果をより関連性の高い順に並べ替えるサービス

## 例 [#example]

```yaml filename="webSearch"
webSearch:
  # Search Provider Configuration
  serperApiKey: "${SERPER_API_KEY}"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}"
  searchProvider: "serper" # Options: "serper", "searxng", "tavily", "keenable"

  # Optional: query options sent to a SearXNG instance
  searxngSearchOptions:
    engines: "google,bing,startpage" # Default: "google,bing,duckduckgo"
    language: "en" # Default: "all"

  # Tavily Configuration (search and/or scraper)
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"

  # Keenable Configuration (search and/or scraper; all credentials optional)
  keenableApiKey: "${KEENABLE_API_KEY}"
  keenableApiUrl: "${KEENABLE_API_URL}"
  keenableSearchOptions:
    maxResults: 8
    site: "example.com"
    attributionTitle: "LibreChat"
    timeout: 15000
  keenableScraperOptions:
    attributionTitle: "LibreChat"
    timeout: 15000

  # Scraper Configuration
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlApiUrl: "${FIRECRAWL_API_URL}"
  firecrawlVersion: "${FIRECRAWL_VERSION}"
  scraperProvider: "firecrawl" # Options: "firecrawl", "serper", "tavily", "keenable"

  # Reranker Configuration
  jinaApiKey: "${JINA_API_KEY}"
  jinaApiUrl: "${JINA_API_URL}"
  cohereApiKey: "${COHERE_API_KEY}"
  rerankerType: "jina" # Options: "jina", "cohere", "none"

  # General Settings
  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)
  # Required for deliberately private self-hosted provider endpoints
  allowedAddresses:
    - "searxng:8080"
```

## SSRF Protection and Private Providers [#ssrf-protection-and-private-providers]

LibreChatは、ウェブ検索、スクレイピング、およびリランキングのために開く各接続を検証します。プライベート、ループバック、リンクローカル、およびクラウドメタデータの宛先は、リダイレクトホップや接続時にプライベートアドレスに解決されるホスト名を含め、デフォルトでブロックされます。

設定済みのSearXNG、Firecrawl、Jina、Tavily互換、Keenable互換、または関連するプロバイダーのendpointが意図的にプライベートである場合は、`webSearch.allowedAddresses`を使用してください：

```yaml filename="webSearch / allowedAddresses"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  allowedAddresses:
    - "searxng:8080"
    - "10.0.0.5:3002"
    - "[fd00::5]:8080"
```

このフィールドはSSRF除外リストであり、厳格な許可リストではありません。パブリックな宛先は引き続き利用可能です。エントリは正確な `host:port`、`private.ip:port`、または `[ipv6]:port` のペアである必要があります。URL、パス、CIDR範囲、ホスト名やIPアドレス単体、およびパブリックIPリテラルは拒否されます。除外されたホスト名は、そのポートで解決されるプライベートIPを信頼するため、プライベートIPリテラルを優先し、DNSを自身で管理している名前のみをリストしてください。

LibreChatは、設定されたHTTP(S)フォワードプロキシエンドポイントを自動的に除外するため、そのエンドポイントに到達可能です。プロキシがリクエストを運ぶ際、宛先を解決し、宛先へのエグレス（送信）をプロキシ自身で強制する必要があります。`NO_PROXY` を使用するとリクエストを直接送信できますが、その場合でもLibreChatの接続ガードが適用されます。このガードはLibreChatがプロバイダーAPIに対して開く接続を対象としていますが、サードパーティのスクレイパーがリクエストデータとして受け取り、独自のインフラストラクチャ上でフェッチするページURLを制限することはできません。

## 検索プロバイダー [#search-providers]

### searchProvider

<OptionTable
  options={[
    ['searchProvider', 'String', '使用する検索プロバイダーを指定します。', 'Options: "serper", "searxng", "tavily", "keenable"'],
  ]}
/>

### serperApiKey

<OptionTable
  options={[
    ['serperApiKey', 'String', 'Serper APIキーの環境変数名。 .env に設定されていない場合、ユーザーはUI経由で入力を求められます。', '${SERPER_API_KEY}'],
  ]}
/>

**注:** [Serper.dev](https://serper.dev/api-keys) から API キーを取得してください。


### searxngInstanceUrl

<OptionTable
  options={[
    ['searxngInstanceUrl', 'String', 'SearXNGインスタンスURLの環境変数名。 .env に設定されていない場合、ユーザーはUI経由で入力を求められます。', '${SEARXNG_INSTANCE_URL}'],
  ]}
/>

### searxngApiKey

<OptionTable
  options={[
    ['searxngApiKey', 'String', 'SearXNG APIキーの環境変数名。.envに設定されていない場合、ユーザーはUI経由で入力を求められます。', '${SEARXNG_API_KEY}'],
  ]}
/>

**注意:** これはオプションであり、お使いの SearXNG インスタンスで認証が必要な場合にのみ必要です。

### searxngSearchOptions

<OptionTable
  options={[
    ['searxngSearchOptions', 'Object', 'すべての検索時にSearXNGインスタンスへ送信されるクエリオプション。すべてのサブキーは任意であり、このブロックはsearchProviderが"searxng"の場合にのみ読み込まれます。', ''],
  ]}
/>

**サブキー:**

<OptionTable
  options={[
    ['engines', 'String or Array of Strings', 'インスタンスがクエリを送信すべきエンジンであり、SearXNGのenginesパラメータとして送信されます。カンマ区切りの文字列またはYAMLリストを受け付けます。いずれもカンマ区切りの形式に正規化され、前後の空白や空のエントリは削除されます。トリミング後に空となる値は、未設定として扱われます。', 'Default: "google,bing,duckduckgo"'],
    ['language', 'String', 'SearXNGのlanguageパラメータとして送信される結果の言語コードです。"en"、"de"、"fr"など、インスタンスが受け入れるコードを使用してください。', 'Default: "all"'],
    ['timeRange', 'String', '公開日で結果を制限します。SearXNGにはtime_rangeとして送信されます。設定されていない場合はリクエストから完全に除外されます。', 'Options: "day", "month", "year"'],
    ['timeout', 'Number', 'インスタンスへの呼び出しに対するHTTPリクエストのタイムアウト時間（ミリ秒）。120000以下の正の整数である必要があります。0はタイムアウトを無効にするため拒否されます。', 'Default: 10000'],
  ]}
/>

<Callout type="info" title="可用性">
`searxngSearchOptions` は `@librechat/agents` の検索ツールを通じて SearXNG にアクセスします。これには `@librechat/agents` v3.6.9 以降が必要です。それより前のバージョンでビルドされたリリースでは、このブロックは無視され、起動に失敗することはありません。そのため、デフォルトの結果が返され続けるインスタンスは、単に古いビルドで実行されている可能性があります。
</Callout>

**例:**

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}" # Optional
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeRange: "month"
    timeout: 10000
```

`engines` は SearXNG 自体が使用するカンマ区切りの形式も受け入れるため、これは上記のリストと同等になります：

```yaml filename="webSearch"
webSearch:
  searxngSearchOptions:
    engines: "google, bing, startpage, qwant"
```

#### エンジンの選択 [#choosing-engines]

エンジン名は、自身のインスタンスで有効になっているエンジンと一致している必要があり、それには以下の2つのことが伴います。

- SearXNGは、認識できないエンジンをエラーとして報告するのではなく無視します。タイプミスやインスタンスで無効化されているエンジンがある場合、失敗するのではなく、結果が少なく表示されます。
- 有効な名前はインスタンス固有のものです。有効なすべてのエンジンを含むJSONを返す `https://your-instance/config` で有効なリストを確認するか、インスタンスの設定ページの **Engines** タブを開いてください。

デフォルトは `google,bing,duckduckgo` です。SearXNGは選択されたエンジンが返す結果をすべて集約するため、1つのエンジンがブロックされても応答全体が失われるのではなく、そのエンジン分の結果が失われるだけです。検索結果が空になるのは、選択されたすべてのエンジンが失敗したか、何も返さなかった場合のみです。

DuckDuckGoは、デフォルトのセットの中で最もブロックされる可能性が高いエンジンです。これは、ほとんどのセルフホストインスタンスに対してCAPTCHAを表示するためです。セット内にエンジンが3つしかない場合、レート制限のかかったGoogleやBingと同時に使用できなくなると、結果が全く得られなくなることがよくあります。`engines`を明示的に設定し、DuckDuckGoを除外し、3つ以上のエンジンをリストアップすることで、そのリスクを軽減できます。

応答が速く、セルフホストのトラフィックを許容するエンジンが最適な初期セットとなります。例：

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - brave
      - startpage
      - qwant
```

エンジンを追加すると検索範囲は広がりますが、SearXNGはセット内の最も遅いエンジンの応答を待ってから返答するため、すべての検索が遅くなります。リストを増やす際は `timeout` に注意してください。

#### これらのオプションが制御しないもの [#what-these-options-do-not-control]

- **Safe search** はこのブロックの一部ではありません。これはトップレベルの [`safeSearch`](#safesearch) キーから取得され、`safesearch` として SearXNG に転送されます。
- **Result categories**はLibreChatによってクエリごとに選択され、検索タイプを`general`、`images`、`videos`、または`news`にマッピングします。これらはここで上書きすることはできません。
- **ページ番号と結果の形式**は固定されています。LibreChatは常にJSON形式で1ページ目を要求するため、インスタンスの`formats`設定で`json`を有効にする必要があります。

### tavilyApiKey

<OptionTable
  options={[
    ['tavilyApiKey', 'String', 'Tavily APIキーの環境変数名。検索とスクレイパーの両方で使用されます。.envに設定されていない場合、ユーザーはUI経由で入力を求められます。', '${TAVILY_API_KEY}'],
  ]}
/>

**注:** [Tavily](https://app.tavily.com/home) から API キーを取得してください。

### tavilySearchUrl

<OptionTable
  options={[
    ['tavilySearchUrl', 'String', 'カスタムTavily Search API URLの環境変数名。オプション。未設定の場合はTavilyホストの検索がデフォルトで使用されます。', '${TAVILY_SEARCH_URL}'],
  ]}
/>

### tavilyExtractUrl

<OptionTable
  options={[
    ['tavilyExtractUrl', 'String', 'カスタムTavily Extract API URL用の環境変数名。オプション。未設定の場合はTavilyホストのextractがデフォルトで使用されます。', '${TAVILY_EXTRACT_URL}'],
  ]}
/>

### tavilySearchOptions

<OptionTable
  options={[
    ['tavilySearchOptions', 'Object', 'Tavily検索の設定オプション', ''],
  ]}
/>

**サブキー:**

<OptionTable
  options={[
    ['searchDepth', 'String', '関連性とレイテンシのトレードオフを制御します。「basic」はURLごとに1つのNLP要約を返します。「advanced」はURLごとに複数の意味的に関連するスニペットを返します（2 APIクレジット）。「fast」は速度と関連性のバランスをとったスニペットを返します。「ultra-fast」は1つのNLP要約によりレイテンシを最小化します。', 'Options: "basic", "advanced", "fast", "ultra-fast". Default: "basic"'],
    ['maxResults', 'Number', '返される検索結果の最大数。', 'Range: 1-20. Default: 5'],
    ['topic', 'String', '検索のカテゴリ。"news" はリアルタイムの更新に、"finance" は財務データに役立ちます。', 'Options: "general", "news", "finance". Default: "general"'],
    ['includeImages', 'Boolean', 'レスポンスに画像を含めます。トップレベルのクエリ画像と結果ごとの画像の両方を返します。', 'Default: false'],
    ['includeAnswer', 'Boolean or String', 'LLMが生成した回答を含めます。"basic" または true で簡潔な回答、"advanced" で詳細な回答になります。', 'Default: false'],
    ['includeRawContent', 'Boolean or String', 'クリーニングおよび解析済みのHTMLコンテンツを含めます。"markdown" または true はMarkdown形式、"text" はプレーンテキスト用です。', 'Default: false'],
    ['includeDomains', 'Array of Strings', '特定のドメインに検索を制限します。最大300ドメインまで。', ''],
    ['excludeDomains', 'Array of Strings', '結果から特定のドメインを除外します。最大150ドメインまで。', ''],
    ['timeRange', 'String', '公開日または最終更新日に基づく期間フィルター。', 'Options: "day", "week", "month", "year"'],
    ['includeImageDescriptions', 'Boolean', 'includeImages が true の場合、各画像の説明テキストも追加してください。', 'Default: false'],
    ['includeFavicon', 'Boolean', '各検索結果のfavicon URLを含めます。', 'Default: false'],
    ['chunksPerSource', 'Number', 'ソースあたりの関連コンテンツチャンクの最大数。searchDepthが"advanced"の場合のみ利用可能です。', 'Range: 1-3. Default: 3'],
    ['safeSearch', 'Boolean', 'Tavily Searchリクエスト用のオプションのTavily safe_searchオーバーライドです。デフォルトでは省略されます。trueに設定する場合、Tavily Enterpriseが必要になることがあります。', 'Default: omitted'],
    ['timeout', 'Number', 'クライアント側のHTTPリクエストタイムアウト（ミリ秒）。Tavily APIの応答を待機し、中断するまでの時間を制御します。', 'Default: 15000'],
  ]}
/>

### keenableApiKey

<OptionTable
  options={[
    ['keenableApiKey', 'String', 'オプションのKeenable APIキー用の環境変数名。Keenableはキーなしでも動作しますが、設定することでパブリックレート制限が緩和されます。', '${KEENABLE_API_KEY}'],
  ]}
/>

### keenableApiUrl

<OptionTable
  options={[
    ['keenableApiUrl', 'String', 'カスタムKeenable検索API URLの環境変数名。ページ取得のendpointは変更されません。', '${KEENABLE_API_URL}'],
  ]}
/>

page-fetch endpointは、環境変数のみの `KEENABLE_FETCH_URL` 変数を使用して個別に設定されます。

### keenableSearchOptions

<OptionTable
  options={[
    ['keenableSearchOptions', 'Object', 'Keenable検索の設定オプション。', ''],
  ]}
/>

<OptionTable
  options={[
    ['maxResults', 'Number', '検索結果の最大数。', 'Range: 1-20. Default: 8'],
    ['site', 'String', '結果を1つのドメインに制限します。', ''],
    ['attributionTitle', 'String', 'X-Keenable-Title アトリビューションヘッダーで送信される値。', ''],
    ['timeout', 'Number', 'クライアント側のHTTPリクエストタイムアウト（ミリ秒単位）。', 'Range: 0-120000. Default: 15000'],
  ]}
/>

## Scrapers [#scrapers]

### firecrawlApiKey

<OptionTable
  options={[
    ['firecrawlApiKey', 'String', 'Firecrawl APIキーの環境変数名。 .env に設定されていない場合、ユーザーはUI経由で入力を求められます。', '${FIRECRAWL_API_KEY}'],
  ]}
/>

**注:** [Firecrawl.dev](https://docs.firecrawl.dev/introduction#api-key) から API キーを取得してください。

### firecrawlApiUrl

<OptionTable
  options={[
    ['firecrawlApiUrl', 'String', 'Firecrawl API URLの環境変数名。 .envに設定されていない場合、ユーザーはUI経由で入力を求められます。', '${FIRECRAWL_API_URL}'],
  ]}
/>

**注:** これはオプションであり、カスタムの Firecrawl インスタンスを使用している場合にのみ必要です。

### firecrawlVersion

<OptionTable
  options={[
    ['firecrawlVersion', 'String', 'Firecrawl APIバージョン（v0またはv1）の環境変数名。', '${FIRECRAWL_VERSION}'],
  ]}
/>

### scraperProvider

<OptionTable
  options={[
    ['scraperProvider', 'String', '使用するスクレイパーサービスを指定します。', 'Options: "firecrawl", "serper", "tavily", "keenable"'],
  ]}
/>

### firecrawlOptions

<OptionTable
  options={[
    ['firecrawlOptions', 'Object', 'Firecrawlスクレイパーの高度な設定オプション', ''],
  ]}
/>

**サブキー:**

#### formats

<OptionTable
  options={[
    ['formats', 'Array of Strings', '出力に含めるフォーマット。', ''],
  ]}
/>

#### includeTags

<OptionTable
  options={[
    ['includeTags', 'Array of Strings', '出力に含めるタグ。', ''],
  ]}
/>

#### excludeTags

<OptionTable
  options={[
    ['excludeTags', 'Array of Strings', '出力から除外するタグ。', ''],
  ]}
/>

#### headers

<OptionTable
  options={[
    ['headers', 'Object', 'リクエストと一緒に送信するヘッダー。Cookieやuser-agentなどを送信するために使用できます。', ''],
  ]}
/>

#### waitFor

<OptionTable
  options={[
    ['waitFor', 'Number', 'コンテンツを取得する前にミリ秒単位の遅延を指定し、ページが読み込まれるまでの十分な時間を確保します。', ''],
  ]}
/>

#### timeout

<OptionTable
  options={[
    ['timeout', 'Integer', 'スクレイピングリクエストのタイムアウト（ミリ秒単位）。負ではない整数である必要があります。', 'Default: 7500'],
  ]}
/>

#### maxAge

<OptionTable
  options={[
    ['maxAge', 'Number', 'ページがこのミリ秒単位の期間より新しい場合、キャッシュされたバージョンを返します。キャッシュされたページのバージョンがこの値より古い場合、ページはスクレイピングされます。', ''],
  ]}
/>

**注:** 非常に新しいデータが必要でない場合、これを有効にすることでスクレイピングを最大500%高速化できます。

#### mobile

<OptionTable
  options={[
    ['mobile', 'Boolean', 'モバイルデバイスからのスクレイピングをエミュレートします。', ''],
  ]}
/>

#### skipTlsVerification

<OptionTable
  options={[
    ['skipTlsVerification', 'Boolean', 'リクエスト送信時にTLS証明書の検証をスキップします。', ''],
  ]}
/>

#### blockAds

<OptionTable
  options={[
    ['blockAds', 'Boolean', '広告ブロックおよびクッキーポップアップブロックを有効にします。', ''],
  ]}
/>

#### removeBase64Images

<OptionTable
  options={[
    ['removeBase64Images', 'Boolean', '出力からすべてのbase 64画像を削除します。これは非常に長くなる可能性があるためです。画像のaltテキストは出力に残りますが、URLはプレースホルダーに置き換えられます。', ''],
  ]}
/>

#### parsePDF

<OptionTable
  options={[
    ['parsePDF', 'Boolean', 'スクレイピング中にPDFファイルがどのように処理されるかを制御します。', ''],
  ]}
/>

#### storeInCache

<OptionTable
  options={[
    ['storeInCache', 'Boolean', 'trueに設定すると、ページはFirecrawlのインデックスとキャッシュに保存されます。スクレイピング活動にデータ保護上の懸念がある場合は、これをfalseに設定すると便利です。機密性の高いスクレイピングに関連する一部のパラメータ（headersなど）を使用すると、このパラメータは強制的にfalseになります。', ''],
  ]}
/>

#### zeroDataRetention

<OptionTable
  options={[
    ['zeroDataRetention', 'Boolean', 'trueに設定すると、このスクレイピングにおけるデータ保持がゼロになります（事前にFirecrawlでの設定が必要です）。', ''],
  ]}
/>

#### location

<OptionTable
  options={[
    ['location', 'Object', 'スクレイピングのための地理的な場所と言語設定。', ''],
  ]}
/>

#### onlyMainContent

<OptionTable
  options={[
    ['onlyMainContent', 'Boolean', 'ページヘッダー、ナビゲーション、フッターなどを除いた、ページ内のメインコンテンツのみを返します。', ''],
  ]}
/>

#### changeTrackingOptions

<OptionTable
  options={[
    ['changeTrackingOptions', 'Object', 'スクレイピングされたコンテンツの変更を追跡するための設定。', ''],
  ]}
/>

**例:**
```yaml filename="webSearch"
webSearch:
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlOptions:
    formats: ["markdown", "rawHtml"]
    includeTags: ["main", "article", ".content"]
    excludeTags: ["nav", "footer", ".ads"]
    waitFor: 2000
    timeout: 10000
    mobile: false
    blockAds: true
    onlyMainContent: true
    location:
      country: "US"
      languages: ["en"]
```

**注:** Firecrawl スクレイパーのオプションとデフォルト値に関する詳細については、[Firecrawl API Documentation](https://docs.firecrawl.dev/api-reference/endpoint/scrape) を参照してください。

### tavilyScraperOptions

<OptionTable
  options={[
    ['tavilyScraperOptions', 'Object', 'Tavily Extract（スクレイパー）の設定オプション', ''],
  ]}
/>

**サブキー:**

<OptionTable
  options={[
    ['extractDepth', 'String', '抽出プロセスの深度。「advanced」はテーブルや埋め込みコンテンツを含むより多くのデータを高い成功率で取得しますが、レイテンシが増加する可能性があります。「basic」は正常に取得したURL 5件につき1クレジット、「advanced」は正常に取得したURL 5件につき2クレジットを消費します。', 'Options: "basic", "advanced". Default: "basic"'],
    ['includeImages', 'Boolean', 'レスポンス内のURLから抽出された画像のリストを含めます。', 'Default: false'],
    ['includeFavicon', 'Boolean', '抽出された各結果のfavicon URLを含めます。', 'Default: false'],
    ['format', 'String', '抽出されたWebページコンテンツのフォーマット。"markdown"はコンテンツをMarkdown形式で返します。"text"はプレーンテキストで返し、レイテンシが増加する可能性があります。', 'Options: "markdown", "text". Default: "markdown"'],
    ['timeout', 'Number', 'ミリ秒単位のタイムアウト。クライアント側のHTTPタイムアウトを制御します。設定された場合、Tavilyに対して秒単位に変換され、1〜60秒の範囲に制限されたサーバー側の抽出タイムアウトも送信されます。', 'Default: 15000 for basic, 30000 for advanced'],
  ]}
/>

**例:**
```yaml filename="webSearch"
webSearch:
  searchProvider: tavily
  scraperProvider: tavily
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  # tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  # tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"
  tavilySearchOptions:
    searchDepth: basic
    maxResults: 5
    topic: general
  tavilyScraperOptions:
    extractDepth: basic
```

**注:** Tavily API オプションの詳細については、[Tavily API Documentation](https://docs.tavily.com) を参照してください。

### keenableScraperOptions

<OptionTable
  options={[
    ['keenableScraperOptions', 'Object', 'Keenableページフェッチの設定オプション', ''],
  ]}
/>

<OptionTable
  options={[
    ['attributionTitle', 'String', 'X-Keenable-Title アトリビューションヘッダーで送信される値。', ''],
    ['timeout', 'Number', 'クライアント側のHTTPリクエストタイムアウト（ミリ秒単位）。', 'Range: 0-120000. Default: 15000'],
  ]}
/>

Keenableは、検索、ページ取得、またはその両方を提供できます。そのパブリックエンドポイントはキーなしで動作しますが、オプションの `keenableApiKey` を設定することでレート制限を引き上げることができます。リランカーキーを不要にするには、`rerankerType: "none"` を設定してください：

```yaml filename="webSearch"
webSearch:
  searchProvider: keenable
  scraperProvider: keenable
  rerankerType: none
  # keenableApiKey: "${KEENABLE_API_KEY}" # Optional
  # keenableApiUrl: "${KEENABLE_API_URL}" # Optional search endpoint
  keenableSearchOptions:
    maxResults: 8
    # site: example.com
  keenableScraperOptions:
    timeout: 15000
```

## Rerankers [#rerankers]

### jinaApiKey

<OptionTable
  options={[
    ['jinaApiKey', 'String', 'Jina APIキーの環境変数名。 .env に設定されていない場合、ユーザーはUI経由で入力を求められます。', '${JINA_API_KEY}'],
  ]}
/>

**注意:** [Jina.ai](https://jina.ai/api-dashboard/) から API キーを取得してください。

### jinaApiUrl

<OptionTable
  options={[
    ['jinaApiUrl', 'String', 'Jina API URLの環境変数名。 .envに設定されていない場合、ユーザーはUI経由で入力を求められます。', '${JINA_API_URL}'],
  ]}
/>

**注:** これはオプションであり、カスタムの Jina インスタンスを使用している場合にのみ必要です。

### cohereApiKey

<OptionTable
  options={[
    ['cohereApiKey', 'String', 'Cohere APIキーの環境変数名。 .env に設定されていない場合、ユーザーはUI経由で入力を求められます。', '${COHERE_API_KEY}'],
  ]}
/>

**注:** APIキーは [Cohere Dashboard](https://dashboard.cohere.com/welcome/login) から取得してください。

### rerankerType

<OptionTable
  options={[
    ['rerankerType', 'String', '使用するリランカーサービスを指定します。リランキングをスキップするには "none" に設定してください。', 'Options: "jina", "cohere", "none"'],
  ]}
/>

## 一般設定 [#general-settings]

### scraperTimeout

<OptionTable
  options={[
    ['scraperTimeout', 'Integer', 'スクレイパーリクエストのタイムアウト（ミリ秒単位）。負ではない整数である必要があります。', 'Default: 7500'],
  ]}
/>

### safeSearch

<OptionTable
  options={[
    ['safeSearch', 'Number', 'セーフサーチのフィルタリングレベル。0 = OFF（フィルタリングなし）、1 = MODERATE（デフォルト）、2 = STRICT（最大レベルのフィルタリング）。', 'Default: 1 (MODERATE)'],
  ]}
/>

**注:** セーフサーチのレベルは、標準的な検索APIの規約に準拠しています。MODERATEフィルタリングがデフォルトで有効になっており、検索の有効性を維持しつつ適切なコンテンツフィルタリングを提供します。Tavilyはこのグローバル設定をデフォルトでは継承しません。Tavilyアカウントが `safe_search` をサポートしている場合にのみ、`tavilySearchOptions.safeSearch` を使用してください。

## 注記 [#notes]

- API keysは、以下の2つの方法で設定できます：
  1. YAML設定で指定された環境変数を設定します
  2. 環境変数が設定されていない場合、ユーザーはUI経由でAPIキーを入力するよう求められます。
- この設定は、各コンポーネント（プロバイダー、スクレイパー、リランカー）に対して複数のサービスをサポートしています。
- 特定のサービスタイプが指定されていない場合、システムはそのカテゴリ内の利用可能なすべてのサービスを試行します。
- セーフサーチには、OFF (0)、MODERATE (1)、STRICT (2) という3段階のコンテンツフィルタリングレベルが用意されています。
- Tavilyはデフォルトではグローバルなセーフサーチ設定を継承しません。Tavilyアカウントが `safe_search` をサポートしている場合にのみ、`tavilySearchOptions.safeSearch` を明示的に設定してください。
- Keenableはキーレス検索とページ取得をサポートしています。オプションのキーを設定することでパブリックなレート制限を引き上げることができ、フェッチURLのオーバーライドは環境変数でのみ可能です。
- SearXNGのクエリ動作（エンジン、言語、期間、リクエストタイムアウト）は `searxngSearchOptions` で設定されます。検索結果が空で返ってくる場合は、[Choosing engines](#choosing-engines) を参照してください。
- 実際のAPIキーをYAML設定に記述しないでください。環境変数名のみを使用してください。

## SearXNG のセットアップ [#setting-up-searxng]

SearXNGは、セルフホスト可能なプライバシー重視のメタ検索エンジンです。
詳細については、[公式SearXNGドキュメント](https://docs.searxng.org/)を参照してください。

LibreChatで使用するために独自のSearXNGインスタンスをセットアップする手順は以下の通りです：

### Docker Desktop を使用する [#using-docker-desktop]

1. **公式のSearXNGイメージを検索する**
   - Docker Desktop を開く
   - **Images** タブで `searxng/searxng` を検索します
   - 公式イメージの **Run** をクリックすると、そのイメージのコンテナが自動的にプルされ、実行されます。

2. **コンテナの実行**
   - ダウンロードが完了した後に表示されるパネルの **Optional Settings** ドロップダウンを展開します。
   - 希望する設定の詳細（ポート番号、コンテナ名など）を設定します。
   - **Run**をクリックしてコンテナを開始します

3. **LibreChat 用に SearXNG を設定する**
   - Docker Desktopの `Files` タブに移動します
   - `/etc/searxng/settings.yaml` に移動します
   - ファイルエディターを開く
   - `formats` セクションに移動します
   - `json` を許容されるフォーマットとして追加し、LibreChat があなたのインスタンスと通信できるようにします。
   - ファイルを保存

4. **コンテナを再起動します**
   - 変更を反映させるには、コンテナを再起動してください。

#### ビデオガイド: [#video-guide]

約1分間で最初から最後までの一連の流れを解説する動画はこちらです：

     <Video src="/videos/min_searxng_docker_setup_hq.mp4" title="SearXNG Docker セットアップのチュートリアル" />

**注:** この例では、インスタンスのURLは http://localhost:55011 です（ポート番号は動画の左上、コンテナ名の下に表示されています）。

### LibreChatでSearXNGを使用するように設定する [#configuring-librechat-to-use-searxng]

LibreChatでは、UI内または `librechat.yaml` を通じてSearXNGを設定できます。

#### UI設定 [#ui-configuration]

1. **チャット入力バーのツール・ドロップダウンを開く**
![Tools configuration button](/images/web-search/tools_highlight.png)

2. **Web Searchの横にある歯車アイコンをクリックします**
![Tools configuration section](/images/web-search/tools_config_highlight.png)

3. **検索プロバイダーのドロップダウンからSearXNGを選択します**
![SearXNG dropdown selection](/images/web-search/searxng_dropdown_highlight.png)

4. **設定の詳細（インスタンスURL、スクレーパーの種類など）を入力し、保存をクリックします**
![Web検索設定を保存](/images/web-search/web_search_save.png)

5. **ツール（tools）ドロップダウン内の Web Search オプションをクリックします**
![チャットインターフェース内の Web search バッジ](/images/web-search/websearch_highlight.png)

6. **Web Searchバッジが有効になり、クエリでWeb検索機能を利用できるようになりました**
![Web search badge confirmation](/images/web-search/search_badge_confirm.png)

#### YAML 設定 [#yaml-configuration]

インスタンス上のすべてのユーザーに対してSearXNGを設定するには（ユーザーごとではなく）、`librechat.yaml` に同じ値を設定します：

```yaml filename="librechat.yaml"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  # searxngApiKey: "${SEARXNG_API_KEY}" # Only if your instance requires authentication
  # Required: a self-hosted instance is a private destination, so exempt it from the connection guard
  allowedAddresses:
    - "localhost:55011"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeout: 10000
```

URLとキーは引き続き環境変数から読み込まれるため、上記の構成と以下を組み合わせてください：

```bash filename=".env"
SEARXNG_INSTANCE_URL=http://localhost:55011
# SEARXNG_API_KEY=your_api_key
```

<Callout type="warning" title="プライベートインスタンスには allowedAddresses エントリが必要です">
`localhost:55011` はループバックアドレスであり、LibreChatの [SSRF protection](#ssrf-protection-and-private-providers) によって接続時にブロックされます。一致する `allowedAddresses` エントリがない場合、SearXNGが正常であっても検索は失敗します。このエントリは、インスタンスURLの正確な `host:port` ペアである必要があります。

LibreChat 自体が Docker 内で実行されている場合、`localhost` は LibreChat コンテナを指し、SearXNG コンテナを指すわけではありません。コンテナが到達可能なホスト名（compose サービス名など）を使用し、代わりにそれを除外してください：

```yaml filename="librechat.yaml"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}" # SEARXNG_INSTANCE_URL=http://searxng:8080
  allowedAddresses:
    - "searxng:8080"
```
</Callout>

このブロックが受け入れるすべてのサブキーについては、[`searxngSearchOptions`](#searxngsearchoptions) を参照してください。

#### 空の結果のトラブルシューティング [#troubleshooting-empty-results]

SearXNGの検索結果が何も返されない場合、それはLibreChatが結果を破棄しているのではなく、リクエストが正常に動作しているインスタンスに到達していないことがほとんどです。以下の手順を順番に確認してください：

1. **インスタンスがプライベートアドレスであり、`allowedAddresses` エントリが存在しません。** LibreChatは接続時にループバックおよびプライベート宛先をブロックするため、セルフホストインスタンスでは、その正確な `host:port` を [`allowedAddresses`](#ssrf-protection-and-private-providers) にリストする必要があります。これにより、空の結果セットを返すのではなく、リクエストが完全にブロックされます。
2. **`json` が `formats` に含まれていません。** LibreChat は結果を JSON として要求します。インスタンス設定ファイルの `formats` セクションに `json` が記載されていない場合、インスタンスはエラーページを返し、すべての検索結果が空になります。これは上記の Docker セットアップのステップ 3 です。
3. **選択されたすべてのエンジンが失敗しました。** SearXNGは応答があったエンジンのみを統合するため、空の応答はどのエンジンからも何も返ってこなかったことを意味します。DuckDuckGoはデフォルトのセット（`google,bing,duckduckgo`）に含まれていますが、ほとんどのセルフホストインスタンスに対してCAPTCHAを要求するため、検索を実行できるエンジンが2つだけになってしまいます。`searxngSearchOptions.engines`を明示的に設定し、DuckDuckGoを除外した上で、他のエンジンをいくつかリストに追加してください。
4. **インスタンス上にエンジン名が存在しません。** SearXNGは認識できないエンジンを警告なしにスキップするため、タイプミスがあると単に結果が少なくなるだけです。リストを `https://your-instance/config` と比較してください。
5. **インスタンスがタイムアウトよりも低速です。** インスタンスが多くのエンジンにクエリを送信している場合や、低速なネットワーク経由で接続されている場合は、`searxngSearchOptions.timeout` をデフォルトの `10000` より高い値に設定してください。

LibreChatが送信する内容を再現するには、インスタンスを直接呼び出します：

```bash
curl -s "http://localhost:55011/search?q=librechat&format=json&engines=google,bing,startpage" | head -c 500
```

これは自身のマシンから実行されるため、LibreChatの接続ガードを完全にバイパスします。ここで `results` 配列が空であれば、問題がSearXNG側にあることが確認できます。ここで結果が表示されるにもかかわらずLibreChatで何も見つからない場合は、`allowedAddresses` に原因があることを示しています。
