---
title: Struktura obiektu filtra wiadomości
icon: ShieldAlert
---

## Przegląd [#overview]

Obiekt `messageFilter` konfiguruje filtry wiadomości po stronie serwera. Obsługiwanym typem filtra jest `messageFilter.pii`.

`messageFilter.pii` odrzuca przesłany tekst wiadomości, który pasuje do wzorców przypominających dane uwierzytelniające lub wyrażeń regularnych zdefiniowanych przez operatora, zanim tekst trafi do moderacji, żądania do modelu lub utrwalenia wiadomości.

## Przykład [#example]

```yaml filename="librechat.yaml"
messageFilter:
  pii:
    starterPatterns:
      - sk_prefix
      - bearer_header
      - api_key_header
    customPatterns:
      - id: anthropic_api_key
        label: Anthropic API key
        regex: 'sk-ant-[A-Za-z0-9_-]{20,}'
```

## Pola [#fields]

### messageFilter.pii

<OptionTable
  options={[
    [
      'pii.starterPatterns',
      'Array of Strings',
      'Opcjonalny podzbiór wbudowanych identyfikatorów wzorców startowych do włączenia. Pomiń, aby włączyć wszystkie wzorce startowe.',
      'starterPatterns: [sk_prefix, bearer_header]',
    ],
    [
      'pii.customPatterns',
      'Array of Objects',
      'Wzorce zdefiniowane przez operatora. Każdy wpis wymaga `id`, `label` oraz `regex`.',
      '',
    ],
  ]}
/>

**Wbudowane identyfikatory wzorców startowych:**

- `sk_prefix`: dopasowuje prefiksy tokenów w stylu `sk-`.
- `bearer_header`: dopasowuje tokeny bearer w tekście.
- `api_key_header`: dopasowuje tekst w formacie nagłówka `api-key`.

### customPatterns

<OptionTable
  options={[
    ['id', 'String', 'Stabilny identyfikator wzorca używany do logów i debugowania.', 'id: anthropic_api_key'],
    [
      'label',
      'String',
      'Czytelna dla człowieka etykieta wyświetlana w komunikacie o odrzuceniu.',
      'label: Anthropic API key',
    ],
    [
      'regex',
      'String',
      'Wyrażenie regularne RE2. LibreChat weryfikuje je podczas ładowania konfiguracji i stosuje globalnie.',
      "regex: 'sk-ant-[A-Za-z0-9_-]{20,}'",
    ],
  ]}
/>

Niestandardowe wzorce wykorzystują silnik RE2 o czasie liniowym. Referencje wsteczne (backreferences) oraz mechanizmy lookaround nie są obsługiwane, a niektóre sekwencje ucieczki różnią się od wyrażeń regularnych JavaScript. LibreChat odrzuca wzorce, których RE2 nie jest w stanie skompilować podczas wczytywania konfiguracji.

## Zachowanie [#behavior]

Gdy przesłany tekst wiadomości pasuje do skonfigurowanego wzorca PII, LibreChat zwraca kod HTTP `400` wraz z `message_filter_pii_block` oraz komunikatem proszącym użytkownika o usunięcie dopasowanego typu danych uwierzytelniających.

Filtr sprawdza:

- Tekst użytkownika trasy czatu przesłany przez interfejs użytkownika LibreChat.
- Treść tekstu we wszystkich rolach wiadomości dostarczonych przez wywołującego w żądaniach Chat Completions zgodnych z OpenAI.
- Treść tekstowa we wszystkich rolach wiadomości dostarczonych przez wywołującego w żądaniach odpowiedzi zgodnych z OpenAI.

Filtr ocenia tekst dostarczony w treści żądania. Nie skanuje on zawartości plików.
