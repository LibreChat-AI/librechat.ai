---
title: Estrutura do Objeto do endpoint Agents
icon: Bot
---

Esta página se aplica ao endpoint [`agents`](/docs/features/agents).

## Exemplo [#example]

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    recursionLimit: 50
    maxRecursionLimit: 100
    maxSubagents: 20
    maxToolCallArgBytes: 65536
    maxDeltaEventsPerTurn: 100000
    maxToolCallArgBytesByTool:
      create_file: 131072
    disableBuilder: false
    # (optional) Agent Capabilities available to all users. Omit the ones you wish to exclude. Defaults to list below.
    # Opt-in capabilities: "programmatic_tools", "stateful_code_sessions", "run_in_background", and "tool_intents".
    # capabilities: ["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]
    # (optional) File citation configuration for file_search capability
    maxCitations: 30 # Maximum total citations in responses (1-50)
    maxCitationsPerFile: 7 # Maximum citations from each file (1-10)
    minRelevanceScore: 0.45 # Minimum relevance score threshold (0.0-1.0)
    titleTiming: immediate
    activityLabel: true
    activityModel: gpt-4.1-nano
    activityPhaseLabel: true
    activityPhaseMaxPerRun: 5
    skills:
      maxCatalogSkills: 20
    statefulCodeSessions:
      allowedEnvironments: ['user', 'agent-user', 'conversation']
      environments:
        - id: managed-default
          name: Managed Code API
          type: managed
          baseURL: https://code.example.com/v1
          default: true
        - id: engineering-vm
          name: Engineering VM
          type: attached
          baseURL: https://code-bridge.example.com/v1
          pairing:
            workerId: engineering-vm
            tokenEnv: CODE_BRIDGE_ADMIN_TOKEN
        - id: personal-workers
          name: Personal Code Workers
          type: attached
          baseURL: https://code-bridge.example.com/v1
          pairing:
            allowPrincipalWorkers: true
            tokenEnv: CODE_BRIDGE_ADMIN_TOKEN
    # eventDriven:
    #   selfUrl: https://librechat.internal
    backgroundTasks:
      completionWakeups: true
    toolApproval:
      enabled: true
      mode: default
      deny: ['mcp:*:delete_*']
      ask: ['mcp:*:*']
    checkpointer:
      type: mongo
      ttl: 86400
    remoteApi:
      auth:
        oidc:
          enabled: false
```

> Esta configuração habilita a interface do construtor para agentes.

## recursionLimit

<OptionTable
  options={[
    [
      'recursionLimit',
      'Number',
      'Define o número padrão de passos que um agente pode realizar em uma execução.',
      'Controls recursion depth to prevent infinite loops. When the limit is reached, LibreChat preserves the partial turn and offers Keep going or Answer now. This value can be configured from the UI up to maxRecursionLimit.',
    ],
  ]}
/>

**Padrão:** `25`

**Exemplo:**

```yaml filename="endpoints / agents / recursionLimit"
recursionLimit: 50
```

Para mais informações sobre as etapas do agente, consulte [Max Agent Steps](/docs/features/agents#max-agent-steps).

## maxRecursionLimit

<OptionTable
  options={[
    [
      'maxRecursionLimit',
      'Number',
      'Define o número máximo absoluto de passos que um agente pode executar em uma execução.',
      'Defines the upper limit for the recursionLimit that can be set from the UI. This prevents users from setting excessively high values.',
    ],
  ]}
/>

**Padrão:** Se omitido, assume o valor de recursionLimit ou 50 se recursionLimit também for omitido.

**Exemplo:**

```yaml filename="endpoints / agents / maxRecursionLimit"
maxRecursionLimit: 100
```

Para mais informações sobre as etapas do agente, consulte [Max Agent Steps](/docs/features/agents#max-agent-steps).

## Disjuntores de fluxo (Stream circuit breakers) [#stream-circuit-breakers]

Essas configurações interrompem a execução de um Agent quando o stream de um provedor parece estar produzindo argumentos de ferramenta descontrolados ou uma sequência ilimitada de eventos delta.

<OptionTable
  options={[
    [
      'maxToolCallArgBytes',
      'Number',
      'Máximo de bytes cumulativos para os argumentos de uma chamada de ferramenta via stream. Defina como 0 para desativar a proteção global.',
      '65536',
    ],
    [
      'maxDeltaEventsPerTurn',
      'Number',
      'Máximo de eventos transmitidos para um turno de geração de modelo. Defina como 0 ou omita para desativar esta proteção.',
      '0 (disabled)',
    ],
    [
      'maxToolCallArgBytesByTool',
      'Object',
      'Substituições de bytes de argumento por ferramenta, indexadas pelo nome da ferramenta voltado para o modelo. Um valor de 0 desativa a proteção para essa ferramenta.',
      'create_file: 131072',
    ],
  ]}
/>

O LibreChat usa um limite global de argumentos de 64 KiB e fornece uma substituição de 128 KiB para `create_file`, cujas cargas úteis legítimas são frequentemente maiores. As entradas em `maxToolCallArgBytesByTool` são mescladas sobre essa substituição integrada. Se qualquer uma das proteções for acionada, o LibreChat interrompe o fluxo do provedor em andamento e informa qual limite foi excedido.

```yaml filename="endpoints / agents / stream circuit breakers"
maxToolCallArgBytes: 65536
maxDeltaEventsPerTurn: 100000
maxToolCallArgBytesByTool:
  create_file: 262144
  large_import: 524288
```

Para desativar todos os limites de tamanho de argumento, defina tanto o limite global quanto a substituição `create_file` fornecida como `0`:

```yaml
maxToolCallArgBytes: 0
maxToolCallArgBytesByTool:
  create_file: 0
```

## titleTiming

<OptionTable
  options={[
    [
      'titleTiming',
      'String',
      'Controla quando os títulos das conversas são gerados para o endpoint agents. Valores válidos: "immediate" ou "final".',
      'Defaults to "immediate".',
    ],
  ]}
/>

**Padrão:** `"immediate"`

**Valores Disponíveis:**

- **`"immediate"`**: Gera o título assim que a solicitação é iniciada, em paralelo com a resposta do modelo, usando a primeira mensagem do usuário.
- **`"final"`**: Adia a geração do título até que a resposta completa seja concluída. Isso preserva o comportamento legado.

**Exemplo:**

```yaml filename="endpoints / agents / titleTiming"
titleTiming: immediate
```

## activityLabel

Defina `activityLabel: true` para agrupar cada bloco de raciocínio do Agente e chamadas de ferramenta sob um cabeçalho de uma linha gerado. Use `activityModel`, `activityEndpoint`, `activityPrompt`, `activityMaxPerRun` e `activityCharLimit` para controlar a chamada de modelo adicional e seu custo. Veja [Agent Activity Groups](/docs/configuration/librechat_yaml/object_structure/shared_endpoint_settings#agent-activity-groups) para a referência completa de campos e regras de precedência.

```yaml filename="endpoints / agents / activity groups"
activityLabel: true
activityEndpoint: openAI
activityModel: gpt-4.1-nano
activityMaxPerRun: 20
activityCharLimit: 600
activityPhaseLabel: true
activityPhaseMaxPerRun: 5
```

`activityPhaseLabel` pode adicionar independentemente um resumo pai recolhido em torno de cada fase de execução que contenha pelo menos duas atividades lógicas. Use `activityPhaseModel`, `activityPhaseEndpoint`, `activityPhasePrompt` e `activityPhaseMaxPerRun` para ajustar essas chamadas; `activityCharLimit` é compartilhado com os rótulos das atividades filhas.

## disableBuilder

<OptionTable
  options={[
    [
      'disableBuilder',
      'Boolean',
      'Controla a visibilidade e o uso da interface do construtor para agentes.',
      'When set to `true`, disables the builder interface for the agent, limiting direct manual interaction.',
    ],
  ]}
/>

**Padrão:** `false`

**Exemplo:**

```yaml filename="endpoints / agents / disableBuilder"
disableBuilder: false
```

## allowedProviders

<OptionTable
  options={[
    [
      'allowedProviders',
      'Array/List of Strings',
      'Especifica uma lista de provedores de endpoint (por exemplo, "openAI", "anthropic", "google") que têm permissão para uso com o recurso de Agents.',
      'If defined, only agents configured with these providers can be initialized. If omitted or empty, all configured providers are allowed.',
    ],
  ]}
/>

**Padrão:** `[]` (lista vazia, todos os provedores permitidos)

**Nota:** Deve ser um dos seguintes, ou um nome de endpoint personalizado conforme definido em sua [configuração](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#name): - `openAI, azureOpenAI, google, anthropic, assistants, azureAssistants, bedrock`

**Exemplo:**

```yaml filename="endpoints / agents / allowedProviders"
allowedProviders:
  - openAI
  - google
```

## capabilities

<OptionTable
  options={[
    [
      'capabilities',
      'Array/List of Strings',
      'Especifica as capacidades de agente disponíveis para todos os usuários para o endpoint agents.',
      'Defines the agent capabilities that are available to all users for the agents endpoint. You can omit the capabilities you wish to exclude from the list.',
    ],
  ]}
/>

**Padrão:** `["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]`

`programmatic_tools`, `stateful_code_sessions`, `run_in_background` e `tool_intents` são opcionais e devem ser adicionados explicitamente. Ferramentas programáticas e sessões com estado também exigem `execute_code` e uma implementação compatível de Code Interpreter. O Agent Builder desativa seleções de MCP programático sem o Code Interpreter, e o servidor remove opções de chamadores programáticos obsoletos quando a execução de código não está disponível. `run_in_background` torna as ferramentas do Code Interpreter elegíveis por padrão e permite a ativação por ferramenta para MCP, Plugins e Actions elegíveis; a execução de código em segundo plano também exige `execute_code` e Code Interpreter.

`stateful_code_sessions` é altamente experimental. Ele requer uma rota de Code Interpreter com perfil `stateful` separada, configurada com [`LIBRECHAT_CODE_BASEURL_STATEFUL`](/docs/configuration/dotenv#stateful-code-interpreter-endpoint) ou [`statefulCodeSessions.environments`](#statefulcodesessions). Requisições stateful falham de forma fechada em vez de recorrer ao serviço stateless normal. No Agent Builder, cada Agent habilitado escolhe um escopo de workspace de usuário, agente-e-usuário ou conversa e, quando backends nomeados estão configurados, um ambiente de execução. Novos Agents começam com o escopo pessoal padrão do usuário e o ambiente de execução padrão da implantação.

**Exemplo:**

```yaml filename="endpoints / agents / capabilities"
capabilities:
  - 'deferred_tools'
  # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
  # Requires execute_code and a Code Interpreter deployment with the Tool Call Server component.
  # - 'programmatic_tools'
  - 'execute_code'
  - 'file_search'
  - 'web_search'
  - 'artifacts'
  - 'subagents'
  - 'actions'
  - 'context'
  - 'skills'
  - 'memory'
  - 'ask_user_question'
  - 'tools'
  - 'chain'
  - 'ocr'
  # Optional and highly experimental: reuse a user-, agent-and-user-, or
  # conversation-scoped workspace. Requires LIBRECHAT_CODE_BASEURL_STATEFUL
  # or a named statefulCodeSessions environment.
  # - 'stateful_code_sessions'
  # Optional: allow eligible Code Interpreter, MCP, Plugin, and Action tools to run as background tasks.
  # - 'run_in_background'
  # Optional: show live model-written intent labels for native and selected MCP tools.
  # - 'tool_intents'
```

**Nota:** Este campo é opcional. Se omitido, o comportamento padrão é incluir todas as capacidades listadas no default.

## statefulCodeSessions

Configura os escopos de compartilhamento e backends de execução opcionais para workspaces de Code Interpreter com estado altamente experimentais.

<Callout type="warning" title="Altamente experimental">
  Stateful Code Sessions, ambientes de execução nomeados, workers conectados e pareamento estão em uma fase inicial de experimentação. Seu comportamento, configuração, características de persistência e protocolo de integração podem mudar substancialmente.
</Callout>

<OptionTable
  options={[
    [
      'allowedEnvironments',
      'Array/List of Strings',
      'Escopos de workspace com estado permitidos: `user`, `agent-user` e/ou `conversation`.',
      '["user", "agent-user", "conversa"]',
    ],
    [
      'environments',
      'Array of Objects',
      'Ambientes de Code API opcionais, nomeados, gerenciados ou anexados, exibidos no Agent Builder.',
      'Unset; LIBRECHAT_CODE_BASEURL_STATEFUL remains the stateful endpoint',
    ],
  ]}
/>

```yaml filename="endpoints / agents / statefulCodeSessions"
statefulCodeSessions:
  allowedEnvironments:
    - 'user'
    - 'agent-user'
    - 'conversation'
  environments:
    - id: managed-default
      name: Managed Code API
      type: managed
      baseURL: https://code.example.com/v1
      default: true
    - id: engineering-vm
      name: Engineering VM
      type: attached
      baseURL: https://code-bridge.example.com/v1
      owner: deployment
      pairing:
        workerId: engineering-vm
        tokenEnv: CODE_BRIDGE_ADMIN_TOKEN
    - id: personal-workers
      name: Personal Code Workers
      type: attached
      baseURL: https://code-bridge.example.com/v1
      owner: deployment
      configSchema:
        permissions:
          fileWrite:
            allowed: [allow, ask, deny]
            default: ask
          commandExecution:
            allowed: [ask, deny]
            default: ask
      pairing:
        allowPrincipalWorkers: true
        tokenEnv: CODE_BRIDGE_ADMIN_TOKEN
```

`allowedEnvironments` é obrigatório sempre que o bloco estiver presente e deve incluir pelo menos um escopo de workspace. Omita o bloco inteiro para permitir todos os três escopos e continuar roteando o trabalho com estado através de [`LIBRECHAT_CODE_BASEURL_STATEFUL`](/docs/configuration/dotenv#stateful-code-interpreter-endpoint). O padrão pessoal e o Agent Builder permitem apenas valores permitidos. Se um administrador restringir a política, um padrão pessoal salvo permanece visível, mas desativado, e novos Agents usam o primeiro escopo permitido. Agents habilitados existentes mantêm seu escopo salvo, mas um escopo agora não permitido falha de forma segura (fails closed) em tempo de execução até que o Agent seja reconfigurado.

Quando `environments` não está vazio, cada entrada requer um `id` único, um `name` de exibição, um `type` como `managed` ou `attached`, e uma `baseURL` HTTP(S) sem string de consulta ou fragmento. Se a lista contiver ambientes executáveis, exatamente uma dessas entradas deve definir `default: true`. O Agent Builder então adiciona um seletor de **Execution environment**; deixá-lo em **Deployment default** utiliza essa entrada padrão. Uma lista contendo apenas planos de controle de pareamento self-service não precisa de um padrão. Um Agent que nomeia um ambiente que é posteriormente removido ou que não está mais acessível falha de forma segura (fails closed) em vez de recorrer a outro backend.

- `managed` aponta diretamente para uma Code API stateful gerenciada por operador.
- `attached` aponta para um backend `remote-bridge` da Code API compatível que delega trabalho para um worker `@librechat/code` de saída. O worker não requer uma porta pública de entrada, mas seu endpoint de execução local ainda precisa de isolamento apropriado.
- `workerId` opcionalmente roteia um ambiente anexado para um worker de saída. Ele é exclusivo do servidor e não é exposto através da configuração de inicialização do cliente.
- `owner` assume o valor padrão `deployment`. Ambientes de propriedade do principal são criados através da API de Ambientes de Código autenticada do LibreChat, protegida por ACLs, e mesclados apenas para solicitações autorizadas; não coloque URLs fornecidas pelo cliente no YAML de deployment.
- `pairing` é opcional e válido apenas para entradas `attached` de propriedade da implantação. `pairing.workerId` identifica um worker gerenciado pelo operador a ser inscrito, enquanto `pairing.allowPrincipalWorkers: true` permite que usuários autorizados inscrevam workers vinculados ao proprietário no plano de controle. Pelo menos um desses campos é obrigatório quando `pairing` está presente.
- `pairing.tokenEnv` nomeia a variável de ambiente que contém o token do administrador da ponte; ela nunca contém o próprio token. O pareamento requer HTTPS, exceto para desenvolvimento em loopback.
- `configSchema.permissions` permite opcionalmente que proprietários autorizados escolham a política de `fileWrite` e `commandExecution` para ambientes pessoais em **Configurações > Ambientes de código**. Cada campo requer uma lista `allowed` não vazia contendo `allow`, `ask` e/ou `deny`; `default` é `ask` quando omitido e deve aparecer em `allowed`. O LibreChat rejeita valores armazenados fora da lista do administrador.
- Não configure `settings` no YAML de implantação. O LibreChat fornece esse campo de escopo de solicitação a partir das preferências validadas pelo servidor do proprietário autenticado.
- Apenas a política de escrita de arquivos e execução de comandos é configurável pelo usuário. Isolamento, rede, montagens, execução privilegiada, entrada, saída e segredos permanecem sob controle do administrador.
- Se tanto o `workerId` de nível superior quanto o `pairing.workerId` estiverem presentes, eles devem coincidir. Uma entrada com `allowPrincipalWorkers: true` e nenhum dos IDs de worker sendo exclusivo de pareamento (pairing-only): ela é omitida dos seletores de execução e não pode definir `default: true`.

A execução anexada habilita automaticamente uma linha de base de aprovação segura: gravações de arquivos e execução de comandos ou códigos solicitam confirmação, enquanto operações de leitura e pesquisa continuam sob a política de ferramentas regular. A linha de base é delimitada ao Agent que utiliza o ambiente anexado. Se nenhuma configuração de usuário estiver disponível, o LibreChat usa `ask`; [`toolApproval.enabled: false`](#toolapproval) é a substituição de emergência do administrador que desativa essa linha de base de ambiente anexado. Chamadores sem suporte a aprovação e retomada falham de forma fechada, a menos que essa substituição explícita seja definida.

A execução anexada requer uma compilação experimental correspondente do remote-bridge do Code Interpreter. O pareamento autentica a conexão de saída do worker; ele não substitui o isolamento de sandbox ou VM. Usuários com permissão de gerenciamento de Code Environment realizam o pareamento, configuram a política de ferramentas permitidas e revogam workers pessoais em **Settings > Code environments**. Veja [Stateful Code Sessions](/docs/features/code_interpreter#stateful-code-sessions).

## eventDriven

Configura a origem interna confiável usada pela entrega de eventos do Agent. A maioria das implantações deve omitir este bloco e usar o listener vinculado ao processo atual.

<OptionTable
  options={[
    [
      'selfUrl',
      'URL',
      'Substitui o ouvinte vinculado ao processo atual para admissão de gatilho interno confiável. Use apenas quando a entrega precisar atravessar outra origem HTTP confiável.',
      '',
    ],
  ]}
/>

```yaml filename="endpoints / agents / eventDriven"
endpoints:
  agents:
    eventDriven:
      selfUrl: https://librechat.internal
```

As continuações de filhos vinculadas são automáticas. Os campos mais antigos `childTurns`, `completionWakeups`, `coalescing`, `actorMailbox`, `checkpointForks` e `durableReceipts` em `eventDriven` não são mais opções de configuração e devem ser removidos. O runtime agora aplica essas garantias de entrega diretamente. O campo separado [`backgroundTasks.completionWakeups`](#backgroundtasks) controla a entrega conversacional para ferramentas em segundo plano concluídas e Subagents desanexados.

Deixe `selfUrl` sem definição para o caminho normal de bound-listener. Defina-o apenas quando a admissão de gatilho interno confiável precisar atravessar outra origem HTTP(S), como um front door TLS. `AGENT_TRIGGERS_SELF_URL` permanece como um fallback de compatibilidade quando este campo é omitido. Veja [Eventos da API de Agentes](/docs/features/agents_api#agent-events) e [Continuação Automática de Pai](/docs/features/subagents#automatic-parent-continuation).

A conclusão da Action do Event Actor detached é selecionada automaticamente a partir do armazenamento de geração integrado. A execução em memória é local ao processo; os fluxos de geração do Redis adicionam recuperação de reinicialização durável e transferência de réplica. Nenhum switch no `librechat.yaml` ou flag de recurso de ambiente é necessário. Veja [Agent Event Runtime](/docs/configuration/dotenv#agent-event-runtime).

## backgroundTasks

Controla se as ferramentas de segundo plano concluídas suportadas e os Subagents desanexados retomam automaticamente o seu Agent pai salvo.

<OptionTable
  options={[
    [
      'completionWakeups',
      'Boolean',
      'Entrega automaticamente conclusões de tarefas em segundo plano suportadas como uma continuação. Defina como false para um comportamento apenas de sondagem (poll-only).',
      'true',
    ],
  ]}
/>

```yaml filename="endpoints / agents / backgroundTasks"
backgroundTasks:
  completionWakeups: true
```

A entrega de conclusão automática é habilitada mesmo quando este bloco é omitido. Defina `completionWakeups: false` para exigir que o Agent colete cada resultado através de `check_background_task`.

A execução de ferramentas em segundo plano comum permanece local ao processo e não sobrevive à perda do seu processo de worker. Uma vez que um resultado de terminal apenas de conteúdo é persistido, sua entrega é durável e pode continuar em outra réplica. Tarefas com artefatos ativos ainda exigem polling na execução proprietária. Subagentes desanexados mantêm seu próprio transcript durável e caminho de resultado terminal separados. O polling manual permanece disponível para status, controles e recuperação em ambos os modos. Veja [Background Tool Calls](/docs/features/agents#background-tool-calls) e [Automatic Parent Continuation](/docs/features/subagents#automatic-parent-continuation).

## toolApproval

Controla a revisão humana para chamadas de ferramentas de Agentes. A política de toda a implantação é desativada quando este bloco é omitido. Um Agente que utiliza um ambiente de Código anexado é a exceção: o LibreChat ativa sua política de segurança de solicitação por padrão (ask-by-default) com escopo definido, a menos que o administrador defina explicitamente `enabled: false`. Quando a revisão é aplicada, as chamadas correspondentes são pausadas até que o usuário envie uma decisão permitida, como aprovar, rejeitar ou editar os argumentos.

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Habilita a aprovação de ferramentas em toda a implantação. Definir explicitamente como false também desativa a linha de base de segurança do ambiente de código anexado.',
      'Omitted',
    ],
    [
      'mode',
      'String',
      'Define o comportamento para chamadas não correspondidas: `default` pergunta, `dontAsk` nega e `bypass` aprova, a menos que seja negado.',
      'default',
    ],
    ['allow', 'Array of strings', 'Padrões glob para chamadas que podem ser executadas sem solicitação.', ''],
    ['deny', 'Array of strings', 'Padrões glob para chamadas que são sempre negadas. Regras de negação prevalecem.', ''],
    ['ask', 'Array of strings', 'Padrões glob para chamadas que sempre exigem revisão.', ''],
    [
      'reason',
      'String',
      'Explicação opcional exibida no prompt de aprovação. Use `{tool}` para inserir o nome da ferramenta.',
      '',
    ],
    [
      'hooks',
      'Array of objects',
      'Módulos de política programática confiáveis para decisões com reconhecimento de contexto. Hooks só podem restringir a política estática.',
      '',
    ],
  ]}
/>

Padrões usam correspondência glob. Ferramentas MCP podem ser delimitadas com `mcp:<server>:<tool>`; por exemplo, `mcp:github:*` corresponde a todas as ferramentas do servidor `github`. O LibreChat aplica regras estáticas e correspondentes de hook aos nomes de tempo de execução do MCP e aliases voltados ao modelo, incluindo ferramentas acessíveis através de Subagents aninhados. As regras estáticas são avaliadas na ordem `deny`, `ask` e depois `allow` antes que o modo selecionado forneça o fallback. Portanto, uma regra de negação (deny) sempre prevalece, inclusive no modo `bypass`.

```yaml filename="endpoints / agents / toolApproval"
toolApproval:
  enabled: true
  mode: default
  allow:
    - 'mcp:analytics:read_*'
  deny:
    - 'mcp:*:delete_*'
  ask:
    - 'mcp:*:*'
  reason: 'Review {tool} before it runs.'
```

Hooks são carregados uma vez na inicialização do servidor e executados no processo do LibreChat. Cada entrada aceita um `module`, um `matcher` regex opcional para o nome da ferramenta e um objeto `options` opcional passado para o construtor do módulo. Carregue apenas código em que você confia. Substituições por função ou locatário não recarregam os módulos de hook; implemente o comportamento contextual dentro do hook.

```yaml filename="endpoints / agents / toolApproval / hooks"
toolApproval:
  enabled: true
  hooks:
    - module: '@acme/librechat-approval-hook'
      matcher: '^mcp:production:'
      options:
        requireTicket: true
```

## checkpointer

Configura o armazenamento para execuções de Agent pausadas por aprovação de ferramenta ou Ask User. Quando qualquer um dos recursos precisa de um checkpointer e este bloco é omitido, o LibreChat usa o MongoDB principal com uma janela de decisão de 24 horas.

<OptionTable
  options={[
    [
      'type',
      'String',
      '`mongo` persiste execuções pausadas entre reinicializações e réplicas; `memory` é local ao processo e destinado ao desenvolvimento.',
      'mongo',
    ],
    ['ttl', 'Number', 'Quanto tempo uma execução pausada aguarda por uma decisão, em segundos.', '86400'],
    [
      'checkpointCollectionName',
      'String',
      'Substituição opcional do nome da coleção de checkpoint do MongoDB.',
      'agent_checkpoints',
    ],
    [
      'checkpointWritesCollectionName',
      'String',
      'Substituição opcional do nome da collection de checkpoint-write do MongoDB.',
      'agent_checkpoint_writes',
    ],
  ]}
/>

```yaml filename="endpoints / agents / checkpointer"
checkpointer:
  type: mongo
  ttl: 86400
```

Use `type: memory` apenas para uma implantação de desenvolvimento de processo único. Uma execução pausada armazenada na memória não pode ser retomada após uma reinicialização ou em outra réplica.

Os checkpoints do MongoDB registram um aviso quando o estado serializado excede 8 MiB e rejeitam uma pausa durável acima de 15 MiB, deixando uma margem abaixo do limite de 16 MiB de documento do MongoDB. Mídias embutidas grandes ou saídas de ferramentas são causas comuns; inicie uma nova conversa ou reduza seu contexto se uma execução relatar `CHECKPOINT_TOO_LARGE`.

## skills

Controla as configurações de Skills em nível de endpoint para agentes.

<OptionTable
  options={[
    [
      'skills.maxCatalogSkills',
      'Number',
      'Limita o número de Skills acessíveis ativas expostas no catálogo visível ao modelo. Deve estar entre 1 e 100.',
      'maxCatalogSkills: 20',
    ],
  ]}
/>

**Padrão:** Nenhum limite configurado além do limite do catálogo de tempo de execução.

**Exemplo:**

```yaml filename="endpoints / agents / skills"
skills:
  maxCatalogSkills: 20
```

Isso não desativa as Skills. Use a capacidade `skills` e o escopo de skill por agente/model-spec para controlar se as Skills estão disponíveis.

## maxSubagents

<OptionTable
  options={[
    [
      'maxSubagents',
      'Number',
      'Número máximo de subagentes explícitos que um único agente pode referenciar.',
      'Applies to the `agent_ids` allowlist on agents and on model specs, and to configured subagent `graphs`, which agents support but model specs do not. Must be a whole number between 1 and 50.',
    ],
  ]}
/>

**Padrão:** `10`

**Intervalo:** `1-50`

**Exemplo:**

```yaml filename="endpoints / agents / maxSubagents"
endpoints:
  agents:
    maxSubagents: 20
```

Aumente este valor quando uma implantação executar agentes com orquestração pesada que precisem delegar tarefas para mais de dez filhos. O valor é lido na inicialização e aplicado em três locais:

- Solicitações de criação, atualização e duplicação de agentes, para `subagents.agent_ids` e `subagents.graphs`
- As listas de permissões (allowlists) de `subagents.agent_ids` nas Model Specs, validadas na mesma passagem de configuração
- O painel do Agent Builder, que para de aceitar novas entradas de subagentes no valor configurado

As Model Specs aceitam apenas `enabled`, `allowSelf` e `agent_ids` em `subagents`. Os `graphs` de subagentes são um recurso de nível de agente: uma chave `graphs` em uma Model Spec é descartada quando a configuração é analisada, sem gerar erro.

<Callout type="warning">
  `maxSubagents` deve ser um número inteiro entre 1 e 50. Um valor fora desse intervalo falha na validação do `librechat.yaml`, e o LibreChat registra o erro e encerra na inicialização em vez de recorrer ao padrão. Iniciar com `CONFIG_BYPASS_VALIDATION=true` ignora o encerramento, mas toda a configuração personalizada é descartada nesse caso, então o limite retorna para `10`. Para voltar ao padrão deliberadamente, remova a chave: um `maxSubagents` omitido resulta em `10` sem erro.
</Callout>

<Callout type="note">
  O limite é aplicado a todo o processo e vem do `librechat.yaml`. Uma substituição no banco de dados por usuário ou por grupo de `endpoints.agents.maxSubagents` é refletida na configuração servida, mas não é aplicada pela validação da requisição.
</Callout>

Aumentar isso apenas altera quantos subagentes um agente pode referenciar. Os limites de profundidade, nó de grafo e configuração de execução não são afetados. Veja [Subagents](/docs/features/subagents#limits).

## maxCitations

<OptionTable
  options={[
    [
      'maxCitations',
      'Number',
      'Controla o número máximo total de citações que podem ser incluídas em uma única resposta do agente.',
      'When using file_search capability, limits the total number of source citations returned to prevent overwhelming responses while ensuring comprehensive coverage.',
    ],
  ]}
/>

**Padrão:** `30`

**Intervalo:** `1-50`

**Exemplo:**

```yaml filename="endpoints / agents / maxCitations"
maxCitations: 30
```

## maxCitationsPerFile

<OptionTable
  options={[
    [
      'maxCitationsPerFile',
      'Number',
      'Limita o número máximo de citações que podem ser extraídas de qualquer arquivo individual.',
      'Ensures citation diversity by preventing any single file from dominating the citations, encouraging representation from multiple sources.',
    ],
  ]}
/>

**Padrão:** `7`

**Intervalo:** `1-10`

**Exemplo:**

```yaml filename="endpoints / agents / maxCitationsPerFile"
maxCitationsPerFile: 7
```

## minRelevanceScore

<OptionTable
  options={[
    [
      'minRelevanceScore',
      'Number',
      'Define o limite mínimo de pontuação de relevância para que as fontes sejam incluídas nas respostas.',
      'Filters out low-quality matches based on vector similarity scores. Higher values (e.g., 0.7) ensure only highly relevant sources are cited, while lower values (e.g., 0.0) include all sources regardless of quality.',
    ],
  ]}
/>

**Padrão:** `0.45` (limite de relevância de 45%)

**Intervalo:** `0.0-1.0`

**Exemplo:**

```yaml filename="endpoints / agents / minRelevanceScore"
minRelevanceScore: 0.45
```

### Exemplos de Configuração de Citação de Arquivos [#file-citation-configuration-examples]

**Configuração Padrão (Balanceada)**

```yaml
endpoints:
  agents:
    maxCitations: 30
    maxCitationsPerFile: 7
    minRelevanceScore: 0.45
```

Fornece citações abrangentes, evitando respostas excessivas e filtrando correspondências de baixa qualidade.

**Configuração Estrita (Alta Qualidade)**

```yaml
endpoints:
  agents:
    maxCitations: 10
    maxCitationsPerFile: 3
    minRelevanceScore: 0.7
```

Inclui apenas citações altamente relevantes com limites rigorosos para respostas focadas.

**Configuração Abrangente (Pesquisa)**

```yaml
endpoints:
  agents:
    maxCitations: 50
    maxCitationsPerFile: 10
    minRelevanceScore: 0.0
```

Extração máxima de informações para tarefas de pesquisa exaustivas, incluindo todas as fontes, independentemente da relevância.

## Capacidades dos Agentes [#agent-capabilities]

O campo `capabilities` permite que você habilite ou desabilite funcionalidades específicas para agentes. As capacidades disponíveis são:

- **deferred_tools**: Permite que agentes descubram ferramentas MCP adiadas em tempo de execução, em vez de carregar todas as ferramentas no contexto antecipadamente.
- **programmatic_tools**: Habilita a Chamada de Ferramentas Programáticas para ferramentas MCP marcadas como Programmatic no Agent Builder. Requer `execute_code` e uma implementação de Code Interpreter com o componente Tool Call Server. Esta funcionalidade é opcional e não está habilitada por padrão.
- **execute_code**: Permite que o agente execute código.
- **stateful_code_sessions**: Permite que agentes que optaram pelo uso reutilizem um workspace do Code Interpreter no escopo de usuário, agente-e-usuário ou conversa. Requer `execute_code`, uma implementação compatível do Code Interpreter e a configuração Avançada por agente. Novos agentes usam o escopo preferencial do usuário conectado. Esta funcionalidade é altamente experimental, não está habilitada por padrão e pode mudar substancialmente durante a experimentação; não trate seu comportamento ou configuração atual como um contrato de produção estável.
- **file_search**: Permite que o agente pesquise e interaja com arquivos. Quando ativado, o comportamento de citação é controlado pelas configurações `maxCitations`, `maxCitationsPerFile` e `minRelevanceScore`.
- **web_search**: Habilita a funcionalidade de pesquisa na web para agentes, permitindo que eles pesquisem e recuperem informações da internet.
- **artifacts**: Permite que o agente gere artefatos interativos (componentes React, HTML, diagramas Mermaid).
- **subagents**: Habilita execuções de agentes filhos em contexto isolado. Veja [Subagents](/docs/features/subagents).
- **actions**: Permite que o agente execute ações predefinidas.
- **context**: Habilita a funcionalidade "Upload as Text" no chat e "File Context" para agentes, permitindo que os usuários enviem arquivos e tenham seu conteúdo extraído e incluído diretamente na conversa.
- **skills**: Habilita Skills no painel lateral, invocação manual com `$`, skills invocadas pelo modelo e listas de permissão de skills de agentes. Veja [Skills](/docs/features/skills).
- **memory**: Permite que agentes usem `set_memory` e `delete_memory` quando a memória está configurada e o usuário tem acesso. Habilitado por padrão; removido em tempo de execução quando a memória está desabilitada.
- **ask_user_question**: Permite que os agentes façam uma pausa para realizar de uma a quatro perguntas relacionadas em um único formulário e retomem com as respostas. Habilitado por padrão.
- **tools**: Concede ao agente acesso a várias ferramentas.
- **chain**: Habilita o recurso Beta para encadeamento de agentes, também conhecido como fluxos de trabalho Mixture-of-Agents (MoA).
- **ocr**: Opcionalmente aprimora o "Upload as Text" no chat e o "File Context" para agentes, permitindo que arquivos sejam enviados e processados com OCR. **Requer que um serviço de OCR seja configurado.**
- **run_in_background**: Torna a execução do Code Interpreter e das ferramentas de shell elegíveis para execução em segundo plano por padrão e habilita a opção de execução em segundo plano por ferramenta para MCP, Plugins e Actions elegíveis. Um interruptor no nível da Action habilita todas as operações elegíveis; Actions OAuth e operações que já definem um parâmetro `run_in_background` são excluídas. Um agente pode optar explicitamente por desativar o Code Interpreter. A configuração permite a execução em segundo plano; o modelo decide por chamada se deve utilizá-la. Requer `execute_code` e uma implementação configurada do Code Interpreter para código. Esta funcionalidade não está habilitada por padrão.
- **tool_intents**: Adiciona rótulos de intenção escritos pelo modelo em tempo real a chamadas de ferramentas elegíveis. Ferramentas nativas, incluindo ferramentas de Code Interpreter, optam automaticamente; ferramentas MCP podem ser habilitadas individualmente ou em massa no Agent Builder. Model Specs usam `describeIntent`. Esta funcionalidade não é habilitada por padrão.

Ao especificar as capabilities, você pode controlar os recursos disponíveis para os usuários ao interagir com agentes.

## Exemplo de Configuração [#example-configuration]

Aqui está um exemplo de configuração do endpoint `agents` com capacidades personalizadas e configurações de citação de arquivos:

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    disableBuilder: false
    # File citation configuration
    maxCitations: 20
    maxCitationsPerFile: 5
    minRelevanceScore: 0.6
    # Custom capabilities
    capabilities:
      # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
      # - 'programmatic_tools'
      - 'execute_code'
      # Optional: makes Code Interpreter background-eligible and enables selected MCP, Plugin, and Action tools.
      # - 'run_in_background'
      # Optional: enables live intent labels for native and selected MCP tools.
      # - 'tool_intents'
      - 'file_search'
      - 'skills'
      - 'subagents'
      - 'actions'
      - 'artifacts'
      - 'context'
      - 'ocr'
      - 'web_search'
```

Neste exemplo:

- A interface do construtor está habilitada
- As citações de arquivos são limitadas a um total de 20, com no máximo 5 por arquivo
- Apenas fontes com 60%+ de relevância são incluídas
- Os agentes do LibreChat têm acesso à execução de código, pesquisa de arquivos (com citações), Skills, Subagentes, ações, artefatos, contexto de arquivos, serviços de OCR (se configurados) e recursos de pesquisa na web.
- O Programmatic Tool Calling permanece desativado, a menos que você adicione a capacidade `programmatic_tools` junto com `execute_code`
- As chamadas de ferramentas em segundo plano permanecem desativadas, a menos que você adicione `run_in_background`; as ferramentas do Code Interpreter tornam-se elegíveis por padrão, enquanto ferramentas MCP, Plugins e Actions elegíveis ainda exigem seleção.
- Os rótulos de intenção de ferramentas permanecem desativados, a menos que você adicione `tool_intents`; as ferramentas nativas então optam automaticamente, enquanto as ferramentas MCP ainda exigem seleção por ferramenta.

## remoteApi

Configuração para autenticação da API de Agentes Remotos. Controla como serviços externos se autenticam ao chamar os endpoints da API de Agentes.

### remoteApi.auth

<OptionTable
  options={[
    [
      'auth',
      'Object',
      'Configuração de autenticação para a Remote Agent API.',
      'Supports API key and/or OIDC Bearer token authentication. If omitted, only API key auth is active.',
    ],
  ]}
/>

#### remoteApi.auth.apiKey

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Habilitar autenticação por chave de API para a Remote Agent API.',
      'When true, requests with a valid LibreChat API key are accepted. Can be used alongside or instead of OIDC.',
    ],
  ]}
/>

**Padrão:** `true`

#### remoteApi.auth.oidc

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Habilitar autenticação por token Bearer OIDC.',
      'When true, the middleware validates Bearer tokens against the configured OIDC issuer via JWKS.',
    ],
    [
      'issuer',
      'String',
      'URL do emissor OIDC.',
      'The base URL of your OIDC provider, such as a Keycloak realm URL. Used for token issuer validation and JWKS discovery if jwksUri is not set.',
    ],
    [
      'jwksUri',
      'String',
      'URL do endpoint JWKS. Opcional.',
      'If omitted, resolved automatically via {issuer}/.well-known/openid-configuration. You can also set OPENID_JWKS_URL as an alternative.',
    ],
    [
      'audience',
      'String',
      'Público-alvo do token esperado. Obrigatório quando a autenticação OIDC está habilitada.',
      'Tokens must contain this value in their aud claim.',
    ],
    [
      'scope',
      'String',
      'Valor de escopo obrigatório. Opcional.',
      'If set, the token must contain this value in its scp or scope claim. Use this to distinguish token intent across different APIs.',
    ],
  ]}
/>

**Padrão:** `enabled: false`

**Exemplo - Apenas OIDC:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: false
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          audience: my-client-id
```

**Exemplo - OIDC com fallback de chave de API:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: true
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          # jwksUri is optional and auto-discovered if omitted
          jwksUri: https://auth.example.com/realms/myrealm/protocol/openid-connect/certs
          audience: my-client-id
```

<Callout type="info">
  A prioridade de resolução do JWKS URI é o `jwksUri` explícito, seguido por `OPENID_JWKS_URL` e, por fim, a descoberta automática via `{issuer}/.well-known/openid-configuration`.
</Callout>

<Callout type="info">
  O mapeamento de usuários OIDC usa a claim `sub` como busca primária, com fallback para as claims `email`, `preferred_username` ou `upn`. O usuário correspondente já deve existir no LibreChat.
</Callout>

## Subagentes [#subagents]

O campo `subagents` controla quais agentes filhos isolados um agente pai pode gerar quando a capacidade `subagents` está disponível.

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Adiciona a ferramenta de criação de subagentes a este agente quando verdadeiro. Padrão: desativado.',
      'enabled: true',
    ],
    [
      'allowSelf',
      'Boolean',
      'Permite que o agente crie a si mesmo em um novo contexto isolado. Padrão: true.',
      'allowSelf: true',
    ],
    [
      'agent_ids',
      'Array/List of Strings',
      'Agentes específicos que este agente pode criar. Limitado por `maxSubagents`, que tem como padrão 10.',
      'agent_ids: ["agent_researcher"]',
    ],
    [
      'graphs',
      'Array of Objects',
      'Equipes de agentes salvas que podem ser executadas como um grafo filho isolado. A lista é limitada por `maxSubagents` (padrão: 10); cada equipe pode conter até 32 membros.',
      '',
    ],
  ]}
/>

```yaml filename="Agent subagents"
subagents:
  enabled: true
  allowSelf: true
  agent_ids:
    - 'agent_researcher'
    - 'agent_reviewer'
```

Cada entrada em `graphs` requer um `type`, `name`, `description`, `agent_ids`, `edges` diretas, `entry_agent_id` e `result_agent_id` exclusivos. As definições de equipe devem formar um grafo acíclico direcionado conectado, cada membro deve estar visível para o usuário e a configuração total é limitada a 50 alvos de Agent exclusivos e 100 configurações de execução expandidas. Veja o [exemplo de equipe salva e comportamento de tempo de execução](/docs/features/subagents#configure-an-agent).

Para comportamento e limites voltados ao usuário, veja [Subagents](/docs/features/subagents).

## Notas [#notes]

- Não é recomendado desativar a interface do construtor, a menos que você esteja usando [modelSpecs](/docs/configuration/librechat_yaml/object_structure/model_specs) para definir uma lista de agentes para escolha.
- A configuração de citação de arquivos (`maxCitations`, `maxCitationsPerFile`, `minRelevanceScore`) só se aplica quando a capacidade `file_search` está habilitada.
- A pontuação de relevância é calculada usando similaridade vetorial, onde 1.0 representa uma correspondência perfeita e 0.0 representa nenhuma similaridade.
- Os limites de citação ajudam a equilibrar a recuperação abrangente de informações com a qualidade e o desempenho da resposta.
- A capacidade `context` funciona sem configuração de OCR usando métodos de análise de texto. O OCR aprimora a qualidade da extração quando configurado.
- A funcionalidade `ocr` requer que um serviço de OCR seja configurado (veja [Configuração de OCR](/docs/configuration/librechat_yaml/object_structure/ocr)).
- `maxSubagents` limita quantos subagentes um único agente pode referenciar. Isso não altera os limites de profundidade ou de grafo documentados em [Subagents](/docs/features/subagents#limits).
