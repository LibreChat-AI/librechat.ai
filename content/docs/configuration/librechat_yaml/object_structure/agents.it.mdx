---
title: Struttura dell'oggetto endpoint Agents
icon: Bot
---

Questa pagina si applica all'endpoint [`agents`](/docs/features/agents).

## Esempio [#example]

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    recursionLimit: 50
    maxRecursionLimit: 100
    maxToolCallArgBytes: 65536
    maxDeltaEventsPerTurn: 100000
    maxToolCallArgBytesByTool:
      create_file: 131072
    disableBuilder: false
    # (optional) Agent Capabilities available to all users. Omit the ones you wish to exclude. Defaults to list below.
    # Opt-in capabilities: "programmatic_tools", "stateful_code_sessions", "run_in_background", and "tool_intents".
    # capabilities: ["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]
    # (optional) File citation configuration for file_search capability
    maxCitations: 30 # Maximum total citations in responses (1-50)
    maxCitationsPerFile: 7 # Maximum citations from each file (1-10)
    minRelevanceScore: 0.45 # Minimum relevance score threshold (0.0-1.0)
    titleTiming: immediate
    activityLabel: true
    activityModel: gpt-4.1-nano
    activityPhaseLabel: true
    activityPhaseMaxPerRun: 5
    maxSubagents: 10 # Maximum explicit subagents per agent (1-50)
    skills:
      maxCatalogSkills: 20
    toolApproval:
      enabled: true
      mode: default
      deny: ['mcp:*:delete_*']
      ask: ['mcp:*:*']
    checkpointer:
      type: mongo
      ttl: 86400
    remoteApi:
      auth:
        oidc:
          enabled: false
```

> Questa configurazione abilita l'interfaccia di creazione per gli agenti.

## recursionLimit

<OptionTable
  options={[
    [
      'recursionLimit',
      'Number',
      'Imposta il numero predefinito di passaggi che un agente può eseguire in un\'esecuzione.',
      'Controls recursion depth to prevent infinite loops. When limit is reached, raises GraphRecursionError. This value can be configured from the UI up to the maxRecursionLimit.',
    ],
  ]}
/>

**Predefinito:** `25`

**Esempio:**

```yaml filename="endpoints / agents / recursionLimit"
recursionLimit: 50
```

Per ulteriori informazioni sui passaggi dell'agente, consulta [Max Agent Steps](/docs/features/agents#max-agent-steps).

## maxRecursionLimit

<OptionTable
  options={[
    [
      'maxRecursionLimit',
      'Number',
      'Imposta il numero massimo assoluto di passaggi che un agente può eseguire in un\'esecuzione.',
      'Defines the upper limit for the recursionLimit that can be set from the UI. This prevents users from setting excessively high values.',
    ],
  ]}
/>

**Predefinito:** Se omesso, viene impostato sul valore di recursionLimit o su 50 se anche recursionLimit è omesso.

**Esempio:**

```yaml filename="endpoints / agents / maxRecursionLimit"
maxRecursionLimit: 100
```

Per ulteriori informazioni sui passaggi dell'agente, consulta [Max Agent Steps](/docs/features/agents#max-agent-steps).

## Interruttori di circuito per lo stream [#stream-circuit-breakers]

Queste impostazioni interrompono l'esecuzione di un Agent quando il flusso di un provider sembra produrre argomenti di strumenti fuori controllo o una sequenza illimitata di eventi delta.

<OptionTable
  options={[
    [
      'maxToolCallArgBytes',
      'Number',
      'Byte cumulativi massimi per gli argomenti di una chiamata a funzione in streaming. Impostare a 0 per disabilitare la protezione globale.',
      '65536',
    ],
    [
      'maxDeltaEventsPerTurn',
      'Number',
      'Numero massimo di eventi in streaming per un turno di generazione del modello. Impostare a 0 o omettere per disabilitare questa protezione.',
      '0 (disabled)',
    ],
    [
      'maxToolCallArgBytesByTool',
      'Object',
      'Override dei byte degli argomenti per strumento, indicizzati in base al nome dello strumento visualizzato dal modello. Un valore pari a 0 disabilita la protezione per quello strumento.',
      'create_file: 131072',
    ],
  ]}
/>

LibreChat utilizza un limite globale di argomenti di 64 KiB e fornisce un override di 128 KiB per `create_file`, i cui payload legittimi sono spesso più grandi. Le voci in `maxToolCallArgBytesByTool` si uniscono a tale override integrato. Se uno dei due controlli viene attivato, LibreChat interrompe lo stream del provider in corso e segnala quale limite è stato superato.

```yaml filename="endpoints / agents / stream circuit breakers"
maxToolCallArgBytes: 65536
maxDeltaEventsPerTurn: 100000
maxToolCallArgBytesByTool:
  create_file: 262144
  large_import: 524288
```

Per disabilitare ogni protezione sulla dimensione degli argomenti, imposta sia il limite globale che l'override `create_file` predefinito a `0`:

```yaml
maxToolCallArgBytes: 0
maxToolCallArgBytesByTool:
  create_file: 0
```

## titleTiming

<OptionTable
  options={[
    [
      'titleTiming',
      'String',
      'Controlla quando vengono generati i titoli delle conversazioni per l\'endpoint agents. Valori validi: "immediate" o "final".',
      'Defaults to "immediate".',
    ],
  ]}
/>

**Predefinito:** `"immediate"`

**Valori disponibili:**

- **`"immediate"`**: Genera il titolo non appena la richiesta ha inizio, in parallelo con la risposta del modello, utilizzando il primo messaggio dell'utente.
- **`"final"`**: Posticipa la generazione del titolo fino al completamento della risposta completa. Questo preserva il comportamento legacy.

**Esempio:**

```yaml filename="endpoints / agents / titleTiming"
titleTiming: immediate
```

## activityLabel

Imposta `activityLabel: true` per raggruppare ogni blocco di ragionamento dell'Agent e le chiamate agli strumenti sotto un'intestazione generata su una riga. Utilizza `activityModel`, `activityEndpoint`, `activityPrompt`, `activityMaxPerRun` e `activityCharLimit` per controllare la chiamata al modello aggiuntiva e il relativo costo. Consulta [Agent Activity Groups](/docs/configuration/librechat_yaml/object_structure/shared_endpoint_settings#agent-activity-groups) per il riferimento completo dei campi e le regole di precedenza.

```yaml filename="endpoints / agents / activity groups"
activityLabel: true
activityEndpoint: openAI
activityModel: gpt-4.1-nano
activityMaxPerRun: 20
activityCharLimit: 600
activityPhaseLabel: true
activityPhaseMaxPerRun: 5
```

`activityPhaseLabel` può aggiungere in modo indipendente un riepilogo principale compresso attorno a ogni fase di esecuzione che contiene almeno due attività logiche. Utilizza `activityPhaseModel`, `activityPhaseEndpoint`, `activityPhasePrompt` e `activityPhaseMaxPerRun` per ottimizzare tali chiamate; `activityCharLimit` è condiviso con le etichette delle attività secondarie.

## disableBuilder

<OptionTable
  options={[
    [
      'disableBuilder',
      'Boolean',
      'Controlla la visibilità e l\'utilizzo dell\'interfaccia builder per gli agenti.',
      'When set to `true`, disables the builder interface for the agent, limiting direct manual interaction.',
    ],
  ]}
/>

**Predefinito:** `false`

**Esempio:**

```yaml filename="endpoints / agents / disableBuilder"
disableBuilder: false
```

## allowedProviders

<OptionTable
  options={[
    [
      'allowedProviders',
      'Array/List of Strings',
      'Specifica un elenco di provider di endpoint (ad esempio, "openAI", "anthropic", "google") autorizzati all\'uso con la funzionalità Agents.',
      'If defined, only agents configured with these providers can be initialized. If omitted or empty, all configured providers are allowed.',
    ],
  ]}
/>

**Predefinito:** `[]` (lista vuota, tutti i provider consentiti)

**Nota:** Deve essere uno dei seguenti, o un nome di endpoint personalizzato come definito nella tua [configurazione](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#name): - `openAI, azureOpenAI, google, anthropic, assistants, azureAssistants, bedrock`

**Esempio:**

```yaml filename="endpoints / agents / allowedProviders"
allowedProviders:
  - openAI
  - google
```

## capabilities

<OptionTable
  options={[
    [
      'capabilities',
      'Array/List of Strings',
      'Specifica le funzionalità dell\'agente disponibili per tutti gli utenti per l\'endpoint agents.',
      'Defines the agent capabilities that are available to all users for the agents endpoint. You can omit the capabilities you wish to exclude from the list.',
    ],
  ]}
/>

**Predefinito:** `["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]`

`programmatic_tools`, `stateful_code_sessions`, `run_in_background` e `tool_intents` sono opzionali e devono essere aggiunti esplicitamente. I programmatic tools e le stateful sessions richiedono inoltre `execute_code` e un deployment di Code Interpreter compatibile. L'esecuzione del codice in background richiede `run_in_background`, `execute_code` e Code Interpreter.

**Esempio:**

```yaml filename="endpoints / agents / capabilities"
capabilities:
  - 'deferred_tools'
  # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
  # Requires execute_code and a Code Interpreter deployment with the Tool Call Server component.
  # - 'programmatic_tools'
  - 'execute_code'
  - 'file_search'
  - 'web_search'
  - 'artifacts'
  - 'subagents'
  - 'actions'
  - 'context'
  - 'skills'
  - 'memory'
  - 'ask_user_question'
  - 'tools'
  - 'chain'
  - 'ocr'
  # Optional: reuse one Code Interpreter workspace per conversation.
  # - 'stateful_code_sessions'
  # Optional: allow selected eligible MCP and Code Interpreter tools to run as background tasks.
  # - 'run_in_background'
  # Optional: show live model-written intent labels for native and selected MCP tools.
  # - 'tool_intents'
```

**Nota:** Questo campo è facoltativo. Se omesso, il comportamento predefinito consiste nell'includere tutte le funzionalità elencate nel default.

## toolApproval

Controlla la revisione umana per le chiamate agli strumenti dell'Agent. Questa funzionalità è disabilitata per impostazione predefinita. Quando è abilitata, le chiamate corrispondenti vengono messe in pausa finché l'utente non invia una decisione consentita, come l'approvazione, il rifiuto o la modifica degli argomenti.

<OptionTable
  options={[
    ['enabled', 'Boolean', 'Abilita l\'approvazione degli strumenti per l\'endpoint Agents.', 'false'],
    [
      'mode',
      'String',
      'Imposta il comportamento per le chiamate non corrispondenti: `default` chiede, `dontAsk` nega e `bypass` approva a meno che non venga negato.',
      'default',
    ],
    ['allow', 'Array of strings', 'Pattern glob per chiamate che possono essere eseguite senza richiesta.', ''],
    ['deny', 'Array of strings', 'Pattern glob per le chiamate che vengono sempre negate. Le regole di negazione hanno la priorità.', ''],
    ['ask', 'Array of strings', 'Pattern glob per le chiamate che richiedono sempre una revisione.', ''],
    [
      'reason',
      'String',
      'Spiegazione facoltativa mostrata nel prompt di approvazione. Usa `{tool}` per inserire il nome dello strumento.',
      '',
    ],
    [
      'hooks',
      'Array of objects',
      'Moduli di policy programmatici affidabili per decisioni basate sul contesto. Gli hook possono solo restringere la policy statica.',
      '',
    ],
  ]}
/>

I pattern utilizzano la corrispondenza glob. Gli strumenti MCP possono essere limitati con `mcp:<server>:<tool>`; ad esempio, `mcp:github:*` corrisponde a ogni strumento del server `github`. La valutazione assegna a `deny` la priorità più alta, inclusa la modalità `bypass`.

```yaml filename="endpoints / agents / toolApproval"
toolApproval:
  enabled: true
  mode: default
  allow:
    - 'mcp:analytics:read_*'
  deny:
    - 'mcp:*:delete_*'
  ask:
    - 'mcp:*:*'
  reason: 'Review {tool} before it runs.'
```

Gli Hooks vengono caricati una volta all'avvio del server ed eseguiti nel processo di LibreChat. Ogni voce accetta un `module`, un `matcher` regex opzionale per il nome dello strumento e un oggetto `options` opzionale passato al builder del modulo. Carica solo codice di cui ti fidi. Gli override per ruolo o tenant non ricaricano i moduli degli hook; implementa il comportamento contestuale all'interno dell'hook.

```yaml filename="endpoints / agents / toolApproval / hooks"
toolApproval:
  enabled: true
  hooks:
    - module: '@acme/librechat-approval-hook'
      matcher: '^mcp:production:'
      options:
        requireTicket: true
```

## checkpointer

Configura l'archiviazione per le esecuzioni degli Agent messe in pausa dall'approvazione degli strumenti o da "Ask User". Quando una delle due funzionalità richiede un checkpointer e questo blocco viene omesso, LibreChat utilizza il database MongoDB principale con una finestra di decisione di 24 ore.

<OptionTable
  options={[
    [
      'type',
      'String',
      '`mongo` mantiene le esecuzioni in pausa tra riavvii e repliche; `memory` è locale al processo e destinato allo sviluppo.',
      'mongo',
    ],
    ['ttl', 'Number', 'Per quanto tempo un\'esecuzione in pausa attende una decisione, in secondi.', '86400'],
    [
      'checkpointCollectionName',
      'String',
      'Sovrascrittura opzionale del nome della collezione di checkpoint MongoDB.',
      'agent_checkpoints',
    ],
    [
      'checkpointWritesCollectionName',
      'String',
      'Override opzionale del nome della collection per i checkpoint-write di MongoDB.',
      'agent_checkpoint_writes',
    ],
  ]}
/>

```yaml filename="endpoints / agents / checkpointer"
checkpointer:
  type: mongo
  ttl: 86400
```

Utilizza `type: memory` solo per un deployment di sviluppo a processo singolo. Un'esecuzione in pausa archiviata nella memoria non può riprendere dopo un riavvio o su un'altra replica.

I checkpoint di MongoDB registrano un avviso quando lo stato serializzato supera gli 8 MiB e rifiutano una pausa persistente oltre i 15 MiB, lasciando un margine di sicurezza al di sotto del limite di 16 MiB per documento di MongoDB. I file multimediali in linea di grandi dimensioni o gli output degli strumenti sono cause comuni; avvia una nuova conversazione o riducine il contesto se un'esecuzione riporta `CHECKPOINT_TOO_LARGE`.

## skills

Controlla le impostazioni delle Skills a livello di endpoint per gli agenti.

<OptionTable
  options={[
    [
      'skills.maxCatalogSkills',
      'Number',
      'Limita il numero di Skill accessibili attive esposte nel catalogo visibile al modello. Deve essere compreso tra 1 e 100.',
      'maxCatalogSkills: 20',
    ],
  ]}
/>

**Predefinito:** Nessun limite configurato oltre al limite del catalogo di runtime.

**Esempio:**

```yaml filename="endpoints / agents / skills"
skills:
  maxCatalogSkills: 20
```

Ciò non disabilita le Skills. Utilizza la funzionalità `skills` e l'ambito delle skill per singolo agente/Model Specs per controllare se le Skills sono disponibili.

## maxSubagents

<OptionTable
  options={[
    [
      'maxSubagents',
      'Number',
      'Numero massimo di subagenti espliciti a cui un singolo agente può fare riferimento.',
      'Applies to the `agent_ids` allowlist on agents and on model specs, and to configured subagent `graphs`, which agents support but model specs do not. Must be a whole number between 1 and 50.',
    ],
  ]}
/>

**Predefinito:** `10`

**Intervallo:** `1-50`

**Esempio:**

```yaml filename="endpoints / agents / maxSubagents"
endpoints:
  agents:
    maxSubagents: 20
```

Solleva questo quando una distribuzione esegue agenti con un'orchestrazione intensiva che devono delegare a più di dieci figli. Il valore viene letto all'avvio e applicato in tre punti:

- Richieste di creazione, aggiornamento e duplicazione di agenti, per `subagents.agent_ids` e `subagents.graphs`
- Le allowlist `subagents.agent_ids` di Model Specs, validate durante lo stesso passaggio di configurazione
- Il pannello Agent Builder, che smette di accettare nuove voci di subagent al valore configurato

Le Model Specs accettano solo `enabled`, `allowSelf` e `agent_ids` sotto `subagents`. I `graphs` dei subagent sono una funzionalità a livello di agente: una chiave `graphs` in una Model Spec viene scartata durante l'analisi della configurazione, senza generare alcun errore.

<Callout type="warning">
  `maxSubagents` deve essere un numero intero compreso tra 1 e 50. Un valore al di fuori di tale intervallo causa il fallimento della convalida di `librechat.yaml`, e LibreChat registra l'errore e si arresta all'avvio invece di ripiegare sul valore predefinito. L'avvio con `CONFIG_BYPASS_VALIDATION=true` evita l'arresto, ma l'intera configurazione personalizzata viene scartata in tal caso, quindi il limite torna a `10`. Per tornare deliberatamente al valore predefinito, rimuovere la chiave: un `maxSubagents` omesso viene risolto in `10` senza alcun errore.
</Callout>

<Callout type="note">
  Il limite è a livello di processo e proviene da `librechat.yaml`. Una sovrascrittura nel database per utente o per gruppo di `endpoints.agents.maxSubagents` viene riflessa nella configurazione servita, ma non viene applicata dalla convalida della richiesta.
</Callout>

Aumentare questo valore modifica solo il numero di sottoagenti a cui un agente può fare riferimento. I limiti di profondità, nodo del grafo e configurazione dell'esecuzione rimangono invariati. Vedi [Subagents](/docs/features/subagents#limits).

## maxCitations

<OptionTable
  options={[
    [
      'maxCitations',
      'Number',
      'Controlla il numero massimo totale di citazioni che possono essere incluse in una singola risposta dell\'agente.',
      'When using file_search capability, limits the total number of source citations returned to prevent overwhelming responses while ensuring comprehensive coverage.',
    ],
  ]}
/>

**Predefinito:** `30`

**Intervallo:** `1-50`

**Esempio:**

```yaml filename="endpoints / agents / maxCitations"
maxCitations: 30
```

## maxCitationsPerFile

<OptionTable
  options={[
    [
      'maxCitationsPerFile',
      'Number',
      'Limita il numero massimo di citazioni che possono essere estratte da un singolo file.',
      'Ensures citation diversity by preventing any single file from dominating the citations, encouraging representation from multiple sources.',
    ],
  ]}
/>

**Predefinito:** `7`

**Intervallo:** `1-10`

**Esempio:**

```yaml filename="endpoints / agents / maxCitationsPerFile"
maxCitationsPerFile: 7
```

## minRelevanceScore

<OptionTable
  options={[
    [
      'minRelevanceScore',
      'Number',
      'Imposta la soglia minima del punteggio di rilevanza affinché le fonti siano incluse nelle risposte.',
      'Filters out low-quality matches based on vector similarity scores. Higher values (e.g., 0.7) ensure only highly relevant sources are cited, while lower values (e.g., 0.0) include all sources regardless of quality.',
    ],
  ]}
/>

**Predefinito:** `0.45` (soglia di rilevanza del 45%)

**Intervallo:** `0.0-1.0`

**Esempio:**

```yaml filename="endpoints / agents / minRelevanceScore"
minRelevanceScore: 0.45
```

### Esempi di configurazione delle citazioni dei file [#file-citation-configuration-examples]

**Configurazione predefinita (Bilanciata)**

```yaml
endpoints:
  agents:
    maxCitations: 30
    maxCitationsPerFile: 7
    minRelevanceScore: 0.45
```

Fornisce citazioni complete evitando risposte eccessive e filtrando le corrispondenze di bassa qualità.

**Configurazione rigorosa (Alta qualità)**

```yaml
endpoints:
  agents:
    maxCitations: 10
    maxCitationsPerFile: 3
    minRelevanceScore: 0.7
```

Include solo citazioni altamente pertinenti con limiti rigorosi per risposte mirate.

**Configurazione completa (Ricerca)**

```yaml
endpoints:
  agents:
    maxCitations: 50
    maxCitationsPerFile: 10
    minRelevanceScore: 0.0
```

Estrazione massima di informazioni per attività di ricerca esaustive, includendo tutte le fonti indipendentemente dalla loro rilevanza.

## Funzionalità degli Agent [#agent-capabilities]

Il campo `capabilities` ti consente di abilitare o disabilitare funzionalità specifiche per gli agenti. Le funzionalità disponibili sono:

- **deferred_tools**: consente agli agenti di scoprire gli strumenti MCP differiti in fase di esecuzione invece di caricare ogni strumento nel contesto in anticipo.
- **programmatic_tools**: Abilita la chiamata a strumenti programmatici (Programmatic Tool Calling) per gli strumenti MCP contrassegnati come Programmatic nel generatore di agenti (Agent Builder). Richiede `execute_code` e un deployment di Code Interpreter con il componente Tool Call Server. Questa funzionalità è facoltativa e non è abilitata per impostazione predefinita.
- **execute_code**: Consente all'agente di eseguire codice.
- **stateful_code_sessions**: consente agli agenti che hanno aderito di riutilizzare un workspace di Code Interpreter per conversazione. Richiede `execute_code`, una distribuzione di Code Interpreter compatibile e l'impostazione avanzata per singolo agente. Questa funzionalità è altamente sperimentale, non è abilitata per impostazione predefinita e potrebbe subire modifiche sostanziali durante la fase di sperimentazione; non considerare il suo comportamento o la sua configurazione attuale come un contratto di produzione stabile.
- **file_search**: Abilita l'agente a cercare e interagire con i file. Quando abilitato, il comportamento delle citazioni è controllato dalle impostazioni `maxCitations`, `maxCitationsPerFile` e `minRelevanceScore`.
- **web_search**: Abilita la funzionalità di ricerca web per gli agenti, consentendo loro di cercare e recuperare informazioni da internet.
- **artifacts**: Abilita l'agente a generare artefatti interattivi (componenti React, HTML, diagrammi Mermaid).
- **subagents**: Abilita l'esecuzione di agenti secondari in contesti isolati. Vedi [Subagents](/docs/features/subagents).
- **actions**: Consente all'agente di eseguire azioni predefinite.
- **context**: Abilita la funzionalità "Carica come testo" nella chat e il "Contesto file" per gli agenti, consentendo agli utenti di caricare file e far sì che il loro contenuto venga estratto e incluso direttamente nella conversazione.
- **skills**: Abilita le Skills nel pannello laterale, l'invocazione manuale tramite `$`, le skills invocate dal modello e le allowlist delle skills per gli agenti. Vedi [Skills](/docs/features/skills).
- **memory**: Consente agli agenti di utilizzare `set_memory` e `delete_memory` quando la memoria è configurata e l'utente ha l'accesso. Abilitato per impostazione predefinita; rimosso in fase di esecuzione quando la memoria è disabilitata.
- **ask_user_question**: Consente agli agenti di mettere in pausa per porre da una a quattro domande correlate in un unico modulo e riprendere con le risposte. Abilitato per impostazione predefinita.
- **tools**: Concede all'agente l'accesso a vari strumenti.
- **chain**: Abilita la funzionalità Beta per il concatenamento degli agenti, nota anche come flussi di lavoro Mixture-of-Agents (MoA).
- **ocr**: Migliora facoltativamente "Carica come testo" nella chat e il "Contesto file" per gli agenti, consentendo ai file di essere caricati ed elaborati tramite OCR. **Richiede la configurazione di un servizio OCR.**
- **run_in_background**: Rende l'esecuzione del Code Interpreter e degli strumenti shell idonei all'esecuzione in background per impostazione predefinita e abilita l'opt-in in background per singolo strumento per gli strumenti MCP. Un agente può esplicitamente escludere il Code Interpreter. L'impostazione consente l'esecuzione in background; il modello decide per ogni chiamata se utilizzarla. Richiede `execute_code` e una distribuzione di Code Interpreter configurata per il codice. Questa funzionalità non è abilitata per impostazione predefinita.
- **tool_intents**: Aggiunge etichette di intento scritte dal modello in tempo reale alle chiamate agli strumenti idonei. Gli strumenti nativi, inclusi gli strumenti Code Interpreter, si attivano automaticamente; gli strumenti MCP possono essere abilitati singolarmente o in blocco nell'Agent Builder. Le Model Specs utilizzano `describeIntent`. Questa funzionalità non è abilitata per impostazione predefinita.

Specificando le funzionalità, puoi controllare le caratteristiche disponibili agli utenti quando interagiscono con gli agenti.

## Esempio di configurazione [#example-configuration]

Ecco un esempio di configurazione dell'endpoint `agents` con funzionalità personalizzate e impostazioni di citazione dei file:

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    disableBuilder: false
    # File citation configuration
    maxCitations: 20
    maxCitationsPerFile: 5
    minRelevanceScore: 0.6
    # Custom capabilities
    capabilities:
      # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
      # - 'programmatic_tools'
      - 'execute_code'
      # Optional: makes Code Interpreter background-eligible and enables selected MCP tools.
      # - 'run_in_background'
      # Optional: enables live intent labels for native and selected MCP tools.
      # - 'tool_intents'
      - 'file_search'
      - 'skills'
      - 'subagents'
      - 'actions'
      - 'artifacts'
      - 'context'
      - 'ocr'
      - 'web_search'
```

In questo esempio:

- L'interfaccia del builder è abilitata
- Le citazioni dei file sono limitate a un totale di 20, con un massimo di 5 per file
- Vengono incluse solo le fonti con una rilevanza pari o superiore al 60%
- Gli agenti LibreChat hanno accesso all'esecuzione di codice, alla ricerca di file (con citazioni), a Skills, Subagents, azioni, artefatti, contesto dei file, servizi OCR se configurati e funzionalità di ricerca web.
- Il Programmatic Tool Calling rimane disabilitato a meno che non si aggiunga la capability `programmatic_tools` insieme a `execute_code`
- Le chiamate agli strumenti in background rimangono disabilitate a meno che non si aggiunga `run_in_background`; gli strumenti di Code Interpreter diventano quindi idonei per impostazione predefinita, mentre gli strumenti MCP richiedono ancora una selezione per singolo strumento.
- Le etichette di intent degli strumenti rimangono disabilitate a meno che non si aggiunga `tool_intents`; gli strumenti nativi vengono quindi abilitati automaticamente, mentre gli strumenti MCP richiedono ancora una selezione per singolo strumento.

## remoteApi

Configurazione per l'autenticazione dell'API Remote Agent. Controlla come i servizi esterni si autenticano quando chiamano gli endpoint dell'API Agents.

### remoteApi.auth

<OptionTable
  options={[
    [
      'auth',
      'Object',
      'Configurazione dell\'autenticazione per la Remote Agent API.',
      'Supports API key and/or OIDC Bearer token authentication. If omitted, only API key auth is active.',
    ],
  ]}
/>

#### remoteApi.auth.apiKey

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Abilita l\'autenticazione tramite chiave API per la Remote Agent API.',
      'When true, requests with a valid LibreChat API key are accepted. Can be used alongside or instead of OIDC.',
    ],
  ]}
/>

**Predefinito:** `true`

#### remoteApi.auth.oidc

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Abilita l\'autenticazione tramite token Bearer OIDC.',
      'When true, the middleware validates Bearer tokens against the configured OIDC issuer via JWKS.',
    ],
    [
      'issuer',
      'String',
      'URL dell\'issuer OIDC.',
      'The base URL of your OIDC provider, such as a Keycloak realm URL. Used for token issuer validation and JWKS discovery if jwksUri is not set.',
    ],
    [
      'jwksUri',
      'String',
      'URL dell\'endpoint JWKS. Opzionale.',
      'If omitted, resolved automatically via {issuer}/.well-known/openid-configuration. You can also set OPENID_JWKS_URL as an alternative.',
    ],
    [
      'audience',
      'String',
      'Pubblico del token previsto. Obbligatorio quando l\'autenticazione OIDC è abilitata.',
      'Tokens must contain this value in their aud claim.',
    ],
    [
      'scope',
      'String',
      'Valore di scope richiesto. Opzionale.',
      'If set, the token must contain this value in its scp or scope claim. Use this to distinguish token intent across different APIs.',
    ],
  ]}
/>

**Predefinito:** `enabled: false`

**Esempio - Solo OIDC:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: false
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          audience: my-client-id
```

**Esempio - OIDC con fallback alla chiave API:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: true
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          # jwksUri is optional and auto-discovered if omitted
          jwksUri: https://auth.example.com/realms/myrealm/protocol/openid-connect/certs
          audience: my-client-id
```

<Callout type="info">
  La priorità di risoluzione dell'URI JWKS è `jwksUri` esplicito, poi `OPENID_JWKS_URL`, e infine l'auto-discovery tramite `{issuer}/.well-known/openid-configuration`.
</Callout>

<Callout type="info">
  La corrispondenza utente OIDC utilizza il claim `sub` come ricerca primaria, con fallback ai claim `email`, `preferred_username` o `upn`. L'utente corrispondente deve già esistere in LibreChat.
</Callout>

## Subagenti [#subagents]

Il campo `subagents` controlla quali agenti secondari isolati un agente principale può generare quando la funzionalità `subagents` è disponibile.

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Aggiunge lo strumento di generazione di subagent a questo agente quando impostato su true. Predefinito: disabilitato.',
      'enabled: true',
    ],
    [
      'allowSelf',
      'Boolean',
      'Consente all\'agente di generarsi in un nuovo contesto isolato. Predefinito: true.',
      'allowSelf: true',
    ],
    [
      'agent_ids',
      'Array/List of Strings',
      'Agenti specifici che questo agente può generare. Limitato da `maxSubagents`, che ha come valore predefinito 10.',
      'agent_ids: ["agent_researcher"]',
    ],
  ]}
/>

```yaml filename="Agent subagents"
subagents:
  enabled: true
  allowSelf: true
  agent_ids:
    - 'agent_researcher'
    - 'agent_reviewer'
```

Per il comportamento e i limiti rivolti all'utente, consulta [Subagents](/docs/features/subagents).

## Note [#notes]

- Non è consigliato disabilitare l'interfaccia del builder a meno che non si stiano utilizzando i [modelSpecs](/docs/configuration/librechat_yaml/object_structure/model_specs) per definire un elenco di agenti tra cui scegliere.
- La configurazione delle citazioni dei file (`maxCitations`, `maxCitationsPerFile`, `minRelevanceScore`) si applica solo quando la funzionalità `file_search` è abilitata.
- Il punteggio di rilevanza viene calcolato utilizzando la similarità vettoriale, dove 1.0 rappresenta una corrispondenza perfetta e 0.0 rappresenta nessuna similarità.
- I limiti di citazione aiutano a bilanciare il recupero completo delle informazioni con la qualità e le prestazioni della risposta.
- La funzionalità `context` funziona senza configurazione OCR utilizzando metodi di analisi del testo. L'OCR migliora la qualità dell'estrazione quando configurato.
- La funzionalità `ocr` richiede che sia configurato un servizio OCR (vedi [Configurazione OCR](/docs/configuration/librechat_yaml/object_structure/ocr)).
- `maxSubagents` limita il numero di subagenti a cui un singolo agente può fare riferimento. Non modifica la profondità o i limiti del grafo documentati in [Subagents](/docs/features/subagents#limits).
