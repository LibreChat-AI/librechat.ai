---
title: Estructura del objeto del endpoint Agents
icon: Bot
---

Esta página se aplica al endpoint [`agents`](/docs/features/agents).

## Ejemplo [#example]

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    recursionLimit: 50
    maxRecursionLimit: 100
    maxToolCallArgBytes: 65536
    maxDeltaEventsPerTurn: 100000
    maxToolCallArgBytesByTool:
      create_file: 131072
    disableBuilder: false
    # (optional) Agent Capabilities available to all users. Omit the ones you wish to exclude. Defaults to list below.
    # Opt-in capabilities: "programmatic_tools", "stateful_code_sessions", "run_in_background", and "tool_intents".
    # capabilities: ["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]
    # (optional) File citation configuration for file_search capability
    maxCitations: 30 # Maximum total citations in responses (1-50)
    maxCitationsPerFile: 7 # Maximum citations from each file (1-10)
    minRelevanceScore: 0.45 # Minimum relevance score threshold (0.0-1.0)
    titleTiming: immediate
    activityLabel: true
    activityModel: gpt-4.1-nano
    activityPhaseLabel: true
    activityPhaseMaxPerRun: 5
    maxSubagents: 10 # Maximum explicit subagents per agent (1-50)
    skills:
      maxCatalogSkills: 20
    toolApproval:
      enabled: true
      mode: default
      deny: ['mcp:*:delete_*']
      ask: ['mcp:*:*']
    checkpointer:
      type: mongo
      ttl: 86400
    remoteApi:
      auth:
        oidc:
          enabled: false
```

> Esta configuración habilita la interfaz del constructor para agentes.

## recursionLimit

<OptionTable
  options={[
    [
      'recursionLimit',
      'Number',
      'Establece el número predeterminado de pasos que un agente puede realizar en una ejecución.',
      'Controls recursion depth to prevent infinite loops. When limit is reached, raises GraphRecursionError. This value can be configured from the UI up to the maxRecursionLimit.',
    ],
  ]}
/>

**Predeterminado:** `25`

**Ejemplo:**

```yaml filename="endpoints / agents / recursionLimit"
recursionLimit: 50
```

Para obtener más información sobre los pasos del agente, consulta [Max Agent Steps](/docs/features/agents#max-agent-steps).

## maxRecursionLimit

<OptionTable
  options={[
    [
      'maxRecursionLimit',
      'Number',
      'Establece el número máximo absoluto de pasos que un agente puede realizar en una ejecución.',
      'Defines the upper limit for the recursionLimit that can be set from the UI. This prevents users from setting excessively high values.',
    ],
  ]}
/>

**Predeterminado:** Si se omite, toma el valor de recursionLimit o 50 si recursionLimit también se omite.

**Ejemplo:**

```yaml filename="endpoints / agents / maxRecursionLimit"
maxRecursionLimit: 100
```

Para obtener más información sobre los pasos del agente, consulta [Max Agent Steps](/docs/features/agents#max-agent-steps).

## Disyuntores de flujo (Stream circuit breakers) [#stream-circuit-breakers]

Estos ajustes detienen la ejecución de un Agent cuando el flujo de un proveedor parece estar produciendo argumentos de herramienta descontrolados o una secuencia ilimitada de eventos delta.

<OptionTable
  options={[
    [
      'maxToolCallArgBytes',
      'Number',
      'Máximo de bytes acumulados para los argumentos de una llamada a herramienta transmitida. Establezca en 0 para desactivar la protección global.',
      '65536',
    ],
    [
      'maxDeltaEventsPerTurn',
      'Number',
      'Máximo de eventos transmitidos para un turno de generación de modelo. Establézcalo en 0 u omítalo para desactivar esta protección.',
      '0 (disabled)',
    ],
    [
      'maxToolCallArgBytesByTool',
      'Object',
      'Anulaciones de bytes de argumento por herramienta, indexadas por el nombre de la herramienta que ve el modelo. Un valor de 0 deshabilita la protección para esa herramienta.',
      'create_file: 131072',
    ],
  ]}
/>

LibreChat utiliza un límite global de argumentos de 64 KiB y ofrece una anulación de 128 KiB para `create_file`, cuyos payloads legítimos suelen ser más grandes. Las entradas en `maxToolCallArgBytesByTool` se fusionan sobre esa anulación integrada. Si cualquiera de los límites se supera, LibreChat aborta el flujo del proveedor en curso e informa qué límite fue excedido.

```yaml filename="endpoints / agents / stream circuit breakers"
maxToolCallArgBytes: 65536
maxDeltaEventsPerTurn: 100000
maxToolCallArgBytesByTool:
  create_file: 262144
  large_import: 524288
```

Para deshabilitar cada protección de tamaño de argumento, establezca tanto el límite global como la anulación `create_file` incluida en `0`:

```yaml
maxToolCallArgBytes: 0
maxToolCallArgBytesByTool:
  create_file: 0
```

## titleTiming

<OptionTable
  options={[
    [
      'titleTiming',
      'String',
      'Controla cuándo se generan los títulos de las conversaciones para el endpoint de agentes. Valores válidos: "immediate" o "final".',
      'Defaults to "immediate".',
    ],
  ]}
/>

**Predeterminado:** `"immediate"`

**Valores disponibles:**

- **`"immediate"`**: Genera el título tan pronto como comienza la solicitud, en paralelo con la respuesta del modelo, utilizando el primer mensaje del usuario.
- **`"final"`**: Aplaza la generación del título hasta que se complete la respuesta completa. Esto preserva el comportamiento heredado.

**Ejemplo:**

```yaml filename="endpoints / agents / titleTiming"
titleTiming: immediate
```

## activityLabel

Establezca `activityLabel: true` para agrupar cada bloque de razonamiento del Agente y llamadas a herramientas bajo un encabezado generado de una sola línea. Utilice `activityModel`, `activityEndpoint`, `activityPrompt`, `activityMaxPerRun` y `activityCharLimit` para controlar la llamada al modelo adicional y su costo. Consulte [Agent Activity Groups](/docs/configuration/librechat_yaml/object_structure/shared_endpoint_settings#agent-activity-groups) para obtener la referencia completa de los campos y las reglas de precedencia.

```yaml filename="endpoints / agents / activity groups"
activityLabel: true
activityEndpoint: openAI
activityModel: gpt-4.1-nano
activityMaxPerRun: 20
activityCharLimit: 600
activityPhaseLabel: true
activityPhaseMaxPerRun: 5
```

`activityPhaseLabel` puede agregar de forma independiente un resumen principal contraído alrededor de cada fase de ejecución que contenga al menos dos actividades lógicas. Utilice `activityPhaseModel`, `activityPhaseEndpoint`, `activityPhasePrompt` y `activityPhaseMaxPerRun` para ajustar esas llamadas; `activityCharLimit` se comparte con las etiquetas de actividad secundarias.

## disableBuilder

<OptionTable
  options={[
    [
      'disableBuilder',
      'Boolean',
      'Controla la visibilidad y el uso de la interfaz del constructor para agentes.',
      'When set to `true`, disables the builder interface for the agent, limiting direct manual interaction.',
    ],
  ]}
/>

**Predeterminado:** `false`

**Ejemplo:**

```yaml filename="endpoints / agents / disableBuilder"
disableBuilder: false
```

## allowedProviders

<OptionTable
  options={[
    [
      'allowedProviders',
      'Array/List of Strings',
      'Especifica una lista de proveedores de endpoint (por ejemplo, "openAI", "anthropic", "google") que tienen permitido su uso con la función de Agents.',
      'If defined, only agents configured with these providers can be initialized. If omitted or empty, all configured providers are allowed.',
    ],
  ]}
/>

**Predeterminado:** `[]` (lista vacía, todos los proveedores permitidos)

**Nota:** Debe ser uno de los siguientes, o un nombre de endpoint personalizado tal como se define en su [configuración](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#name): - `openAI, azureOpenAI, google, anthropic, assistants, azureAssistants, bedrock`

**Ejemplo:**

```yaml filename="endpoints / agents / allowedProviders"
allowedProviders:
  - openAI
  - google
```

## capabilities

<OptionTable
  options={[
    [
      'capabilities',
      'Array/List of Strings',
      'Especifica las capacidades de agente disponibles para todos los usuarios para el endpoint de agentes.',
      'Defines the agent capabilities that are available to all users for the agents endpoint. You can omit the capabilities you wish to exclude from the list.',
    ],
  ]}
/>

**Predeterminado:** `["deferred_tools", "execute_code", "file_search", "web_search", "artifacts", "subagents", "actions", "context", "skills", "memory", "ask_user_question", "tools", "chain", "ocr"]`

`programmatic_tools`, `stateful_code_sessions`, `run_in_background` y `tool_intents` son opcionales y deben agregarse explícitamente. Las herramientas programáticas y las sesiones con estado también requieren `execute_code` y un despliegue de Code Interpreter compatible. La ejecución de código en segundo plano requiere `run_in_background`, `execute_code` y Code Interpreter.

**Ejemplo:**

```yaml filename="endpoints / agents / capabilities"
capabilities:
  - 'deferred_tools'
  # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
  # Requires execute_code and a Code Interpreter deployment with the Tool Call Server component.
  # - 'programmatic_tools'
  - 'execute_code'
  - 'file_search'
  - 'web_search'
  - 'artifacts'
  - 'subagents'
  - 'actions'
  - 'context'
  - 'skills'
  - 'memory'
  - 'ask_user_question'
  - 'tools'
  - 'chain'
  - 'ocr'
  # Optional: reuse one Code Interpreter workspace per conversation.
  # - 'stateful_code_sessions'
  # Optional: allow selected eligible MCP and Code Interpreter tools to run as background tasks.
  # - 'run_in_background'
  # Optional: show live model-written intent labels for native and selected MCP tools.
  # - 'tool_intents'
```

**Nota:** Este campo es opcional. Si se omite, el comportamiento predeterminado es incluir todas las capacidades enumeradas en el valor predeterminado.

## toolApproval

Controla la revisión humana para las llamadas a herramientas de Agent. Esta función está desactivada de forma predeterminada. Cuando está activada, las llamadas coincidentes se pausan hasta que el usuario envía una decisión permitida, como aprobar, rechazar o editar los argumentos.

<OptionTable
  options={[
    ['enabled', 'Boolean', 'Habilita la aprobación de herramientas para el endpoint Agents.', 'false'],
    [
      'mode',
      'String',
      'Establece el comportamiento para llamadas no coincidentes: `default` pregunta, `dontAsk` deniega y `bypass` aprueba a menos que se deniegue.',
      'default',
    ],
    ['allow', 'Array of strings', 'Patrones glob para llamadas que pueden ejecutarse sin solicitar confirmación.', ''],
    ['deny', 'Array of strings', 'Patrones glob para llamadas que siempre se deniegan. Las reglas de denegación tienen prioridad.', ''],
    ['ask', 'Array of strings', 'Patrones glob para llamadas que siempre requieren revisión.', ''],
    [
      'reason',
      'String',
      'Explicación opcional que se muestra en el aviso de aprobación. Usa `{tool}` para insertar el nombre de la herramienta.',
      '',
    ],
    [
      'hooks',
      'Array of objects',
      'Módulos de políticas programáticas de confianza para decisiones conscientes del contexto. Los Hooks solo pueden restringir la política estática.',
      '',
    ],
  ]}
/>

Los patrones utilizan coincidencia glob. Las herramientas MCP pueden ser delimitadas con `mcp:<server>:<tool>`; por ejemplo, `mcp:github:*` coincide con todas las herramientas del servidor `github`. La evaluación otorga a `deny` la prioridad más alta, incluso en modo `bypass`.

```yaml filename="endpoints / agents / toolApproval"
toolApproval:
  enabled: true
  mode: default
  allow:
    - 'mcp:analytics:read_*'
  deny:
    - 'mcp:*:delete_*'
  ask:
    - 'mcp:*:*'
  reason: 'Review {tool} before it runs.'
```

Los Hooks se cargan una vez al iniciar el servidor y se ejecutan en el proceso de LibreChat. Cada entrada acepta un `module`, un `matcher` regex opcional para el nombre de la herramienta y un objeto `options` opcional que se pasa al constructor del módulo. Solo cargue código en el que confíe. Las anulaciones por rol o inquilino no recargan los módulos de hook; implemente el comportamiento contextual dentro del hook.

```yaml filename="endpoints / agents / toolApproval / hooks"
toolApproval:
  enabled: true
  hooks:
    - module: '@acme/librechat-approval-hook'
      matcher: '^mcp:production:'
      options:
        requireTicket: true
```

## checkpointer

Configura el almacenamiento para las ejecuciones de Agentes pausadas por la aprobación de herramientas o Ask User. Cuando cualquiera de estas funciones requiere un checkpointer y este bloque se omite, LibreChat utiliza la base de datos MongoDB principal con una ventana de decisión de 24 horas.

<OptionTable
  options={[
    [
      'type',
      'String',
      '`mongo` persiste las ejecuciones pausadas entre reinicios y réplicas; `memory` es local al proceso y está destinado al desarrollo.',
      'mongo',
    ],
    ['ttl', 'Number', 'Cuánto tiempo espera una ejecución pausada para tomar una decisión, en segundos.', '86400'],
    [
      'checkpointCollectionName',
      'String',
      'Anulación opcional del nombre de la colección de puntos de control de MongoDB.',
      'agent_checkpoints',
    ],
    [
      'checkpointWritesCollectionName',
      'String',
      'Anulación opcional del nombre de la colección de puntos de control de escritura de MongoDB.',
      'agent_checkpoint_writes',
    ],
  ]}
/>

```yaml filename="endpoints / agents / checkpointer"
checkpointer:
  type: mongo
  ttl: 86400
```

Use `type: memory` solo para un despliegue de desarrollo de proceso único. Una ejecución pausada almacenada en la memoria no puede reanudarse después de un reinicio o en otra réplica.

Los puntos de control de MongoDB registran una advertencia cuando el estado serializado supera los 8 MiB y rechazan una pausa duradera por encima de los 15 MiB, dejando un margen por debajo del límite de documentos de 16 MiB de MongoDB. Los archivos multimedia integrados o las salidas de herramientas de gran tamaño son causas comunes; inicie una nueva conversación o reduzca su contexto si una ejecución informa `CHECKPOINT_TOO_LARGE`.

## skills

Controla la configuración de Skills a nivel de endpoint para los agentes.

<OptionTable
  options={[
    [
      'skills.maxCatalogSkills',
      'Number',
      'Limita el número de Skills accesibles y activas expuestas en el catálogo visible para el modelo. Debe estar entre 1 y 100.',
      'maxCatalogSkills: 20',
    ],
  ]}
/>

**Predeterminado:** Sin límite configurado más allá del límite del catálogo en tiempo de ejecución.

**Ejemplo:**

```yaml filename="endpoints / agents / skills"
skills:
  maxCatalogSkills: 20
```

Esto no deshabilita Skills. Utiliza la capacidad `skills` y el alcance de habilidades por agente/Model Specs para controlar si las Skills están disponibles.

## maxSubagents

<OptionTable
  options={[
    [
      'maxSubagents',
      'Number',
      'Número máximo de subagentes explícitos que un solo agente puede referenciar.',
      'Applies to the `agent_ids` allowlist on agents and on model specs, and to configured subagent `graphs`, which agents support but model specs do not. Must be a whole number between 1 and 50.',
    ],
  ]}
/>

**Predeterminado:** `10`

**Rango:** `1-50`

**Ejemplo:**

```yaml filename="endpoints / agents / maxSubagents"
endpoints:
  agents:
    maxSubagents: 20
```

Aumente este valor cuando una implementación ejecute agentes con una orquestación intensiva que necesiten delegar en más de diez hijos. El valor se lee al inicio y se aplica en tres lugares:

- Solicitudes de creación, actualización y duplicación de agentes, para `subagents.agent_ids` y `subagents.graphs`
- Las listas de permitidos (allowlists) de `subagents.agent_ids` en Model Specs, validadas en la misma pasada de configuración
- El panel de Agent Builder, que deja de aceptar nuevas entradas de subagentes en el valor configurado

Las Model Specs solo aceptan `enabled`, `allowSelf` y `agent_ids` bajo `subagents`. Los `graphs` de los subagentes son una característica a nivel de agente: una clave `graphs` en una Model Spec se descarta cuando se analiza la configuración, sin generar un error.

<Callout type="warning">
  `maxSubagents` debe ser un número entero entre 1 y 50. Un valor fuera de ese rango provoca un error en la validación de `librechat.yaml`, y LibreChat registra el error y se cierra al iniciarse en lugar de recurrir al valor predeterminado. Iniciar con `CONFIG_BYPASS_VALIDATION=true` omite el cierre, pero en ese caso se descarta toda la configuración personalizada, por lo que el límite vuelve a ser `10`. Para volver al valor predeterminado deliberadamente, elimine la clave: un `maxSubagents` omitido se resuelve en `10` sin errores.
</Callout>

<Callout type="note">
  El límite es a nivel de proceso y proviene de `librechat.yaml`. Una anulación en la base de datos por usuario o por grupo de `endpoints.agents.maxSubagents` se refleja en la configuración servida, pero no es aplicada por la validación de la solicitud.
</Callout>

Aumentar esto solo cambia cuántos subagentes puede referenciar un agente. Los límites de profundidad, nodo de grafo y configuración de ejecución no se ven afectados. Consulta [Subagents](/docs/features/subagents#limits).

## maxCitations

<OptionTable
  options={[
    [
      'maxCitations',
      'Number',
      'Controla el número total máximo de citas que se pueden incluir en una sola respuesta del agente.',
      'When using file_search capability, limits the total number of source citations returned to prevent overwhelming responses while ensuring comprehensive coverage.',
    ],
  ]}
/>

**Predeterminado:** `30`

**Rango:** `1-50`

**Ejemplo:**

```yaml filename="endpoints / agents / maxCitations"
maxCitations: 30
```

## maxCitationsPerFile

<OptionTable
  options={[
    [
      'maxCitationsPerFile',
      'Number',
      'Limita el número máximo de citas que se pueden extraer de cualquier archivo individual.',
      'Ensures citation diversity by preventing any single file from dominating the citations, encouraging representation from multiple sources.',
    ],
  ]}
/>

**Predeterminado:** `7`

**Rango:** `1-10`

**Ejemplo:**

```yaml filename="endpoints / agents / maxCitationsPerFile"
maxCitationsPerFile: 7
```

## minRelevanceScore

<OptionTable
  options={[
    [
      'minRelevanceScore',
      'Number',
      'Establece el umbral mínimo de puntuación de relevancia para que las fuentes se incluyan en las respuestas.',
      'Filters out low-quality matches based on vector similarity scores. Higher values (e.g., 0.7) ensure only highly relevant sources are cited, while lower values (e.g., 0.0) include all sources regardless of quality.',
    ],
  ]}
/>

**Predeterminado:** `0.45` (umbral de relevancia del 45%)

**Rango:** `0.0-1.0`

**Ejemplo:**

```yaml filename="endpoints / agents / minRelevanceScore"
minRelevanceScore: 0.45
```

### Ejemplos de configuración de citas de archivos [#file-citation-configuration-examples]

**Configuración predeterminada (Equilibrada)**

```yaml
endpoints:
  agents:
    maxCitations: 30
    maxCitationsPerFile: 7
    minRelevanceScore: 0.45
```

Proporciona citas exhaustivas a la vez que evita respuestas abrumadoras y filtra las coincidencias de baja calidad.

**Configuración estricta (Alta calidad)**

```yaml
endpoints:
  agents:
    maxCitations: 10
    maxCitationsPerFile: 3
    minRelevanceScore: 0.7
```

Solo incluye citas altamente relevantes con límites estrictos para respuestas enfocadas.

**Configuración integral (Investigación)**

```yaml
endpoints:
  agents:
    maxCitations: 50
    maxCitationsPerFile: 10
    minRelevanceScore: 0.0
```

Extracción máxima de información para tareas de investigación exhaustivas, incluyendo todas las fuentes independientemente de su relevancia.

## Capacidades de los agentes [#agent-capabilities]

El campo `capabilities` le permite habilitar o deshabilitar funcionalidades específicas para los agentes. Las capacidades disponibles son:

- **deferred_tools**: Permite a los agentes descubrir herramientas MCP diferidas en tiempo de ejecución en lugar de cargar cada herramienta en el contexto por adelantado.
- **programmatic_tools**: Habilita la llamada a herramientas programáticas (Programmatic Tool Calling) para herramientas MCP marcadas como Programmatic en el Agent Builder. Requiere `execute_code` y un despliegue de Code Interpreter con el componente Tool Call Server. Esta capacidad es opcional y no está habilitada de forma predeterminada.
- **execute_code**: Permite al agente ejecutar código.
- **stateful_code_sessions**: Permite que los agentes que hayan optado por esta función reutilicen un espacio de trabajo de Code Interpreter por conversación. Requiere `execute_code`, una implementación compatible de Code Interpreter y la configuración avanzada por agente. Esta capacidad es altamente experimental, no está habilitada de forma predeterminada y puede cambiar sustancialmente durante la experimentación; no trate su comportamiento o configuración actual como un contrato de producción estable.
- **file_search**: Permite al agente buscar e interactuar con archivos. Cuando está habilitado, el comportamiento de las citas se controla mediante los ajustes `maxCitations`, `maxCitationsPerFile` y `minRelevanceScore`.
- **web_search**: Habilita la funcionalidad de búsqueda web para los agentes, permitiéndoles buscar y recuperar información de internet.
- **artifacts**: Permite al agente generar artefactos interactivos (componentes de React, HTML, diagramas de Mermaid).
- **subagents**: Habilita ejecuciones de agentes secundarios con contexto aislado. Consulta [Subagents](/docs/features/subagents).
- **actions**: Permite al agente realizar acciones predefinidas.
- **context**: Habilita la funcionalidad "Subir como texto" en el chat y "Contexto de archivo" para los agentes, permitiendo a los usuarios subir archivos y que su contenido sea extraído e incluido directamente en la conversación.
- **skills**: Habilita Skills en el panel lateral, invocación manual mediante `$`, skills invocadas por el modelo y listas de permitidos de skills para agentes. Consulta [Skills](/docs/features/skills).
- **memory**: Permite a los agentes usar `set_memory` y `delete_memory` cuando la memoria está configurada y el usuario tiene acceso. Habilitado de forma predeterminada; se elimina en tiempo de ejecución cuando la memoria está desactivada.
- **ask_user_question**: Permite que los agentes hagan una pausa para realizar de una a cuatro preguntas relacionadas en un solo formulario y continuar con las respuestas. Habilitado de forma predeterminada.
- **tools**: Otorga al agente acceso a varias herramientas.
- **chain**: Habilita la función beta para el encadenamiento de agentes, también conocido como flujos de trabajo Mixture-of-Agents (MoA).
- **ocr**: Mejora opcionalmente "Subir como texto" en el chat y el "Contexto de archivo" para los agentes, permitiendo que los archivos sean subidos y procesados con OCR. **Requiere que se configure un servicio de OCR.**
- **run_in_background**: Hace que la ejecución de Code Interpreter y las herramientas de shell sean elegibles para ejecutarse en segundo plano de forma predeterminada y habilita la opción de segundo plano por herramienta para las herramientas MCP. Un agente puede excluir explícitamente a Code Interpreter. La configuración permite la ejecución en segundo plano; el modelo decide por cada llamada si desea utilizarla. Requiere `execute_code` y una implementación de Code Interpreter configurada para el código. Esta capacidad no está habilitada de forma predeterminada.
- **tool_intents**: Añade etiquetas de intención escritas por el modelo en tiempo real a las llamadas de herramientas elegibles. Las herramientas nativas, incluyendo las herramientas de Code Interpreter, se activan automáticamente; las herramientas MCP pueden habilitarse individualmente o de forma masiva en el Agent Builder. Model specs utiliza `describeIntent`. Esta capacidad no está habilitada de forma predeterminada.

Al especificar las capacidades, puedes controlar las funciones disponibles para los usuarios al interactuar con agentes.

## Ejemplo de configuración [#example-configuration]

Aquí hay un ejemplo de cómo configurar el endpoint `agents` con capacidades personalizadas y ajustes de citación de archivos:

```yaml filename="Agents Endpoint"
endpoints:
  agents:
    disableBuilder: false
    # File citation configuration
    maxCitations: 20
    maxCitationsPerFile: 5
    minRelevanceScore: 0.6
    # Custom capabilities
    capabilities:
      # Optional: enables Programmatic Tool Calling for MCP tools marked Programmatic in the Agent Builder.
      # - 'programmatic_tools'
      - 'execute_code'
      # Optional: makes Code Interpreter background-eligible and enables selected MCP tools.
      # - 'run_in_background'
      # Optional: enables live intent labels for native and selected MCP tools.
      # - 'tool_intents'
      - 'file_search'
      - 'skills'
      - 'subagents'
      - 'actions'
      - 'artifacts'
      - 'context'
      - 'ocr'
      - 'web_search'
```

En este ejemplo:

- La interfaz del constructor está habilitada
- Las citas de archivos están limitadas a un total de 20, con un máximo de 5 por archivo
- Solo se incluyen las fuentes con una relevancia del 60% o superior
- Los agentes de LibreChat tienen acceso a ejecución de código, búsqueda de archivos (con citas), Skills, Subagentes, acciones, artefactos, contexto de archivos, servicios de OCR si están configurados, y capacidades de búsqueda web.
- La invocación de herramientas programática permanece deshabilitada a menos que añada la capacidad `programmatic_tools` junto con `execute_code`
- Las llamadas a herramientas en segundo plano permanecen deshabilitadas a menos que agregues `run_in_background`; las herramientas de Code Interpreter se vuelven elegibles de forma predeterminada, mientras que las herramientas MCP aún requieren una selección por herramienta.
- Las etiquetas de intención de herramientas permanecen desactivadas a menos que añadas `tool_intents`; las herramientas nativas se activan automáticamente, mientras que las herramientas MCP aún requieren una selección por herramienta.

## remoteApi

Configuración para la autenticación de la API de agentes remotos. Controla cómo se autentican los servicios externos al llamar a los endpoints de la API de agentes.

### remoteApi.auth

<OptionTable
  options={[
    [
      'auth',
      'Object',
      'Configuración de autenticación para la Remote Agent API.',
      'Supports API key and/or OIDC Bearer token authentication. If omitted, only API key auth is active.',
    ],
  ]}
/>

#### remoteApi.auth.apiKey

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Habilitar la autenticación mediante clave API para la Remote Agent API.',
      'When true, requests with a valid LibreChat API key are accepted. Can be used alongside or instead of OIDC.',
    ],
  ]}
/>

**Predeterminado:** `true`

#### remoteApi.auth.oidc

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Habilitar la autenticación mediante token Bearer OIDC.',
      'When true, the middleware validates Bearer tokens against the configured OIDC issuer via JWKS.',
    ],
    [
      'issuer',
      'String',
      'URL del emisor OIDC.',
      'The base URL of your OIDC provider, such as a Keycloak realm URL. Used for token issuer validation and JWKS discovery if jwksUri is not set.',
    ],
    [
      'jwksUri',
      'String',
      'URL del endpoint JWKS. Opcional.',
      'If omitted, resolved automatically via {issuer}/.well-known/openid-configuration. You can also set OPENID_JWKS_URL as an alternative.',
    ],
    [
      'audience',
      'String',
      'Audiencia de token esperada. Requerido cuando la autenticación OIDC está habilitada.',
      'Tokens must contain this value in their aud claim.',
    ],
    [
      'scope',
      'String',
      'Valor de alcance requerido. Opcional.',
      'If set, the token must contain this value in its scp or scope claim. Use this to distinguish token intent across different APIs.',
    ],
  ]}
/>

**Predeterminado:** `enabled: false`

**Ejemplo - Solo OIDC:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: false
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          audience: my-client-id
```

**Ejemplo - OIDC con respaldo de API key:**

```yaml filename="endpoints / agents / remoteApi"
endpoints:
  agents:
    remoteApi:
      auth:
        apiKey:
          enabled: true
        oidc:
          enabled: true
          issuer: https://auth.example.com/realms/myrealm
          # jwksUri is optional and auto-discovered if omitted
          jwksUri: https://auth.example.com/realms/myrealm/protocol/openid-connect/certs
          audience: my-client-id
```

<Callout type="info">
  La prioridad de resolución de JWKS URI es `jwksUri` explícito, luego `OPENID_JWKS_URL`, y finalmente la detección automática a través de `{issuer}/.well-known/openid-configuration`.
</Callout>

<Callout type="info">
  La coincidencia de usuarios OIDC utiliza la reclamación `sub` como búsqueda principal, con respaldo a las reclamaciones `email`, `preferred_username` o `upn`. El usuario coincidente ya debe existir en LibreChat.
</Callout>

## Subagentes [#subagents]

El campo `subagents` controla qué agentes secundarios aislados puede generar un agente principal cuando la capacidad `subagents` está disponible.

<OptionTable
  options={[
    [
      'enabled',
      'Boolean',
      'Añade la herramienta de generación de subagentes a este agente cuando es true. Predeterminado: desactivado.',
      'enabled: true',
    ],
    [
      'allowSelf',
      'Boolean',
      'Permite que el agente se genere a sí mismo en un contexto aislado nuevo. Predeterminado: true.',
      'allowSelf: true',
    ],
    [
      'agent_ids',
      'Array/List of Strings',
      'Agentes específicos que este agente puede generar. Limitado por `maxSubagents`, cuyo valor predeterminado es 10.',
      'agent_ids: ["agent_researcher"]',
    ],
  ]}
/>

```yaml filename="Agent subagents"
subagents:
  enabled: true
  allowSelf: true
  agent_ids:
    - 'agent_researcher'
    - 'agent_reviewer'
```

Para el comportamiento y los límites orientados al usuario, consulte [Subagents](/docs/features/subagents).

## Notas [#notes]

- No se recomienda deshabilitar la interfaz del generador a menos que esté utilizando [modelSpecs](/docs/configuration/librechat_yaml/object_structure/model_specs) para definir una lista de agentes para elegir.
- La configuración de citas de archivos (`maxCitations`, `maxCitationsPerFile`, `minRelevanceScore`) solo se aplica cuando la capacidad `file_search` está habilitada.
- La puntuación de relevancia se calcula utilizando similitud vectorial, donde 1.0 representa una coincidencia perfecta y 0.0 representa ninguna similitud.
- Los límites de citas ayudan a equilibrar la recuperación integral de información con la calidad y el rendimiento de la respuesta.
- La capacidad `context` funciona sin configuración de OCR utilizando métodos de análisis de texto. El OCR mejora la calidad de la extracción cuando está configurado.
- La capacidad `ocr` requiere que se configure un servicio de OCR (consulte [Configuración de OCR](/docs/configuration/librechat_yaml/object_structure/ocr)).
- `maxSubagents` limita cuántos subagentes puede referenciar un solo agente. No cambia los límites de profundidad o de grafo documentados en [Subagents](/docs/features/subagents#limits).
