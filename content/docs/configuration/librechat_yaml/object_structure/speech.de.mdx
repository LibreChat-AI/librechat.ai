---
title: Sprachkonfiguration
icon: Mic
---

## Übersicht [#overview]

Das `speech`-Objekt ermöglicht es Ihnen, Text-to-Speech (TTS)- und Speech-to-Text (STT)-Anbieter direkt in Ihrer `librechat.yaml`-Konfigurationsdatei zu konfigurieren. Dies ermöglicht serverseitige Sprachdienste, ohne dass Benutzer ihre eigenen API-Schlüssel konfigurieren müssen.

**Felder unter `speech`:**

- `tts` - Konfigurationen für Text-to-Speech-Anbieter
- `stt` – Konfigurationen für Speech-to-Text-Anbieter
- `speechTab` - Standard-UI-Einstellungen für Sprachfunktionen

**Hinweise:**

- Mehrere Anbieter können gleichzeitig konfiguriert werden
- Benutzer können ihren bevorzugten Anbieter aus den verfügbaren Optionen auswählen.
- API-Schlüssel in der Konfigurationsdatei sollten aus Sicherheitsgründen Umgebungsvariablen-Referenzen verwenden

## Beispiel [#example]

```yaml filename="speech"
speech:
  tts:
    allowedAddresses: ["tts.internal:8080"]
    openai:
      apiKey: "${TTS_API_KEY}"
      model: "tts-1"
      voices: ["alloy", "echo", "fable", "onyx", "nova", "shimmer"]
    elevenlabs:
      apiKey: "${ELEVENLABS_API_KEY}"
      model: "eleven_multilingual_v2"
      voices: ["voice-id-1", "voice-id-2"]
  stt:
    allowedAddresses: ["stt.internal:8080"]
    openai:
      apiKey: "${STT_API_KEY}"
      model: "whisper-1"
  speechTab:
    conversationMode: true
    advancedMode: false
    speechToText: true
    textToSpeech: true
```

---

## SSRF-Schutz [#ssrf-protection]

LibreChat schützt vom Betreiber bereitgestellte STT- und TTS-URLs zum Zeitpunkt der Verbindung. Private, Loopback-, Link-Local- und Cloud-Metadata-Ziele werden standardmäßig blockiert und Weiterleitungen für Sprachanfragen sind deaktiviert.

`speech.tts.allowedAddresses` und `speech.stt.allowedAddresses` sind separate Ausnahmelisten, keine strikten Positivlisten (Allowlists). Fügen Sie einen exakten privaten `host:port`, `private.ip:port` oder `[ipv6]:port` nur dann hinzu, wenn dieser Abschnitt einen vertrauenswürdigen selbst gehosteten Dienst erreichen muss. Verwenden Sie keine URL, keinen Pfad, keinen CIDR-Bereich, keinen reinen Host/IP oder eine öffentliche IP-Adresse. Standard-Ports werden normalisiert, daher wird eine HTTPS-URL ohne expliziten Port als Port `443` geprüft.

Ein ausgenommener Hostname vertraut jeder privaten Adresse, zu der er auf diesem Port aufgelöst wird. Bevorzugen Sie nach Möglichkeit ein privates IP-Literal und listen Sie nur Namen auf, deren DNS Sie kontrollieren und die nicht von einem Angreifer umgeleitet werden können.

Wenn ein Forward-Proxy eine Hostnamen-Anfrage weiterleitet, führt der Proxy die Ziel-DNS-Auflösung und den Egress durch, weshalb der Proxy seine eigene SSRF-Richtlinie durchsetzen muss. Ein buchstäbliches privates Ziel wird weiterhin vor der Proxy-Verarbeitung blockiert, es sei denn, seine genaue Adresse und sein Port sind ausgenommen.

---

## tts

Das `tts` Objekt konfiguriert Text-to-Speech-Anbieter. Es können mehrere Anbieter konfiguriert werden, und Benutzer können wählen, welchen sie verwenden möchten.

<OptionTable
  options={[
    [
      'allowedAddresses',
      'Array of Strings',
      'Vertrauenswürdige private Host:Port-Ausnahmen für TTS-Verbindungs-SSRF-Prüfungen. Öffentliche Ziele bleiben verfügbar.',
      'allowedAddresses: ["tts.internal:8080"]',
    ],
  ]}
/>

### openai

OpenAI TTS-Konfiguration unter Verwendung von Modellen wie `tts-1` oder `tts-1-hd`.

<OptionTable
  options={[
    ['url', 'String', 'Benutzerdefinierte API-URL (optional). Zu verwenden für OpenAI-kompatible endpoints.', ''],
    ['apiKey', 'String', 'OpenAI API-Schlüssel. Verwenden Sie den Umgebungsvariablen-Verweis.', 'Required'],
    ['model', 'String', 'Zu verwendendes TTS-Modell (z. B. "tts-1", "tts-1-hd").', 'Required'],
    ['voices', 'Array of Strings', 'Verfügbare Sprachoptionen, die Benutzer auswählen können.', 'Required'],
  ]}
/>

**Beispiel:**
```yaml filename="speech / tts / openai"
tts:
  openai:
    apiKey: "${TTS_API_KEY}"
    model: "tts-1"
    voices: ["alloy", "echo", "fable", "onyx", "nova", "shimmer"]
```

### azureOpenAI

Azure OpenAI TTS Konfiguration.

<OptionTable
  options={[
    ['instanceName', 'String', 'Azure OpenAI Instanzname.', 'Required'],
    ['apiKey', 'String', 'Azure OpenAI API-Schlüssel.', 'Required'],
    ['deploymentName', 'String', 'Der Name der Bereitstellung für das TTS-Modell.', 'Required'],
    ['apiVersion', 'String', 'Azure OpenAI API-Version.', 'Required'],
    ['model', 'String', 'TTS-Modellkennung.', 'Required'],
    ['voices', 'Array of Strings', 'Verfügbare Sprachoptionen.', 'Required'],
  ]}
/>

**Beispiel:**
```yaml filename="speech / tts / azureOpenAI"
tts:
  azureOpenAI:
    instanceName: "my-azure-instance"
    apiKey: "${AZURE_TTS_API_KEY}"
    deploymentName: "tts-deployment"
    apiVersion: "2024-02-15-preview"
    model: "tts-1"
    voices: ["alloy", "echo", "nova"]
```

### elevenlabs

ElevenLabs TTS-Konfiguration für hochwertige Sprachsynthese.

<OptionTable
  options={[
    ['url', 'String', 'Benutzerdefinierte API-URL (optional).', ''],
    ['websocketUrl', 'String', 'WebSocket-URL für Streaming (optional).', ''],
    ['apiKey', 'String', 'ElevenLabs API-Schlüssel.', 'Required'],
    ['model', 'String', 'ElevenLabs-Modell (z. B. "eleven_multilingual_v2").', 'Required'],
    ['voices', 'Array of Strings', 'Für die Auswahl verfügbare Voice IDs.', 'Required'],
    ['voice_settings', 'Object', 'Einstellungen zur Stimmenanpassung (optional).', ''],
    ['pronunciation_dictionary_locators', 'Array of Strings', 'IDs für Aussprachewörterbücher (optional).', ''],
  ]}
/>

**voice_settings Unter-Keys:**
<OptionTable
  options={[
    ['similarity_boost', 'Number', 'Verbesserung der Stimmenähnlichkeit (0-1).', ''],
    ['stability', 'Number', 'Sprachstabilität (0-1).', ''],
    ['style', 'Number', 'Stil-Übertreibung (0-1).', ''],
    ['use_speaker_boost', 'Boolean', 'Lautsprecherverstärkung aktivieren.', ''],
  ]}
/>

**Beispiel:**
```yaml filename="speech / tts / elevenlabs"
tts:
  elevenlabs:
    apiKey: "${ELEVENLABS_API_KEY}"
    model: "eleven_multilingual_v2"
    voices: ["21m00Tcm4TlvDq8ikWAM", "AZnzlk1XvdvUeBnXmlld"]
    voice_settings:
      stability: 0.5
      similarity_boost: 0.75
      use_speaker_boost: true
```

### localai

LocalAI TTS-Konfiguration für selbst gehostete Sprachsynthese.

<OptionTable
  options={[
    ['url', 'String', 'LocalAI-Server-URL.', 'Required'],
    ['apiKey', 'String', 'API-Schlüssel, falls die Authentifizierung aktiviert ist (optional).', ''],
    ['voices', 'Array of Strings', 'Verfügbare Sprachmodelle.', 'Required'],
    ['backend', 'String', 'Zu verwendendes TTS-Backend (z. B. "piper").', 'Required'],
  ]}
/>

**Beispiel:**
```yaml filename="speech / tts / localai"
tts:
  localai:
    url: "http://localhost:8080"
    voices: ["en-us-amy-low", "en-us-danny-low"]
    backend: "piper"
```

---

## stt

Das `stt` Objekt konfiguriert Speech-to-Text-Anbieter.

<OptionTable
  options={[
    [
      'allowedAddresses',
      'Array of Strings',
      'Vertrauenswürdige private Host:Port-Ausnahmen für STT-Verbindungszeit-SSRF-Prüfungen. Öffentliche Ziele bleiben verfügbar.',
      'allowedAddresses: ["stt.internal:8080"]',
    ],
  ]}
/>

### openai [#openai-1]

OpenAI Whisper STT Konfiguration.

<OptionTable
  options={[
    ['url', 'String', 'Benutzerdefinierte API-URL (optional). Zu verwenden für OpenAI-kompatible endpoints.', ''],
    ['apiKey', 'String', 'OpenAI API-Schlüssel. Verwenden Sie den Umgebungsvariablen-Verweis.', 'Required'],
    ['model', 'String', 'Zu verwendendes STT-Modell (z. B. "whisper-1").', 'Required'],
  ]}
/>

**Beispiel:**
```yaml filename="speech / stt / openai"
stt:
  openai:
    apiKey: "${STT_API_KEY}"
    model: "whisper-1"
```

### azureOpenAI [#azureopenai-1]

Azure OpenAI Whisper STT Konfiguration.

<OptionTable
  options={[
    ['instanceName', 'String', 'Azure OpenAI Instanzname.', 'Required'],
    ['apiKey', 'String', 'Azure OpenAI API-Schlüssel.', 'Required'],
    ['deploymentName', 'String', 'Der Bereitstellungsname für das Whisper-Modell.', 'Required'],
    ['apiVersion', 'String', 'Azure OpenAI API-Version.', 'Required'],
  ]}
/>

**Beispiel:**
```yaml filename="speech / stt / azureOpenAI"
stt:
  azureOpenAI:
    instanceName: "my-azure-instance"
    apiKey: "${AZURE_STT_API_KEY}"
    deploymentName: "whisper-deployment"
    apiVersion: "2024-02-15-preview"
```

---

## speechTab

Das `speechTab`-Objekt konfiguriert die Standard-UI-Einstellungen für Sprachfunktionen. Diese Einstellungen steuern, was Benutzer standardmäßig im Bereich für Spracheinstellungen sehen.

<OptionTable
  options={[
    ['conversationMode', 'Boolean', 'Konversationsmodus standardmäßig aktivieren.', 'false'],
    ['advancedMode', 'Boolean', 'Erweiterte Spracheinstellungen standardmäßig anzeigen.', 'false'],
    ['speechToText', 'Boolean or Object', 'STT standardmäßig aktivieren oder detaillierte STT-Einstellungen konfigurieren.', 'false'],
    ['textToSpeech', 'Boolean or Object', 'TTS standardmäßig aktivieren oder detaillierte TTS-Einstellungen konfigurieren.', 'false'],
  ]}
/>

### speechToText (Objektformat) [#speechtotext-object-format]

Bei der Verwendung eines Objekts anstelle eines booleschen Werts:

<OptionTable
  options={[
    [
      'engineSTT',
      'String',
      'Standard-STT-Engine. Verwenden Sie `"browser"` oder `"external"`. Die Legacy-Werte `"openai"` und `"azureOpenAI"` werden akzeptiert und zu `"external"` normalisiert.',
      'browser',
    ],
    ['languageSTT', 'String', 'Standardsprache für STT.', ''],
    ['autoTranscribeAudio', 'Boolean', 'Das Mikrofon eingeschaltet lassen, anstatt bei der ersten Pause zu stoppen. Mit einer externen Engine wird zudem die Stilleerkennung basierend auf `decibelValue` aktiviert.', ''],
    ['decibelValue', 'Number', 'Dezibel-Schwellenwert für die Stilleerkennung. Bereich -100 bis -30, Standard -45.', ''],
    ['autoSendText', 'Number', 'Sekunden, die nach der Transkription vor dem automatischen Senden gewartet werden soll. `0` sendet sofort; `-1` deaktiviert das automatische Senden.', ''],
  ]}
/>

### textToSpeech (Objektformat) [#texttospeech-object-format]

Bei der Verwendung eines Objekts anstelle eines booleschen Werts:

<OptionTable
  options={[
    [
      'engineTTS',
      'String',
      'Standard-TTS-Engine. Verwenden Sie `"browser"` oder `"external"`. Legacy-Anbieterwerte werden akzeptiert und zu `"external"` normalisiert.',
      'browser',
    ],
    ['voice', 'String', 'Standard-Stimmwahl.', ''],
    ['languageTTS', 'String', 'Standardsprache für TTS.', ''],
    ['automaticPlayback', 'Boolean', 'TTS-Antworten automatisch abspielen.', ''],
    ['playbackRate', 'Number', 'Standard-Wiedergabegeschwindigkeit (1.0 = normal). Bereich: 0.25–4.0.', ''],
    ['cacheTTS', 'Boolean', 'TTS-Audio für wiederholte Wiedergabe zwischenspeichern.', ''],
  ]}
/>

**Beispiel:**
```yaml filename="speech / speechTab"
speechTab:
  conversationMode: false
  advancedMode: false
  speechToText:
    engineSTT: "external"
    autoTranscribeAudio: true
    decibelValue: -45
  textToSpeech:
    engineTTS: "external"
    voice: "nova"
    automaticPlayback: false
    playbackRate: 1.0
    cacheTTS: true
```

---

## Vollständiges Beispiel [#complete-example]

```yaml filename="librechat.yaml"
version: 1.3.15
cache: true

speech:
  tts:
    openai:
      apiKey: "${TTS_API_KEY}"
      model: "tts-1-hd"
      voices: ["alloy", "echo", "fable", "onyx", "nova", "shimmer"]
    elevenlabs:
      apiKey: "${ELEVENLABS_API_KEY}"
      model: "eleven_multilingual_v2"
      voices: ["21m00Tcm4TlvDq8ikWAM", "AZnzlk1XvdvUeBnXmlld"]
      voice_settings:
        stability: 0.5
        similarity_boost: 0.75
  stt:
    openai:
      apiKey: "${STT_API_KEY}"
      model: "whisper-1"
  speechTab:
    conversationMode: false
    advancedMode: false
    speechToText: true
    textToSpeech:
      engineTTS: "external"
      voice: "nova"
      automaticPlayback: false
```

---

## Hinweise [#notes]

- Verwenden Sie in Konfigurationsdateien immer Umgebungsvariablen-Referenzen (z. B. `${API_KEY}`) für API-Schlüssel.
- Es können mehrere TTS-Anbieter konfiguriert werden; Benutzer wählen ihre bevorzugte Option in der UI aus.
- Die `speechTab`-Einstellungen definieren Standardwerte, die Benutzer in ihren persönlichen Einstellungen überschreiben können.
- `browser` verwendet die integrierte Sprachunterstützung des Browsers; `external` verwendet die serverseitigen Anbieter, die unter `speech.stt` oder `speech.tts` konfiguriert sind.
- Bestehende `openai`- und `azureOpenAI`-STT-Standardeinstellungen sowie `openai`-, `azureOpenAI`-, `elevenlabs`- und `localai`-TTS-Standardeinstellungen werden zu `external` migriert; eine nicht verfügbare externe Engine greift auf `browser` zurück.
- Für eine detaillierte Funktionsdokumentation siehe [Speech to Text & Text to Speech](/docs/configuration/stt_tts)
