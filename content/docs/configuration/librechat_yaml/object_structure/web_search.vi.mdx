---
title: Cấu hình Tìm kiếm Web
icon: Search
---

Cấu hình `webSearch` cho phép bạn tùy chỉnh chức năng tìm kiếm web trong LibreChat, bao gồm các nhà cung cấp dịch vụ tìm kiếm, công cụ thu thập nội dung và công cụ xếp hạng lại kết quả.

## Tổng quan [#overview]

Tính năng tìm kiếm web bao gồm ba thành phần chính:

1. **Search Providers**: Các dịch vụ thực hiện tìm kiếm web ban đầu
2. **Scrapers**: Các dịch vụ trích xuất nội dung từ các trang web
3. **Rerankers**: Các dịch vụ sắp xếp lại kết quả tìm kiếm để có độ liên quan tốt hơn

## Ví dụ [#example]

```yaml filename="webSearch"
webSearch:
  # Search Provider Configuration
  serperApiKey: "${SERPER_API_KEY}"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}"
  searchProvider: "serper" # Options: "serper", "searxng", "tavily", "keenable"

  # Optional: query options sent to a SearXNG instance
  searxngSearchOptions:
    engines: "google,bing,startpage" # Default: "google,bing,duckduckgo"
    language: "en" # Default: "all"

  # Tavily Configuration (search and/or scraper)
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"

  # Keenable Configuration (search and/or scraper; all credentials optional)
  keenableApiKey: "${KEENABLE_API_KEY}"
  keenableApiUrl: "${KEENABLE_API_URL}"
  keenableSearchOptions:
    maxResults: 8
    site: "example.com"
    attributionTitle: "LibreChat"
    timeout: 15000
  keenableScraperOptions:
    attributionTitle: "LibreChat"
    timeout: 15000

  # Scraper Configuration
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlApiUrl: "${FIRECRAWL_API_URL}"
  firecrawlVersion: "${FIRECRAWL_VERSION}"
  scraperProvider: "firecrawl" # Options: "firecrawl", "serper", "tavily", "keenable"

  # Reranker Configuration
  jinaApiKey: "${JINA_API_KEY}"
  jinaApiUrl: "${JINA_API_URL}"
  cohereApiKey: "${COHERE_API_KEY}"
  rerankerType: "jina" # Options: "jina", "cohere", "none"

  # General Settings
  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)
  # Required for deliberately private self-hosted provider endpoints
  allowedAddresses:
    - "searxng:8080"
```

## Bảo vệ SSRF và các nhà cung cấp riêng tư [#ssrf-protection-and-private-providers]

LibreChat xác thực từng kết nối mà nó mở để tìm kiếm web, thu thập dữ liệu (scraping) và xếp hạng lại (reranking). Các đích đến riêng tư, loopback, link-local và cloud-metadata bị chặn theo mặc định, bao gồm cả các bước chuyển hướng (redirect hops) và tên máy chủ phân giải thành địa chỉ riêng tư tại thời điểm kết nối.

Sử dụng `webSearch.allowedAddresses` khi một endpoint được cấu hình cho SearXNG, Firecrawl, Jina, tương thích với Tavily, tương thích với Keenable hoặc các nhà cung cấp liên quan là cố ý ở chế độ riêng tư:

```yaml filename="webSearch / allowedAddresses"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  allowedAddresses:
    - "searxng:8080"
    - "10.0.0.5:3002"
    - "[fd00::5]:8080"
```

Trường này là danh sách miễn trừ SSRF, không phải là danh sách cho phép nghiêm ngặt. Các đích đến công khai vẫn khả dụng. Các mục nhập phải là các cặp `host:port`, `private.ip:port` hoặc `[ipv6]:port` chính xác; các URL, đường dẫn, dải CIDR, host/IP thuần và các IP công khai cụ thể sẽ bị từ chối. Một tên máy chủ được miễn trừ sẽ tin tưởng bất kỳ IP riêng nào mà nó phân giải tới trên cổng đó, vì vậy hãy ưu tiên sử dụng IP riêng cụ thể và chỉ liệt kê những tên mà bạn kiểm soát DNS.

LibreChat tự động loại trừ một endpoint forward-proxy HTTP(S) đã được cấu hình để có thể truy cập được. Khi proxy thực hiện một yêu cầu, nó sẽ phân giải đích đến và phải tự thực thi việc kiểm soát lưu lượng truy cập ra ngoài (egress). `NO_PROXY` có thể thực hiện yêu cầu trực tiếp, nơi mà cơ chế bảo vệ kết nối của LibreChat vẫn được áp dụng. Cơ chế bảo vệ này bao gồm các kết nối mà LibreChat mở tới các API của nhà cung cấp; nó không thể hạn chế một URL trang mà trình thu thập dữ liệu (scraper) của bên thứ ba nhận được dưới dạng dữ liệu yêu cầu và tự tìm nạp trên cơ sở hạ tầng của riêng họ.

## Các nhà cung cấp tìm kiếm [#search-providers]

### searchProvider

<OptionTable
  options={[
    ['searchProvider', 'String', 'Chỉ định nhà cung cấp tìm kiếm sẽ sử dụng.', 'Options: "serper", "searxng", "tavily", "keenable"'],
  ]}
/>

### serperApiKey

<OptionTable
  options={[
    ['serperApiKey', 'String', 'Tên biến môi trường cho khóa API Serper. Nếu không được thiết lập trong .env, người dùng sẽ được yêu cầu cung cấp thông qua giao diện người dùng (UI).', '${SERPER_API_KEY}'],
  ]}
/>

**Lưu ý:** Lấy khóa API của bạn từ [Serper.dev](https://serper.dev/api-keys)


### searxngInstanceUrl

<OptionTable
  options={[
    ['searxngInstanceUrl', 'String', 'Tên biến môi trường cho URL của instance SearXNG. Nếu không được thiết lập trong .env, người dùng sẽ được nhắc cung cấp thông qua giao diện người dùng.', '${SEARXNG_INSTANCE_URL}'],
  ]}
/>

### searxngApiKey

<OptionTable
  options={[
    ['searxngApiKey', 'String', 'Tên biến môi trường cho khóa API SearXNG. Nếu không được thiết lập trong .env, người dùng sẽ được yêu cầu cung cấp thông qua giao diện người dùng (UI).', '${SEARXNG_API_KEY}'],
  ]}
/>

**Lưu ý:** Đây là tùy chọn và chỉ cần thiết nếu instance SearXNG của bạn yêu cầu xác thực.

### searxngSearchOptions

<OptionTable
  options={[
    ['searxngSearchOptions', 'Object', 'Các tùy chọn truy vấn được gửi đến instance SearXNG của bạn trong mỗi lần tìm kiếm. Mọi subkey đều là tùy chọn và khối này chỉ được đọc khi searchProvider là "searxng".', ''],
  ]}
/>

**Các khóa con:**

<OptionTable
  options={[
    ['engines', 'String or Array of Strings', 'Các engine mà instance của bạn nên truy vấn, được gửi đến SearXNG dưới dạng tham số engines. Chấp nhận một chuỗi được phân tách bằng dấu phẩy hoặc một danh sách YAML; cả hai đều được chuẩn hóa về dạng phân tách bằng dấu phẩy, với các khoảng trắng thừa và mục trống bị loại bỏ. Một giá trị trống sau khi cắt tỉa sẽ được coi là chưa thiết lập.', 'Default: "google,bing,duckduckgo"'],
    ['language', 'String', 'Mã ngôn ngữ kết quả, được gửi đến SearXNG dưới dạng tham số ngôn ngữ. Sử dụng mã mà instance của bạn chấp nhận, ví dụ như "en", "de", hoặc "fr".', 'Default: "all"'],
    ['timeRange', 'String', 'Giới hạn kết quả theo ngày xuất bản, được gửi đến SearXNG dưới dạng time_range. Bị loại bỏ hoàn toàn khỏi yêu cầu khi không được thiết lập.', 'Options: "day", "month", "year"'],
    ['timeout', 'Number', 'Thời gian chờ yêu cầu HTTP tính bằng mili giây cho các lệnh gọi đến instance của bạn. Phải là một số nguyên dương không lớn hơn 120000; 0 sẽ bị từ chối vì nó sẽ vô hiệu hóa thời gian chờ.', 'Default: 10000'],
  ]}
/>

<Callout type="info" title="Tính khả dụng">
`searxngSearchOptions` kết nối với SearXNG thông qua công cụ tìm kiếm trong `@librechat/agents` và yêu cầu `@librechat/agents` phiên bản v3.6.9 trở lên. Các bản phát hành được xây dựng dựa trên phiên bản cũ hơn sẽ bỏ qua khối này thay vì gặp lỗi khi khởi động, vì vậy một instance liên tục trả về kết quả mặc định có thể chỉ đang chạy một bản build cũ.
</Callout>

**Ví dụ:**

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}" # Optional
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeRange: "month"
    timeout: 10000
```

`engines` cũng chấp nhận định dạng phân tách bằng dấu phẩy mà chính SearXNG sử dụng, vì vậy điều này tương đương với danh sách ở trên:

```yaml filename="webSearch"
webSearch:
  searxngSearchOptions:
    engines: "google, bing, startpage, qwant"
```

#### Chọn engine [#choosing-engines]

Tên các engine phải khớp với các engine đã được bật trên instance của bạn, và có hai điều cần lưu ý từ đó:

- SearXNG sẽ bỏ qua một công cụ mà nó không nhận diện được thay vì báo lỗi. Một lỗi đánh máy hoặc một công cụ bị vô hiệu hóa trên instance của bạn sẽ dẫn đến việc hiển thị ít kết quả hơn thay vì gây ra lỗi.
- Các tên hợp lệ là dành riêng cho từng instance. Hãy kiểm tra danh sách đã bật tại `https://your-instance/config`, nơi trả về JSON bao gồm mọi engine đã được bật, hoặc mở tab **Engines** trong trang tùy chọn instance của bạn.

Mặc định là `google,bing,duckduckgo`. SearXNG tổng hợp bất cứ kết quả nào mà các công cụ được chọn trả về, vì vậy một công cụ bị chặn chỉ khiến bạn mất đi phần kết quả của công cụ đó thay vì toàn bộ phản hồi, và tìm kiếm chỉ trả về kết quả trống khi tất cả các công cụ được chọn đều thất bại hoặc không trả về gì cả.

DuckDuckGo là công cụ trong tập hợp mặc định có khả năng bị chặn cao nhất, vì nó thường gửi CAPTCHA đến hầu hết các instance tự lưu trữ. Với chỉ ba công cụ trong tập hợp, việc mất đi DuckDuckGo cùng lúc với Google hoặc Bing khi bị giới hạn tốc độ (rate-limited) là nguyên nhân phổ biến dẫn đến việc không nhận được kết quả nào cả. Việc thiết lập `engines` một cách rõ ràng, loại bỏ DuckDuckGo và liệt kê nhiều hơn ba công cụ đều giúp giảm thiểu rủi ro đó.

Các engine phản hồi nhanh và chịu được lưu lượng truy cập tự lưu trữ (self-hosted) là lựa chọn khởi đầu tốt nhất, ví dụ:

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - brave
      - startpage
      - qwant
```

Việc thêm nhiều công cụ tìm kiếm sẽ mở rộng phạm vi bao phủ nhưng cũng làm chậm mọi lượt tìm kiếm, vì SearXNG phải đợi công cụ chậm nhất trong tập hợp phản hồi. Hãy lưu ý đến `timeout` khi bạn mở rộng danh sách này.

#### Những tùy chọn này không kiểm soát điều gì [#what-these-options-do-not-control]

- **Safe search** không nằm trong khối này. Nó đến từ khóa cấp cao nhất [`safeSearch`](#safesearch) và được chuyển tiếp đến SearXNG dưới dạng `safesearch`.
- **Result categories** được LibreChat chọn cho mỗi truy vấn, hệ thống sẽ ánh xạ loại tìm kiếm sang `general`, `images`, `videos`, hoặc `news`. Các danh mục này không thể bị ghi đè tại đây.
- **Số trang và định dạng kết quả** là cố định. LibreChat luôn yêu cầu trang 1 ở định dạng JSON, đó là lý do tại sao instance của bạn phải bật `json` trong phần `formats`.

### tavilyApiKey

<OptionTable
  options={[
    ['tavilyApiKey', 'String', 'Tên biến môi trường cho khóa API Tavily. Được sử dụng cho cả tìm kiếm và trình thu thập dữ liệu. Nếu không được thiết lập trong .env, người dùng sẽ được nhắc cung cấp thông qua giao diện người dùng.', '${TAVILY_API_KEY}'],
  ]}
/>

**Lưu ý:** Lấy API key của bạn từ [Tavily](https://app.tavily.com/home)

### tavilySearchUrl

<OptionTable
  options={[
    ['tavilySearchUrl', 'String', 'Tên biến môi trường cho URL API Tìm kiếm Tavily tùy chỉnh. Tùy chọn; mặc định là tìm kiếm được lưu trữ trên Tavily khi không được thiết lập.', '${TAVILY_SEARCH_URL}'],
  ]}
/>

### tavilyExtractUrl

<OptionTable
  options={[
    ['tavilyExtractUrl', 'String', 'Tên biến môi trường cho URL API Tavily Extract tùy chỉnh. Tùy chọn; mặc định sử dụng dịch vụ extract được lưu trữ bởi Tavily khi không được thiết lập.', '${TAVILY_EXTRACT_URL}'],
  ]}
/>

### tavilySearchOptions

<OptionTable
  options={[
    ['tavilySearchOptions', 'Object', 'Các tùy chọn cấu hình cho tìm kiếm Tavily.', ''],
  ]}
/>

**Các khóa con:**

<OptionTable
  options={[
    ['searchDepth', 'String', 'Kiểm soát sự cân bằng giữa mức độ liên quan và độ trễ. "basic" trả về một bản tóm tắt NLP cho mỗi URL. "advanced" trả về nhiều đoạn trích liên quan về mặt ngữ nghĩa cho mỗi URL (tốn 2 tín dụng API). "fast" cân bằng giữa tốc độ và mức độ liên quan với các đoạn trích. "ultra-fast" giảm thiểu độ trễ với một bản tóm tắt NLP.', 'Options: "basic", "advanced", "fast", "ultra-fast". Default: "basic"'],
    ['maxResults', 'Number', 'Số lượng kết quả tìm kiếm tối đa được trả về.', 'Range: 1-20. Default: 5'],
    ['topic', 'String', 'Danh mục của tìm kiếm. "news" hữu ích cho các cập nhật theo thời gian thực. "finance" cho dữ liệu tài chính.', 'Options: "general", "news", "finance". Default: "general"'],
    ['includeImages', 'Boolean', 'Bao gồm hình ảnh trong phản hồi. Trả về cả hình ảnh truy vấn cấp cao nhất và hình ảnh theo từng kết quả.', 'Default: false'],
    ['includeAnswer', 'Boolean or String', 'Bao gồm câu trả lời do LLM tạo. "basic" hoặc true cho câu trả lời nhanh, "advanced" cho câu trả lời chi tiết.', 'Default: false'],
    ['includeRawContent', 'Boolean or String', 'Bao gồm nội dung HTML đã được làm sạch và phân tích cú pháp. "markdown" hoặc true cho định dạng markdown, "text" cho văn bản thuần túy.', 'Default: false'],
    ['includeDomains', 'Array of Strings', 'Giới hạn tìm kiếm trong các tên miền cụ thể. Tối đa 300 tên miền.', ''],
    ['excludeDomains', 'Array of Strings', 'Loại trừ các tên miền cụ thể khỏi kết quả. Tối đa 150 tên miền.', ''],
    ['timeRange', 'String', 'Bộ lọc phạm vi thời gian dựa trên ngày xuất bản hoặc ngày cập nhật gần nhất.', 'Options: "day", "week", "month", "year"'],
    ['includeImageDescriptions', 'Boolean', 'Khi includeImages là true, hãy thêm cả văn bản mô tả cho mỗi hình ảnh.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Bao gồm URL favicon cho mỗi kết quả tìm kiếm.', 'Default: false'],
    ['chunksPerSource', 'Number', 'Số lượng đoạn nội dung liên quan tối đa trên mỗi nguồn. Chỉ khả dụng khi searchDepth là "advanced".', 'Range: 1-3. Default: 3'],
    ['safeSearch', 'Boolean', 'Ghi đè safe_search tùy chọn của Tavily cho các yêu cầu Tìm kiếm Tavily. Mặc định bị bỏ qua; true có thể yêu cầu Tavily Enterprise.', 'Default: omitted'],
    ['timeout', 'Number', 'Thời gian chờ yêu cầu HTTP phía máy khách tính bằng mili giây. Kiểm soát thời gian chờ phản hồi từ Tavily API trước khi hủy bỏ.', 'Default: 15000'],
  ]}
/>

### keenableApiKey

<OptionTable
  options={[
    ['keenableApiKey', 'String', 'Tên biến môi trường cho khóa API Keenable tùy chọn. Keenable hoạt động mà không cần khóa; việc cấu hình khóa sẽ nâng giới hạn tốc độ công khai.', '${KEENABLE_API_KEY}'],
  ]}
/>

### keenableApiUrl

<OptionTable
  options={[
    ['keenableApiUrl', 'String', 'Tên biến môi trường cho URL API tìm kiếm Keenable tùy chỉnh. Điều này không thay đổi endpoint tìm nạp trang.', '${KEENABLE_API_URL}'],
  ]}
/>

Endpoint page-fetch được cấu hình riêng biệt với biến môi trường chỉ định `KEENABLE_FETCH_URL`.

### keenableSearchOptions

<OptionTable
  options={[
    ['keenableSearchOptions', 'Object', 'Các tùy chọn cấu hình cho tìm kiếm Keenable.', ''],
  ]}
/>

<OptionTable
  options={[
    ['maxResults', 'Number', 'Số lượng kết quả tìm kiếm tối đa.', 'Range: 1-20. Default: 8'],
    ['site', 'String', 'Giới hạn kết quả trong một tên miền.', ''],
    ['attributionTitle', 'String', 'Giá trị được gửi trong tiêu đề attribution X-Keenable-Title.', ''],
    ['timeout', 'Number', 'Thời gian chờ yêu cầu HTTP phía máy khách tính bằng mili giây.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

## Scrapers [#scrapers]

### firecrawlApiKey

<OptionTable
  options={[
    ['firecrawlApiKey', 'String', 'Tên biến môi trường cho khóa API Firecrawl. Nếu không được thiết lập trong .env, người dùng sẽ được nhắc cung cấp khóa này thông qua giao diện người dùng.', '${FIRECRAWL_API_KEY}'],
  ]}
/>

**Lưu ý:** Lấy khóa API của bạn từ [Firecrawl.dev](https://docs.firecrawl.dev/introduction#api-key)

### firecrawlApiUrl

<OptionTable
  options={[
    ['firecrawlApiUrl', 'String', 'Tên biến môi trường cho URL API Firecrawl. Nếu không được thiết lập trong .env, người dùng sẽ được nhắc cung cấp thông qua giao diện người dùng.', '${FIRECRAWL_API_URL}'],
  ]}
/>

**Lưu ý:** Đây là tùy chọn và chỉ cần thiết nếu bạn đang sử dụng một instance Firecrawl tùy chỉnh.

### firecrawlVersion

<OptionTable
  options={[
    ['firecrawlVersion', 'String', 'Tên biến môi trường cho phiên bản Firecrawl API (v0 hoặc v1).', '${FIRECRAWL_VERSION}'],
  ]}
/>

### scraperProvider

<OptionTable
  options={[
    ['scraperProvider', 'String', 'Chỉ định dịch vụ scraper nào sẽ được sử dụng.', 'Options: "firecrawl", "serper", "tavily", "keenable"'],
  ]}
/>

### firecrawlOptions

<OptionTable
  options={[
    ['firecrawlOptions', 'Object', 'Các tùy chọn cấu hình nâng cao cho trình thu thập dữ liệu Firecrawl.', ''],
  ]}
/>

**Các khóa con:**

#### formats

<OptionTable
  options={[
    ['formats', 'Array of Strings', 'Các định dạng cần bao gồm trong đầu ra.', ''],
  ]}
/>

#### includeTags

<OptionTable
  options={[
    ['includeTags', 'Array of Strings', 'Các thẻ cần bao gồm trong đầu ra.', ''],
  ]}
/>

#### excludeTags

<OptionTable
  options={[
    ['excludeTags', 'Array of Strings', 'Các thẻ cần loại trừ khỏi đầu ra.', ''],
  ]}
/>

#### headers

<OptionTable
  options={[
    ['headers', 'Object', 'Các tiêu đề (headers) cần gửi kèm theo yêu cầu. Có thể được sử dụng để gửi cookie, user-agent, v.v.', ''],
  ]}
/>

#### waitFor

<OptionTable
  options={[
    ['waitFor', 'Number', 'Chỉ định độ trễ tính bằng mili giây trước khi tìm nạp nội dung, cho phép trang có đủ thời gian để tải.', ''],
  ]}
/>

#### timeout

<OptionTable
  options={[
    ['timeout', 'Integer', 'Thời gian chờ tính bằng mili giây cho yêu cầu thu thập dữ liệu. Phải là một số nguyên không âm.', 'Default: 7500'],
  ]}
/>

#### maxAge

<OptionTable
  options={[
    ['maxAge', 'Number', 'Trả về phiên bản được lưu trong bộ nhớ đệm của trang nếu nó mới hơn khoảng thời gian này tính bằng mili giây. Nếu phiên bản được lưu trong bộ nhớ đệm của trang cũ hơn giá trị này, trang đó sẽ được thu thập lại.', ''],
  ]}
/>

**Lưu ý:** Nếu bạn không cần dữ liệu cực kỳ mới, việc bật tính năng này có thể tăng tốc độ thu thập dữ liệu của bạn lên 500%.

#### mobile

<OptionTable
  options={[
    ['mobile', 'Boolean', 'Mô phỏng việc thu thập dữ liệu từ thiết bị di động.', ''],
  ]}
/>

#### skipTlsVerification

<OptionTable
  options={[
    ['skipTlsVerification', 'Boolean', 'Bỏ qua xác minh chứng chỉ TLS khi thực hiện các yêu cầu.', ''],
  ]}
/>

#### blockAds

<OptionTable
  options={[
    ['blockAds', 'Boolean', 'Bật tính năng chặn quảng cáo và chặn cửa sổ bật lên cookie.', ''],
  ]}
/>

#### removeBase64Images

<OptionTable
  options={[
    ['removeBase64Images', 'Boolean', 'Loại bỏ tất cả hình ảnh base 64 khỏi đầu ra, vốn có thể rất dài. Văn bản thay thế (alt text) của hình ảnh vẫn được giữ lại trong đầu ra, nhưng URL sẽ được thay thế bằng một trình giữ chỗ.', ''],
  ]}
/>

#### parsePDF

<OptionTable
  options={[
    ['parsePDF', 'Boolean', 'Kiểm soát cách các tệp PDF được xử lý trong quá trình thu thập dữ liệu.', ''],
  ]}
/>

#### storeInCache

<OptionTable
  options={[
    ['storeInCache', 'Boolean', 'Nếu được đặt là true, trang sẽ được lưu trữ trong chỉ mục và bộ nhớ đệm của Firecrawl. Việc đặt thành false sẽ hữu ích nếu hoạt động thu thập dữ liệu của bạn có thể liên quan đến các vấn đề bảo vệ dữ liệu. Việc sử dụng một số tham số liên quan đến thu thập dữ liệu nhạy cảm (headers) sẽ buộc tham số này phải là false.', ''],
  ]}
/>

#### zeroDataRetention

<OptionTable
  options={[
    ['zeroDataRetention', 'Boolean', 'Nếu được đặt là true, tùy chọn này sẽ cho phép không lưu giữ dữ liệu cho quá trình thu thập này (yêu cầu thiết lập trước trên Firecrawl).', ''],
  ]}
/>

#### location

<OptionTable
  options={[
    ['location', 'Object', 'Vị trí địa lý và cài đặt ngôn ngữ cho việc thu thập dữ liệu.', ''],
  ]}
/>

#### onlyMainContent

<OptionTable
  options={[
    ['onlyMainContent', 'Boolean', 'Chỉ trả về nội dung chính của trang, không bao gồm tiêu đề, thanh điều hướng, chân trang, v.v.', ''],
  ]}
/>

#### changeTrackingOptions

<OptionTable
  options={[
    ['changeTrackingOptions', 'Object', 'Cấu hình để theo dõi các thay đổi trong nội dung được thu thập.', ''],
  ]}
/>

**Ví dụ:**
```yaml filename="webSearch"
webSearch:
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlOptions:
    formats: ["markdown", "rawHtml"]
    includeTags: ["main", "article", ".content"]
    excludeTags: ["nav", "footer", ".ads"]
    waitFor: 2000
    timeout: 10000
    mobile: false
    blockAds: true
    onlyMainContent: true
    location:
      country: "US"
      languages: ["en"]
```

**Lưu ý:** Để biết thông tin chi tiết về các tùy chọn và cài đặt mặc định của trình thu thập dữ liệu Firecrawl, hãy xem [Tài liệu API Firecrawl](https://docs.firecrawl.dev/api-reference/endpoint/scrape).

### tavilyScraperOptions

<OptionTable
  options={[
    ['tavilyScraperOptions', 'Object', 'Các tùy chọn cấu hình cho Tavily Extract (scraper).', ''],
  ]}
/>

**Các khóa con:**

<OptionTable
  options={[
    ['extractDepth', 'String', 'Độ sâu của quá trình trích xuất. "advanced" truy xuất nhiều dữ liệu hơn bao gồm bảng và nội dung nhúng với tỷ lệ thành công cao hơn nhưng có thể làm tăng độ trễ. "basic" tốn 1 credit cho mỗi 5 URL thành công, "advanced" tốn 2 credit cho mỗi 5 URL thành công.', 'Options: "basic", "advanced". Default: "basic"'],
    ['includeImages', 'Boolean', 'Bao gồm một danh sách các hình ảnh được trích xuất từ các URL trong phản hồi.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Bao gồm URL favicon cho mỗi kết quả được trích xuất.', 'Default: false'],
    ['format', 'String', 'Định dạng của nội dung trang web được trích xuất. "markdown" trả về nội dung ở định dạng markdown. "text" trả về văn bản thuần túy và có thể làm tăng độ trễ.', 'Options: "markdown", "text". Default: "markdown"'],
    ['timeout', 'Number', 'Thời gian chờ tính bằng mili giây. Kiểm soát thời gian chờ HTTP phía máy khách. Khi được thiết lập, nó cũng gửi đến Tavily một thời gian chờ trích xuất phía máy chủ đã được chuyển đổi sang giây và giới hạn trong khoảng 1-60 giây.', 'Default: 15000 for basic, 30000 for advanced'],
  ]}
/>

**Ví dụ:**
```yaml filename="webSearch"
webSearch:
  searchProvider: tavily
  scraperProvider: tavily
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  # tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  # tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"
  tavilySearchOptions:
    searchDepth: basic
    maxResults: 5
    topic: general
  tavilyScraperOptions:
    extractDepth: basic
```

**Lưu ý:** Để biết thông tin chi tiết về các tùy chọn Tavily API, hãy xem [Tavily API Documentation](https://docs.tavily.com).

### keenableScraperOptions

<OptionTable
  options={[
    ['keenableScraperOptions', 'Object', 'Các tùy chọn cấu hình cho tính năng tìm nạp trang Keenable.', ''],
  ]}
/>

<OptionTable
  options={[
    ['attributionTitle', 'String', 'Giá trị được gửi trong tiêu đề attribution X-Keenable-Title.', ''],
    ['timeout', 'Number', 'Thời gian chờ yêu cầu HTTP phía máy khách tính bằng mili giây.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

Keenable có thể cung cấp tính năng tìm kiếm, tìm nạp trang hoặc cả hai. Các endpoint công khai của nó hoạt động mà không cần khóa; `keenableApiKey` tùy chọn sẽ giúp tăng giới hạn tốc độ. Hãy đặt `rerankerType: "none"` để tránh yêu cầu khóa reranker:

```yaml filename="webSearch"
webSearch:
  searchProvider: keenable
  scraperProvider: keenable
  rerankerType: none
  # keenableApiKey: "${KEENABLE_API_KEY}" # Optional
  # keenableApiUrl: "${KEENABLE_API_URL}" # Optional search endpoint
  keenableSearchOptions:
    maxResults: 8
    # site: example.com
  keenableScraperOptions:
    timeout: 15000
```

## Rerankers [#rerankers]

### jinaApiKey

<OptionTable
  options={[
    ['jinaApiKey', 'String', 'Tên biến môi trường cho khóa API Jina. Nếu không được thiết lập trong .env, người dùng sẽ được yêu cầu cung cấp khóa này thông qua giao diện người dùng.', '${JINA_API_KEY}'],
  ]}
/>

**Lưu ý:** Lấy API key của bạn từ [Jina.ai](https://jina.ai/api-dashboard/)

### jinaApiUrl

<OptionTable
  options={[
    ['jinaApiUrl', 'String', 'Tên biến môi trường cho URL API Jina. Nếu không được thiết lập trong .env, người dùng sẽ được nhắc cung cấp thông qua giao diện người dùng.', '${JINA_API_URL}'],
  ]}
/>

**Lưu ý:** Đây là tùy chọn và chỉ cần thiết nếu bạn đang sử dụng một instance Jina tùy chỉnh.

### cohereApiKey

<OptionTable
  options={[
    ['cohereApiKey', 'String', 'Tên biến môi trường cho khóa API Cohere. Nếu không được thiết lập trong .env, người dùng sẽ được yêu cầu cung cấp thông qua giao diện người dùng.', '${COHERE_API_KEY}'],
  ]}
/>

**Lưu ý:** Lấy API key của bạn từ [Cohere Dashboard](https://dashboard.cohere.com/welcome/login)

### rerankerType

<OptionTable
  options={[
    ['rerankerType', 'String', 'Chỉ định dịch vụ reranker nào sẽ được sử dụng. Đặt thành "none" để bỏ qua việc reranking.', 'Options: "jina", "cohere", "none"'],
  ]}
/>

## Cài đặt chung [#general-settings]

### scraperTimeout

<OptionTable
  options={[
    ['scraperTimeout', 'Integer', 'Thời gian chờ tính bằng mili giây cho các yêu cầu trình thu thập dữ liệu. Phải là một số nguyên không âm.', 'Default: 7500'],
  ]}
/>

### safeSearch

<OptionTable
  options={[
    ['safeSearch', 'Number', 'Mức lọc tìm kiếm an toàn. 0 = TẮT (không lọc), 1 = TRUNG BÌNH (mặc định), 2 = NGHIÊM NGẶT (lọc tối đa).', 'Default: 1 (MODERATE)'],
  ]}
/>

**Lưu ý:** Các cấp độ tìm kiếm an toàn tuân theo các quy ước API tìm kiếm tiêu chuẩn. Bộ lọc MODERATE được bật theo mặc định để cung cấp khả năng lọc nội dung hợp lý trong khi vẫn duy trì hiệu quả tìm kiếm. Tavily không kế thừa cài đặt toàn cục này theo mặc định; chỉ sử dụng `tavilySearchOptions.safeSearch` nếu tài khoản Tavily của bạn hỗ trợ `safe_search`.

## Ghi chú [#notes]

- Các API keys có thể được cấu hình theo hai cách:
  1. Thiết lập các biến môi trường được chỉ định trong cấu hình YAML
  2. Nếu các biến môi trường không được thiết lập, người dùng sẽ được nhắc cung cấp các khóa API thông qua giao diện người dùng (UI).
- Cấu hình hỗ trợ nhiều dịch vụ cho mỗi thành phần (providers, scrapers, rerankers)
- Nếu một loại dịch vụ cụ thể không được chỉ định, hệ thống sẽ thử tất cả các dịch vụ khả dụng trong danh mục đó
- Safe search cung cấp ba cấp độ lọc nội dung: OFF (0), MODERATE (1) và STRICT (2)
- Tavily không kế thừa cài đặt tìm kiếm an toàn (safe search) toàn cục theo mặc định; hãy đặt `tavilySearchOptions.safeSearch` một cách rõ ràng chỉ khi tài khoản Tavily của bạn hỗ trợ `safe_search`
- Keenable hỗ trợ tìm kiếm không cần khóa (keyless) và tìm nạp trang; khóa tùy chọn của nó giúp nâng cao giới hạn tốc độ công khai và ghi đè URL tìm nạp của nó chỉ dành cho môi trường.
- Hành vi truy vấn của SearXNG (công cụ tìm kiếm, ngôn ngữ, phạm vi thời gian, thời gian chờ yêu cầu) được cấu hình trong `searxngSearchOptions`; xem [Choosing engines](#choosing-engines) nếu kết quả tìm kiếm trả về trống.
- Không bao giờ đặt các API key thực tế vào cấu hình YAML - chỉ sử dụng tên biến môi trường

## Thiết lập SearXNG [#setting-up-searxng]

SearXNG là một công cụ tìm kiếm meta tập trung vào quyền riêng tư mà bạn có thể tự lưu trữ (self-host). 
Để biết thêm thông tin, hãy xem [tài liệu chính thức của SearXNG](https://docs.searxng.org/).

Dưới đây là các bước để thiết lập instance SearXNG của riêng bạn để sử dụng với LibreChat:

### Sử dụng Docker Desktop [#using-docker-desktop]

1. **Tìm kiếm image SearXNG chính thức**
   - Mở Docker Desktop
   - Tìm kiếm `searxng/searxng` trong tab **Images**
   - Nhấp vào **Run** trên image chính thức để tự động tải xuống và chạy một container của image đó

2. **Chạy container**
   - Mở rộng menu thả xuống **Optional Settings** trong bảng điều khiển xuất hiện sau khi quá trình tải xuống hoàn tất.
   - Thiết lập các chi tiết cấu hình mong muốn của bạn (số cổng, tên container, v.v.)
   - Nhấp vào **Run** để khởi chạy container

3. **Cấu hình SearXNG cho LibreChat**
   - Điều hướng đến tab `Files` trong Docker Desktop
   - Đi đến `/etc/searxng/settings.yaml`
   - Mở trình chỉnh sửa tệp
   - Điều hướng đến phần `formats`
   - Thêm `json` làm định dạng chấp nhận được để LibreChat có thể giao tiếp với instance của bạn
   - Lưu tệp

4. **Khởi động lại container**
   - Khởi động lại container để các thay đổi có hiệu lực

#### Hướng dẫn bằng video: [#video-guide]

Dưới đây là video hướng dẫn bạn thực hiện quy trình từ đầu đến cuối trong khoảng một phút:

     <Video src="/videos/min_searxng_docker_setup_hq.mp4" title="Hướng dẫn thiết lập SearXNG bằng Docker" />

**Lưu ý:** Trong ví dụ này, URL của instance là http://localhost:55011 (số cổng được tìm thấy bên dưới tên container ở góc trên cùng bên trái vào cuối video)

### Cấu hình LibreChat để sử dụng SearXNG [#configuring-librechat-to-use-searxng]

Bạn có thể cấu hình SearXNG trong LibreChat thông qua giao diện người dùng hoặc thông qua `librechat.yaml`.

#### Cấu hình giao diện (UI) [#ui-configuration]

1. **Mở menu thả xuống công cụ trong thanh nhập liệu trò chuyện**
![Nút cấu hình công cụ](/images/web-search/tools_highlight.png)

2. **Nhấp vào biểu tượng bánh răng bên cạnh Web Search**
![Tools configuration section](/images/web-search/tools_config_highlight.png)

3. **Chọn SearXNG từ menu thả xuống Search Provider**
![SearXNG dropdown selection](/images/web-search/searxng_dropdown_highlight.png)

4. **Nhập chi tiết cấu hình của bạn (ví dụ: URL instance, loại scraper, v.v.) và nhấp vào lưu**
![Lưu cấu hình tìm kiếm web](/images/web-search/web_search_save.png)

5. **Nhấp vào tùy chọn Web Search trong menu thả xuống công cụ**
![Web search badge in chat interface](/images/web-search/websearch_highlight.png)

6. **Huy hiệu Web Search hiện đã được kích hoạt, nghĩa là các truy vấn của bạn giờ đây có thể sử dụng chức năng tìm kiếm trên web**
![Web search badge confirmation](/images/web-search/search_badge_confirm.png)

#### Cấu hình YAML [#yaml-configuration]

Để cấu hình SearXNG cho tất cả mọi người trên instance thay vì cho từng người dùng, hãy đặt các giá trị tương tự trong `librechat.yaml`:

```yaml filename="librechat.yaml"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  # searxngApiKey: "${SEARXNG_API_KEY}" # Only if your instance requires authentication
  # Required: a self-hosted instance is a private destination, so exempt it from the connection guard
  allowedAddresses:
    - "localhost:55011"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeout: 10000
```

URL và key vẫn được đọc từ môi trường, vì vậy hãy kết hợp cấu hình trên với:

```bash filename=".env"
SEARXNG_INSTANCE_URL=http://localhost:55011
# SEARXNG_API_KEY=your_api_key
```

<Callout type="warning" title="Các instance riêng tư cần một mục allowedAddresses">
`localhost:55011` là một địa chỉ loopback, mà tính năng [SSRF protection](#ssrf-protection-and-private-providers) của LibreChat sẽ chặn tại thời điểm kết nối. Nếu không có mục `allowedAddresses` tương ứng, quá trình tìm kiếm sẽ thất bại ngay cả khi SearXNG đang hoạt động bình thường. Mục này phải là cặp `host:port` chính xác từ URL instance của bạn.

When LibreChat itself runs in Docker, `localhost` is the LibreChat container, not the SearXNG one. Use a hostname the container can reach, such as the compose service name, and exempt that instead:

```yaml filename="librechat.yaml"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}" # SEARXNG_INSTANCE_URL=http://searxng:8080
  allowedAddresses:
    - "searxng:8080"
```
</Callout>

Xem [`searxngSearchOptions`](#searxngsearchoptions) để biết tất cả các khóa con mà khối này chấp nhận.

#### Khắc phục sự cố kết quả trống [#troubleshooting-empty-results]

Một tìm kiếm SearXNG không trả về kết quả nào hầu như luôn là do yêu cầu không bao giờ đến được một instance đang hoạt động, thay vì LibreChat loại bỏ kết quả. Hãy thực hiện theo thứ tự sau:

1. **Instance này là một địa chỉ riêng tư không có mục `allowedAddresses`.** LibreChat chặn các đích loopback và riêng tư tại thời điểm kết nối, vì vậy một instance tự lưu trữ cần phải liệt kê chính xác `host:port` của nó trong [`allowedAddresses`](#ssrf-protection-and-private-providers). Điều này chặn yêu cầu ngay lập tức thay vì trả về một tập kết quả trống.
2. **`json` bị thiếu trong `formats`.** LibreChat yêu cầu kết quả trả về dưới dạng JSON. Nếu phần `formats` trong tệp cài đặt instance của bạn không liệt kê `json`, instance sẽ trả về trang lỗi và mọi tìm kiếm đều không có kết quả. Đây là bước 3 của quá trình thiết lập Docker ở trên.
3. **Mọi engine được chọn đều thất bại.** SearXNG hợp nhất các engine có phản hồi, vì vậy phản hồi trống nghĩa là không có engine nào trả về kết quả. DuckDuckGo nằm trong tập hợp mặc định (`google,bing,duckduckgo`) và thường gửi CAPTCHA đến hầu hết các instance tự lưu trữ (self-hosted), khiến chỉ còn lại hai engine thực hiện tìm kiếm. Hãy thiết lập `searxngSearchOptions.engines` một cách rõ ràng, loại bỏ DuckDuckGo và liệt kê thêm một vài engine khác.
4. **Một tên công cụ không tồn tại trên instance của bạn.** SearXNG sẽ âm thầm bỏ qua các công cụ mà nó không nhận diện được, vì vậy lỗi đánh máy chỉ đồng nghĩa với việc có ít kết quả hơn. Hãy so sánh danh sách của bạn với `https://your-instance/config`.
5. **Instance chậm hơn thời gian chờ (timeout).** Hãy tăng `searxngSearchOptions.timeout` cao hơn mức mặc định `10000` nếu instance của bạn truy vấn nhiều engine hoặc nằm sau đường truyền mạng chậm.

Bạn có thể tái tạo những gì LibreChat gửi bằng cách gọi trực tiếp instance đó:

```bash
curl -s "http://localhost:55011/search?q=librechat&format=json&engines=google,bing,startpage" | head -c 500
```

Điều này chạy từ máy của riêng bạn, vì vậy nó bỏ qua hoàn toàn cơ chế bảo vệ kết nối của LibreChat. Một mảng `results` trống ở đây xác nhận vấn đề nằm ở phía SearXNG; nếu có kết quả ở đây trong khi LibreChat vẫn không tìm thấy gì thì vấn đề nằm ở `allowedAddresses`.
