---
title: Configuration de la recherche Web
icon: Search
---

La configuration `webSearch` vous permet de personnaliser la fonctionnalité de recherche web au sein de LibreChat, y compris les fournisseurs de recherche, les scrapers de contenu et les rerankers de résultats.

## Vue d'ensemble [#overview]

La fonctionnalité de recherche web se compose de trois éléments principaux :

1. **Fournisseurs de recherche** : Services qui effectuent la recherche web initiale
2. **Scrapers** : Services qui extraient du contenu à partir de pages web
3. **Rerankers** : Services qui réordonnent les résultats de recherche pour une meilleure pertinence

## Exemple [#example]

```yaml filename="webSearch"
webSearch:
  # Search Provider Configuration
  serperApiKey: "${SERPER_API_KEY}"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}"
  searchProvider: "serper" # Options: "serper", "searxng", "tavily", "keenable"

  # Optional: query options sent to a SearXNG instance
  searxngSearchOptions:
    engines: "google,bing,startpage" # Default: "google,bing,duckduckgo"
    language: "en" # Default: "all"

  # Tavily Configuration (search and/or scraper)
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"

  # Keenable Configuration (search and/or scraper; all credentials optional)
  keenableApiKey: "${KEENABLE_API_KEY}"
  keenableApiUrl: "${KEENABLE_API_URL}"
  keenableSearchOptions:
    maxResults: 8
    site: "example.com"
    attributionTitle: "LibreChat"
    timeout: 15000
  keenableScraperOptions:
    attributionTitle: "LibreChat"
    timeout: 15000

  # Scraper Configuration
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlApiUrl: "${FIRECRAWL_API_URL}"
  firecrawlVersion: "${FIRECRAWL_VERSION}"
  scraperProvider: "firecrawl" # Options: "firecrawl", "serper", "tavily", "keenable"

  # Reranker Configuration
  jinaApiKey: "${JINA_API_KEY}"
  jinaApiUrl: "${JINA_API_URL}"
  cohereApiKey: "${COHERE_API_KEY}"
  rerankerType: "jina" # Options: "jina", "cohere", "none"

  # General Settings
  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)
  # Required for deliberately private self-hosted provider endpoints
  allowedAddresses:
    - "searxng:8080"
```

## Protection SSRF et fournisseurs privés [#ssrf-protection-and-private-providers]

LibreChat valide chaque connexion qu'il ouvre pour la recherche web, le scraping et le reranking. Les destinations privées, de bouclage (loopback), locales au lien (link-local) et les métadonnées cloud sont bloquées par défaut, y compris les redirections et les noms d'hôtes qui se résolvent en une adresse privée au moment de la connexion.

Utilisez `webSearch.allowedAddresses` lorsqu'un endpoint configuré pour SearXNG, Firecrawl, Jina, compatible avec Tavily, compatible avec Keenable ou un fournisseur associé est délibérément privé :

```yaml filename="webSearch / allowedAddresses"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  allowedAddresses:
    - "searxng:8080"
    - "10.0.0.5:3002"
    - "[fd00::5]:8080"
```

Ce champ est une liste d'exemption SSRF, et non une liste d'autorisation stricte. Les destinations publiques restent disponibles. Les entrées doivent être des paires exactes `host:port`, `private.ip:port` ou `[ipv6]:port` ; les URL, chemins, plages CIDR, hôtes/IP nus et adresses IP publiques littérales sont rejetés. Un nom d'hôte exempté fait confiance à l'adresse IP privée vers laquelle il se résout sur ce port ; privilégiez donc une adresse IP privée littérale et ne listez que les noms dont vous contrôlez le DNS.

LibreChat exempte automatiquement un endpoint de proxy de transfert HTTP(S) configuré afin qu'il puisse être atteint. Lorsque le proxy achemine une requête, il résout la destination et doit appliquer lui-même la sortie vers cette destination. `NO_PROXY` peut rendre une requête directe, là où la protection de connexion de LibreChat s'applique toujours. Cette protection couvre les connexions que LibreChat ouvre vers les API des fournisseurs ; elle ne peut pas restreindre une URL de page qu'un scraper tiers reçoit en tant que données de requête et qu'il récupère sur sa propre infrastructure.

## Fournisseurs de recherche [#search-providers]

### searchProvider

<OptionTable
  options={[
    ['searchProvider', 'String', 'Spécifie quel fournisseur de recherche utiliser.', 'Options: "serper", "searxng", "tavily", "keenable"'],
  ]}
/>

### serperApiKey

<OptionTable
  options={[
    ['serperApiKey', 'String', 'Nom de la variable d\'environnement pour la clé API Serper. Si elle n\'est pas définie dans .env, les utilisateurs seront invités à la fournir via l\'interface utilisateur.', '${SERPER_API_KEY}'],
  ]}
/>

**Note :** Obtenez votre clé API sur [Serper.dev](https://serper.dev/api-keys)


### searxngInstanceUrl

<OptionTable
  options={[
    ['searxngInstanceUrl', 'String', 'Nom de la variable d\'environnement pour l\'URL de l\'instance SearXNG. S\'il n\'est pas défini dans .env, les utilisateurs seront invités à le fournir via l\'interface utilisateur.', '${SEARXNG_INSTANCE_URL}'],
  ]}
/>

### searxngApiKey

<OptionTable
  options={[
    ['searxngApiKey', 'String', 'Nom de la variable d\'environnement pour la clé API SearXNG. Si elle n\'est pas définie dans .env, les utilisateurs seront invités à la fournir via l\'interface utilisateur.', '${SEARXNG_API_KEY}'],
  ]}
/>

**Note :** Ceci est facultatif et n'est nécessaire que si votre instance SearXNG nécessite une authentification.

### searxngSearchOptions

<OptionTable
  options={[
    ['searxngSearchOptions', 'Object', 'Options de requête envoyées à votre instance SearXNG à chaque recherche. Chaque sous-clé est facultative, et le bloc n\'est lu que lorsque searchProvider est "searxng".', ''],
  ]}
/>

**Sous-clés :**

<OptionTable
  options={[
    ['engines', 'String or Array of Strings', 'Moteurs que votre instance doit interroger, envoyés à SearXNG en tant que paramètre engines. Accepte une chaîne séparée par des virgules ou une liste YAML ; les deux sont normalisées sous forme de chaîne séparée par des virgules, les espaces environnants et les entrées vides étant supprimés. Une valeur vide après nettoyage est considérée comme non définie.', 'Default: "google,bing,duckduckgo"'],
    ['language', 'String', 'Code de langue du résultat, envoyé à SearXNG en tant que paramètre de langue. Utilisez un code accepté par votre instance, tel que "en", "de" ou "fr".', 'Default: "all"'],
    ['timeRange', 'String', 'Restreint les résultats par date de publication, envoyés à SearXNG sous le nom de time_range. Omis entièrement de la requête lorsqu\'il n\'est pas défini.', 'Options: "day", "month", "year"'],
    ['timeout', 'Number', 'Délai d\'expiration de la requête HTTP en millisecondes pour les appels vers votre instance. Doit être un entier positif inférieur ou égal à 120000 ; 0 est rejeté car cela désactiverait le délai d\'expiration.', 'Default: 10000'],
  ]}
/>

<Callout type="info" title="Disponibilité">
`searxngSearchOptions` accède à SearXNG via l'outil de recherche dans `@librechat/agents`, et nécessite `@librechat/agents` v3.6.9 ou une version ultérieure. Les versions compilées avec une version antérieure ignorent le bloc au lieu d'échouer au démarrage ; ainsi, une instance qui continue de renvoyer des résultats par défaut peut simplement exécuter une version plus ancienne.
</Callout>

**Exemple :**

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngApiKey: "${SEARXNG_API_KEY}" # Optional
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeRange: "month"
    timeout: 10000
```

`engines` accepte également la forme séparée par des virgules que SearXNG utilise lui-même, donc ceci est équivalent à la liste ci-dessus :

```yaml filename="webSearch"
webSearch:
  searxngSearchOptions:
    engines: "google, bing, startpage, qwant"
```

#### Choisir les moteurs [#choosing-engines]

Les noms des moteurs doivent correspondre aux moteurs activés sur votre propre instance, et deux choses en découlent :

- SearXNG ignore un moteur qu'il ne reconnaît pas au lieu de signaler une erreur. Une faute de frappe, ou un moteur désactivé sur votre instance, se traduit par un nombre de résultats réduit plutôt que par un échec.
- Les noms valides sont spécifiques à l'instance. Vérifiez la liste activée sur `https://your-instance/config`, qui renvoie un JSON incluant chaque moteur activé, ou ouvrez l'onglet **Engines** de la page des préférences de votre instance.

La valeur par défaut est `google,bing,duckduckgo`. SearXNG agrège tout ce que les moteurs sélectionnés renvoient ; ainsi, un moteur bloqué vous coûte seulement sa part des résultats plutôt que la réponse entière, et une recherche ne revient vide que lorsque chaque moteur sélectionné échoue ou ne renvoie rien.

DuckDuckGo est le moteur de cet ensemble par défaut le plus susceptible d'être bloqué, car il présente des CAPTCHA à la plupart des instances auto-hébergées. Avec seulement trois moteurs dans l'ensemble, le perdre en même temps qu'un Google ou Bing limité par le débit est un moyen courant de se retrouver sans aucun résultat. Définir explicitement `engines`, en excluant DuckDuckGo, et lister plus de trois moteurs permettent tous de réduire ce risque.

Les moteurs qui répondent rapidement et tolèrent le trafic auto-hébergé constituent le meilleur ensemble de départ, par exemple :

```yaml filename="webSearch"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - brave
      - startpage
      - qwant
```

Ajouter plus de moteurs élargit la couverture mais ralentit également chaque recherche, car SearXNG attend le moteur le plus lent de l'ensemble avant de répondre. Gardez le `timeout` à l'esprit lorsque vous agrandissez la liste.

#### Ce que ces options ne contrôlent pas [#what-these-options-do-not-control]

- **Safe search** ne fait pas partie de ce bloc. Il provient de la clé de premier niveau [`safeSearch`](#safesearch) et est transmis à SearXNG en tant que `safesearch`.
- Les **catégories de résultats** sont choisies par requête par LibreChat, qui fait correspondre le type de recherche à `general`, `images`, `videos` ou `news`. Elles ne peuvent pas être remplacées ici.
- Le **numéro de page et le format de résultat** sont fixes. LibreChat demande toujours la page 1 au format JSON, c'est pourquoi votre instance doit avoir `json` activé sous `formats`.

### tavilyApiKey

<OptionTable
  options={[
    ['tavilyApiKey', 'String', 'Nom de la variable d\'environnement pour la clé API Tavily. Utilisé à la fois pour la recherche et le scraper. S\'il n\'est pas défini dans .env, les utilisateurs seront invités à le fournir via l\'interface utilisateur.', '${TAVILY_API_KEY}'],
  ]}
/>

**Remarque :** Obtenez votre clé API auprès de [Tavily](https://app.tavily.com/home)

### tavilySearchUrl

<OptionTable
  options={[
    ['tavilySearchUrl', 'String', 'Nom de la variable d\'environnement pour une URL d\'API Tavily Search personnalisée. Optionnel ; utilise la recherche hébergée par Tavily par défaut si non défini.', '${TAVILY_SEARCH_URL}'],
  ]}
/>

### tavilyExtractUrl

<OptionTable
  options={[
    ['tavilyExtractUrl', 'String', 'Nom de la variable d\'environnement pour une URL d\'API Tavily Extract personnalisée. Optionnel ; utilise l\'extracteur hébergé par Tavily par défaut si non défini.', '${TAVILY_EXTRACT_URL}'],
  ]}
/>

### tavilySearchOptions

<OptionTable
  options={[
    ['tavilySearchOptions', 'Object', 'Options de configuration pour la recherche Tavily.', ''],
  ]}
/>

**Sous-clés :**

<OptionTable
  options={[
    ['searchDepth', 'String', 'Contrôle le compromis entre pertinence et latence. "basic" renvoie un résumé NLP par URL. "advanced" renvoie plusieurs extraits sémantiquement pertinents par URL (2 crédits API). "fast" équilibre vitesse et pertinence avec des extraits. "ultra-fast" minimise la latence avec un seul résumé NLP.', 'Options: "basic", "advanced", "fast", "ultra-fast". Default: "basic"'],
    ['maxResults', 'Number', 'Le nombre maximal de résultats de recherche à retourner.', 'Range: 1-20. Default: 5'],
    ['topic', 'String', 'La catégorie de la recherche. "news" est utile pour les mises à jour en temps réel. "finance" pour les données financières.', 'Options: "general", "news", "finance". Default: "general"'],
    ['includeImages', 'Boolean', 'Inclure des images dans la réponse. Renvoie à la fois les images de la requête de premier niveau et les images par résultat.', 'Default: false'],
    ['includeAnswer', 'Boolean or String', 'Inclure une réponse générée par LLM. "basic" ou true pour une réponse rapide, "advanced" pour une réponse détaillée.', 'Default: false'],
    ['includeRawContent', 'Boolean or String', 'Inclure le contenu HTML nettoyé et analysé. "markdown" ou true pour le format markdown, "text" pour le texte brut.', 'Default: false'],
    ['includeDomains', 'Array of Strings', 'Restreindre la recherche à des domaines spécifiques. Maximum 300 domaines.', ''],
    ['excludeDomains', 'Array of Strings', 'Exclure des domaines spécifiques des résultats. Maximum 150 domaines.', ''],
    ['timeRange', 'String', 'Filtre de plage temporelle basé sur la date de publication ou de dernière mise à jour.', 'Options: "day", "week", "month", "year"'],
    ['includeImageDescriptions', 'Boolean', 'Lorsque includeImages est défini sur true, ajoutez également un texte descriptif pour chaque image.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Inclure l\'URL du favicon pour chaque résultat de recherche.', 'Default: false'],
    ['chunksPerSource', 'Number', 'Nombre maximal de segments de contenu pertinents par source. Uniquement disponible lorsque searchDepth est défini sur "advanced".', 'Range: 1-3. Default: 3'],
    ['safeSearch', 'Boolean', 'Remplacement optionnel de safe_search pour les requêtes de recherche Tavily. Omis par défaut ; true peut nécessiter Tavily Enterprise.', 'Default: omitted'],
    ['timeout', 'Number', 'Délai d\'expiration de la requête HTTP côté client en millisecondes. Contrôle la durée d\'attente de la réponse de l\'API Tavily avant d\'abandonner.', 'Default: 15000'],
  ]}
/>

### keenableApiKey

<OptionTable
  options={[
    ['keenableApiKey', 'String', 'Nom de la variable d\'environnement pour la clé API Keenable optionnelle. Keenable fonctionne sans clé ; en configurer une augmente les limites de débit publiques.', '${KEENABLE_API_KEY}'],
  ]}
/>

### keenableApiUrl

<OptionTable
  options={[
    ['keenableApiUrl', 'String', 'Nom de la variable d\'environnement pour une URL d\'API de recherche Keenable personnalisée. Cela ne modifie pas l\'endpoint de récupération de page.', '${KEENABLE_API_URL}'],
  ]}
/>

Le point de terminaison page-fetch est configuré séparément avec la variable d'environnement uniquement `KEENABLE_FETCH_URL`.

### keenableSearchOptions

<OptionTable
  options={[
    ['keenableSearchOptions', 'Object', 'Options de configuration pour la recherche Keenable.', ''],
  ]}
/>

<OptionTable
  options={[
    ['maxResults', 'Number', 'Nombre maximal de résultats de recherche.', 'Range: 1-20. Default: 8'],
    ['site', 'String', 'Limite les résultats à un seul domaine.', ''],
    ['attributionTitle', 'String', 'Valeur envoyée dans l\'en-tête d\'attribution X-Keenable-Title.', ''],
    ['timeout', 'Number', 'Délai d\'expiration de la requête HTTP côté client en millisecondes.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

## Scrapers [#scrapers]

### firecrawlApiKey

<OptionTable
  options={[
    ['firecrawlApiKey', 'String', 'Nom de la variable d\'environnement pour la clé API Firecrawl. Si elle n\'est pas définie dans .env, les utilisateurs seront invités à la fournir via l\'interface utilisateur.', '${FIRECRAWL_API_KEY}'],
  ]}
/>

**Remarque :** Obtenez votre clé API sur [Firecrawl.dev](https://docs.firecrawl.dev/introduction#api-key)

### firecrawlApiUrl

<OptionTable
  options={[
    ['firecrawlApiUrl', 'String', 'Nom de la variable d\'environnement pour l\'URL de l\'API Firecrawl. S\'il n\'est pas défini dans .env, les utilisateurs seront invités à le fournir via l\'interface utilisateur.', '${FIRECRAWL_API_URL}'],
  ]}
/>

**Note :** Ceci est facultatif et n'est nécessaire que si vous utilisez une instance Firecrawl personnalisée.

### firecrawlVersion

<OptionTable
  options={[
    ['firecrawlVersion', 'String', 'Nom de la variable d\'environnement pour la version de l\'API Firecrawl (v0 ou v1).', '${FIRECRAWL_VERSION}'],
  ]}
/>

### scraperProvider

<OptionTable
  options={[
    ['scraperProvider', 'String', 'Spécifie quel service de scraping utiliser.', 'Options: "firecrawl", "serper", "tavily", "keenable"'],
  ]}
/>

### firecrawlOptions

<OptionTable
  options={[
    ['firecrawlOptions', 'Object', 'Options de configuration avancées pour le scraper Firecrawl.', ''],
  ]}
/>

**Sous-clés :**

#### formats

<OptionTable
  options={[
    ['formats', 'Array of Strings', 'Formats à inclure dans la sortie.', ''],
  ]}
/>

#### includeTags

<OptionTable
  options={[
    ['includeTags', 'Array of Strings', 'Tags à inclure dans la sortie.', ''],
  ]}
/>

#### excludeTags

<OptionTable
  options={[
    ['excludeTags', 'Array of Strings', 'Étiquettes à exclure de la sortie.', ''],
  ]}
/>

#### headers

<OptionTable
  options={[
    ['headers', 'Object', 'En-têtes à envoyer avec la requête. Peuvent être utilisés pour envoyer des cookies, un user-agent, etc.', ''],
  ]}
/>

#### waitFor

<OptionTable
  options={[
    ['waitFor', 'Number', 'Spécifiez un délai en millisecondes avant de récupérer le contenu, permettant à la page suffisamment de temps pour se charger.', ''],
  ]}
/>

#### timeout

<OptionTable
  options={[
    ['timeout', 'Integer', 'Délai d\'attente en millisecondes pour la requête de scraping. Doit être un entier non négatif.', 'Default: 7500'],
  ]}
/>

#### maxAge

<OptionTable
  options={[
    ['maxAge', 'Number', 'Renvoie une version mise en cache de la page si elle est plus récente que cette durée en millisecondes. Si une version mise en cache de la page est plus ancienne que cette valeur, la page sera récupérée.', ''],
  ]}
/>

**Remarque :** Si vous n'avez pas besoin de données extrêmement récentes, l'activation de cette option peut accélérer vos extractions de 500 %.

#### mobile

<OptionTable
  options={[
    ['mobile', 'Boolean', 'Émuler le scraping depuis un appareil mobile.', ''],
  ]}
/>

#### skipTlsVerification

<OptionTable
  options={[
    ['skipTlsVerification', 'Boolean', 'Ignorer la vérification du certificat TLS lors des requêtes.', ''],
  ]}
/>

#### blockAds

<OptionTable
  options={[
    ['blockAds', 'Boolean', 'Active le blocage des publicités et des fenêtres contextuelles de cookies.', ''],
  ]}
/>

#### removeBase64Images

<OptionTable
  options={[
    ['removeBase64Images', 'Boolean', 'Supprime toutes les images en base 64 de la sortie, ce qui peut être extrêmement long. Le texte alternatif de l\'image reste dans la sortie, mais l\'URL est remplacée par un espace réservé.', ''],
  ]}
/>

#### parsePDF

<OptionTable
  options={[
    ['parsePDF', 'Boolean', 'Contrôle la manière dont les fichiers PDF sont traités lors du scraping.', ''],
  ]}
/>

#### storeInCache

<OptionTable
  options={[
    ['storeInCache', 'Boolean', 'Si défini sur true, la page sera stockée dans l\'index et le cache de Firecrawl. Définir cette valeur sur false est utile si votre activité de scraping peut soulever des problèmes de protection des données. L\'utilisation de certains paramètres associés au scraping sensible (headers) forcera ce paramètre à false.', ''],
  ]}
/>

#### zeroDataRetention

<OptionTable
  options={[
    ['zeroDataRetention', 'Boolean', 'Si défini sur true, cela activera la rétention de données nulle pour ce scrape (nécessite une configuration préalable sur Firecrawl).', ''],
  ]}
/>

#### location

<OptionTable
  options={[
    ['location', 'Object', 'Paramètres de localisation géographique et de langue pour le scraping.', ''],
  ]}
/>

#### onlyMainContent

<OptionTable
  options={[
    ['onlyMainContent', 'Boolean', 'Ne renvoyez que le contenu principal de la page, à l\'exclusion des en-têtes, des barres de navigation, des pieds de page, etc.', ''],
  ]}
/>

#### changeTrackingOptions

<OptionTable
  options={[
    ['changeTrackingOptions', 'Object', 'Configuration pour le suivi des modifications dans le contenu récupéré.', ''],
  ]}
/>

**Exemple :**
```yaml filename="webSearch"
webSearch:
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  firecrawlOptions:
    formats: ["markdown", "rawHtml"]
    includeTags: ["main", "article", ".content"]
    excludeTags: ["nav", "footer", ".ads"]
    waitFor: 2000
    timeout: 10000
    mobile: false
    blockAds: true
    onlyMainContent: true
    location:
      country: "US"
      languages: ["en"]
```

**Remarque :** Pour des informations détaillées sur les options et les valeurs par défaut du scraper Firecrawl, consultez la [Documentation de l'API Firecrawl](https://docs.firecrawl.dev/api-reference/endpoint/scrape).

### tavilyScraperOptions

<OptionTable
  options={[
    ['tavilyScraperOptions', 'Object', 'Options de configuration pour Tavily Extract (scraper).', ''],
  ]}
/>

**Sous-clés :**

<OptionTable
  options={[
    ['extractDepth', 'String', 'La profondeur du processus d\'extraction. "advanced" récupère davantage de données, y compris les tableaux et le contenu intégré, avec un meilleur taux de réussite, mais peut augmenter la latence. "basic" coûte 1 crédit pour 5 URL réussies, "advanced" coûte 2 crédits pour 5 URL réussies.', 'Options: "basic", "advanced". Default: "basic"'],
    ['includeImages', 'Boolean', 'Inclure une liste d\'images extraites des URLs dans la réponse.', 'Default: false'],
    ['includeFavicon', 'Boolean', 'Inclure l\'URL du favicon pour chaque résultat extrait.', 'Default: false'],
    ['format', 'String', 'Le format du contenu de la page web extrait. "markdown" renvoie le contenu au format markdown. "text" renvoie du texte brut et peut augmenter la latence.', 'Options: "markdown", "text". Default: "markdown"'],
    ['timeout', 'Number', 'Délai d\'expiration en millisecondes. Contrôle le délai d\'expiration HTTP côté client. Lorsqu\'il est défini, il envoie également à Tavily un délai d\'expiration d\'extraction côté serveur converti en secondes et limité entre 1 et 60 s.', 'Default: 15000 for basic, 30000 for advanced'],
  ]}
/>

**Exemple :**
```yaml filename="webSearch"
webSearch:
  searchProvider: tavily
  scraperProvider: tavily
  tavilyApiKey: "${TAVILY_API_KEY}"
  # Optional: custom Tavily-compatible endpoints
  # tavilySearchUrl: "${TAVILY_SEARCH_URL}"
  # tavilyExtractUrl: "${TAVILY_EXTRACT_URL}"
  tavilySearchOptions:
    searchDepth: basic
    maxResults: 5
    topic: general
  tavilyScraperOptions:
    extractDepth: basic
```

**Remarque :** Pour des informations détaillées sur les options de l'API Tavily, consultez la [Tavily API Documentation](https://docs.tavily.com).

### keenableScraperOptions

<OptionTable
  options={[
    ['keenableScraperOptions', 'Object', 'Options de configuration pour la récupération de page Keenable.', ''],
  ]}
/>

<OptionTable
  options={[
    ['attributionTitle', 'String', 'Valeur envoyée dans l\'en-tête d\'attribution X-Keenable-Title.', ''],
    ['timeout', 'Number', 'Délai d\'expiration de la requête HTTP côté client en millisecondes.', 'Range: 0-120000. Default: 15000'],
  ]}
/>

Keenable peut effectuer des recherches, récupérer des pages, ou les deux. Ses endpoints publics fonctionnent sans clé ; le paramètre optionnel `keenableApiKey` augmente les limites de débit. Définissez `rerankerType: "none"` pour éviter d'avoir besoin d'une clé de reranker :

```yaml filename="webSearch"
webSearch:
  searchProvider: keenable
  scraperProvider: keenable
  rerankerType: none
  # keenableApiKey: "${KEENABLE_API_KEY}" # Optional
  # keenableApiUrl: "${KEENABLE_API_URL}" # Optional search endpoint
  keenableSearchOptions:
    maxResults: 8
    # site: example.com
  keenableScraperOptions:
    timeout: 15000
```

## Rerankers [#rerankers]

### jinaApiKey

<OptionTable
  options={[
    ['jinaApiKey', 'String', 'Nom de la variable d\'environnement pour la clé API Jina. Si elle n\'est pas définie dans .env, les utilisateurs seront invités à la fournir via l\'interface utilisateur.', '${JINA_API_KEY}'],
  ]}
/>

**Note :** Obtenez votre clé API sur [Jina.ai](https://jina.ai/api-dashboard/)

### jinaApiUrl

<OptionTable
  options={[
    ['jinaApiUrl', 'String', 'Nom de la variable d\'environnement pour l\'URL de l\'API Jina. S\'il n\'est pas défini dans .env, les utilisateurs seront invités à le fournir via l\'interface utilisateur.', '${JINA_API_URL}'],
  ]}
/>

**Note :** Ceci est facultatif et n'est nécessaire que si vous utilisez une instance Jina personnalisée.

### cohereApiKey

<OptionTable
  options={[
    ['cohereApiKey', 'String', 'Nom de la variable d\'environnement pour la clé API Cohere. Si elle n\'est pas définie dans .env, les utilisateurs seront invités à la fournir via l\'interface utilisateur.', '${COHERE_API_KEY}'],
  ]}
/>

**Note :** Obtenez votre clé API depuis le [Cohere Dashboard](https://dashboard.cohere.com/welcome/login)

### rerankerType

<OptionTable
  options={[
    ['rerankerType', 'String', 'Spécifie quel service de reranker utiliser. Définissez sur "none" pour ignorer le reranking.', 'Options: "jina", "cohere", "none"'],
  ]}
/>

## Paramètres généraux [#general-settings]

### scraperTimeout

<OptionTable
  options={[
    ['scraperTimeout', 'Integer', 'Délai d\'attente en millisecondes pour les requêtes du scraper. Doit être un entier non négatif.', 'Default: 7500'],
  ]}
/>

### safeSearch

<OptionTable
  options={[
    ['safeSearch', 'Number', 'Niveau de filtrage de la recherche sécurisée. 0 = DÉSACTIVÉ (aucun filtrage), 1 = MODÉRÉ (par défaut), 2 = STRICT (filtrage maximal).', 'Default: 1 (MODERATE)'],
  ]}
/>

**Note :** Les niveaux de recherche sécurisée (Safe search) s'alignent sur les conventions standard des API de recherche. Le filtrage MODERATE est activé par défaut pour fournir un filtrage de contenu raisonnable tout en maintenant l'efficacité de la recherche. Tavily n'hérite pas de ce paramètre global par défaut ; utilisez `tavilySearchOptions.safeSearch` uniquement si votre compte Tavily prend en charge `safe_search`.

## Notes [#notes]

- Les clés API peuvent être configurées de deux manières :
  1. Définissez les variables d'environnement spécifiées dans la configuration YAML
  2. Si les variables d'environnement ne sont pas définies, les utilisateurs seront invités à fournir les clés API via l'interface utilisateur.
- La configuration prend en charge plusieurs services pour chaque composant (providers, scrapers, rerankers)
- Si aucun type de service spécifique n'est précisé, le système essaiera tous les services disponibles dans cette catégorie.
- La recherche sécurisée (Safe search) propose trois niveaux de filtrage de contenu : OFF (0), MODERATE (1) et STRICT (2)
- Tavily n'hérite pas du paramètre de recherche sécurisée global par défaut ; définissez `tavilySearchOptions.safeSearch` explicitement uniquement lorsque votre compte Tavily prend en charge `safe_search`.
- Keenable prend en charge la recherche sans clé et la récupération de pages ; sa clé optionnelle augmente les limites de débit public, et sa substitution d'URL de récupération est réservée à l'environnement.
- Le comportement des requêtes SearXNG (moteurs, langue, plage temporelle, délai d'expiration de la requête) est configuré sous `searxngSearchOptions` ; voir [Choosing engines](#choosing-engines) si les recherches ne renvoient aucun résultat.
- Ne mettez jamais de clés API réelles dans la configuration YAML - utilisez uniquement des noms de variables d'environnement

## Configuration de SearXNG [#setting-up-searxng]

SearXNG est un méta-moteur de recherche axé sur la confidentialité que vous pouvez auto-héberger. 
Pour plus d'informations, consultez la [documentation officielle de SearXNG](https://docs.searxng.org/).

Voici les étapes pour configurer votre propre instance SearXNG à utiliser avec LibreChat :

### Utilisation de Docker Desktop [#using-docker-desktop]

1. **Rechercher l'image officielle de SearXNG**
   - Ouvrez Docker Desktop
   - Recherchez `searxng/searxng` dans l'onglet **Images**
   - Cliquez sur **Run** sur l'image officielle pour automatiquement télécharger et exécuter un conteneur de l'image

2. **Exécution du conteneur**
   - Développez le menu déroulant **Optional Settings** dans le panneau qui apparaît une fois le téléchargement terminé.
   - Définissez les détails de configuration souhaités (numéro de port, nom du conteneur, etc.)
   - Cliquez sur **Run** pour démarrer le conteneur

3. **Configurer SearXNG pour LibreChat**
   - Accédez à l'onglet `Files` dans Docker Desktop
   - Allez dans `/etc/searxng/settings.yaml`
   - Ouvrir l'éditeur de fichiers
   - Accédez à la section `formats`
   - Ajoutez `json` comme format acceptable afin que LibreChat puisse communiquer avec votre instance
   - Enregistrez le fichier

4. **Redémarrer le conteneur**
   - Redémarrez le conteneur pour que les modifications prennent effet

#### Guide vidéo : [#video-guide]

Voici une vidéo pour vous guider tout au long du processus, du début à la fin, en une minute environ :

     <Video src="/videos/min_searxng_docker_setup_hq.mp4" title="Guide de configuration de SearXNG avec Docker" />

**Remarque :** Dans cet exemple, l'URL de l'instance est http://localhost:55011 (le numéro de port se trouve sous le nom du conteneur en haut à gauche à la fin de la vidéo)

### Configuration de LibreChat pour utiliser SearXNG [#configuring-librechat-to-use-searxng]

Vous pouvez configurer SearXNG dans LibreChat via l'interface utilisateur ou par le biais de `librechat.yaml`.

#### Configuration de l'interface utilisateur [#ui-configuration]

1. **Ouvrez le menu déroulant des outils dans la barre de saisie du chat**
![Bouton de configuration des outils](/images/web-search/tools_highlight.png)

2. **Cliquez sur l'icône d'engrenage à côté de Web Search**
![Tools configuration section](/images/web-search/tools_config_highlight.png)

3. **Sélectionnez SearXNG dans le menu déroulant Search Provider**
![SearXNG dropdown selection](/images/web-search/searxng_dropdown_highlight.png)

4. **Saisissez vos détails de configuration (par exemple, l'URL de l'instance, le type de scraper, etc.) et cliquez sur enregistrer**
![Enregistrer la configuration de la recherche web](/images/web-search/web_search_save.png)

5. **Cliquez sur l'option Web Search dans le menu déroulant des outils**
![Web search badge in chat interface](/images/web-search/websearch_highlight.png)

6. **Le badge Web Search devrait maintenant être activé, ce qui signifie que vos requêtes peuvent désormais utiliser la fonctionnalité de recherche sur le Web**
![Web search badge confirmation](/images/web-search/search_badge_confirm.png)

#### Configuration YAML [#yaml-configuration]

Pour configurer SearXNG pour tout le monde sur l'instance au lieu de le faire par utilisateur, définissez les mêmes valeurs dans `librechat.yaml` :

```yaml filename="librechat.yaml"
webSearch:
  searchProvider: "searxng"
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}"
  # searxngApiKey: "${SEARXNG_API_KEY}" # Only if your instance requires authentication
  # Required: a self-hosted instance is a private destination, so exempt it from the connection guard
  allowedAddresses:
    - "localhost:55011"
  searxngSearchOptions:
    engines:
      - google
      - bing
      - startpage
      - qwant
    language: "en"
    timeout: 10000
```

L'URL et la clé sont toujours lues depuis l'environnement, donc associez la configuration ci-dessus avec :

```bash filename=".env"
SEARXNG_INSTANCE_URL=http://localhost:55011
# SEARXNG_API_KEY=your_api_key
```

<Callout type="warning" title="Les instances privées nécessitent une entrée allowedAddresses">
`localhost:55011` est une adresse de bouclage (loopback), que la [protection SSRF](#ssrf-protection-and-private-providers) de LibreChat bloque au moment de la connexion. Sans l'entrée `allowedAddresses` correspondante, la recherche échoue même si SearXNG est opérationnel. L'entrée doit être la paire `host:port` exacte provenant de l'URL de votre instance.

Lorsque LibreChat lui-même s'exécute dans Docker, `localhost` correspond au conteneur LibreChat, et non à celui de SearXNG. Utilisez un nom d'hôte que le conteneur peut atteindre, tel que le nom du service compose, et excluez-le plutôt :

```yaml filename="librechat.yaml"
webSearch:
  searxngInstanceUrl: "${SEARXNG_INSTANCE_URL}" # SEARXNG_INSTANCE_URL=http://searxng:8080
  allowedAddresses:
    - "searxng:8080"
```
</Callout>

Voir [`searxngSearchOptions`](#searxngsearchoptions) pour toutes les sous-clés acceptées par ce bloc.

#### Dépannage des résultats vides [#troubleshooting-empty-results]

Une recherche SearXNG qui ne renvoie rien signifie presque toujours que la requête n'atteint jamais une instance fonctionnelle, plutôt que LibreChat ne rejette les résultats. Suivez ces étapes dans l'ordre :

1. **L'instance est une adresse privée sans entrée `allowedAddresses`.** LibreChat bloque les destinations de bouclage (loopback) et privées au moment de la connexion ; une instance auto-hébergée doit donc avoir son `host:port` exact listé sous [`allowedAddresses`](#ssrf-protection-and-private-providers). Cela bloque la requête immédiatement plutôt que de renvoyer un jeu de résultats vide.
2. **`json` est manquant dans `formats`.** LibreChat demande des résultats au format JSON. Si la section `formats` de votre fichier de configuration d'instance ne liste pas `json`, l'instance répond avec une page d'erreur et chaque recherche revient vide. Il s'agit de l'étape 3 de la configuration Docker ci-dessus.
3. **Tous les moteurs sélectionnés ont échoué.** SearXNG fusionne les moteurs qui ont répondu, donc une réponse vide signifie qu'aucun d'entre eux n'a rien renvoyé. DuckDuckGo fait partie de l'ensemble par défaut (`google,bing,duckduckgo`) et présente des CAPTCHA à la plupart des instances auto-hébergées, ce qui ne laisse que deux moteurs pour effectuer la recherche. Définissez `searxngSearchOptions.engines` explicitement, excluez DuckDuckGo et listez quelques moteurs supplémentaires.
4. **Un nom de moteur n'existe pas sur votre instance.** SearXNG ignore silencieusement les moteurs qu'il ne reconnaît pas, donc une faute de frappe signifie simplement moins de résultats. Comparez votre liste avec `https://your-instance/config`.
5. **L'instance est plus lente que le délai d'expiration.** Augmentez `searxngSearchOptions.timeout` au-delà de la valeur par défaut `10000` si votre instance interroge de nombreux moteurs ou se trouve derrière un chemin réseau lent.

Vous pouvez reproduire ce que LibreChat envoie en appelant l'instance directement :

```bash
curl -s "http://localhost:55011/search?q=librechat&format=json&engines=google,bing,startpage" | head -c 500
```

Ceci s'exécute depuis votre propre machine, ce qui contourne entièrement la protection de connexion de LibreChat. Un tableau `results` vide ici confirme que le problème vient du côté de SearXNG ; si des résultats apparaissent ici alors que LibreChat ne trouve toujours rien, cela indique un problème avec `allowedAddresses`.
