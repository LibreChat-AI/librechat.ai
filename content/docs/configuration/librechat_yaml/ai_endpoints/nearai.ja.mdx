---
title: NEAR AI Cloud
icon: NearAI
description: LibreChatでNEAR AI Cloudをカスタムendpointとして設定する。
---

NEAR AI Cloud は、`endpoints.custom` を通じて LibreChat に追加できる OpenAI 互換の endpoint を提供しています。

## APIキーを取得する [#get-an-api-key]

[NEAR AI Cloud](https://cloud.near.ai/) からAPIキーを作成します。それを `.env` ファイルに追加してください：

```bash filename=".env"
NEARAI_API_KEY=your-api-key
```

## 設定 [#configuration]

`librechat.yaml` の `endpoints.custom` の下にエンドポイントを追加します：

```yaml filename="librechat.yaml"
    - name: "nearai"
      apiKey: "${NEARAI_API_KEY}"
      baseURL: "https://cloud-api.near.ai/v1"
      models:
        default:
          - "z-ai/glm-5.2"
          - "deepseek-ai/DeepSeek-V4-Flash"
        fetch: true
      titleConvo: true
      titleModel: "z-ai/glm-5.2"
      modelDisplayLabel: "NEAR AI Cloud"
```

各ユーザーが `.env` から読み込むのではなく、LibreChat UI を通じて独自のキーを提供できるようにするには、`apiKey: "user_provided"` を設定します。

## 注記 [#notes]

- `fetch: true` を設定すると、LibreChatはNEAR AI CloudのOpenAI互換 `/v1/models` endpointからモデルリストを読み込みます。`default` 配列は初期選択のみに使用されます。
- [NEAR AI Cloud model catalog](https://cloud-api.near.ai/v1/model/list) に記載されている通りに、正確なモデルIDを使用してください。
- モデルの可用性は時間の経過とともに変化する可能性があります。選択したモデルが利用できない場合は、カタログから別のチャット可能なモデルを選択してください。
