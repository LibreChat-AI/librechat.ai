---
title: xAI
icon: XAI
description: LibreChat에서 xAI를 커스텀 endpoint로 구성하기.
---

xAI는 LibreChat에 사용자 지정 endpoint로 추가할 수 있는 OpenAI 호환 API를 통해 Grok 모델을 제공합니다.

## API 키 발급받기 [#get-an-api-key]

[xAI console](https://console.x.ai/)에서 키를 생성하세요. 이를 `.env` 파일에 추가하세요:

```bash filename=".env"
XAI_API_KEY=your-api-key
```

## 구성 [#configuration]

`librechat.yaml`의 `endpoints.custom` 아래에 해당 endpoint를 추가하세요:

```yaml filename="librechat.yaml"
    - name: "xai"
      apiKey: "${XAI_API_KEY}"
      baseURL: "https://api.x.ai/v1"
      models:
        default: ["grok-beta"]
        fetch: false
      titleConvo: true
      titleMethod: "completion"
      titleModel: "grok-beta"
      summarize: false
      summaryModel: "grok-beta"
      modelDisplayLabel: "Grok"
```

## 참고 사항 [#notes]

- `titleMethod: "completion"`은 Grok이 지원하는 방식인 채팅 완료(chat completions) 엔드포인트를 통해 대화 제목을 생성합니다.
