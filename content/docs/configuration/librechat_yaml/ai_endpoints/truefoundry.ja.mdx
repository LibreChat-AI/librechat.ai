---
title: TrueFoundry AI Gateway
icon: TrueFoundry
description: LibreChatでTrueFoundry AI Gatewayをカスタムendpointとして設定する。
---

TrueFoundry AI Gatewayは、アプリケーションとLLMプロバイダーの間に位置するエンタープライズ向けのプロキシ層であり、組み込みの可観測性とガバナンスを備えたOpenAI互換の統合インターフェースを通じて、1000以上のモデルへのアクセスを提供します。

## APIキーを取得する [#get-an-api-key]

Personal Access Tokenを使用して認証します。[PATガイド](https://docs.truefoundry.com/gateway/authentication#personal-access-token-pat)に従ってトークンを生成し、ゲートウェイURLとともに`.env`ファイルに追加してください：

```bash filename=".env"
TRUEFOUNDRY_API_KEY=your-api-key
TRUEFOUNDRY_GATEWAY_URL=your-gateway-url
```

TrueFoundryコンソールの統合コードスニペットから、ゲートウェイURLとモデル名を取得します。ゲートウェイに追加したものと同じモデル名を使用してください。[クイックスタートガイド](https://docs.truefoundry.com/gateway/quick-start)でセットアップの手順を確認できます。

## 設定 [#configuration]

`librechat.yaml` の `endpoints.custom` の下にエンドポイントを追加します：

```yaml filename="librechat.yaml"
    - name: "TrueFoundry"
      apiKey: "${TRUEFOUNDRY_API_KEY}"
      baseURL: "${TRUEFOUNDRY_GATEWAY_URL}"
      models:
        default: ["openai-main/gpt-4o-mini", "openai-main/gpt-4o"]
        fetch: true
      titleConvo: true
      titleModel: "current_model"
      summarize: false
      summaryModel: "current_model"
      modelDisplayLabel: "TrueFoundry:OpenAI"
```

## 注記 [#notes]

- モデル名は、ゲートウェイで設定した統合によって名前空間が指定されます（例: `openai-main/gpt-4o`）。TrueFoundryコンソールに表示されている名前と一致させてください。
- 詳細は、[TrueFoundry docs](https://docs.truefoundry.com/docs/introduction) を参照してください。
