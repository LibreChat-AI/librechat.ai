---
title: Ollama
icon: Ollama
description: Exemple de configuration pour Ollama
---

> Clé API Ollama : Requise mais ignorée - [Ollama OpenAI Compatibility](https://github.com/ollama/ollama/blob/main/docs/openai.md)

**Notes :**

- **Connu :** icône fournie.
- Téléchargez des modèles avec la commande ollama run. Consultez la [Ollama Library](https://ollama.com/library)
- Il est recommandé d'utiliser la valeur "current_model" pour le `titleModel` afin d'éviter de charger plus d'un modèle par conversation.
    - Cela utilisera dynamiquement le modèle de conversation actuel pour la génération du titre.
- L'exemple inclut une liste des 5 modèles les plus populaires de la bibliothèque Ollama, mise à jour pour la dernière fois le 1er mars 2024, pour votre commodité.

```yaml filename="librechat.yaml"
  custom:
    - name: "Ollama"
      apiKey: "ollama"
      # use 'host.docker.internal' instead of localhost if running LibreChat in a docker container
      baseURL: "http://localhost:11434/v1/" 
      models:
        default: [
          "llama2",
          "mistral",
          "codellama",
          "dolphin-mixtral",
          "mistral-openorca"
          ]
        # fetching list of models is supported but the `name` field must start
        # with `ollama` (case-insensitive), as it does in this example.
        fetch: true
      titleConvo: true
      titleModel: "current_model"
      summarize: false
      summaryModel: "current_model"
      modelDisplayLabel: "Ollama"
```

<Callout type="tip" title="Ollama -> llama3">
    
Remarque : Une fois que `stop` a été supprimé des [paramètres par défaut](/docs/configuration/librechat_yaml/object_structure/default_params), le problème souligné ci-dessous ne devrait plus exister.

Cependant, si vous rencontrez un comportement où `llama3` ne cesse pas de générer, ajoutez ce bloc `addParams` à la configuration :

```yaml filename="librechat.yaml"
  custom:
    - name: "Ollama"
      apiKey: "ollama"
      baseURL: "http://host.docker.internal:11434/v1/"
      models:
        default: [
          "llama3"
        ]
        fetch: false # fetching list of models is not supported
      titleConvo: true
      titleModel: "current_model"
      summarize: false
      summaryModel: "current_model"
      modelDisplayLabel: "Ollama"
      addParams:
          "stop": [
              "<|start_header_id|>",
              "<|end_header_id|>",
              "<|eot_id|>",
              "<|reserved_special_token"
          ]
```

Si vous utilisez uniquement `llama3` avec **Ollama**, il est acceptable de définir le paramètre `stop` au niveau de la configuration via `addParams`.

Cependant, si vous utilisez plusieurs modèles, il est désormais recommandé d'ajouter des séquences d'arrêt depuis l'interface via les paramètres de conversation et les préréglages.

Par exemple, nous pouvons omettre `addParams` :

```yaml filename="librechat.yaml"
- name: "Ollama"
    apiKey: "ollama"
    baseURL: "http://host.docker.internal:11434/v1/" 
    models:
    default: [
        "llama3:latest",
        "mistral"
        ]
    fetch: false # fetching list of models is not supported
    titleConvo: true
    titleModel: "current_model"
    modelDisplayLabel: "Ollama"
```

Et utilisez ces paramètres (il est préférable de les enregistrer également) :

![image](https://github.com/danny-avila/LibreChat/assets/110412045/57460b8c-308a-4d21-9dfe-f48a2ac85099)

</Callout>
