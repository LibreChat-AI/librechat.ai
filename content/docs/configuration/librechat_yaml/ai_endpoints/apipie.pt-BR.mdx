---
title: APIpie
icon: Apipie
description: Configure o APIpie como um endpoint personalizado no LibreChat.
---

APIpie é um agregador que expõe modelos de diversos provedores através de um único endpoint compatível com OpenAI.

## Obtenha uma API key [#get-an-api-key]

Crie uma chave em [apipie.ai/dashboard/profile/api-keys](https://apipie.ai/dashboard/profile/api-keys) e adicione-a ao seu arquivo `.env`:

```bash filename=".env"
APIPIE_API_KEY=your-api-key
```

## Configuração [#configuration]

Adicione o endpoint em `endpoints.custom` no seu `librechat.yaml`:

```yaml filename="librechat.yaml"
    - name: "APIpie"
      apiKey: "${APIPIE_API_KEY}"
      baseURL: "https://apipie.ai/v1/"
      models:
        default: [
          "gpt-4",
          "gpt-4-turbo",
          "gpt-3.5-turbo",
          "claude-3-opus",
          "claude-3-sonnet",
          "claude-3-haiku",
          "llama-3-70b-instruct",
          "llama-3-8b-instruct",
          "gemini-pro-1.5",
          "gemini-pro",
          "mistral-large",
          "mistral-medium",
          "mistral-small",
          "mistral-tiny",
          "mixtral-8x22b",
          ]
        fetch: false
      titleConvo: true
      titleModel: "claude-3-haiku"
      summarize: false
      summaryModel: "claude-3-haiku"
      dropParams: ["stream"]
      modelDisplayLabel: "APIpie"
```

<Callout type="tip" title="Buscar e ordenar os modelos" collapsible>
Este script em Python pode buscar e ordenar os modelos de LLM para você. A saída é salva em `models.txt`, formatada para facilitar a inserção na configuração yaml.

```py filename="fetch.py"
import json
import requests

def fetch_and_order_models():
    # API endpoint
    url = "https://apipie.ai/models"

    # headers as per request example
    headers = {"Accept": "application/json"}

    # request parameters
    params = {"type": "llm"}

    # make request
    response = requests.get(url, headers=headers, params=params)

    # parse JSON response
    data = response.json()

    # extract an ordered list of unique model IDs
    model_ids = sorted(set([model["id"] for model in data]))

    # write result to a text file
    with open("models.txt", "w") as file:
        json.dump(model_ids, file, indent=2)

# execute the function
if __name__ == "__main__":
    fetch_and_order_models()
```
</Callout>

## Notas [#notes]

- A busca automática de modelos não é suportada, portanto `fetch` está definido como `false` e a lista de modelos é definida inline. Use o script acima para regenerar a lista quando novos modelos forem adicionados.
- Os resultados da nomeação de conversas podem ser inconsistentes.
- `dropParams: ["stream"]` remove o parâmetro `stream`, que não é suportado atualmente.
