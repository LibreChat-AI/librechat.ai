---
title: xAI
icon: XAI
description: Configureer xAI als een aangepast endpoint in LibreChat.
---

xAI stelt zijn Grok-modellen beschikbaar via een OpenAI-compatibele API die je aan LibreChat kunt toevoegen als een custom endpoint.

## Een API-sleutel verkrijgen [#get-an-api-key]

Maak een sleutel aan via de [xAI console](https://console.x.ai/). Voeg deze toe aan je `.env` bestand:

```bash filename=".env"
XAI_API_KEY=your-api-key
```

## Configuratie [#configuration]

Voeg het endpoint toe onder `endpoints.custom` in je `librechat.yaml`:

```yaml filename="librechat.yaml"
    - name: "xai"
      apiKey: "${XAI_API_KEY}"
      baseURL: "https://api.x.ai/v1"
      models:
        default: ["grok-beta"]
        fetch: false
      titleConvo: true
      titleMethod: "completion"
      titleModel: "grok-beta"
      summarize: false
      summaryModel: "grok-beta"
      modelDisplayLabel: "Grok"
```

## Opmerkingen [#notes]

- `titleMethod: "completion"` genereert gesprektitels via het chat completions endpoint, wat wordt ondersteund door Grok.
