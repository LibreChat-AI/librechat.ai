---
title: xAI
icon: XAI
description: Konfiguriere xAI als benutzerdefinierten endpoint in LibreChat.
---

xAI stellt seine Grok-Modelle über eine OpenAI-kompatible API bereit, die Sie in LibreChat als benutzerdefinierten endpoint hinzufügen können.

## Einen API-Schlüssel erhalten [#get-an-api-key]

Erstellen Sie einen Schlüssel über die [xAI console](https://console.x.ai/). Fügen Sie ihn Ihrer `.env` Datei hinzu:

```bash filename=".env"
XAI_API_KEY=your-api-key
```

## Konfiguration [#configuration]

Fügen Sie den endpoint unter `endpoints.custom` in Ihrer `librechat.yaml` hinzu:

```yaml filename="librechat.yaml"
    - name: "xai"
      apiKey: "${XAI_API_KEY}"
      baseURL: "https://api.x.ai/v1"
      models:
        default: ["grok-beta"]
        fetch: false
      titleConvo: true
      titleMethod: "completion"
      titleModel: "grok-beta"
      summarize: false
      summaryModel: "grok-beta"
      modelDisplayLabel: "Grok"
```

## Hinweise [#notes]

- `titleMethod: "completion"` generiert Konversationstitel über den Chat-Completions-endpoint, was von Grok unterstützt wird.
