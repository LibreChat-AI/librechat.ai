---
title: Databricks
icon: Databricks
description: Configureer Databricks als een aangepast endpoint in LibreChat.
---

Databricks serveert foundation models en je eigen fine-tuned modellen via Mosaic AI Model Serving, dat een OpenAI-compatibel serving endpoint beschikbaar stelt.

## Een API-sleutel verkrijgen [#get-an-api-key]

[Meld je aan voor Databricks](https://www.databricks.com/try-databricks#account) en genereer een persoonlijke toegangstoken vanuit je workspace. Voeg deze toe aan je `.env` bestand:

```bash filename=".env"
DATABRICKS_API_KEY=your-api-key
```

## Configuratie [#configuration]

Voeg het endpoint toe onder `endpoints.custom` in je `librechat.yaml`:

```yaml filename="librechat.yaml"
    - name: 'Databricks'
      apiKey: '${DATABRICKS_API_KEY}'
      baseURL: 'https://your_databricks_serving_endpoint_url_here_ending_with/invocations'
      models:
        default: [
          "databricks-meta-llama-3-70b-instruct",
        ]
        fetch: false
      titleConvo: true
      titleModel: 'current_model'
      directEndpoint: true # required
      titleMessageRole: 'user' # required
```

## Opmerkingen [#notes]

- Databricks stelt een volledig completions endpoint beschikbaar dat eindigt op `invocations` voor `serving-endpoints`, dus [directEndpoint](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#directendpoint) is vereist.
- Stel [titleMessageRole](/docs/configuration/librechat_yaml/object_structure/custom_endpoint#titlemessagerole) in op `user` voor het genereren van titels. Een op zichzelf staand `system` bericht wordt niet ondersteund.
