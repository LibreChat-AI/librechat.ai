---
title: NEAR AI Cloud
icon: NearAI
description: Configura NEAR AI Cloud come endpoint personalizzato in LibreChat.
---

NEAR AI Cloud fornisce un endpoint compatibile con OpenAI che puoi aggiungere a LibreChat tramite `endpoints.custom`.

## Ottieni una API key [#get-an-api-key]

Crea una chiave API da [NEAR AI Cloud](https://cloud.near.ai/). Aggiungila al tuo file `.env`:

```bash filename=".env"
NEARAI_API_KEY=your-api-key
```

## Configurazione [#configuration]

Aggiungi l'endpoint sotto `endpoints.custom` nel tuo `librechat.yaml`:

```yaml filename="librechat.yaml"
    - name: "nearai"
      apiKey: "${NEARAI_API_KEY}"
      baseURL: "https://cloud-api.near.ai/v1"
      models:
        default:
          - "z-ai/glm-5.2"
          - "deepseek-ai/DeepSeek-V4-Flash"
        fetch: true
      titleConvo: true
      titleModel: "z-ai/glm-5.2"
      modelDisplayLabel: "NEAR AI Cloud"
```

Per consentire a ogni utente di fornire la propria chiave tramite l'interfaccia utente di LibreChat invece di leggerne una da `.env`, imposta `apiKey: "user_provided"`.

## Note [#notes]

- Con `fetch: true`, LibreChat carica l'elenco dei modelli dall'endpoint `/v1/models` compatibile con OpenAI di NEAR AI Cloud. L'array `default` rappresenta solo la selezione iniziale.
- Utilizza gli ID dei modelli esattamente come appaiono nel [catalogo dei modelli di NEAR AI Cloud](https://cloud-api.near.ai/v1/model/list).
- La disponibilità dei modelli può cambiare nel tempo. Se un modello selezionato non è disponibile, scegli un altro modello in grado di gestire le chat dal catalogo.
