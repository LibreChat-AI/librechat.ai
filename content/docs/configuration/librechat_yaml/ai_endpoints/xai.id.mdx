---
title: xAI
icon: XAI
description: Konfigurasikan xAI sebagai endpoint kustom di LibreChat.
---

xAI menyediakan model Grok mereka melalui API yang kompatibel dengan OpenAI yang dapat Anda tambahkan ke LibreChat sebagai endpoint kustom.

## Dapatkan API key [#get-an-api-key]

Buat kunci dari [xAI console](https://console.x.ai/). Tambahkan ke file `.env` Anda:

```bash filename=".env"
XAI_API_KEY=your-api-key
```

## Konfigurasi [#configuration]

Tambahkan endpoint di bawah `endpoints.custom` dalam `librechat.yaml` Anda:

```yaml filename="librechat.yaml"
    - name: "xai"
      apiKey: "${XAI_API_KEY}"
      baseURL: "https://api.x.ai/v1"
      models:
        default: ["grok-beta"]
        fetch: false
      titleConvo: true
      titleMethod: "completion"
      titleModel: "grok-beta"
      summarize: false
      summaryModel: "grok-beta"
      modelDisplayLabel: "Grok"
```

## Catatan [#notes]

- `titleMethod: "completion"` menghasilkan judul percakapan melalui endpoint chat completions, yang didukung oleh Grok.
