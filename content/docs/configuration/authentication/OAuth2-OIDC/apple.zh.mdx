---
title: Apple
icon: Apple
description: 了解如何配置 LibreChat 以使用 Apple 进行用户身份验证。
---


## 先决条件 [#prerequisites]

在开始之前，请确保您已具备以下条件：

- **Apple Developer Account:** 如果您还没有账号，请在此[注册](https://developer.apple.com/programs/enroll/)。

---

## 创建新的 App ID [#creating-a-new-app-id]

### 1. 登录 Apple Developer Console [#1-log-in-to-the-apple-developer-console]

- **操作：**
- 访问 [Apple Developer](https://developer.apple.com/) 并使用您的 Apple ID 登录。


### 2. 导航至 Identifiers [#2-navigate-to-identifiers]

- 前往 **Certificates, Identifiers & Profiles**。
- 点击侧边栏中的 **Identifiers**。


### 3. 创建一个新的 App ID [#3-create-a-new-app-id]

1. 点击 **"+"** 按钮以添加新的标识符。
2. 选择 **App IDs** 并点击 **Continue**。
3. 选择 **App** 并点击 **Continue**。
4. 为您的 App ID 输入一个 **Description**（例如 `LibreChat App ID`）。
5. 设置 **Bundle ID**（例如 `com.yourdomain.librechat`）。
6. 点击 **Continue**，然后点击 **Register**。

- **图像引用：**
- ![Create App ID](https://user-images.githubusercontent.com/5569219/59017558-6d643600-8861-11e9-927b-a4952b56f34e.png)
*图 1：创建新的 App ID*

- ![Select App](https://github.com/user-attachments/assets/7b67c1bb-dea0-4475-ad45-e3c13ad514d5)
*图 2：选择 App Identifier*

### 4. 启用 "Sign in with Apple" [#4-enable-sign-in-with-apple]

1. 创建 App ID 后，点击它进行编辑。
2. 在 **Capabilities** 下，找到并勾选 **Sign in with Apple**。
3. 点击 **Save**。

- **图像参考：**
- ![Enable Sign in with Apple](https://user-images.githubusercontent.com/5569219/59017720-dea3e900-8861-11e9-898e-f486c093edd8.png)
*图 3：启用“使用 Apple 登录”*

---

## 创建 Services ID [#creating-a-services-id]

### 1. 导航至 Identifiers [#1-navigate-to-identifiers]

- 在 **Certificates, Identifiers & Profiles** 部分中，点击 **Identifiers**。

### 2. 创建一个新的 Services ID [#2-create-a-new-services-id]

1. 点击 **"+"** 按钮。
2. 选择 **Services IDs** 并点击 **Continue**。
3. 输入一个 **Description**（例如 `LibreChat Services ID`）。
4. 输入一个 **Identifier**（例如 `com.yourdomain.librechat.services`）。
5. 点击 **Continue**，然后点击 **Register**。

- **图像引用：**
- ![Select Services ID](https://user-images.githubusercontent.com/5569219/59017808-16ab2c00-8862-11e9-8beb-4da7bb509b0c.png)
*图 4：选择 Services ID*

- ![Create Services ID](https://github.com/user-attachments/assets/cac99e43-a6d7-4fb8-890d-eabd87a60e7d)
*图 5：创建服务 ID*

### 3. 配置 "Sign in with Apple" [#3-configure-sign-in-with-apple]

1. 点击新创建的 Services ID。
2. 在 **Capabilities** 下，点击 **Sign in with Apple** 旁边的 **Configure**。
3. 输入您的 **Domains**（例如 `your-domain.com`）和 **Return URLs**（例如 `https://your-domain.com/oauth/apple/callback`）。
4. 点击 **Next**，然后点击 **Register**。

- **图像参考：**
- ![Configure Sign in with Apple](https://github.com/user-attachments/assets/9309e1c1-6f98-49fc-a87d-bb5f46e200f7)
*图 6：为 Services ID 配置“使用 Apple 登录”*

- ![Web Authentication Configuration](https://github.com/user-attachments/assets/d1ca9ad2-e555-4083-a974-b26239c9694f)
*图 7：Web 身份验证配置*

- ![Web Authentication Configuration](https://github.com/user-attachments/assets/8facac9e-002f-458b-8049-63c91afc30de)
*图 8：保存编辑服务 ID 配置*


---

## 创建密钥 [#creating-a-key]

### 1. 导航至 Keys [#1-navigate-to-keys]

- 在 **Certificates, Identifiers & Profiles** 部分中，点击 **Keys**。

### 2. 创建新密钥 [#2-create-a-new-key]

1. 点击 **"+"** 按钮以添加新密钥。
2. 输入一个 **Key Name**（例如 `LibreChatSignInWithApple`）。
3. 在 **Capabilities** 下选择 **Sign in with Apple**。
4. 点击 **Configure** 并选择已创建的 App ID（例如 `com.yourdomain.librechat`），然后点击 **Save**。
5. 点击 **Continue**，然后点击 **Register**。

- **图像引用：**
- ![Create Key](https://github.com/user-attachments/assets/6db095dd-79dd-485d-a57a-8b1ea523b67f)
*图 8：创建新密钥*

- ![Configure Key](https://github.com/user-attachments/assets/30b593ab-b8f3-4d94-b56d-1eeac1900a1f)
*图 9：配置带有 App ID 的 Key*

- ![Register a New Key](https://github.com/user-attachments/assets/91aa4d6e-9b5c-4f7c-bb1a-5980015ee15d)
*图 10：注册密钥*

### 3. 下载私钥 [#3-download-the-private-key]

1. 创建密钥后，点击 **Download**。
2. **重要提示：** 请妥善保存 `.p8` 文件。您将无法再次下载该文件。
3. 请注意 **Key ID**；你需要在 `.env` 文件中使用它。

- **图像参考：**
- ![Download Your Key](https://github.com/user-attachments/assets/79e5aefa-9797-4fa7-bdf9-cb4b526ab3dd)
*图 11：下载私钥*

---

## 配置 LibreChat [#configuring-librechat]

### 1. 更新 `.env` 配置 [#1-update-env-configuration]

将以下 Apple OAuth2 配置添加到您的 `.env` 文件中：

```env filename=".env"
DOMAIN_CLIENT=https://your-domain.com # use http://localhost:3080 if not using a custom domain
DOMAIN_SERVER=https://your-domain.com # use http://localhost:3080 if not using a custom domain

# Apple
APPLE_CLIENT_ID=com.yourdomain.librechat.services
APPLE_TEAM_ID=YOUR_TEAM_ID
APPLE_KEY_ID=YOUR_KEY_ID
APPLE_PRIVATE_KEY_PATH=/path/to/AuthKey.p8 # Absolute path to your downloaded .p8 file
APPLE_CALLBACK_URL=/oauth/apple/callback
```

> **注意：**
> - 将 `com.yourdomain.librechat.services` 替换为您实际的 Services ID。
> - 将 `YOUR_TEAM_ID` 和 `YOUR_KEY_ID` 替换为您 Apple Developer 账户中的相应值。
> - 如果使用 Docker，请确保 `.p8` 文件在 Docker 容器内可访问，并相应地更新 `APPLE_PRIVATE_KEY_PATH`。

### 2. 重启 LibreChat [#2-restart-librechat]

更新 `.env` 文件后，请重启 LibreChat 以应用更改。

- **如果使用 Docker：**

```bash
docker compose up -d
  ```

---

## 故障排除 [#troubleshooting]

如果在设置过程中遇到问题，请考虑以下解决方案：

- **无效的重定向 URI：**
    - 请确保您在 Apple Developer Console 中设置的重定向 URI (`https://your-domain.com/oauth/apple/callback`) 与您 `.env` 文件中指定的 URI (`APPLE_CALLBACK_URL`) 完全一致。

- **私钥问题：**
    - 请验证您的 `.p8` 文件路径 (`APPLE_PRIVATE_KEY_PATH`) 是否正确。
    - 确保 LibreChat 对 `.p8` 文件拥有读取权限。

- **团队 ID 和密钥 ID 错误：**
    - 请仔细核对 `.env` 文件中的 `APPLE_TEAM_ID` 和 `APPLE_KEY_ID` 是否与您的 Apple Developer Account 中的信息一致。

- **域名验证失败：**
    - 确保验证文件已正确上传到您域名的根目录。
    - 请核实配置过程中输入的域名是否存在拼写错误。

- **Docker 配置问题：**
    - 如果使用 Docker，请确认 `.p8` 文件已正确挂载，并且 `APPLE_PRIVATE_KEY_PATH` 中的路径在容器内可访问。

- **检查日志：**
    - 检查 LibreChat 日志中是否有与 Apple 身份验证相关的任何错误消息。这可以提供关于可能出现问题的具体见解。
