---
title: 概述
icon: BookOpen
description: 如何为 LibreChat 配置社交登录
---

本节将介绍如何配置 LibreChat 的 OAuth2 和 OpenID Connect。

<ThemeImage
  light="https://github.com/danny-avila/LibreChat/assets/32828263/786fa525-73c4-4640-b4cf-91925ad8802e"
  dark="https://github.com/danny-avila/LibreChat/assets/32828263/dddc34c6-9602-4177-89e8-4c0db01b0eac"
  alt="OAuth2 和 OpenID Connect 登录界面"
/>

## OAuth2

- [Apple](/docs/configuration/authentication/OAuth2-OIDC/apple)
- [Discord](/docs/configuration/authentication/OAuth2-OIDC/discord)
- [Facebook](/docs/configuration/authentication/OAuth2-OIDC/facebook)
- [GitHub](/docs/configuration/authentication/OAuth2-OIDC/github)
- [Google](/docs/configuration/authentication/OAuth2-OIDC/google)

## OpenID Connect [#openid-connect]

- [Auth0](/docs/configuration/authentication/OAuth2-OIDC/auth0)
- [AWS Cognito](/docs/configuration/authentication/OAuth2-OIDC/aws)
- [Azure Entra/AD](/docs/configuration/authentication/OAuth2-OIDC/azure)
- [Keycloak](/docs/configuration/authentication/OAuth2-OIDC/keycloak)
- [重用 OpenID 令牌以实现登录会话](/docs/configuration/authentication/OAuth2-OIDC/token-reuse)

### OpenID JWT 用户缓存 [#openid-jwt-user-cache]

在高请求速率的部署中，可以设置 `AUTH_USER_CACHE_MODE=on`，以便将用于 OpenID JWT 身份验证的已解析用户文档缓存五秒。这可以在不延长身份验证令牌生命周期的情况下，减少短时间请求激增期间的重复数据库读取。

缓存默认处于关闭状态，需要设置 `USE_REDIS=true` 并使用由 Redis 支持的 `AUTH_USER_DOC` 命名空间。当不满足这些要求时，LibreChat 会禁用该模式并记录一条警告。缓存的文档在存储前会经过清理，并在用户更新时失效。

```bash filename=".env"
USE_REDIS=true
AUTH_USER_CACHE_MODE=on
```

## 排查 OpenID Connect 问题 [#troubleshooting-openid-connect]

如果您在使用 OpenID Connect 身份验证时遇到问题：

1. **启用 Header 调试日志记录**：在环境变量中设置 `DEBUG_OPENID_REQUESTS=true`，以便在记录 URL 的同时记录请求头（敏感数据会被屏蔽）。请注意：请求 URL 始终会在调试级别进行记录。
2. **检查重定向 URI**：确保您的回调 URL 在您的提供商和 LibreChat 配置之间完全一致
3. **验证作用域 (Verify Scopes)**：确保所有必需的作用域均已正确配置
4. **查看提供商日志**：检查您的身份提供商日志以查找身份验证错误
5. **验证令牌 (Validate Tokens)**：确保您的提供商正在颁发带有预期声明的有效令牌
6. **确保生成 _nonce_**：某些身份提供商会在请求中缺少 `nonce` URL 参数时自动生成该参数。设置 `OPENID_GENERATE_NONCE=true` 以强制 openid-client 生成它。

### 管理面板重定向 [#admin-panel-redirects]

如果 Admin Panel 托管在与 LibreChat 不同的 URL 上，请在 LibreChat API 环境中设置 [`ADMIN_PANEL_URL`](/docs/features/admin_panel#librechat-redirect-url)。这会告知 LibreChat 在管理员 OAuth 或 SSO 回调完成后将管理员重定向至何处。
