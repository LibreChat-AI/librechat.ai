---
title: Firebase CDN
icon: Firebase
description: >-
  Tài liệu này cung cấp hướng dẫn thiết lập Firebase Storage làm CDN cho
  LibreChat
---

Firebase Storage tích hợp với CDN toàn cầu của Firebase Hosting, cho phép bạn phân phối các tệp được lưu trữ trong Firebase Storage thông qua các vị trí biên (edge locations) trên khắp thế giới. Đây là một trong những tùy chọn lưu trữ tệp hỗ trợ CDN của LibreChat, bên cạnh [CloudFront for S3](/docs/configuration/cdn/cloudfront).

<Callout type="info" title="Những gì bạn cần">
Một tài khoản Google và khoảng 10 phút. Bạn sẽ tạo một dự án Firebase, bật Cloud Storage, đăng ký một ứng dụng web để lấy thông tin xác thực, sau đó trỏ LibreChat đến đó.
</Callout>

## Tạo một dự án Firebase [#create-a-firebase-project]

<Steps>
<Step>

**Mở Firebase và đăng nhập.** Truy cập [trang web Firebase](https://firebase.google.com/), nhấp vào **Get started** và đăng nhập bằng tài khoản Google của bạn.

</Step>
<Step>

**Đặt tên cho dự án của bạn.** Bạn có thể sử dụng lại cùng một dự án như Google OAuth nếu bạn đã có sẵn một dự án.

<Frame>
![Đặt tên cho dự án Firebase](https://github.com/danny-avila/LibreChat/assets/81851188/dccce3e0-b639-41ef-8142-19d24911c65c)
</Frame>

</Step>
<Step>

**Cấu hình Google Analytics (tùy chọn).** Bạn có thể tắt Google Analytics cho dự án này.

<Frame>
![Google Analytics toggle](https://github.com/danny-avila/LibreChat/assets/81851188/5d4d58c5-451c-498b-97c0-f123fda79514)
</Frame>

</Step>
<Step>

**Tạo dự án.** Đợi 20-30 giây để quá trình cung cấp hoàn tất, sau đó nhấp vào **Continue**.

<Frame>
![Dự án đã sẵn sàng, nhấp vào Tiếp tục](https://github.com/danny-avila/LibreChat/assets/81851188/6929802e-a30b-4b1e-b124-1d4b281d0403)
</Frame>

</Step>
</Steps>

## Bật Lưu trữ Đám mây [#enable-cloud-storage]

<Steps>
<Step>

**Mở Tất cả Sản phẩm.** Từ bảng điều khiển dự án, nhấp vào **All Products**.

<Frame>
![Menu Tất cả sản phẩm](https://github.com/danny-avila/LibreChat/assets/81851188/92866c82-2b03-4ebe-807e-73a0ccce695e)
</Frame>

</Step>
<Step>

**Chọn Storage**, sau đó nhấp vào **Get Started**.

<Frame>
![Select Storage](https://github.com/danny-avila/LibreChat/assets/81851188/b22dcda1-256b-494b-a835-a05aeea02e89)
</Frame>

<Frame>
![Storage Get Started](https://github.com/danny-avila/LibreChat/assets/81851188/c3f0550f-8184-4c79-bb84-fa79655b7978)
</Frame>

</Step>
<Step>

**Xác nhận các quy tắc bảo mật.** Nhấp vào **Next** để tiếp tục.

<Frame>
![Các bước thiết lập quy tắc bảo mật](https://github.com/danny-avila/LibreChat/assets/81851188/2a65632d-fe22-4c71-b8f1-aac53ee74fb6)
</Frame>

</Step>
<Step>

**Chọn một vị trí Lưu trữ Đám mây**, sau đó hoàn tất thiết lập và quay lại **Tổng quan Dự án**.

<Frame>
![Chọn vị trí Lưu trữ Đám mây](https://github.com/danny-avila/LibreChat/assets/81851188/c094d4bc-8e5b-43c7-96d9-a05bcf4e2af6)
</Frame>

</Step>
</Steps>

## Đăng ký Web App [#register-a-web-app]

<Steps>
<Step>

**Thêm một ứng dụng web.** Trên trang Tổng quan Dự án (Project Overview), nhấp vào **+ Add app** bên dưới tên dự án của bạn, sau đó chọn **Web**.

<Frame>
![Thêm một ứng dụng web](https://github.com/danny-avila/LibreChat/assets/81851188/22dab877-93cb-4828-9436-10e14374e57e)
</Frame>

</Step>
<Step>

**Đăng ký ứng dụng** và đặt cho nó một biệt danh.

<Frame>
![Đăng ký ứng dụng](https://github.com/danny-avila/LibreChat/assets/81851188/0a1b0a75-7285-4f03-95cf-bf971bd7d874)
</Frame>

</Step>
<Step>

**Sao chép các giá trị `firebaseConfig` của bạn.** Lưu cấu hình được hiển thị vào một nơi an toàn.

<Frame>
![Các giá trị cấu hình Firebase](https://github.com/danny-avila/LibreChat/assets/81851188/056754ad-9d36-4662-888e-f189ddb38fd3)
</Frame>

</Step>
<Step>

**Thêm các giá trị vào tệp `.env` của bạn.** Ánh xạ từng giá trị `firebaseConfig` tương ứng với biến phù hợp:

```bash filename=".env"
FIREBASE_API_KEY=api_key                          # apiKey
FIREBASE_AUTH_DOMAIN=auth_domain                  # authDomain
FIREBASE_PROJECT_ID=project_id                    # projectId
FIREBASE_STORAGE_BUCKET=storage_bucket            # storageBucket
FIREBASE_MESSAGING_SENDER_ID=messaging_sender_id  # messagingSenderId
FIREBASE_APP_ID=1:your_app_id                     # appId
```

</Step>
</Steps>

## Cập nhật Quy tắc Lưu trữ [#update-storage-rules]

<Steps>
<Step>

**Các quy tắc Open Storage.** Quay lại **Project Overview**, chọn **Storage**, sau đó mở tab **Rules**.

<Frame>
![Open Storage](https://github.com/danny-avila/LibreChat/assets/32828263/16a0f850-cdd4-4875-8342-ab67bfb59804)
</Frame>

</Step>
<Step>

**Cho phép quyền đọc và ghi.** Thay đổi `allow read, write: if false;` thành `if true;` để khớp với các quy tắc bên dưới:

```js filename="storage.rules"
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /images/{userId}/{fileName} {
      allow read, write: if true;
    }
  }
}
```

<Frame>
![Updated storage rules](https://github.com/danny-avila/LibreChat/assets/32828263/c190011f-c1a6-47c7-986e-8d309b5f8704)
</Frame>

</Step>
<Step>

**Công bố các thay đổi của bạn.**

<Frame>
![Publish rules](https://github.com/danny-avila/LibreChat/assets/32828263/5e6a17c3-5aba-419a-a18f-be910b1f25d5)
</Frame>

</Step>
</Steps>

## Cấu hình LibreChat [#configure-librechat]

Đặt `fileStrategy` thành `firebase` trong tệp cấu hình `librechat.yaml` của bạn để LibreChat sử dụng Firebase làm nơi lưu trữ tệp:

```yaml filename="librechat.yaml"
version: 1.3.5
cache: true
fileStrategy: 'firebase'
```

Để biết thêm về tệp này, hãy xem [hướng dẫn librechat.yaml](/docs/configuration/librechat_yaml).

## Bật CORS cho xuất tệp PNG [#enable-cors-for-png-exports]

<Callout type="warning" title="Chỉ cần thiết cho việc xuất tệp PNG">
Việc xuất các cuộc trò chuyện dưới dạng PNG sẽ tải hình ảnh trực tiếp từ Firebase Storage trong trình duyệt. Nếu không có chính sách CORS cho phép tên miền của bạn, các yêu cầu đó sẽ bị chặn. Hãy bỏ qua phần này nếu bạn không xuất các cuộc trò chuyện dưới dạng PNG.
</Callout>

<Steps>
<Step>

**Tạo tệp cấu hình CORS.** Trong trình soạn thảo văn bản, hãy tạo `cors.json` và cho phép truy cập từ tên miền của bạn:

```json filename="cors.json"
[
  {
    "origin": ["https://ai.example.com"],
    "method": ["GET", "POST", "DELETE", "PUT"],
    "maxAgeSeconds": 3600
  }
]
```

</Step>
<Step>

**Áp dụng cấu hình.** Từ thư mục chứa `cors.json`, hãy chạy lệnh dưới đây, thay thế `<your-cloud-storage-bucket>` bằng tên bucket của bạn:

```shell
gsutil cors set cors.json gs://<your-cloud-storage-bucket>
```

</Step>
<Step>

**Xác minh các cài đặt.** Truy xuất chính sách đang hoạt động và xác nhận rằng nó khớp với `cors.json`:

```shell
gsutil cors get gs://<your-cloud-storage-bucket>
```

</Step>
<Step>

**Kiểm tra.** Xuất cuộc trò chuyện dưới dạng PNG từ origin được cho phép của bạn. Nếu mọi thứ được cấu hình chính xác, quá trình xuất sẽ thành công mà không gặp lỗi CORS.

</Step>
</Steps>

<Callout type="info" title="Mẹo bảo mật">
Chỉ cho phép CORS đối với các nguồn đáng tin cậy, đồng thời giới hạn các phương thức và tiêu đề (headers) theo đúng nhu cầu thực tế của quá trình triển khai của bạn.
</Callout>
