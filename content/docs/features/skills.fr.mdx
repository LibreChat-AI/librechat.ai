---
title: Compétences
icon: ScrollText
description: >-
  Créez des instructions SKILL.md réutilisables que les agents peuvent invoquer
  manuellement, automatiquement ou à chaque tour.
---

Les Skills sont des ensembles d'instructions réutilisables pour les Agents LibreChat. Un skill est centré sur un fichier `SKILL.md` : le frontmatter décrit quand le skill doit être utilisé, et le corps en markdown fournit à l'agent la procédure, les règles, les exemples ou les références à suivre.

Les Skills sont utiles pour les tâches répétitives telles que :

- Application des directives de marque ou de rédaction
- Suivi des listes de contrôle d'examen interne
- Exécution d'un flux de travail de recherche standard
- Préparation d'un flux de travail d'outil spécialisé
- Empaqueter des scripts, références et ressources réutilisables avec un fichier d'instructions

LibreChat ne fournit pas de bibliothèque de compétences. Chaque compétence provient de votre propre instance : [écrite ou téléchargée dans l'interface utilisateur](#create-a-skill), [créée par un agent lors d'une discussion](#runtime-skill-authoring), [montée depuis le système de fichiers](#deployment-skills), ou [synchronisée depuis un dépôt GitHub](#github-skill-sync).

## Activer les Skills [#enable-skills]

La fonctionnalité d'agent `skills` est activée par défaut. Les administrateurs peuvent la supprimer de la liste des fonctionnalités de l'endpoint agents pour masquer les Skills aux utilisateurs.

```yaml filename="librechat.yaml"
endpoints:
  agents:
    capabilities:
      - 'deferred_tools'
      - 'execute_code'
      - 'file_search'
      - 'web_search'
      - 'artifacts'
      - 'subagents'
      - 'actions'
      - 'context'
      - 'skills'
      - 'memory'
      - 'ask_user_question'
      - 'tools'
      - 'chain'
      - 'ocr'
```

Les autorisations de rôle contrôlent qui peut utiliser, créer, partager et partager publiquement des skills. L'utilisation et la création de skills sont autorisées par défaut ; le partage et le partage public sont désactivés par défaut pour les rôles non administrateurs. Les administrateurs modifient ces paramètres depuis les paramètres d'administration dans le panneau Skills.

Si la fonctionnalité est désactivée, ou si votre rôle ne dispose pas de l'autorisation d'utilisation, Skills n'apparaît pas du tout dans le panneau latéral.

## Utiliser une Skill [#use-a-skill]

<Steps>
<Step>

**Open Skills** depuis le panneau latéral. Le panneau répertorie les skills que vous possédez et ceux qui ont été partagés avec vous, avec une zone de filtrage pour les longues listes. Sélectionner un skill ouvre sa page, où vous pouvez lire les instructions et parcourir les fichiers qui y sont associés.

</Step>
<Step>

**Vérifiez que la compétence est disponible.** Chaque compétence dispose d'un commutateur **Disponible pour l'agent**. Les compétences que vous possédez et les compétences de déploiement sont disponibles par défaut ; les compétences partagées suivent la valeur par défaut configurée par l'administrateur jusqu'à ce que vous les remplaciez.

</Step>
<Step>

**Donnez l'accès à l'agent.** Dans le générateur d'agents, activez **Enable skills**, puis décidez si l'agent voit le catalogue complet ou uniquement les compétences que vous choisissez. Voir [Agent Scope](#agent-scope).

</Step>
<Step>

**Invoquez la compétence.** Tapez `$` dans la zone de message et sélectionnez une compétence par son nom. Rien n'est inséré dans le texte de votre message : la compétence est mise en file d'attente pour ce tour et s'affiche sous forme de puce au-dessus du compositeur. Le modèle peut également choisir une compétence de lui-même, et les compétences `always-apply` ne nécessitent aucune action.

</Step>
</Steps>

## Créer une compétence [#create-a-skill]

Ouvrez **Skills** depuis le panneau latéral et utilisez le bouton **+**, qui propose deux chemins :

- **Rédiger des instructions de compétence** — une boîte de dialogue avec un nom, une description et le corps de l'instruction.
- **Télécharger une skill** — un fichier `.md`, `.zip` ou `.skill` qui contient un `SKILL.md`.

Un agent peut également créer une compétence pour vous pendant qu'il travaille, sans passer par ce panneau. Voir [Runtime Skill Authoring](#runtime-skill-authoring).

`SKILL.md` minimal :

```md filename="SKILL.md"
---
name: brand-guidelines
description: Use when writing public-facing content that must follow the company voice and terminology.
---

# Brand Guidelines

Write in a concise, practical tone.
Prefer active voice.
Use product terminology consistently.
```

### Frontmatter [#frontmatter]

<OptionTable
  options={[
    [
      'name',
      'String',
      'Identifiant stable en kebab-case. Il doit commencer par une lettre minuscule ou un chiffre et peut contenir des lettres minuscules, des chiffres et des traits d\'union.',
      'name: brand-guidelines',
    ],
    [
      'description',
      'String',
      'Le texte déclencheur le plus important. Décrivez quand le modèle doit utiliser la compétence. `when-to-use` est accepté comme alias lorsque `description` est absent.',
      'description: Use when writing public-facing launch copy.',
    ],
    [
      'always-apply',
      'Boolean',
      'Amorce automatiquement la compétence à chaque tour où elle est active. `alwaysApply` est accepté comme alias.',
      'always-apply: true',
    ],
    [
      'user-invocable',
      'Boolean',
      'Définir sur false pour masquer la compétence de l\'invocation manuelle via `$`. Par défaut : true.',
      'user-invocable: false',
    ],
    [
      'disable-model-invocation',
      'Boolean',
      'Définissez sur true pour exclure la compétence du catalogue de compétences invoquées par le modèle. L\'invocation manuelle reste autorisée sauf si `user-invocable` est défini sur false.',
      'disable-model-invocation: true',
    ],
    [
      'allowed-tools',
      'Array/List of Strings',
      'Réunit temporairement ces outils dans l\'ensemble d\'outils effectifs de l\'agent lorsque la compétence est appliquée manuellement ou systématiquement.',
      'allowed-tools: ["execute_code"]',
    ],
    [
      'compatibility',
      'String',
      'Notes de compatibilité facultatives, telles que les outils, services ou hypothèses d\'exécution requis. LibreChat conserve ces métadonnées mais ne les impose pas.',
      'compatibility: Requires the GitHub MCP server.',
    ],
  ]}
/>

Les clés sont comparées sans tenir compte de la casse, et les champs booléens n'acceptent que `true` ou `false`. La suppression d'une clé booléenne lors d'une sauvegarde ultérieure l'efface, donc supprimer `disable-model-invocation` rétablit l'invocation du modèle.

Les champs de frontmatter reconnus sont soumis à une vérification de type. Les champs non reconnus sont conservés sous forme de métadonnées limitées et compatibles JSON au lieu de rendre le Skill invalide ; les valeurs d'extension trop volumineuses ou profondément imbriquées sont toujours rejetées.

Gardez la `description` d'une Skill invoquée par un modèle à 250 caractères ou moins et placez ses expressions déclencheuses les plus importantes en premier. Des descriptions plus longues peuvent toujours être importées, mais LibreChat les tronque dans le catalogue visible par le modèle et enregistre un avertissement nommant chaque Skill concernée.

### Limites de nommage et de taille [#naming-and-size-limits]

- `name` est requis, doit correspondre à `^[a-z0-9][a-z0-9-]*$`, et est limité à 64 caractères. Il doit être unique parmi vos propres compétences ; le rechargement d'un nom existant échouera avec une erreur de conflit au lieu d'écraser l'original.
- Les noms ne peuvent pas commencer par `anthropic-` ou `claude-`, et ne peuvent pas être exactement `help`, `clear`, `compact`, `model`, `exit`, `quit`, `settings`, `anthropic` ou `claude`. Ces noms entreraient en conflit avec des commandes réelles.
- `description` est requis et limité à 1024 caractères. Les descriptions de moins de 20 caractères sont acceptées avec un avertissement, car elles se déclenchent rarement de manière fiable.
- Le corps de l'instruction est limité à 100 000 caractères.

### Télécharger un bundle [#upload-a-bundle]

Une archive `.zip` ou `.skill` doit contenir un fichier `SKILL.md` à la racine de l'archive ou exactement dans un sous-dossier. Un dossier enveloppant unique est supprimé lors de l'importation ; les structures plus profondes ne sont pas détectées.

<FileTree>
  <FileTree.Folder name="brand-guidelines" defaultOpen>
    <FileTree.File name="SKILL.md" />
    <FileTree.Folder name="references" defaultOpen>
      <FileTree.File name="tone-examples.md" />
    </FileTree.Folder>
    <FileTree.Folder name="scripts">
      <FileTree.File name="lint-copy.py" />
    </FileTree.Folder>
  </FileTree.Folder>
</FileTree>

Limites d'importation :

- Extensions acceptées : `.md`, `.zip`, `.skill`.
- 50 Mo par téléchargement par défaut. Les administrateurs peuvent modifier cette limite avec [`fileConfig.skills.fileSizeLimit`](/docs/configuration/librechat_yaml/object_structure/file_config#skills) dans `librechat.yaml`.
- Jusqu'à 500 entrées par archive, et 10 Mo par fichier groupé.
- Un téléchargement `.md` sans `name` dans le frontmatter dérive le nom de la compétence à partir du nom du fichier.
- Les entrées avec des chemins non sécurisés, tels que les chemins de traversée ou les chemins absolus, sont signalées comme des erreurs et ignorées ; le reste de la compétence est tout de même importé.

Les compétences provenant de `DEPLOYMENT_SKILLS_DIR` ou de la synchronisation des compétences GitHub sont en lecture seule. Seules les compétences créées dans LibreChat peuvent être modifiées via l'interface utilisateur.

## Modes d'invocation [#invocation-modes]

Les Skills peuvent atteindre un agent de trois manières :

- **Manuel** : l'utilisateur tape `$` dans le chat et sélectionne une compétence dans la fenêtre contextuelle.
- **Model-invoked** : le modèle choisit une compétence dans le catalogue de compétences injecté et appelle l'outil de compétence.
- **Toujours appliquer** : la compétence est amorcée à chaque tour lorsqu'elle est active.

Ce qui entre réellement dans le contexte diffère selon le chemin :

- Le catalogue invoqué par le modèle ne contient que des noms et des descriptions, jusqu'à 100 compétences, chaque entrée étant limitée à environ 250 caractères. Les corps des instructions ne font pas partie du catalogue.
- L'invocation manuelle et l'application systématique (always-apply) préparent le corps complet du fichier `SKILL.md` pour ce tour, jusqu'à 10 compétences manuelles et 20 compétences à application systématique, avec un total combiné de 30. Lorsque la limite combinée est dépassée, les compétences à application systématique sont supprimées en premier afin que l'intention explicite de l'utilisateur soit préservée.

L'invocation manuelle est une intention explicite de l'utilisateur. Elle peut utiliser des compétences qui sont masquées de l'invocation par le modèle avec `disable-model-invocation: true`, tant que `user-invocable` n'est pas défini sur false.

## Portée de l'agent [#agent-scope]

Les Agents doivent avoir l'option **Enable skills** activée avant de pouvoir utiliser ou créer des Skills. Le commutateur séparé **Use all skills** contrôle l'exposition dans le catalogue.

- **Tous :** activez **Utiliser toutes les compétences** pour exposer le catalogue actif complet visible par l'utilisateur, y compris les compétences ajoutées ultérieurement.
- **Sélectionné :** laissez **Use all skills** désactivé et ajoutez des Skills individuelles pour n'exposer que ces entrées.
- **Authoring only :** laissez **Use all skills** désactivé sans aucune Skill sélectionnée. Les entrées de catalogue existantes ne sont pas exposées, mais l'agent peut créer une Skill au cours d'une conversation lorsque l'utilisateur dispose de l'autorisation de création de Skill.
- **Désactivé :** désactivez **Enable skills** pour désactiver à la fois l'utilisation du catalogue et la création au moment de l'exécution.

Cela permet aux administrateurs d'exposer la fonctionnalité Skills globalement tout en gardant l'ensemble des compétences utilisables de chaque agent ciblé.

Désactiver **Use all skills** restaure la sélection qui était active avant que l'option ne soit activée. Les agents existants conservent le comportement hérité où les Skills activées sans portée explicite signifient le catalogue complet.

## Création de compétences à l'exécution [#runtime-skill-authoring]

Un agent avec les Skills activés peut créer ou modifier des fichiers de Skill réutilisables pendant son exécution. Les fichiers d'exécution utilisent le même espace de nom `skills/{skillName}/...` que les bundles téléchargés, de sorte que l'agent peut créer un `SKILL.md` ainsi que les références, scripts ou ressources associés dans le cadre d'un flux de travail réussi.

La création suit le modèle de permission normal des Skills :

- La création d'une nouvelle Skill nécessite l'autorisation **Create** (Créer) de la Skill de l'utilisateur.
- La modification d'une Skill existante nécessite à la fois un accès en modification et son inclusion dans le périmètre du catalogue actif de l'agent ; les Skills déployées et synchronisées avec GitHub restent en lecture seule.
- Un agent en mode création uniquement peut créer une nouvelle Skill sans recevoir le catalogue existant de l'utilisateur.

La création au moment de l'exécution (runtime authoring) ne contourne pas la capacité de déploiement ni les autorisations des utilisateurs. La suppression de `skills` des capacités de l'endpoint Agents masque la fonctionnalité, et la désactivation de **Enable skills** sur l'agent désactive ses outils de création.

## Compétences actives et partagées [#active-and-shared-skills]

Les utilisateurs peuvent activer ou désactiver les compétences à l'aide du commutateur **Available to agent**. Les compétences possédées et déployées sont activées par défaut. Les compétences partagées utilisent la valeur par défaut configurée par l'administrateur jusqu'à ce que l'utilisateur les remplace.

Les compétences inactives sont exclues de :

- Le popover `$`
- Le catalogue de compétences invoquées par le modèle
- Amorçage toujours appliqué

Le partage d'une compétence accorde un accès en lecture, en modification ou en tant que propriétaire à des utilisateurs, groupes ou rôles spécifiques via la boîte de dialogue de partage standard, et nécessite l'autorisation de partage sur votre rôle. Consultez [Access Control](/docs/features/access_control) pour le modèle complet.

## Fichiers groupés [#bundled-files]

Les bundles de compétences téléchargés peuvent inclure des fichiers aux côtés de `SKILL.md`, tels que :

- `references/...`
- `scripts/...`
- `assets/...`

Le backend stocke ces fichiers avec la skill. L'agent peut résoudre les fichiers de skill lorsque la skill est active et dans le périmètre. Les fichiers imbriqués conservent leurs chemins relatifs et sont adressés via l'espace de nommage `skills/{skillName}/...` exposé au modèle. Dans la visionneuse de Skill, les liens Markdown relatifs se résolvent à partir du fichier actuel et ouvrent le fichier référencé au sein de la même Skill. Lorsque Code Interpreter est activé, les mêmes fichiers sont montés sur `/mnt/data/skills/{skillName}/...` afin que les opérations shell et de code puissent les utiliser directement. La modification de `SKILL.md` fait progresser la version de la Skill ; avant toute exécution de code ou programmatique, LibreChat vérifie que chaque référence de fichier monté correspond à cette version et retéléverse le bundle complet si une référence est obsolète.

La page d'une skill dispose également d'une arborescence de fichiers avec des actions **New File**, **New Folder** et **Upload File**, jusqu'à 10 Mo par fichier ajouté. `SKILL.md` lui-même y est réservé : modifiez plutôt le corps de la skill.

<Callout type="note" title="Conseil de rédaction">
  Gardez la `description` spécifique. C'est le signal le plus fort pour les compétences invoquées par le modèle. Une description courte ou vague entraînera un sous-déclenchement.
</Callout>

## Compétences de déploiement [#deployment-skills]

Les administrateurs peuvent déployer des Skills en lecture seule depuis le système de fichiers avec `DEPLOYMENT_SKILLS_DIR`.

```bash filename=".env"
DEPLOYMENT_SKILLS_DIR=./skill
```

Le répertoire par défaut est `./skill` à la racine du projet. Chaque skill se trouve dans son propre sous-répertoire contenant un `SKILL.md`, avec tous les fichiers groupés à côté. LibreChat charge les Skills de déploiement au démarrage et les expose aux utilisateurs ayant la fonctionnalité Skills activée.

Compétences de déploiement :

- Sont en lecture seule dans l'interface utilisateur
- Utilisez `deployment` comme source
- Ont la priorité sur les Skills persistés portant le même nom
- Nécessite un redémarrage de LibreChat après l'ajout, la suppression ou la modification de fichiers

Les [Agent Plugins](/docs/features/agent_plugins) expérimentaux peuvent également regrouper des Skills de déploiement en lecture seule avec des serveurs MCP associés. Un Skill situé dans le répertoire de déploiement autonome est prioritaire sur un Skill de plugin portant le même nom.

## Synchronisation des compétences GitHub [#github-skill-sync]

Les administrateurs peuvent répliquer des Skills depuis des dépôts GitHub avec `skillSync.github` dans `librechat.yaml`. Il s'agit de la méthode pratique pour partager un ensemble organisé de skills au sein d'une équipe.

```yaml filename="librechat.yaml"
skillSync:
  github:
    enabled: true
    intervalMinutes: 60
    runOnStartup: true
    sources:
      - id: librechat-skills
        owner: your-org
        repo: your-skills-repo
        ref: main
        paths:
          - skills
        skillDiscoveryDepth: 2
        token: '${GITHUB_SKILLS_TOKEN}'
```

Synchronisation des compétences GitHub :

- Analyse les chemins de dépôt configurés à la recherche de `SKILL.md`
- Traite le répertoire contenant chaque `SKILL.md` comme une racine de compétence, dans la limite des niveaux `skillDiscoveryDepth` du chemin configuré.
- Importe les fichiers groupés à côté de chaque compétence
- Stocke les Skills en miroir avec `source: "github"`
- Met à jour les Skills en miroir lorsque le dépôt amont change
- Supprime les Skills en miroir qui n'existent plus dans la source configurée
- Publie des Skills valides même lorsqu'un autre Skill dans la source est invalide ou en conflit
- Signale aux administrateurs les exécutions partiellement réussies et les détails des compétences ignorées (skipped-Skill) limitées.
- Prend en charge les exécutions planifiées, au démarrage et déclenchées manuellement par l'administrateur

Une Skill ignorée conserve son dernier miroir connu comme fonctionnel lorsqu'il en existe un. Les échecs à l'échelle de la source, tels que l'authentification, la limitation de débit, la perte de propriété de synchronisation ou une restauration infructueuse, entraînent toujours l'échec de l'exécution complète de la source.

Utilisez un jeton d'accès personnel GitHub à grain fin avec des autorisations de lecture seule pour Contents et Metadata pour le dépôt sélectionné. Voir [Skill Sync Object Structure](/docs/configuration/librechat_yaml/object_structure/skill_sync) pour tous les champs, les options d'identification, le scoping de tenant et les endpoints de synchronisation d'administration.
